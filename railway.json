{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python -c 'from alembic.config import main; main(argv=[\"upgrade\",\"head\"])' && python -c 'import os,uvicorn; uvicorn.run(\"app.main:app\", host=\"0.0.0.0\", port=int(os.environ.get(\"PORT\",\"8000\")))'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
