// Temporary shell — the real dashboard layout lands in plan 02-06.
// Renders on the token background so the theme toggle is spot-checkable.
function App() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-[2rem] leading-tight font-bold">
        Chris's Health Dashboard
      </h1>
    </main>
  )
}

export default App
