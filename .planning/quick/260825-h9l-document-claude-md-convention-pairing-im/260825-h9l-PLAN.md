---
phase: quick-260825-h9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md's Conventions section documents how to pair the `impeccable` skill with the GSD workflow for frontend design work, replacing the 'not yet established' placeholder"
    - "A developer reading CLAUDE.md can tell, without asking, when to invoke `impeccable` directly (pure critique, no GSD command) versus when to route through a GSD entry point (`/gsd-sketch`, `/gsd-ui-phase`, `/gsd-ui-review`, `/gsd-quick`, `/gsd-execute-phase`)"
    - "The new section states plainly that `impeccable` never Edits/Writes files itself — actual edits always go through a GSD command, consistent with the existing GSD Workflow Enforcement section"
    - "No other section of CLAUDE.md (Project, Technology Stack, Architecture, Project Skills, GSD Workflow Enforcement, Developer Profile) changes"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Conventions section (inside the `<!-- GSD:conventions-start -->` / `<!-- GSD:conventions-end -->` markers) documenting the impeccable + GSD pairing convention, in the file's existing terse table/bullet house style"
      contains: "impeccable"
  key_links:
    - from: "## Conventions (impeccable + GSD pairing)"
      to: "## GSD Workflow Enforcement"
      via: "explicit restatement that impeccable never Edits/Writes — edits go through a GSD entry point"
      pattern: "never Edit|never.*Write"
    - from: "## Conventions (impeccable + GSD pairing)"
      to: "### Constraints > Accessibility (non-negotiable)"
      via: "citing the existing accessibility floor as the reason pure-critique impeccable calls are valuable here"
      pattern: "48px|accessibility"
---

<objective>
Replace the placeholder text in CLAUDE.md's `## Conventions` section with a concise, terse-style
convention that documents how to pair the `impeccable` skill (a general-purpose, globally-installed
Claude Code frontend design-critique skill — NOT part of GSD, found at `~/.claude/skills/impeccable`)
with this project's GSD workflow for frontend work on the React/Recharts dashboard.

Purpose: This project's `CLAUDE.md` already carries a "GSD Workflow Enforcement" section requiring
all file edits to go through a GSD entry point. Without an explicit convention, a future session
could either (a) never think to reach for `impeccable`'s design judgment on this accessibility-heavy
UI, or (b) let `impeccable` edit files directly and bypass GSD's planning/commit/tracking discipline.
This task documents the correct pairing so both risks are closed by house rule, not by memory.

Output: An updated `## Conventions` section in `/Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md`,
and nothing else in that file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<starting_state>
Facts already established — do not spend context re-deriving these:

- `CLAUDE.md`'s current `## Conventions` block (lines 138-142, between the
  `<!-- GSD:conventions-start source:CONVENTIONS.md -->` and `<!-- GSD:conventions-end -->` HTML
  comment markers) reads exactly:
  ```
  ## Conventions

  Conventions not yet established. Will populate as patterns emerge during development.
  ```
  Only the paragraph line changes. The `## Conventions` heading and both HTML comment markers are
  preserved verbatim — they are GSD's own section-management markers, used to keep this file in
  sync with `.planning/CONVENTIONS.md`-style sources.
- `impeccable` is installed globally at `~/.claude/skills/impeccable` (confirmed present on this
  machine) — it is not a GSD skill and is not listed under CLAUDE.md's "Project Skills" section
  (which only covers repo-local `.claude/skills/`, `.agents/skills/`, etc. — `impeccable` lives
  outside that scope entirely, hence documenting it here in Conventions instead).
- The GSD commands `/gsd-sketch`, `/gsd-ui-phase`, and `/gsd-ui-review` all exist as installed GSD
  skills on this machine (confirmed via `~/.claude/skills/gsd-sketch`, `~/.claude/skills/gsd-ui-phase`,
  `~/.claude/skills/gsd-ui-review`) — safe to reference by name.
- CLAUDE.md's existing house style (see the "Technology Stack" and "Key Configuration Decisions"
  sections above Conventions) is terse: short lead sentences, then a markdown table or bullet list,
  no prose padding. Match that style — do not write narrative paragraphs.
- CLAUDE.md already states the project's hard accessibility floor under `### Constraints` >
  "**Accessibility (non-negotiable)**": "≥48px click targets; high contrast; ≥18px body fonts;
  keyboard navigable as fallback; no drag, hover-only, or precise-pointing interactions." The new
  Conventions text should reference this floor (not restate it in full) as the reason pure-critique
  `impeccable` calls are a strong fit for this project.
- CLAUDE.md already states the "GSD Workflow Enforcement" rule: "Before using Edit, Write, or other
  file-changing tools, start work through a GSD command." The new Conventions text must be
  consistent with, and explicitly reference, this existing rule rather than contradicting or
  duplicating it.
</starting_state>

<tasks>

<task type="auto">
  <name>Task 1: Replace the Conventions placeholder with the impeccable + GSD pairing convention</name>
  <files>CLAUDE.md</files>
  <action>
Open CLAUDE.md and locate the block between `&lt;!-- GSD:conventions-start source:CONVENTIONS.md --&gt;`
and `&lt;!-- GSD:conventions-end --&gt;`. Keep the `## Conventions` heading and both comment markers
exactly as they are. Replace only the placeholder paragraph ("Conventions not yet established. Will
populate as patterns emerge during development.") with new content covering, in terse table/bullet
style matching the rest of the file:

1. A short lead: `impeccable` (global Claude Code skill, not part of GSD — installed at
   `~/.claude/skills/impeccable`) supplies frontend design judgment and critique (hierarchy,
   spacing, contrast, motion, accessibility); GSD supplies planning, commit, and tracking
   discipline. State explicitly, referencing the existing "GSD Workflow Enforcement" section by
   name, that any actual file edit still goes through a GSD entry point — `impeccable` itself
   never Edits/Writes files directly; it informs judgment, a GSD command executes and commits.

2. A table (or tight bullet list) mapping five situations to the correct path — do not compress
   these into fewer than five rows, each is a distinct decision point:
   - Pure critique/audit with no edits -> invoke `impeccable` directly, any time, no GSD command
     needed, since nothing is written to the repo. Note this is a strong fit given the project's
     accessibility floor already stated in CLAUDE.md's Constraints (≥48px targets, ≥18px body
     text, high contrast, no hover-only/drag, voice-first) — reference that constraint, do not
     restate its full text.
   - Early UI idea / throwaway mockup -> `/gsd-sketch` for the disposable HTML sketch, then an
     `impeccable` critique pass on the sketch before it becomes a real plan.
   - Formal design contract for a phase -> `/gsd-ui-phase` (produces UI-SPEC.md) — hold that spec
     to `impeccable`'s bar explicitly, given the accessibility constraints.
   - Retroactive audit of already-built frontend -> `/gsd-ui-review` (6-pillar visual audit);
     optionally layer a direct `impeccable` pass for a second opinion.
   - Applying actual fixes -> route through `/gsd-quick` (small polish) or `/gsd-execute-phase`
     (planned work) so edits stay tracked; run `impeccable` first for design judgment on the
     specific component, then let the GSD command execute and commit.

Do not touch any other section of CLAUDE.md — not Project, Technology Stack, Architecture, Project
Skills, GSD Workflow Enforcement, or Developer Profile. Do not alter any other file.
  </action>
  <verify>
    <automated>grep -q "impeccable" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md</automated>
    <automated>grep -c "Conventions not yet established" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md | grep -qx 0</automated>
    <automated>grep -q "gsd-sketch" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md</automated>
    <automated>grep -q "gsd-ui-phase" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md</automated>
    <automated>grep -q "gsd-ui-review" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md</automated>
    <automated>grep -c "GSD:conventions-start" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md | grep -qx 1</automated>
    <automated>grep -c "GSD:conventions-end" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md | grep -qx 1</automated>
    <automated>grep -A1 "GSD:conventions-end" /Users/dp/Documents/GitHub/Health-Visualizer/CLAUDE.md | tail -1 | grep -q "GSD:architecture-start"</automated>
    <automated>git diff --stat CLAUDE.md | grep -c "CLAUDE.md" | grep -qx 1</automated>
  </verify>
  <done>
CLAUDE.md's `## Conventions` section (between the existing GSD comment markers, which are
untouched) documents the `impeccable` + GSD pairing: pure critique goes direct to `impeccable`,
sketches go through `/gsd-sketch`, formal design contracts through `/gsd-ui-phase`, retroactive
audits through `/gsd-ui-review`, and actual fixes through `/gsd-quick` or `/gsd-execute-phase` —
all consistent with the existing "GSD Workflow Enforcement" section. No other section of CLAUDE.md
changed, and no code files changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None introduced | This plan edits one markdown documentation section in a project instruction file. No code path, network boundary, input parser, or dependency is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260825-h9l-01 | Tampering | CLAUDE.md (project instructions) | accept | Documentation-only change reviewed by the developer before merge via normal git diff; no runtime behavior, no secrets, no external input involved. |
</threat_model>

<verification>
Run from the repo root after the task completes:

```
grep -q "impeccable" CLAUDE.md
grep -c "Conventions not yet established" CLAUDE.md | grep -qx 0
git diff CLAUDE.md
```

Eyeball the `git diff` output: it must show changes confined to the lines between
`<!-- GSD:conventions-start source:CONVENTIONS.md -->` and `<!-- GSD:conventions-end -->`, with
every other section's text byte-identical to before.
</verification>

<success_criteria>
- [ ] CLAUDE.md's `## Conventions` section no longer contains the "not yet established" placeholder
- [ ] The new section documents the `impeccable` + GSD pairing convention covering all five paths
      (pure critique, sketch, formal design contract, retroactive audit, applying fixes)
- [ ] The new section explicitly states `impeccable` never Edits/Writes files itself, consistent
      with the existing "GSD Workflow Enforcement" section
- [ ] The new section is in the same terse table/bullet house style as the rest of CLAUDE.md
- [ ] No other section of CLAUDE.md changed; no code files changed
</success_criteria>

<output>
Create `.planning/quick/260825-h9l-document-claude-md-convention-pairing-im/260825-h9l-SUMMARY.md`
when done.
</output>
