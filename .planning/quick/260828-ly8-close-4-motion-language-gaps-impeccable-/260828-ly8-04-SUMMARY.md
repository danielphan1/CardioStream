---
phase: quick-260828-ly8
plan: 04
subsystem: ui
tags: [react, tailwind, vitest, playwright, motion, accessibility, records]

# Dependency graph
requires:
  - phase: quick-260828-ly8 (plan 01-03, sibling plans in same quick task)
    provides: ChartDeck.tsx's proven FadeSwap pattern (this plan mirrors it, does not depend on the sibling plans' output)
provides:
  - AddRecordPage's Lab/Incident/Procedure field-set swap now fades in via a 250ms opacity+scale transition (local FadeSwap, mirroring ChartDeck.tsx's already-shipped pattern) instead of snapping instantly into view
  - The fade replays on every record-type switch (not just first mount), proven both by unit tests and a live multi-switch Playwright walkthrough
  - Existing fieldsKey-driven reset mechanics (post-submit clearing, silent type-switch discard, D-03) relocated to a new wrapping div, unchanged in effect
affects: [add-record-page, motion-language, impeccable-animate-survey]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Local FadeSwap helper (double-rAF shown-state flip, motion-reduce-gated 250ms opacity+scale transition) duplicated per-file rather than extracted to a shared module, matching this quick task's plan-level file-ownership isolation constraint (deliberate, documented tradeoff)"

key-files:
  created: []
  modified:
    - frontend/src/components/AddRecordPage.tsx
    - frontend/src/components/AddRecordPage.test.tsx

key-decisions:
  - "Combined Task 1's TDD RED and GREEN phases into one final commit (rather than separate test()/feat() commits) to satisfy Task 2's own explicit verify contract (`git show --name-only HEAD` must equal exactly the two AddRecordPage files) and the plan's success criteria ('committed in one commit'). RED was still confirmed failing and GREEN confirmed passing before squashing — only the git-commit granularity was consolidated, mirroring the same consolidation decision made in the 260827-kir sibling quick task."
  - "Live verification used an ephemeral frontend/.env.local (gitignored via frontend/.gitignore's *.local pattern) to point the isolated Vite dev server at the isolated backend on port 8014, deleted immediately after the verification script ran."

patterns-established: []

requirements-completed: []

# Metrics
duration: ~35min
completed: 2026-08-28
---

# Quick Task 260828-ly8 Plan 04: Close motion-language gap #4 (AddRecordPage FadeSwap) Summary

**AddRecordPage's Lab/Incident/Procedure field-set swap now replays ChartDeck.tsx's exact 250ms opacity+scale FadeSwap transition on every record-type switch, with the pre-existing reset key relocated (not altered) onto a new wrapping div.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-28T18:44Z (worktree base correction + reads)
- **Completed:** 2026-08-28T18:52Z
- **Tasks:** 2 (both `type="auto"`, no checkpoints)
- **Files modified:** 2

## Accomplishments
- Added a module-level `FadeSwap` function to `AddRecordPage.tsx` that mirrors `ChartDeck.tsx`'s own `FadeSwap` (double-rAF `shown` state, `transition-[opacity,transform] duration-[250ms] ease-in-out motion-reduce:transition-none`, `scale-95 opacity-0` → `scale-100 opacity-100`), minus ChartDeck's `h-full w-full` sizing classes since AddRecordPage's field-set block is a natural-height form section
- Relocated `key={fieldsKey}` from the three individual `LabFields`/`IncidentFields`/`ProcedureFields` elements onto one new outer `<div key={fieldsKey}>` wrapping the `<FadeSwap>` — the key change still fully unmounts/remounts everything below it, so D-03's post-submit clearing and silent type-switch discard are unaffected, and the fade now replays on every swap as a direct consequence
- `handleTypeChange`, `handleSubmit`, the `fieldsKey` computation, `isSubmitting`, `canSubmit`, the type-switcher button group, the Submit button, and the success/error `<section>` blocks are byte-for-byte unchanged
- Two new `AddRecordPage.test.tsx` assertions (inside the existing `describe` block, no pre-existing tests touched) lock the entrance-lifecycle (opacity-0/scale-95 → opacity-100/scale-100) and the structural transition-class contract
- Live Playwright walkthrough against isolated dev servers (frontend :5184, backend :8014) confirmed: initial Lab mount fades in and settles at opacity 1; clicking Incident is caught mid-transition at opacity 0 / `scale-95` / `opacity-0` className immediately after the click, then settles at opacity 1 — proving the fade genuinely replays, not a one-shot first-mount animation; Procedure settles the same way; switching back to Lab shows an empty "Test name" field (silent-discard intact); and under `emulateMedia({ reducedMotion: "reduce" })` the Lab → Incident switch still settles at opacity 1 (state change never suppressed)

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED/GREEN, TDD-sequenced): Wrap the field-set swap in FadeSwap, relocate the reset key** — RED confirmed via `npx vitest run src/components/AddRecordPage.test.tsx` (2 new tests failed against current source, 6 pre-existing tests passed), then GREEN confirmed (all 8 pass) after the source edit. Landed together with Task 2 in the single combined commit below (see Decisions Made).
2. **Task 2: Live-verify across all three record types and reduced-motion, then commit** - `5495650` (`fix(frontend): add ChartDeck-mirrored FadeSwap transition to AddRecordPage's field-set swap (impeccable animate survey)`)

**Plan metadata:** pending (orchestrator commits SUMMARY.md/STATE.md separately)

## Files Created/Modified
- `frontend/src/components/AddRecordPage.tsx` - added `FadeSwap` helper + `ReactNode`/`useEffect` imports; replaced the three individually-keyed conditional field-set elements with `<div key={fieldsKey}><FadeSwap>{...three conditionals, key removed...}</FadeSwap></div>`
- `frontend/src/components/AddRecordPage.test.tsx` - added `waitFor` to the existing `@testing-library/react` import; appended two new `it(...)` cases to the existing `describe("AddRecordPage (Plan 08-03)", ...)` block asserting the FadeSwap wrapper's entrance lifecycle and transition-class contract

## Decisions Made
- Squashed the RED (`test(...)`) and GREEN (`feat(...)`) commits into the single `fix(...)` commit specified by Task 2's `<action>` and locked by its `<verify>` (`git show --name-only HEAD` must equal exactly `AddRecordPage.test.tsx AddRecordPage.tsx`). RED-then-GREEN sequencing was still followed and confirmed at each step (`git reset --soft HEAD~2` combined the two intermediate commits without altering file contents or losing the verified RED/GREEN transitions) — only the git history granularity changed, matching this quick task's plan-level convention already established in the sibling `260827-kir` task.
- Used a gitignored `frontend/.env.local` (`*.local` pattern in `frontend/.gitignore`) rather than a scratch-directory Vite instance to point the isolated port-5184 dev server at the isolated port-8014 backend — deleted immediately after the verification script completed, confirmed via `git status --short` showing zero diff.

## Deviations from Plan

None - plan executed exactly as written. `FadeSwap`'s implementation, the key relocation, and the test assertions match the plan's `<action>`/`<behavior>` spec verbatim (including dropping ChartDeck's `h-full w-full` sizing classes as directed).

## Issues Encountered
- `frontend/node_modules` was absent in this fresh worktree (gitignored, as expected) — ran `npm install` before any verification/test commands could run. Routine worktree setup, not a plan deviation.
- Worktree HEAD was found behind the plan's expected base commit (`d6297ac...`) at agent startup; corrected via `git reset --hard` to the correct base per the mandatory `<worktree_branch_check>` step, after confirming HEAD was properly attached to the per-agent branch `worktree-agent-acc8a9997672a1450` (not a protected ref). Routine worktree setup, not a plan deviation.
- `backend/.env` and `backend/dev.db` are gitignored and absent from this worktree's `backend/` directory — copied from the main checkout (`/Users/dp/Documents/GitHub/Health-Visualizer/backend/`) into the scratch verification directory, per the plan's own recipe. Deleted afterward along with all other scratch verification artifacts.
- `npm install --no-save playwright` in the scratch directory resolved into the parent scratchpad's `node_modules` (npm's directory-walk-up behavior) rather than the immediate scratch subdirectory — worked correctly once the script imported from that resolved path; left in place as shared session-scratch tooling (outside the repo, not committed).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- No follow-on work required by this fix; the FadeSwap wrapper, relocated key, and its regression tests are self-contained and fully verified live.
- Full regression sweep confirmed zero collateral impact: 30 test files / 365 tests passed (363 baseline + 2 net-new), `tsc -b` exit 0 with zero errors, `oxlint` exit 0 with exactly the same 3 pre-existing unrelated warnings (`records/{ProcedureFields,IncidentFields,LabFields}.tsx`).
- This closes gap #4 of the 4-plan impeccable animate survey queued in this quick task (`260828-ly8`); plans 01 (GuideOverlay fade), 02 (ChartTooltip entrance), and 03 (DateRangePicker fade-in) are separate, independent parallel executors' responsibility — no file overlap, no coordination needed with this plan.

---
*Phase: quick-260828-ly8*
*Completed: 2026-08-28*

## Self-Check: PASSED

Both modified source files (`frontend/src/components/AddRecordPage.tsx`, `frontend/src/components/AddRecordPage.test.tsx`) exist on disk; commit `5495650` verified present in `git log --oneline --all` and contains exactly those two files (`git show --name-only --format='' HEAD`).
