---
phase: quick-260828-ly8
plan: 03
subsystem: ui
tags: [react, motion, accessibility, tailwind, requestAnimationFrame, vitest, playwright]

# Dependency graph
requires: []
provides:
  - "DateRangePicker's own root div fades in from opacity-0 to opacity-100 over 250ms on every mount, mirroring the double-rAF idiom already established by ChartDeck.tsx's FadeSwap"
  - "motion-reduce:transition-none skips the animated transition under prefers-reduced-motion while the state change (full visibility) is never suppressed"
affects: [impeccable-animate-survey-gap-3]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Entrance-only opacity fade on a component's own root (no wrapper div needed) driven by a local `shown` boolean flipped via double requestAnimationFrame in a useEffect with an empty dependency array — safe here specifically because the parent's `{condition && <Component />}` mount pattern already guarantees a fresh instance (and therefore a fresh `shown = false`) on every open, no key/reset coordination required"

key-files:
  created:
    - frontend/src/components/DateRangePicker.test.tsx
  modified:
    - frontend/src/components/DateRangePicker.tsx

key-decisions:
  - "Followed the plan's explicit single-commit design: TDD RED/GREEN cycle for Task 1 was done in the working tree without intermediate commits, then Task 2's live verification (through FilterBar's real Custom… toggle) gated the one combined commit containing both DateRangePicker.tsx and DateRangePicker.test.tsx — matching the plan's own automated Task 2 verify script, which checks HEAD contains exactly these two files."
  - "Used the main checkout's existing backend/.venv (read-only, not a git operation) to run the isolated verification backend server, since this worktree has no venv of its own and installing one was out of scope for a motion-only frontend fix."

requirements-completed: []

# Metrics
duration: ~15min
completed: 2026-08-29
---

# Quick Task 260828-ly8 Plan 03: DateRangePicker fade-in Summary

**Added a 250ms opacity fade-in-on-mount to DateRangePicker's own root div (impeccable animate survey gap #3), live-verified through FilterBar's real Custom… toggle including a reduced-motion pass.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-08-29T01:52:26Z
- **Tasks:** 2/2 completed
- **Files modified:** 2 (1 new test file, 1 modified component)

## Accomplishments
- FilterBar's `{customOpen && <DateRangePicker .../>}` disclosure panel now fades in (opacity-0 → opacity-100, ≤250ms) instead of snapping instantly into view, closing the last zero-transition motion gap flagged by the impeccable animate survey.
- Fade replays on every open (not a one-shot animation) because FilterBar's conditional mount already gives DateRangePicker a fresh instance — verified live by closing and reopening the panel three times through the real "Custom…" button.
- Under emulated `prefers-reduced-motion: reduce`, the panel still becomes fully visible (opacity reaches 1) — only the animated transition is skipped via `motion-reduce:transition-none`, the state change itself is never suppressed.
- Close stays an instant unmount exactly as before — no height/layout/margin animation was added or attempted for the panel's real `react-day-picker` calendar (dynamic height would fight the browser's layout engine, per the project's own motion guidance).
- `fromText`/`toText` state, `handleSelect`, `handleApply`, and the `DayPicker` configuration are byte-for-byte unchanged from the pre-edit source.

## Task Commits

Task 1's TDD RED/GREEN cycle (test file + implementation) was carried in the working tree without an intermediate commit, per this plan's explicit Task 2 instruction and its automated verify script (`git show --name-only --format='' HEAD` must equal exactly the two DateRangePicker files). Task 2's live verification gated the single combined commit:

1. **Task 1 + Task 2 combined** — `b460dbc` (fix): `fix(frontend): add opacity fade-in to DateRangePicker's custom-range panel (impeccable animate survey)`

_Note: this deviates from the generic per-task-commit default because the plan's own Task 2 verify step explicitly checks for a single HEAD commit containing exactly the two files — see Deviations below._

## Files Created/Modified
- `frontend/src/components/DateRangePicker.tsx` - Added `shown` boolean state (double-rAF-driven, mounted via an empty-dependency-array `useEffect`) controlling `opacity-0`/`opacity-100` on the component's own root div, alongside unconditional `transition-opacity duration-[250ms] ease-in-out motion-reduce:transition-none` classes.
- `frontend/src/components/DateRangePicker.test.tsx` - New test file: entrance-lifecycle test (starts opacity-0, reaches opacity-100 via `waitFor`), structural transition-class lock test, and a baseline smoke test (From/To inputs + disabled Apply button render correctly).

## Decisions Made
- Commit structure: followed the plan's explicit instruction literally — Task 1 has no commit step in the plan text, and Task 2's automated verify checks for a single HEAD commit with exactly the two DateRangePicker files. Initially committed a separate `test(...)` RED commit per the generic TDD gate guidance, then recognized the conflict with this plan's specific verify script, soft-reset it, and re-committed everything as one commit per the plan's literal instructions.
- Live verification used the main checkout's existing `backend/.venv` (not committed, not a git operation) since this isolated worktree has no Python virtualenv of its own — installing one from scratch was out of scope for a client-side-only motion fix.

## Deviations from Plan

**1. [Rule 3 - blocking issue] Backend `.env`/`dev.db` and Python venv did not exist in this isolated worktree**
- **Found during:** Task 2 (live verification server setup)
- **Issue:** The plan's live-verification recipe assumes a `backend/.env` and `backend/dev.db` exist to copy into a scratch location, and that a Python environment is available to run `uvicorn`. This worktree, being freshly created, had neither `backend/.env`/`dev.db` (both are gitignored, dev-only artifacts) nor its own `.venv`.
- **Fix:** Copied `backend/.env` and `backend/dev.db` from the main checkout into a scratch location (as the plan already directs for the `.env`/`dev.db` copy step), edited the scratch `.env`'s `CORS_ORIGINS` to `["http://localhost:5183"]`, and ran the isolated backend server using the main checkout's existing `backend/.venv/bin/uvicorn` (a read-only reuse of an existing local tool install, not a git operation and not a change to this worktree's committed files).
- **Files modified:** None (verification-only, scratch artifacts deleted after use).
- **Commit:** N/A (no repo files changed by this fix).

**2. [Plan-literal deviation, non-bug] Split-then-recombined commit for the TDD cycle**
- **Found during:** Task 1 → Task 2 transition
- **Issue:** The generic TDD execution guidance (RED/GREEN commit gates) initially led to a separate `test(...)` commit for the failing tests. This plan's Task 2 has its own explicit, automated verify (`git show --name-only --format='' HEAD` must equal exactly `DateRangePicker.test.tsx DateRangePicker.tsx`), which a two-commit RED/GREEN history would fail.
- **Fix:** Soft-reset the premature RED commit (`git reset --soft HEAD~1`) before starting Task 2, restoring the uncommitted-but-staged state, then followed Task 2's literal instructions to produce one combined commit after live verification passed.
- **Files modified:** None beyond the two files already in scope.
- **Commit:** `b460dbc` (the single resulting commit).

## Known Stubs

None.

## Threat Flags

None. This plan's own `<threat_model>` already covers the only new surface (the local `shown` state, derived purely from an unconditional mount-time effect with no externally-controlled input path) and the ephemeral, `--no-save`, never-committed Playwright install used for live verification.

## Live Verification Evidence

Stood up isolated dev servers (frontend :5183, backend :8013) with a scratch-copied `.env` (`CORS_ORIGINS=["http://localhost:5183"]`) and `dev.db`. Used Playwright (installed `--no-save` in a scratch directory, resolved from cache, never touching `frontend/package.json`) to drive the real app through `FilterBar`'s "Custom…" toggle:

1. Clicked "Custom…" → polled the panel root's computed `opacity` until it read `"1"` (fade-in completed) → screenshot confirmed the From/To inputs, calendar, and Apply button fully visible and legible.
2. Clicked "Custom…" again → confirmed the `From` input detached from the DOM promptly (instant unmount, unchanged). Reopened → confirmed the root's className contained `opacity-0` immediately after the click, then polled until it reached `opacity-100` — proving the fade replays on every open.
3. Closed, emulated `prefers-reduced-motion: reduce`, reopened → confirmed the panel still reached `opacity: 1` (full visibility never suppressed) → screenshot confirmed the panel fully visible under reduced motion.

All checks passed. Scratch script, screenshots, `.env`/`dev.db` copies, and both isolated dev servers were deleted/torn down afterward — no verification artifacts remain in the repo.

Full regression suite after both tasks: `npx vitest run` → 31 test files passed, 366 tests passed (363 baseline + 3 new); `npx tsc -b` → exit 0, zero errors; `npx oxlint` → exit 0, exactly the same 3 pre-existing unrelated `react-hooks(exhaustive-deps)` warnings in `src/components/records/{ProcedureFields,LabFields,IncidentFields}.tsx`, zero new warnings.

## Self-Check: PASSED

- FOUND: frontend/src/components/DateRangePicker.tsx (modified, fade-in present)
- FOUND: frontend/src/components/DateRangePicker.test.tsx (created, 3 tests)
- FOUND commit b460dbc in `git log --oneline --all`
