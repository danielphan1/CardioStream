---
phase: quick-260828-2l6
plan: 01
subsystem: ui
tags: [recharts, react, chart-labels, accessibility, impeccable]

# Dependency graph
requires:
  - phase: quick-260827-25p
    provides: bandLabel() label-vs-label collision fix (Elevated/Stage 1 dropped to no label) — this plan builds on that scope boundary, does not revert it
provides:
  - estimateChipWidth(text, fontSize) pure sizing helper in chartData.ts
  - makeBandLabelChip(cat) factory in BPTimeline.tsx rendering a solid category-color pill (rect + text) as the ReferenceArea label content
affects: [BPTimeline.tsx, any future BP Timeline chart-band-label work]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Custom SVG label-content factory functions closing over viewBox (mirrors the existing makeEndLabel pattern) for Recharts label overrides that need a solid background, not bare text"]

key-files:
  created: []
  modified:
    - frontend/src/lib/chartData.ts
    - frontend/src/lib/chartData.test.ts
    - frontend/src/components/charts/BPTimeline.tsx

key-decisions:
  - "Consolidated Task 1's TDD RED/GREEN sub-commits into Task 3's single final commit (chose the plan's explicit, automated file-count verification over the generic per-gate TDD commit default) — RED-then-GREEN discipline was still followed and verified (3 failing tests confirmed before implementation, 24/24 passing after), only the git history granularity changed"

patterns-established:
  - "Pattern: chart-band label chips reuse categoryColor()/CHIP_TEXT from palette.ts rather than inventing new colors — same rule as FilterBar/StatsStrip chips"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-08-28
---

# Quick Task 260828-2l6: BP Timeline Band-Label Chip Backing Summary

**Replaced bare-text AHA band labels on the BP Timeline hero chart with solid category-color pill chips (rect + text SVG), so plotted BP lines can no longer visually cut through a band's label.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-08-28T09:05:03Z
- **Completed:** 2026-08-28T09:10:06Z
- **Tasks:** 3 (3/3 complete)
- **Files modified:** 3

## Accomplishments
- Added `estimateChipWidth(text, fontSize)` pure sizing helper (`chartData.ts`) with 3 new unit tests, following TDD RED→GREEN discipline
- Replaced `bandLabel()` (bare SVG `<text>`, Recharts `label` prop) with `makeBandLabelChip(cat)`, a factory returning a solid rounded-pill `<rect>` + centered `<text>` label content — same visual language as FilterBar/StatsStrip category chips
- Wired the new chip to the 4 remaining labeled bands (Hypotension, Normal, Stage 2, Hypertensive Crisis); Elevated and Stage 1 correctly remain label-less, untouched from the prior fix (quick task 260827-25p)
- Zero clinical-color/CSS-var changes — chips reuse the existing `categoryColor()`/`CHIP_TEXT` tokens

## Task Commits

Each task's changes are captured in a single consolidated commit per the plan's Task 3 design (see Deviations below for why Task 1's TDD sub-commits were folded in):

1. **Task 1 + Task 2 + Task 3: estimateChipWidth helper, BandLabelChip render, single commit** - `852e0a9` (fix)

_TDD RED/GREEN discipline for Task 1 was followed and verified live (3 failing tests confirmed, then 24/24 passing) before being folded into the final consolidated commit — see Deviations._

## Files Created/Modified
- `frontend/src/lib/chartData.ts` - Added `CHIP_CHAR_WIDTH_FACTOR` constant + exported `estimateChipWidth(text, fontSize)` pure sizing helper
- `frontend/src/lib/chartData.test.ts` - Added `describe("estimateChipWidth", ...)` with 3 cases (mid-length name, longest name, degenerate empty string)
- `frontend/src/components/charts/BPTimeline.tsx` - Removed `bandLabel()` + its JSDoc; added `BandLabelChipProps` type, `CHIP_FONT_SIZE`/`CHIP_PAD_X`/`CHIP_PAD_Y`/`CHIP_OFFSET` constants, and `makeBandLabelChip(cat)` factory; rewired the 4 remaining labeled `ReferenceArea` elements to `label={hero ? makeBandLabelChip("<Category>") : undefined}`; added a top-of-file doc-comment bullet documenting the zIndex rationale

## Decisions Made
- Consolidated the TDD sub-commits into one final commit for the plan's Task 3 (see Deviations) — the plan's own automated verify steps require `git show --stat HEAD` / `git show --name-only HEAD` to show exactly the 3 planned files in a single commit, which is incompatible with also landing 2 of those files in earlier separate RED/GREEN commits. Honored the plan's specific, verified requirement over the generic per-gate TDD commit default.
- No REFACTOR commit needed — the GREEN implementation was already clean (no duplication, single responsibility, JSDoc included from the start).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed frontend dependencies (`npm install`)**
- **Found during:** Task 1 (running the RED test to confirm failure)
- **Issue:** This worktree had no `node_modules/` — `npx vitest` failed at Vite config load (`Cannot find package 'vite'`), not a code issue but missing local dependency install.
- **Fix:** Ran `npm install` in `frontend/` — installed from the existing `package-lock.json`, no new/different packages added, nothing that requires the package-legitimacy checkpoint.
- **Files modified:** None tracked (node_modules is gitignored; package-lock.json unchanged, `npm install` was a no-op on the lockfile).
- **Verification:** `npx vitest run` subsequently ran successfully.
- **Committed in:** N/A (no file changes to commit — node_modules is gitignored)

**2. [Process note, not a Rule 1-4 deviation] Consolidated Task 1's TDD commits into Task 3's single commit**
- **Found during:** Task 3 (staging the final commit)
- **Issue:** Task 1 (`tdd="true"`) produced two separate commits (`test(...)` RED, `feat(...)` GREEN) per the standard TDD execution flow, each landing one of the 3 planned files. Task 3's explicit instructions and automated verify steps require a single commit containing exactly all 3 planned files (`chartData.ts`, `chartData.test.ts`, `BPTimeline.tsx`) — checked via `git show --name-only HEAD | wc -l == 3`.
- **Fix:** `git reset --soft` back to the plan's pre-dispatch base commit (preserving all working-tree changes across the 3 files), then created the single consolidated commit `852e0a9` as Task 3 specifies. The RED→GREEN TDD workflow discipline was still followed and independently verified in the execution transcript (3 failing tests confirmed before implementation; 24/24 passing confirmed after) — only the git commit granularity changed to match the plan's explicit, automated requirement.
- **Files modified:** `frontend/src/lib/chartData.ts`, `frontend/src/lib/chartData.test.ts`, `frontend/src/components/charts/BPTimeline.tsx`
- **Verification:** `git show --name-only --format='' HEAD | wc -l` → `3`; full test suite 359/359 passing; `tsc -b` and `oxlint` clean.
- **Committed in:** `852e0a9`

---

**Total deviations:** 2 (1 auto-fixed blocking dependency install, 1 commit-strategy process reconciliation — no code/behavior impact)
**Impact on plan:** No scope creep. The dependency install was required to run any verification at all. The commit consolidation only changes git history shape, not code — TDD discipline (write failing test, confirm RED, implement, confirm GREEN) was fully followed before the commit was made.

## Issues Encountered
None beyond the two items documented above.

## User Setup Required

None - no external service configuration required.

## Human Verification (non-blocking, pending)

The plan's Task 2 `<verify>` block includes a `<human-check>`: with both servers running locally, visually confirm on the default "All data" view that no plotted line crosses through any of the four band-label chips (Hypotension, Normal, Stage 2, Hypertensive Crisis), and that Elevated/Stage 1 still show no label. This quick task run is autonomous and did not spin up the local dev/backend stack to perform this check — per this run's constraints, it is noted here as pending rather than blocking. All automated verification (structural grep checks, unit tests, type-check, lint, Data-Is-Locked color diff) passed; the live-render check is the one remaining verification step before this fix can be considered fully closed per the plan's own `<verification>` section.

## Next Phase Readiness

BP Timeline hero chart's 4 remaining labeled bands now render collision-proof solid chip labels. No blockers for future work. Recommend the human visual check above be performed at the next opportunity to close out the plan's `<verification>` section in full.

---
*Phase: quick-260828-2l6*
*Completed: 2026-08-28*

## Self-Check: PASSED

- FOUND: frontend/src/lib/chartData.ts
- FOUND: frontend/src/lib/chartData.test.ts
- FOUND: frontend/src/components/charts/BPTimeline.tsx
- FOUND: commit 852e0a9
