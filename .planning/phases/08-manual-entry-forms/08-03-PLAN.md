---
phase: 08-manual-entry-forms
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - frontend/src/store/view.ts
  - frontend/src/components/Header.tsx
  - frontend/src/App.tsx
  - frontend/src/components/AddRecordPage.tsx
  - frontend/src/components/AddRecordPage.test.tsx
  - .planning/phases/08-manual-entry-forms/08-VALIDATION.md
autonomous: true
requirements: [OVERLAY-02]

must_haves:
  truths:
    - "A caregiver can reach the Add Record view from the Header, from anywhere on the dashboard, without a page reload or URL change (D-01)."
    - "A caregiver can select Lab, Incident, or Procedure via a ≥48px segmented control; selecting a type swaps the visible field set (D-02)."
    - "A caregiver can submit a valid Lab, Incident, or Procedure and see an inline 'Added 1 {type}.' confirmation that fires as soon as the POST mutation resolves successfully — no forced refetch, no page reload (D-04/D-05, ROADMAP Phase 8 success criterion 4)."
    - "After a successful submit, the form clears and the caregiver stays on the Add Record view (D-03) — it does not navigate back to the dashboard."
    - "The Submit button is aria-disabled until the active field-set reports a valid, ready-to-send body (D-06) — never reachable via keyboard activation while invalid."
    - "A failed submit shows one fixed, UI-authored error sentence — never a raw ApiError message, status code, or backend 422 detail (mirrors UploadPage's T-05-13 discipline)."
  artifacts:
    - path: "frontend/src/store/view.ts"
      provides: "View widened to \"dashboard\" | \"upload\" | \"records\""
      contains: "\"records\""
    - path: "frontend/src/components/Header.tsx"
      provides: "Three-state nav: Upload + Add Record shown together on dashboard; single Back to dashboard elsewhere"
      contains: "go(\"records\")"
    - path: "frontend/src/App.tsx"
      provides: "RecordsView branch mounting AddRecordPage alongside the existing Dashboard/UploadView branches"
      contains: "view === \"records\""
    - path: "frontend/src/components/AddRecordPage.tsx"
      provides: "Type switcher + active field-set + Submit + inline confirmation/error, the phase's primary deliverable"
      min_lines: 100
    - path: "frontend/src/components/AddRecordPage.test.tsx"
      provides: "Behavior coverage for type-switch discard, disabled-until-valid gating, successful submit + clear, and the generic-error branch, across all three record types"
      min_lines: 80
    - path: ".planning/phases/08-manual-entry-forms/08-VALIDATION.md"
      provides: "Validation strategy closed out once this plan's full frontend suite is green — nyquist_compliant/wave_0_complete flipped to true, Approval no longer pending"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "frontend/src/components/Header.tsx"
      to: "frontend/src/store/view.ts"
      via: "go(\"records\") onClick"
      pattern: "go\\(\"records\"\\)"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/AddRecordPage.tsx"
      via: "RecordsView() renders <AddRecordPage />"
      pattern: "<AddRecordPage"
    - from: "frontend/src/components/AddRecordPage.tsx"
      to: "frontend/src/hooks/useCreateRecord.ts"
      via: "useCreateLab() / useCreateIncident() / useCreateProcedure(), mutate(draftBody, { onSuccess, onError })"
      pattern: "useCreate(Lab|Incident|Procedure)"
    - from: "frontend/src/components/AddRecordPage.tsx"
      to: "frontend/src/components/records/{Lab,Incident,Procedure}Fields.tsx"
      via: "key={`${recordType}-${resetSeq}`} onDraftChange={setDraftBody}"
      pattern: "key=\\{`\\$\\{recordType\\}-\\$\\{resetSeq\\}`\\}"
---

<objective>
Wire the new "Add Record" surface into the app's navigation (three-state Header, `view.ts`
union widening, `App.tsx` routing), and build `AddRecordPage.tsx` — the container that mounts
Plan 08-02's field-sets by type, gates a shared Submit button on their reported draft body,
calls the correct one of Plan 08-01's `useCreateLab`/`useCreateIncident`/`useCreateProcedure`
mutation hooks, and renders the D-03/D-04-locked inline confirmation-and-clear / error loop.

This is the plan where OVERLAY-02 becomes true end-to-end: a caregiver can actually reach the
page, fill a form, submit it, and see it land — everything Plans 08-01/08-02 built becomes
observable here for the first time.

Purpose: close the loop CONTEXT.md D-01 through D-08 describe — navigation, type switch, submit
gating, confirmation, and reset — using ONLY the already-built pieces from this phase's two prior
plans, no new business logic.
Output: `AddRecordPage.tsx`, `AddRecordPage.test.tsx`, the three small navigation-file edits
that make the page reachable, and the phase's `08-VALIDATION.md` closed out once the full
frontend suite is confirmed green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-manual-entry-forms/08-CONTEXT.md
@.planning/phases/08-manual-entry-forms/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation — widen View, reshape Header to three states, wire App.tsx</name>
  <files>frontend/src/store/view.ts, frontend/src/components/Header.tsx, frontend/src/App.tsx</files>
  <read_first>
    frontend/src/store/view.ts
    frontend/src/components/Header.tsx
    frontend/src/App.tsx
    .planning/phases/08-manual-entry-forms/08-UI-SPEC.md (section: "Header nav (3-state, resolves the binary-toggle gap — RESEARCH Pattern 1)")
  </read_first>
  <action>
    In `frontend/src/store/view.ts`, widen `export type View = "dashboard" | "upload";` to
    `export type View = "dashboard" | "upload" | "records";`. No other change — `go()` already
    accepts any `View` value.

    In `frontend/src/components/Header.tsx`, replace the current binary toggle (the single
    `<button onClick={() => go(onDashboard ? "upload" : "dashboard")}>` block, computed from
    `const onDashboard = view === "dashboard"`) with a three-state shape: when `onDashboard` is
    true, render TWO buttons side by side — the existing "Upload" button completely unchanged
    (`onClick={() => go("upload")}`, `Upload` icon, same className), and a new second button with
    `onClick={() => go("records")}`, a `ClipboardPlus` icon (add to the existing `lucide-react`
    import line alongside `LogOut, Moon, Sailboat, Sun, Upload`), and text "Add Record" — reuse
    the EXACT existing button className string verbatim (`"flex items-center gap-2 rounded-lg
    border-2 border-[var(--color-ink)] bg-[var(--color-sky)] px-4 py-2 text-[20px] font-bold
    text-[var(--color-ink)]"`). When `onDashboard` is false (either `"upload"` or `"records"`),
    render exactly one button — `onClick={() => go("dashboard")}`, `Upload` icon, text "Back to
    dashboard" — identical to the current else-branch, completely unchanged. No direct
    upload↔records cross-link. Both new/kept buttons stay exempt from the 48px floor (discreet
    caregiver-admin zone, same rationale already documented in this file's header comment).

    In `frontend/src/App.tsx`, import `AddRecordPage` from `"./components/AddRecordPage"`
    alongside the existing `UploadPage` import. Add a new function `RecordsView()` immediately
    after `UploadView()`, copying `UploadView`'s exact body but swapping `<UploadPage />` for
    `<AddRecordPage />` (same `<div className="min-h-screen bg-[var(--color-foam)]"><Header
    />...</div>` wrapper). In the `App()` function, add `if (view === "records") return
    <RecordsView />;` immediately after the existing `if (view === "upload") return
    <UploadView />;` line, before the `Dashboard` fallback.
  </action>
  <acceptance_criteria>
    - `frontend/src/store/view.ts`'s `View` type includes `"records"` as a third member.
    - `frontend/src/components/Header.tsx` renders both "Upload" and "Add Record" buttons when
      `view === "dashboard"`, and exactly one "Back to dashboard" button for every other view
      value — source inspection confirms no nested-ternary reshape of the old binary toggle
      remains (the old single `<button onClick={() => go(onDashboard ? "upload" : "dashboard")}>`
      block no longer exists).
    - `frontend/src/App.tsx` has a `RecordsView` function and an `if (view === "records") return
      <RecordsView />;` branch, positioned before the `Dashboard` fallback.
    - `cd frontend && npx tsc -b` exits 0 (this task alone will NOT compile clean until Task 2
      creates `AddRecordPage.tsx` in this same plan — run the type-check as this task's final
      step, after Task 2, or accept a transient error on `AddRecordPage` import until Task 2
      lands; the plan-level verification below is the authoritative gate).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "go(\"records\")" src/components/Header.tsx</automated>
  </verify>
  <done>view.ts, Header.tsx, and App.tsx all reference the new "records" view consistently; Header shows two buttons on the dashboard and one everywhere else; App.tsx routes view==="records" to a RecordsView that mounts AddRecordPage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AddRecordPage — type switcher, submit gating, confirmation/error, reset</name>
  <files>frontend/src/components/AddRecordPage.tsx, frontend/src/components/AddRecordPage.test.tsx, .planning/phases/08-manual-entry-forms/08-VALIDATION.md</files>
  <read_first>
    frontend/src/components/UploadPage.tsx
    frontend/src/components/UploadPage.test.tsx
    frontend/src/components/CommandBar.test.tsx
    frontend/src/components/FilterBar.tsx
    frontend/src/components/records/LabFields.tsx
    frontend/src/components/records/IncidentFields.tsx
    frontend/src/components/records/ProcedureFields.tsx
    frontend/src/hooks/useCreateRecord.ts
    frontend/src/api/client.ts
    .planning/phases/08-manual-entry-forms/08-VALIDATION.md
    .planning/phases/08-manual-entry-forms/08-UI-SPEC.md (sections: "Copywriting Contract", "Phase-Specific Component Contract")
  </read_first>
  <behavior>
    - On mount, the type switcher shows Lab/Incident/Procedure buttons with `role="group"
      aria-label="Record type"`, Lab pressed (`aria-pressed="true"`) by default, and the Lab
      field-set is mounted; Submit is `aria-disabled="true"` (no fields filled yet).
    - Filling Lab's required fields (date + test name) flips Submit to `aria-disabled="false"`
      (via `LabFields`' `onDraftChange` reporting a non-null body).
    - Clicking Incident after partially filling Lab: the Lab field-set unmounts (its typed values
      are gone — verified by switching back to Lab and finding the test-name input empty again),
      Incident's field-set mounts fresh, Submit returns to `aria-disabled="true"`.
    - Submitting a valid Lab (mocked `postLab` resolves) shows a `role="status"` region containing
      "Added 1 lab result.", and the Lab field-set remounts empty (a previously-typed value is no
      longer present) — no navigation away from the Add Record view.
    - Submitting a valid Incident (mocked `postIncident` resolves) shows "Added 1 incident."; a
      valid Procedure (mocked `postProcedure` resolves) shows "Added 1 procedure."
    - A rejected mutation (e.g. mocked `postLab` rejects with `new ApiError(422, "/labs")`) shows
      a `role="alert"` region containing "Something went wrong saving that lab result. Nothing
      was added — please try again." — the raw `422` and the string "API request failed" never
      appear anywhere in the rendered output.
    - No `getJson`/`getLabs`/`getIncidents`/`getProcedures`-style read call ever fires from this
      component — the confirmation fires directly off the POST mutation's `onSuccess` resolving,
      with no forced refetch of any kind (D-05); the confirmation text itself is a fixed template
      keyed off `recordType` (via the local `NOUN` map), not derived from the resolved record's
      field values — "built from the POST response" means "gated on the POST promise resolving,"
      not "reads fields off the resolved record."
  </behavior>
  <action>
    Create `frontend/src/components/AddRecordPage.tsx`. Import `useState` from `"react"`;
    `CheckCircle2, FlaskConical, Siren, Stethoscope, TriangleAlert` from `"lucide-react"`; the
    three `useCreateLab, useCreateIncident, useCreateProcedure` hooks from
    `"../hooks/useCreateRecord"`; `LabFields` from `"./records/LabFields"`; `IncidentFields` from
    `"./records/IncidentFields"`; `ProcedureFields` from `"./records/ProcedureFields"`; and
    `LabResultCreate, IncidentCreate, ProcedureCreate` types from `"../api/types"`.

    Local state: `recordType: "lab" | "incident" | "procedure"` (default `"lab"`); `resetSeq:
    number` (default `0`); `draftBody: LabResultCreate | IncidentCreate | ProcedureCreate | null`
    (default `null`); `submitState: { status: "idle" } | { status: "success"; noun: string } |
    { status: "error"; noun: string }` (default `{ status: "idle" }`). Call all three mutation
    hooks unconditionally at the top of the component (`const createLab = useCreateLab(); const
    createIncident = useCreateIncident(); const createProcedure = useCreateProcedure();` — React
    hook rules, all three always called, only one ever invoked per submit).

    Define `NOUN: Record<typeof recordType, string> = { lab: "lab result", incident: "incident",
    procedure: "procedure" }` and a `SUBMIT_LABEL` map `{ lab: "Add Lab Result", incident: "Add
    Incident", procedure: "Add Procedure" }`.

    `fieldsKey = \`${recordType}-${resetSeq}\`` — passed as the `key` prop on whichever field-set
    is mounted below, forcing a full remount (fresh internal state) on every type switch AND on
    every successful submit.

    `function handleTypeChange(next: typeof recordType) { setRecordType(next); setDraftBody(null);
    setSubmitState({ status: "idle" }); }` — called from each type-switcher button's `onClick`.

    `function handleSubmit() { if (draftBody === null) return; const noun = NOUN[recordType]; if
    (recordType === "lab") { createLab.mutate(draftBody as LabResultCreate, { onSuccess: () => {
    setSubmitState({ status: "success", noun }); setDraftBody(null); setResetSeq((n) => n + 1); },
    onError: () => setSubmitState({ status: "error", noun }) }); } else if (recordType ===
    "incident") { /* same shape with createIncident.mutate(draftBody as IncidentCreate, {...}) */
    } else { /* same shape with createProcedure.mutate(draftBody as ProcedureCreate, {...}) */
    }` — the `aria-disabled` guard (`if (draftBody === null) return;`) mirrors
    `DateRangePicker.tsx`'s `handleApply` discipline: `aria-disabled` does not prevent a click
    handler from firing, so the guard must live inside the handler itself. Note that `onSuccess`'s
    callback does not read the resolved record argument at all — the confirmation is purely "the
    POST resolved" plus the fixed `NOUN[recordType]` string, never a value read off the response
    body (D-05's substantive requirement is "no forced refetch," not "echo response fields").

    Render, inside `<main className="mx-auto flex max-w-[720px] flex-col gap-8
    bg-[var(--color-foam)] px-4 py-8 md:px-8 xl:px-16">` (copied verbatim from
    `UploadPage.tsx`): an `<h2 className="text-2xl font-bold leading-tight
    text-[var(--color-ink)]">Add a record</h2>` and `<p className="text-lg
    text-[var(--color-ink)]">Log a new lab result, incident, or procedure. Choose a type below,
    then fill in the details.</p>` in a `flex flex-col gap-4` wrapper (mirrors `UploadPage`'s
    heading block); a type-switcher `<div role="group" aria-label="Record type" className="flex
    flex-wrap gap-2">` containing three buttons (Lab / Incident / Procedure, values `"lab"` /
    `"incident"` / `"procedure"`), each `aria-pressed={recordType === key}`,
    `onClick={() => handleTypeChange(key)}`, `className={recordType === key ? activeClass :
    inactiveClass}` where `inactiveClass`/`activeClass` are the exact two constants from
    `FilterBar.tsx` (`"min-h-12 rounded-lg px-4 text-[20px] font-bold bg-[var(--color-sky)]
    text-[var(--color-ink)] border-2 border-[var(--color-ink)]"` /
    `"min-h-12 rounded-lg px-4 text-[20px] font-bold bg-[var(--color-accent)]
    text-[var(--color-accent-text)] border-2 border-[var(--color-accent)]"`), text-only labels
    "Lab" / "Incident" / "Procedure" (no icons in the switcher itself); then conditionally mount
    exactly one of `<LabFields key={fieldsKey} onDraftChange={setDraftBody} />` /
    `<IncidentFields key={fieldsKey} onDraftChange={setDraftBody} />` / `<ProcedureFields
    key={fieldsKey} onDraftChange={setDraftBody} />` based on `recordType`; then the Submit
    button — `type="button"`, `onClick={handleSubmit}`, `aria-disabled={draftBody === null}`,
    className switching between the enabled/disabled variants from `DateRangePicker.tsx`'s Apply
    button verbatim (`"min-h-12 self-start rounded-lg bg-[var(--color-accent)] px-6 text-[20px]
    font-bold text-[var(--color-accent-text)]"` when `draftBody !== null`, else `"min-h-12
    cursor-not-allowed self-start rounded-lg border-2 border-dashed border-[var(--color-ink)]
    bg-[var(--color-sky)] px-6 text-[20px] font-bold text-[var(--color-ink)]"`), text from
    `SUBMIT_LABEL[recordType]`; then, when `submitState.status === "success"`, a `<section
    role="status" aria-label="Add record result" className="flex items-start gap-2 rounded-lg
    border-2 border-[var(--color-ink)] bg-[var(--color-sky)] p-6 text-[var(--color-ink)]">`
    containing a `CheckCircle2` icon (`aria-hidden="true"`, `size={24}`,
    `className="mt-0.5 shrink-0"`) and `<p className="text-lg text-[var(--color-ink)]">Added 1
    {submitState.noun}.</p>` — copied structurally from `UploadPage.tsx`'s success section; when
    `submitState.status === "error"`, a `<section role="alert" aria-label="Add record notice"
    className="flex items-start gap-2 rounded-lg border-2 border-[var(--color-ink)]
    bg-[var(--color-sky)] p-6 text-lg text-[var(--color-ink)]">` containing a `TriangleAlert` icon
    and `<p><span className="font-bold">Something went wrong saving that {submitState.noun}.
    </span>{" "}Nothing was added — please try again.</p>` — never render `err.message`,
    `err.status`, or any backend detail; this is the ONLY error copy, unlike `UploadPage`'s
    two-kind (`not-omron`/`generic`) split (D-04 does not ask for a 422-specific message).

    Create `frontend/src/components/AddRecordPage.test.tsx`. Mock only `postLab`, `postIncident`,
    `postProcedure` from `"../api/client"` using the exact `vi.mock("../api/client",
    async (importOriginal) => { const actual = await importOriginal<typeof
    import("../api/client")>(); return { ...actual, postLab: vi.fn(), postIncident: vi.fn(),
    postProcedure: vi.fn() }; });` pattern from `CommandBar.test.tsx` (keep the real `ApiError`
    class so `instanceof` checks are exercised truthfully). Use the same `QueryClientProvider`
    render-wrapper pattern as `CommandBar.test.tsx` (`new QueryClient({ defaultOptions: { queries:
    { retry: false }, mutations: { retry: false } } })`). Cover every case listed in this task's
    `<behavior>` block: initial state (Lab active, Submit `aria-disabled="true"`); required-field
    gating flips Submit enabled; switching type discards the prior type's typed values (switch
    Lab→Incident→Lab, assert the Lab test-name input is empty again); a successful Lab submit
    shows "Added 1 lab result." via `role="status"` and clears the field-set (assert the
    previously-typed test-name value is gone post-success); a successful Incident submit shows
    "Added 1 incident."; a successful Procedure submit shows "Added 1 procedure."; a rejected
    `postLab` call (`mockPostLab.mockRejectedValue(new ApiError(422, "/labs"))`) shows the fixed
    generic-error sentence via `role="alert"` and asserts `screen.queryByText(/422/)` and
    `screen.queryByText(/API request failed/)` are both absent.

    Once `cd frontend && npx vitest run` passes for the FULL frontend suite (this plan's
    `<verification>` gate below — `AddRecordPage.test.tsx` from this task plus every pre-existing
    test, including Plan 08-02's `LabFields.test.tsx`/`ProcedureFields.test.tsx`/
    `IncidentFields.test.tsx`), update `.planning/phases/08-manual-entry-forms/08-VALIDATION.md`:
    set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`; change the file's final
    `**Approval:** pending` line to `**Approval:** confirmed — Wave 0 test files
    (LabFields.test.tsx, ProcedureFields.test.tsx, IncidentFields.test.tsx,
    AddRecordPage.test.tsx) exist and the full frontend suite is green as of Plan 08-03.`; and
    flip every `⬜ pending` row in the "Per-Requirement Verification Map" table to `✅ green`
    (all five OVERLAY-02 behaviors listed there are exercised by `AddRecordPage.test.tsx`'s
    passing assertions by this point in phase execution) and check every box in the "Validation
    Sign-Off" section.
  </action>
  <acceptance_criteria>
    - `AddRecordPage.tsx` exports a component with no props, mounts exactly one of
      `LabFields`/`IncidentFields`/`ProcedureFields` at a time keyed on `` `${recordType}-${resetSeq}` ``.
    - The Submit button's `onClick` handler contains an early `return` guard on `draftBody ===
      null` — `aria-disabled` alone never blocks the click handler from running.
    - `handleTypeChange` resets `draftBody` to `null` and `submitState` to `{ status: "idle" }`.
    - A successful `mutate` call's `onSuccess` callback resets `draftBody` to `null` and increments
      `resetSeq`.
    - No `getJson`, `getReadings`, `getLabs`, `getIncidents`, or `getProcedures` import appears
      anywhere in `AddRecordPage.tsx` — grep confirms zero occurrences of `getJson` in the file.
    - `AddRecordPage.test.tsx` mocks exactly `postLab`, `postIncident`, `postProcedure` (verified
      by inspecting the `vi.mock` call) and keeps the real `ApiError` class.
    - `cd frontend && npx vitest run src/components/AddRecordPage.test.tsx` passes with 0
      failures, covering all cases in this task's `<behavior>` block.
    - `cd frontend && npx tsc -b` exits 0.
    - `.planning/phases/08-manual-entry-forms/08-VALIDATION.md` frontmatter has
      `nyquist_compliant: true` and `wave_0_complete: true`, and its `**Approval:**` line is no
      longer `pending`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run src/components/AddRecordPage.test.tsx && grep -q "nyquist_compliant: true" ../.planning/phases/08-manual-entry-forms/08-VALIDATION.md</automated>
  </verify>
  <done>AddRecordPage.tsx implements the full type-switch/submit/confirmation/reset loop against Plan 08-01/08-02's existing contracts, AddRecordPage.test.tsx proves it for all three record types plus the type-switch-discard and generic-error branches, and 08-VALIDATION.md is closed out (nyquist_compliant/wave_0_complete true, Approval no longer pending) once the full frontend suite is confirmed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caregiver browser (`AddRecordPage`) → FastAPI `/labs`, `/incidents`, `/procedures` | This plan is the first place in the phase where a real network call fires — via `useCreateLab`/`useCreateIncident`/`useCreateProcedure` (Plan 08-01), which call `postJson` (already Bearer-gated, already 401→logout, unchanged since Phase 5). No new endpoint, no new auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Information Disclosure | `AddRecordPage.tsx`'s error branch (`submitState.status === "error"`) | mitigate | Only one fixed, UI-authored sentence per record-type noun renders on any mutation failure — never `err.message`, `err.status`, or a backend 422 `detail` string. Mirrors `UploadPage.tsx`'s established D-10/T-05-13 discipline (closed in Phase 5), reused verbatim here. Test-enforced: `AddRecordPage.test.tsx` asserts `/422/` and `/API request failed/` are absent from the DOM after a rejected mutation. |
| T-08-05 | Tampering (client-side validation bypass) | Submit button's `aria-disabled` gate | accept | Not a security boundary — the backend (Pydantic `*Create` models, Phase 7 T-07-02, closed) independently validates and 422s every request regardless of what the client allowed the caregiver to attempt. The `if (draftBody === null) return;` guard inside `handleSubmit` exists for UX correctness (an `aria-disabled` button is still clickable), not as a security control. |
| T-08-06 | Tampering (XSS) | Confirmation/error sentence rendering (`{submitState.noun}` interpolated into JSX text) | accept | `noun` is drawn from a fixed internal `NOUN` map (`"lab result"`/`"incident"`/`"procedure"`), never from caregiver-typed input — there is no injection surface here at all, stricter than even React's standard auto-escaping guarantee. |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | Zero new packages installed by this plan. |

This plan introduces no new trust boundary beyond "a client calls three already-audited,
already-gated Phase 7 endpoints" (RESEARCH.md's phase-wide conclusion). The one genuinely new
correctness surface (NaN→null coercion) was already mitigated in Plan 08-02's field-sets before
a body ever reaches this plan's `mutate()` calls.
</threat_model>

<verification>
- `cd frontend && npx tsc -b` exits 0 for the whole frontend (all three plans' files compiling
  together).
- `cd frontend && npx vitest run` passes in full (dates.test.ts extension from 08-01, all three
  field-set test files from 08-02, AddRecordPage.test.tsx from this plan, and every pre-existing
  test file — confirms the Header reshape and App.tsx routing change broke nothing elsewhere).
- `.planning/phases/08-manual-entry-forms/08-VALIDATION.md` reflects `nyquist_compliant: true`
  once the above is green (Task 2's closing step).
- Manual smoke check (not automated, but confirm before considering the phase done): `cd frontend
  && npm run dev`, log in, click "Add Record" in the Header, submit one Lab, one Incident, and one
  Procedure, and confirm each shows its "Added 1 {type}." sentence and the form is empty
  afterward.
</verification>

<success_criteria>
- A caregiver can submit a new lab result through a form with ≥48px targets and no
  drag/precision interactions (ROADMAP Phase 8 success criterion 1).
- A caregiver can submit a new incident through an accessible form (success criterion 2).
- A caregiver can submit a new procedure through an accessible form (success criterion 3).
- A submitted record shows up in the confirmation immediately, without a page reload (success
  criterion 4) — the confirmation fires directly off the POST mutation resolving, no forced
  refetch.
- The Header, view store, and App router all agree on the three-state navigation shape; no dead
  binary-toggle code remains.
- The phase's `08-VALIDATION.md` no longer sits in draft state — its Wave 0 gaps are closed and
  its Approval line reflects the green full-suite run.
</success_criteria>

<output>
Create `.planning/phases/08-manual-entry-forms/08-03-SUMMARY.md` when done
</output>
</output>
</content>
