---
phase: 03-agent-via-text-input
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/api/types.ts
  - frontend/src/lib/agent.ts
  - frontend/src/lib/agent.test.ts
  - frontend/src/hooks/useAgent.ts
autonomous: true
requirements: [VOICE-06, VOICE-08, SEC-02]
must_haves:
  truths:
    - "applyAgentFilters mutates the zustand filter store from outside the React tree via useFilters.getState() — reset first, then only the fields present in the delta, so existing filters carry over (D-13)"
    - "composeConfirmation produces the full-state echo from POST-apply store state — 'Showing blood pressure, last 30 days, mornings' — deterministically, never from model text (VOICE-06, D-07)"
    - "postAgent posts {text, context} to /agent with the same error discipline as getJson — the identical call voice will drive in Phase 4 (VOICE-08, D-12)"
    - "applyAgentFilters records which filter groups changed in a pulse signal store for FilterBar's D-08 highlight"
    - "No Anthropic identifier, key, or SDK reference exists anywhere in frontend source (SEC-02)"
  artifacts:
    - path: "frontend/src/lib/agent.ts"
      provides: "applyAgentFilters, composeConfirmation, useAgentPulse signal store"
      contains: "export function composeConfirmation"
      min_lines: 60
    - path: "frontend/src/api/client.ts"
      provides: "postJson + postAgent with three-branch ApiError discipline"
      contains: "export async function postJson"
    - path: "frontend/src/api/types.ts"
      provides: "AgentRequest/AgentReply/AppliedFilters/ClarifyContext TS mirrors"
      contains: "export type AgentReply"
    - path: "frontend/src/hooks/useAgent.ts"
      provides: "useMutation wrapper around postAgent"
    - path: "frontend/src/lib/agent.test.ts"
      provides: "unit tests for apply + confirmation composition"
  key_links:
    - from: "frontend/src/lib/agent.ts"
      to: "frontend/src/store/filters.ts"
      via: "useFilters.getState() action calls outside the React tree"
      pattern: "useFilters\\.getState\\(\\)"
    - from: "frontend/src/lib/agent.ts"
      to: "frontend/src/lib/dates.ts"
      via: "presetLabel/fmtLongDate/parseDateOnly reuse in confirmation"
      pattern: "parseDateOnly"
    - from: "frontend/src/api/client.ts"
      to: "/agent"
      via: "postAgent fetch POST"
      pattern: "postJson.*agent"
---

<objective>
Build the frontend agent foundation: the typed POST client (`postJson`/`postAgent`), TS mirrors of the backend reply models, the store-mutation handler `applyAgentFilters` (D-13 carry-over semantics), the deterministic full-state confirmation composer (VOICE-06, D-07), the D-08 pulse signal, and the TanStack v5 mutation hook — all unit-tested against the real zustand store with no backend running.

Purpose: The CommandBar (plan 03-04) becomes pure presentation once these primitives exist and are proven; voice (Phase 4) reuses every one of them unchanged (VOICE-08).
Output: `lib/agent.ts` + tests, extended api client/types, `useAgent` hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-via-text-input/03-CONTEXT.md
@.planning/phases/03-agent-via-text-input/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: postJson client + AgentReply type mirrors</name>
  <files>frontend/src/api/client.ts, frontend/src/api/types.ts</files>
  <read_first>
    - frontend/src/api/client.ts (getJson's exact three-branch error discipline to mirror: ApiError(0) on fetch throw, ApiError(status) on !res.ok, ApiError(status) on unparseable 2xx body; file-header rule that raw errors never render)
    - frontend/src/api/types.ts (hand-written mirror conventions with contract comments; existing `ChartId` and `BPCategory` — REUSE, do not redefine)
  </read_first>
  <action>
    1. `frontend/src/api/client.ts`: add `postJson<TBody, TRes>(path: string, body: TBody): Promise<TRes>` mirroring `getJson`'s structure exactly — `method: "POST"`, `headers: { "Content-Type": "application/json" }`, `body: JSON.stringify(body)`, same `BASE` prefix, and ALL three failure branches (`ApiError(0)` for network/CORS, `ApiError(res.status)` for non-ok, `ApiError(res.status)` for bad JSON body). Then `postAgent(body: AgentRequest): Promise<AgentReply> = postJson("/agent", body)`.
    2. `frontend/src/api/types.ts`: append mirrors of the backend agent models (plan 03-01 schemas.py contract — keep field names byte-identical to the JSON wire form):
       - `ClarifyContext = { original_text: string; question: string }`
       - `AgentRequest = { text: string; context: ClarifyContext | null }`
       - `AppliedFilters = { activeChart?: ChartId | null; datePreset?: "7d" | "30d" | "90d" | "all" | null; customRange?: { from: string; to: string } | null; amPm?: "all" | "AM" | "PM" | null; bpCategory?: "all" | BPCategory | null; reset?: boolean }` — note the wire key is `from` (backend serializes `from_` with alias "from"); canonical labels with spaces, canonical "AM"/"PM" (server does all token translation — nothing translates here, per PATTERNS "Canonical labels vs. wire tokens")
       - `AgentReply = { kind: "applied" | "clarify" | "refuse" | "unclear"; filters: AppliedFilters | null; message: string; context: ClarifyContext | null }`
       Each with a one-line contract comment citing API-04/VOICE-08.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/lib src/store</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/api/client.ts` contains `export async function postJson` and `export function postAgent` (or async equivalent), with `ApiError(0, path)` present in the new function's catch branch
    - `frontend/src/api/types.ts` contains `export type AgentReply` with the four-kind union and `export type AppliedFilters` referencing existing `ChartId` and `BPCategory` (no redefinition of either)
    - `npx tsc --noEmit` exits 0
    - `grep -ri "anthropic" frontend/src` returns no matches (SEC-02)
  </acceptance_criteria>
  <done>Typed POST path to /agent exists with the established error discipline; wire types mirror the backend contract exactly.</done>
</task>

<task type="auto">
  <name>Task 2: applyAgentFilters + composeConfirmation + pulse signal + useAgent hook, unit-tested</name>
  <files>frontend/src/lib/agent.ts, frontend/src/lib/agent.test.ts, frontend/src/hooks/useAgent.ts</files>
  <read_first>
    - frontend/src/store/filters.ts (the complete action surface: setActiveChart, setDatePreset, setCustomRange, setAmPm, setBpCategory, showAllData; note setCustomRange sets datePreset "custom" itself and setDatePreset clears customRange — rely on these, don't duplicate)
    - frontend/src/lib/dates.ts (presetLabel, fmtLongDate, parseDateOnly — parseDateOnly is MANDATORY for "YYYY-MM-DD" strings, `new Date("YYYY-MM-DD")` is the known off-by-one pitfall)
    - frontend/src/lib/dates.test.ts (describe/it style + state factory pattern to copy)
    - frontend/src/components/FilterBar.tsx lines 60-66 (the in-codebase full-state sentence precedent)
    - frontend/src/hooks/useStats.ts (hook file shape to adapt query→mutation)
  </read_first>
  <action>
    1. `frontend/src/lib/agent.ts` (header comment citing VOICE-06, D-07, D-08, D-13, RESEARCH Pattern 4):
       - `export type PulseField = "chart" | "dateRange" | "amPm" | "bpCategory"`.
       - `export const useAgentPulse = create<{ seq: number; fields: PulseField[]; mark: (fields: PulseField[]) => void }>(...)` — tiny zustand signal store; `mark` increments `seq` and replaces `fields`. FilterBar (plan 03-04) subscribes for the D-08 highlight; keeping it here (not in useFilters) preserves the filter store as the pure command schema.
       - `export function applyAgentFilters(f: AppliedFilters): PulseField[]` — via `const s = useFilters.getState()`: if `f.reset` call `s.showAllData()` FIRST; then per-field deltas only for present values: `f.activeChart` → `setActiveChart`; `f.datePreset` (and not "custom") → `setDatePreset`; `f.customRange?.from && f.customRange?.to` → `setCustomRange(from, to)` passing strings straight through (Pitfall 7 — no Date construction); `f.amPm` → `setAmPm`; `f.bpCategory` → `setBpCategory`. Collect touched PulseFields (reset touches all four), call `useAgentPulse.getState().mark(touched)`, return the list. Unmentioned fields untouched = D-13 carry-over.
       - `export function composeConfirmation(state: { activeChart: ChartId; datePreset: DatePreset; customRange: { from: string | null; to: string | null }; amPm: "all" | "AM" | "PM"; bpCategory: "all" | BPCategory }, latestReading: string | null): string` — deterministic template, LOCKED format: `"Showing {chartPhrase}, {rangePhrase}{ampmSuffix}{categorySuffix}"` where chartPhrase = { bp_timeline: "blood pressure", pulse_trend: "pulse", bp_categories: "BP categories", am_pm_comparison: "the AM vs PM comparison" }; rangePhrase = "all data" for preset "all", lowercased presetLabel for day presets ("last 30 days"), and `"${fmtLongDate(from)} through ${fmtLongDate(to)}"` for "custom" (format from/to via parseDateOnly-safe path — reuse whatever dates.ts exposes; never `new Date(str)`); ampmSuffix = ", mornings" for "AM", ", evenings" for "PM", "" for "all"; categorySuffix = `, ${bpCategory} readings only` when not "all". The D-07/VOICE-06 canonical example MUST hold: bp_timeline + 30d + AM + all → exactly `"Showing blood pressure, last 30 days, mornings"`.
    2. `frontend/src/hooks/useAgent.ts`: `export function useAgent() { return useMutation({ mutationFn: postAgent }); }` — no query cache for agent replies (RESEARCH anti-pattern); reply handling lives at the call site's onSuccess.
    3. `frontend/src/lib/agent.test.ts` (dates.test.ts style; the zustand store is real — reset it between tests via `useFilters.setState` back to defaults in `beforeEach`):
       - applyAgentFilters `{activeChart: "pulse_trend"}` with pre-set datePreset "30d"/amPm "AM" → chart changes AND datePreset/amPm survive (D-13).
       - `{reset: true}` after filters set → store back to defaults (datePreset "all", amPm "all", bpCategory "all").
       - `{reset: true, activeChart: "pulse_trend", amPm: "AM"}` → reset applied first, then deltas (chart pulse_trend, amPm AM, others default).
       - `{customRange: {from: "2025-02-01", to: "2025-04-30"}}` → store datePreset becomes "custom", customRange strings stored verbatim.
       - `{datePreset: "30d"}` → customRange cleared (store built-in) — assert.
       - Pulse: after `{activeChart: "pulse_trend", amPm: "AM"}`, `useAgentPulse.getState().fields` equals ["chart", "amPm"] (order-insensitive) and seq incremented; reset marks all four.
       - composeConfirmation exact strings: canonical example above; custom-range state → `"Showing blood pressure, February 1, 2025 through April 30, 2025"`; pulse + PM + "Stage 2" → `"Showing pulse, all data, evenings, Stage 2 readings only"` (adjust expected strings to the locked template — tests define the contract).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/agent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/agent.ts` contains `export function applyAgentFilters`, `export function composeConfirmation`, `export const useAgentPulse`
    - `npx vitest run src/lib/agent.test.ts` exits 0 with ≥8 passing tests
    - Test asserts exact string "Showing blood pressure, last 30 days, mornings" (VOICE-06/D-07 canonical example)
    - Test proves partial command carry-over (D-13) and reset-first ordering
    - `frontend/src/lib/agent.ts` contains no `new Date(` call on customRange strings (Pitfall 7)
    - `cd frontend && npx vitest run` exits 0 (full frontend suite still green)
  </acceptance_criteria>
  <done>Store mutation and confirmation composition are proven primitives; Phase 4 voice and plan 03-04's CommandBar both consume them unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /agent response → store mutation | server-validated filter delta is the only thing that mutates UI state |
| model-origin strings → DOM | clarification/message text renders as React text nodes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering (XSS via model text) | agent reply message rendering | mitigate | strings rendered exclusively as React text nodes (auto-escaped); no dangerouslySetInnerHTML anywhere in phase files |
| T-03-07 | Tampering (unvalidated output reaching store) | applyAgentFilters | mitigate | only server-composed AppliedFilters fields with closed TS unions reach store actions; unknown fields ignored by construction |
| T-03-08 | Information disclosure (key in frontend) | api/client.ts | mitigate | frontend only ever calls POST /agent; grep gate asserts no "anthropic" reference in frontend/src (SEC-02) |
| T-03-09 | Information disclosure (raw errors) | postJson | mitigate | three-branch ApiError discipline; status codes only, messages never rendered (existing client.ts rule) |
| T-03-SC | Tampering (supply chain) | npm installs | accept | no new npm packages this phase (zustand/TanStack/vitest already installed) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit && npx vitest run` — full typecheck + suite green
- `grep -ri anthropic frontend/src` → empty (SEC-02)
</verification>

<success_criteria>
- postAgent/AgentReply wire contract matches backend plan 03-01 field-for-field
- applyAgentFilters proven for carry-over (D-13), reset ordering, custom-range string passthrough
- composeConfirmation emits the VOICE-06 canonical string exactly (D-07)
- Pulse signal (D-08 producer side) recorded per apply
</success_criteria>

<output>
Create `.planning/phases/03-agent-via-text-input/03-02-SUMMARY.md` when done
</output>
