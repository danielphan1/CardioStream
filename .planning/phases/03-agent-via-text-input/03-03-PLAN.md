---
phase: 03-agent-via-text-input
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/app/agent/service.py
  - backend/app/routers/agent.py
  - backend/app/main.py
  - backend/tests/test_agent_route.py
  - backend/tests/fixtures/agent_utterances.json
  - backend/tests/test_agent_fixtures.py
autonomous: true
requirements: [API-04, API-05, VOICE-07, VOICE-09, SEC-02]
user_setup:
  - service: anthropic
    why: "Live Claude calls for /agent interpretation and the live fixture suite"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (console.anthropic.com); place in backend/.env"
must_haves:
  truths:
    - "POST /agent returns a server-composed, Pydantic-validated AgentReply for every input — raw model output is never executed or passed through unvalidated; the only Claude-authored string reaching the frontend is the short clarify question, as display-only text (API-04)"
    - "Every model-side failure (APIError, timeout, ValidationError, stop_reason refusal/max_tokens, missing parsed_output, missing key, InvalidRange) collapses to a friendly 200 unclear/unavailable reply — never a raw error or 500 (VOICE-07, D-11)"
    - "Medical-interpretation requests return the fixed refusal template plus the relevant chart delta (VOICE-09, D-10); data questions return an applied chart delta plus the stats-bar message (D-16)"
    - "Clarify replies echo a one-turn ClarifyContext the client resends; the server stays stateless (D-12)"
    - "Symbolic date ranges are resolved in the service using min/max reading anchors queried in the route — Claude never computes dates (API-05, D-15)"
    - "slowapi limits /agent to 20/minute and the Anthropic client is constructed backend-only from Settings (SEC-02)"
    - "The ~30-utterance live fixture suite (garbled transcripts, medical questions, clarifications, gibberish, D-09 confident guesses) exists, is @pytest.mark.live, and is excluded from default runs"
  artifacts:
    - path: "backend/app/agent/service.py"
      provides: "interpret() pipeline + call_claude with lazy keyless-safe client"
      contains: "def interpret"
      min_lines: 80
    - path: "backend/app/routers/agent.py"
      provides: "POST /agent route with slowapi limit, get_interpreter dependency, anchor queries"
      contains: "def get_interpreter"
    - path: "backend/tests/fixtures/agent_utterances.json"
      provides: "~30-utterance eval suite incl. garbled transcripts"
    - path: "backend/tests/test_agent_route.py"
      provides: "deterministic route tests via dependency override incl. 429 + never-500"
    - path: "backend/tests/test_agent_fixtures.py"
      provides: "live-marked eval runner"
  key_links:
    - from: "backend/app/routers/agent.py"
      to: "backend/app/agent/service.py"
      via: "get_interpreter dependency returning interpret"
      pattern: "get_interpreter"
    - from: "backend/app/agent/service.py"
      to: "backend/app/agent/resolver.py"
      via: "resolve_date_range on DashboardCommand.date_range"
      pattern: "resolve_date_range"
    - from: "backend/app/main.py"
      to: "backend/app/routers/agent.py"
      via: "include_router with verify_token dependency + limiter state"
      pattern: "agent\\.router"
---

<objective>
Ship `POST /agent`: the interpretation service (structured-outputs call with full guard rails, variant→AgentReply mapping, token→canonical translation, date resolution), the rate-limited authenticated route, main.py wiring (CORS POST fix, limiter, router include), deterministic route tests via dependency override, and the ~30-utterance live fixture suite.

Purpose: This is the API-04/API-05 core — the full text→validated-command pipeline the CommandBar (03-04) and Phase 4 voice both drive (VOICE-08).
Output: Working keyless-degrading endpoint + green deterministic tests + live suite ready for the 03-05 verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-via-text-input/03-CONTEXT.md
@.planning/phases/03-agent-via-text-input/03-RESEARCH.md
@.planning/phases/03-agent-via-text-input/03-PATTERNS.md
@.planning/phases/03-agent-via-text-input/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: interpret() service — guarded Claude call + variant mapping</name>
  <files>backend/app/agent/service.py</files>
  <read_first>
    - backend/app/agent/schemas.py (AgentOutput variants, AgentReply/AppliedFilters/ClarifyContext, BP_TOKEN_TO_LABEL/AMPM_TOKEN_TO_LABEL maps — from 03-01)
    - backend/app/agent/resolver.py (resolve_date_range signature, ResolvedDates shape, InvalidRange)
    - backend/app/agent/copy.py (UNCLEAR_MESSAGE, UNAVAILABLE_MESSAGE, MEDICAL_REFUSAL templates, DATA_QUESTION_MESSAGE, CHART_PHRASES)
    - backend/app/agent/prompt.py (SYSTEM_PROMPT, build_messages)
    - backend/app/config.py (get_settings lazy-singleton pattern)
    - .planning/phases/03-agent-via-text-input/03-RESEARCH.md Code Example 1 (exact call shape + guard order) and Pitfalls 5/9
  </read_first>
  <action>
    Create `backend/app/agent/service.py` (module docstring pinning: guard order per Pitfall 5; every failure branch → friendly 200 reply, VOICE-07; model text never passes through except Clarification.question as display-only string, API-04).

    1. `_get_client() -> Anthropic | None` — lazy module-level singleton per RESEARCH Code Example 1: empty `get_settings().anthropic_api_key` → None (Pitfall 9); else `Anthropic(api_key=key, timeout=15.0, max_retries=1)` cached in a module global. If the constructor kwarg names differ in anthropic 0.117 (RESEARCH A4), adapt to the SDK's actual names — do not drop the timeout/retry limits.
    2. `call_claude(text: str, context: ClarifyContext | None) -> AgentOutput | None` — exactly Code Example 1: `client.messages.parse(model="claude-haiku-4-5", max_tokens=1024, temperature=0, system=SYSTEM_PROMPT, messages=build_messages(text, context), output_format=AgentOutput)`; catch `(APIError, ValidationError)` → None; `msg.stop_reason in ("refusal", "max_tokens")` → None; return `msg.parsed_output` (may still be None).
    3. `interpret(text: str, context: ClarifyContext | None, earliest: date | None, latest: date | None) -> AgentReply` — the pipeline:
       - No key (`_get_client()` is None) → `AgentReply(kind="unclear", message=UNAVAILABLE_MESSAGE)`.
       - `call_claude` returns None → `AgentReply(kind="unclear", message=UNCLEAR_MESSAGE)`.
       - Match `output.result` by `action` tag:
         * `DashboardCommand` → build `AppliedFilters`: `activeChart` = chart token passthrough; date via `resolve_date_range(cmd.date_range, earliest, latest)` — `InvalidRange` → unclear reply; ResolvedDates preset → `datePreset`, custom → `customRange` (from_/to strings); `amPm` = `AMPM_TOKEN_TO_LABEL[cmd.am_pm]` when present; `bpCategory` = `BP_TOKEN_TO_LABEL[cmd.bp_category]` when present; `reset` = cmd.reset. Reply `kind="applied"`, `message=""` (frontend composes the D-07 echo — never authored here), `context=None`.
         * `DataQuestion` → `kind="applied"`, `filters=AppliedFilters(activeChart=chart)` (or empty AppliedFilters when chart None), `message=DATA_QUESTION_MESSAGE` (D-16).
         * `Clarification` → `kind="clarify"`, `message=q.question.strip()` (display-only, escaped client-side), `context=ClarifyContext(original_text=(context.original_text if context else text), question=q.question)` — preserves the TRUE original across one repeated clarify while keeping exactly one turn (D-12).
         * `MedicalRefusal` → `kind="refuse"`, `filters=AppliedFilters(activeChart=chart)` when chart present else None, `message=` MEDICAL_REFUSAL_WITH_CHART formatted with `CHART_PHRASES[chart]` (or MEDICAL_REFUSAL_PLAIN) — fixed template, never model prose (D-10, VOICE-09).
         * `Unintelligible` → `kind="unclear"`, `message=UNCLEAR_MESSAGE` (D-11).
       - Wrap the whole body in a final `except Exception` → unclear reply (absolute never-500 backstop; log via `logging` at warning level WITHOUT including the API key or full model payload).
    4. `Interpreter` type alias: `Callable[[str, ClarifyContext | None, date | None, date | None], AgentReply]`.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.agent.service import interpret; from datetime import date; r = interpret('show my pulse', None, date(2025,2,22), date(2025,6,13)); print(r.kind, r.message)" | grep -q "unclear" && echo KEYLESS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/agent/service.py` contains `def interpret(`, `def call_claude(`, `def _get_client(`
    - With no ANTHROPIC_API_KEY, `interpret(...)` returns kind "unclear" with UNAVAILABLE_MESSAGE (no exception, no network call) — verified by the command above printing KEYLESS-OK
    - Source contains `stop_reason` guard and catches `(APIError, ValidationError)` (grep both strings present)
    - `message=""` on the applied branch for DashboardCommand (frontend owns the D-07 echo)
    - `grep -c "temperature=0" backend/app/agent/service.py` ≥ 1; model string is exactly "claude-haiku-4-5"
  </acceptance_criteria>
  <done>interpret() maps all five variants to server-composed replies with fixed copy, resolves dates via the resolver, and cannot raise for any model-side outcome.</done>
</task>

<task type="auto">
  <name>Task 2: /agent route, slowapi, main.py wiring, deterministic route tests</name>
  <files>backend/app/routers/agent.py, backend/app/main.py, backend/tests/test_agent_route.py</files>
  <read_first>
    - backend/app/routers/stats.py (router conventions: pinned-invariants docstring, Annotated Depends, sync def, and the exact unfiltered `db.scalar(select(func.max(Reading.datetime_)))` anchor query — note trailing underscore on `datetime_`)
    - backend/app/main.py (current CORS block with GET-only methods — MUST change; router-include-with-verify_token pattern)
    - backend/app/auth.py (verify_token no-op stub — attach via router include, never per-route)
    - backend/tests/conftest.py (client fixture with dependency_overrides + `.clear()` cleanup)
    - backend/tests/test_api_readings.py (test style: docstring contract, parametrize, 422-never-500 pattern)
    - .planning/phases/03-agent-via-text-input/03-RESEARCH.md Code Example 3 + Pitfalls 1/6
  </read_first>
  <action>
    1. `backend/app/routers/agent.py`:
       - Module-level `limiter = Limiter(key_func=get_remote_address)` (from slowapi).
       - `def get_interpreter() -> Interpreter: return interpret` — dependency mirroring get_db's override-ability (RESEARCH Pattern 3).
       - Route: `@router.post("/agent", response_model=AgentReply)` ABOVE `@limiter.limit("20/minute")`; signature MUST include `request: Request` (Pitfall 6 — slowapi silently no-ops without it), `body: AgentRequest`, `db: Annotated[Session, Depends(get_db)]`, `interpreter: Annotated[Interpreter, Depends(get_interpreter)]`. Body: query BOTH anchors unfiltered — `latest = db.scalar(select(func.max(Reading.datetime_)))`, `earliest = db.scalar(select(func.min(Reading.datetime_)))` — convert to `.date()` when not None, and `return interpreter(body.text, body.context, earliest_date, latest_date)` wrapped in try/except Exception → `AgentReply(kind="unclear", message=UNCLEAR_MESSAGE)` (route-level never-500 backstop). Pinned-invariants docstring cites API-04/API-05, Pitfalls 1/5/6, and "DB access via get_db only".
    2. `backend/app/main.py`:
       - CORS: `allow_methods=["GET", "POST"]`, `allow_headers=["Authorization", "Content-Type"]` (Pitfall 1 — preflight currently fails).
       - slowapi: `from app.routers.agent import limiter`; `app.state.limiter = limiter`; `app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)` (imports from slowapi/slowapi.errors).
       - `app.include_router(agent.router, dependencies=[Depends(verify_token)])` alongside the existing two includes (router-level auth, Phase 5 flips enforcement).
    3. `backend/tests/test_agent_route.py` (module docstring listing the contract; use the existing `client`/`session` fixtures; add an autouse fixture calling `limiter.reset()` before each test so the 20/minute budget never bleeds across tests):
       - Fake-interpreter tests via `app.dependency_overrides[get_interpreter]` (conftest's `.clear()` already cleans up): applied reply with `AppliedFilters(activeChart="pulse_trend", datePreset="30d", amPm="AM")` → 200, `kind == "applied"`, filters echoed with those exact values; clarify reply → context object round-trips in the response JSON (D-12); refuse reply → fixed message text present (D-10); unclear reply → message contains an example command (D-11).
       - customRange serialization: fake reply with CustomRange(from_="2025-02-01", to="2025-04-30") → response JSON has `filters.customRange.from == "2025-02-01"` (alias "from" on the wire — FastAPI response_model serializes by alias).
       - Never-500: fake interpreter that raises RuntimeError → 200 with kind "unclear" (VOICE-07).
       - Validation: missing `text` → 422; text of 501 chars → 422 (API-level, distinct from model-path failures).
       - Anchor wiring: seed two readings via the session fixture (reuse test_api_readings' `_reading` factory shape), fake interpreter that RECORDS its earliest/latest args → assert they equal the seeded min/max dates.
       - Rate limit (verifies RESEARCH A3 slowapi/FastAPI-0.139 compat): loop 20 POSTs → all 200; 21st → 429. If slowapi proves incompatible with the current Starlette, implement the documented fallback — a ~20-line in-process sliding-window dependency raising HTTPException(429) — and keep this test green; note the swap in the SUMMARY.
       - CORS preflight: `client.options("/agent", headers={"Origin": "http://localhost:5173", "Access-Control-Request-Method": "POST", "Access-Control-Request-Headers": "content-type"})` → 200 and `access-control-allow-methods` contains POST (Pitfall 1 regression guard).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_agent_route.py -x -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `POST /agent` with `{"text": "show my pulse"}` via TestClient returns 200 with a JSON body containing `kind` (keyless → "unclear")
    - Route decorator order is `@router.post` above `@limiter.limit("20/minute")` and the signature contains `request: Request` (source assertions)
    - `backend/app/main.py` contains `allow_methods=["GET", "POST"]` and `"Content-Type"` in allow_headers
    - 21st request in the rate-limit test returns 429; interpreter-raises test returns 200 kind "unclear"
    - Full backend suite (`python -m pytest -q`) green, keyless
  </acceptance_criteria>
  <done>/agent is live behind the auth stub with working rate limiting, CORS preflight passes for POST, and route behavior is deterministically proven without any network access.</done>
</task>

<task type="auto">
  <name>Task 3: ~30-utterance fixture suite + live-marked eval runner</name>
  <files>backend/tests/fixtures/agent_utterances.json, backend/tests/test_agent_fixtures.py</files>
  <read_first>
    - .planning/phases/03-agent-via-text-input/03-RESEARCH.md Code Example 4 (entry shape + live-marker discipline)
    - backend/app/agent/service.py (interpret signature the runner calls)
    - backend/pyproject.toml (confirm `live` marker + `addopts = "-m 'not live'"` landed in 03-01)
  </read_first>
  <action>
    1. `backend/tests/fixtures/agent_utterances.json` — 30–34 entries, each `{"id": "...", "utterance": "...", "context": null | {original_text, question}, "expect": {...}}` where `expect` supports: `kind` (exact), `kind_in` (list, for legitimately-either cases), and optional filter-field expectations (`chart`, `datePreset`, `customRange` from/to, `amPm`, `bpCategory`, `reset`). Cover (roadmap SC4 + D-09..D-16):
       - Chart switches (4): "show my pulse", "heart rate please" (synonym), "show blood pressure", "show the categories chart".
       - Presets/reset (5): "last 30 days", "past week" → 7d, "last 90 days", "show all data" → reset true, "start over" → reset true.
       - AM/PM + combos (4): "mornings only" → amPm AM, "just the evenings" → PM, the ROADMAP SC1 canonical "show my blood pressure for the last 30 days, mornings only" → chart bp_timeline + datePreset 30d + amPm AM, "pulse in the mornings" → both fields.
       - Categories (3): "show hypertensive crisis readings" → bpCategory "Hypertensive Crisis", "only stage 2" → "Stage 2", "normal readings only" → "Normal".
       - Symbolic dates (4, D-15): "february through april" → customRange 2025-02-01..2025-04-30, "since june" → 2025-06-01..2025-06-13, "show me may" → 2025-05-01..2025-05-31, "last 14 days" → customRange 2025-05-31..2025-06-13.
       - Garbled transcripts (4, D-09 confident-guess): "sho me blod preshure last thurty days" → bp_timeline + 30d, "puls trend pls" → pulse_trend, "shw mornins" → amPm AM (or kind_in [applied, clarify]), "blood pressure for the last thirty days" (number words) → 30d.
       - Medical refusals (4, VOICE-09/D-10): "is my blood pressure dangerous?" → kind refuse + chart bp_timeline, "should I be worried about my pulse?" → refuse + pulse_trend, "am I going to be okay?" → refuse, "what do these numbers mean for my health?" → refuse.
       - Data questions (2, D-16): "what's my average blood pressure?" → kind applied + message expectation flag `has_message: true`, "how many readings do I have?" → kind_in [applied].
       - Clarify + follow-up (2, D-12): "show me that one" → kind_in [clarify, unclear]; entry with `context: {"original_text": "show me the numbers", "question": "<any prior question>"}` and utterance "the pulse one" → kind applied + chart pulse_trend.
       - Gibberish (2, D-11): "asdf qwerty zxcv" → unclear, "banana helicopter tuesday" → kind_in [unclear, clarify].
       - Relative adjustment rejection (1, D-14): "zoom out a bit" → kind_in [unclear, clarify] (never applied with a date change).
    2. `backend/tests/test_agent_fixtures.py`: `pytestmark = [pytest.mark.live, pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY"), reason="needs real key")]`; load the JSON, `@pytest.mark.parametrize` over entries by id; call the REAL `interpret(utterance, context, date(2025,2,22), date(2025,6,13))`; assert `kind`/`kind_in`; for applied entries assert each specified filter field equals the expectation; assert intent/fields only, never message wording (model-adjacent copy may evolve). Module docstring: cost ~$0.01–0.05/run on haiku; run via `python -m pytest -m live tests/test_agent_fixtures.py -v` at the phase gate (03-05).
  </action>
  <verify>
    <automated>cd backend && python -c "import json; d=json.load(open('tests/fixtures/agent_utterances.json')); assert len(d) >= 30, len(d); ks=[e['expect'] for e in d]; assert any('refuse' in str(k) for k in ks) and any('unclear' in str(k) for k in ks); print('FIXTURES', len(d))" && python -m pytest -q && python -m pytest -m live --collect-only -q | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `agent_utterances.json` parses and contains ≥30 entries including ≥4 garbled-transcript ids, ≥4 medical-refusal expectations, ≥2 gibberish→unclear, and ≥1 context-bearing clarify follow-up
    - `python -m pytest -q` (default run) collects ZERO tests from test_agent_fixtures.py (addopts excludes live)
    - `python -m pytest -m live --collect-only` collects ≥30 parametrized fixture tests
    - Runner asserts fields/kind only — `grep -c "message ==" backend/tests/test_agent_fixtures.py` returns 0
  </acceptance_criteria>
  <done>The SC4 evidence suite exists and is wired for the 03-05 live gate; CI/default runs remain deterministic and free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /agent | untrusted user text/transcript enters the backend |
| backend → Anthropic API | paid external call with secret credential |
| Claude output → AgentReply | model output is untrusted input (API-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering (prompt injection) | interpret/build_messages | mitigate | user text only in user-role messages; static system prompt; output constrained to closed schema; worst case = wrong filter, visible via D-07 echo |
| T-03-11 | Tampering (unvalidated model output) | service.py variant mapping | mitigate | SDK parse validates against AgentOutput; service re-maps to server-composed AgentReply with fixed copy; only Clarification.question passes through, as display-only text (API-04) |
| T-03-12 | Information disclosure (API key) | _get_client / logging | mitigate | key read from Settings only; never logged, never in any response field; grep gate in 03-05 (SEC-02) |
| T-03-13 | DoS / cost abuse | /agent route | mitigate | slowapi 20/minute per-IP + AgentRequest max_length=500 + max_tokens=1024 + 15s timeout + max_retries=1; 429 test proves the limiter fires |
| T-03-14 | Information disclosure (raw errors/traces) | route + service backstops | mitigate | double try/except → templated 200 unclear reply; never-500 route test with raising interpreter (VOICE-07) |
| T-03-15 | Elevation (auth bypass) | router include | accept (Phase 5) | verify_token attached at include now; enforcement gate flips in Phase 5 per roadmap — accepted interim risk on a local-only app |
| T-03-SC | Tampering (supply chain) | anthropic/slowapi installs | accept | both [OK]-approved in RESEARCH Package Legitimacy Audit; installed in 03-01 |
</threat_model>

<verification>
- `cd backend && python -m pytest -q` — full suite green, keyless, zero live tests collected
- `curl -s -X POST localhost:8000/agent -H 'Content-Type: application/json' -d '{"text":"show my pulse"}'` against a running dev server → 200 JSON with `"kind":"unclear"` (keyless degradation) — optional manual spot-check
- Rate-limit test proves 429 on the 21st request (A3 compat evidence)
</verification>

<success_criteria>
- POST /agent live behind auth stub; every failure path returns friendly 200 copy (VOICE-07)
- Server-composed replies only; date resolution anchored to real DB min/max (API-04/API-05)
- ≥30-entry fixture suite with garbled + medical + clarify-context entries, live-marked and default-excluded (roadmap SC4 scaffold)
</success_criteria>

<output>
Create `.planning/phases/03-agent-via-text-input/03-03-SUMMARY.md` when done
</output>
