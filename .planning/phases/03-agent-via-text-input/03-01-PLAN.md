---
phase: 03-agent-via-text-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/agent/__init__.py
  - backend/app/agent/schemas.py
  - backend/app/agent/resolver.py
  - backend/app/agent/prompt.py
  - backend/app/agent/copy.py
  - backend/tests/test_agent_schemas.py
  - backend/tests/test_agent_resolver.py
autonomous: true
requirements: [API-04, API-05, VOICE-09, SEC-02]
must_haves:
  truths:
    - "The Claude-facing AgentOutput schema is a closed, constraint-free tagged union — lowercase snake tokens, no numeric min/max, no Field(discriminator=) — so structured outputs cannot emit anything outside the vocabulary (API-04, D-14)"
    - "Symbolic date ranges (presets, last_n_days, month names, from/to) resolve server-side anchored to the latest reading with inclusive ends — Claude never computes absolute dates (API-05, D-15)"
    - "Partial commands leave unmentioned fields None so existing filters carry over; reset:true maps to showAllData (D-13)"
    - "The system prompt encodes the confident-guess-vs-clarify boundary (D-09), medical refusal routing (D-10), gibberish-to-unclear routing (D-11), data-question routing (D-16), and one-turn clarify memory via build_messages (D-12)"
    - "Refusal, didn't-catch, and data-question copy are fixed server-side templates with example commands — never model prose (VOICE-09, D-10, D-11, D-16)"
    - "ANTHROPIC_API_KEY is read only via pydantic-settings with an empty-string default — the app boots and all tests pass keyless (SEC-02)"
  artifacts:
    - path: "backend/app/agent/schemas.py"
      provides: "AgentOutput Claude-facing union + AgentRequest/AgentReply/AppliedFilters API models + token-to-canonical-label maps"
      min_lines: 80
    - path: "backend/app/agent/resolver.py"
      provides: "resolve_date_range + n_day_range mirroring frontend resolveFilters arithmetic"
      contains: "def resolve_date_range"
    - path: "backend/app/agent/prompt.py"
      provides: "SYSTEM_PROMPT constant + build_messages(text, context)"
      contains: "def build_messages"
    - path: "backend/app/agent/copy.py"
      provides: "UNCLEAR_MESSAGE, UNAVAILABLE_MESSAGE, MEDICAL_REFUSAL templates, DATA_QUESTION_MESSAGE"
    - path: "backend/tests/test_agent_schemas.py"
      provides: "union parsing + lowercase normalization + build_messages/copy tests"
    - path: "backend/tests/test_agent_resolver.py"
      provides: "date-resolution parity tests ported from frontend dates.test.ts"
  key_links:
    - from: "backend/app/agent/resolver.py"
      to: "frontend/src/lib/dates.ts"
      via: "parity of anchor arithmetic (anchor − (N−1) through anchor, inclusive)"
      pattern: "n_day_range"
    - from: "backend/app/config.py"
      to: "ANTHROPIC_API_KEY env var"
      via: "pydantic-settings field with env_file support"
      pattern: "anthropic_api_key"
---

<objective>
Build the backend agent foundation: dependency pins, the API-key setting, the Claude-facing structured-outputs schema (API-04, D-13/D-14), the API request/reply models, the symbolic date resolver (API-05, D-15), the static system prompt + one-turn message builder (D-09/D-12/D-16), and the fixed safety copy templates (VOICE-09, D-10/D-11) — all fully unit-tested without any network access.

Purpose: Every downstream piece (service, route, fixtures) consumes these contracts. Getting the wire schema and resolver right first means the Claude call in plan 03-03 is pure plumbing.
Output: `backend/app/agent/` package (schemas, resolver, prompt, copy), updated config/pyproject, two green test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-via-text-input/03-CONTEXT.md
@.planning/phases/03-agent-via-text-input/03-RESEARCH.md
@.planning/phases/03-agent-via-text-input/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies, settings, and the two schema families (Claude-facing + API-facing)</name>
  <files>backend/pyproject.toml, backend/app/config.py, backend/app/agent/__init__.py, backend/app/agent/schemas.py, backend/tests/test_agent_schemas.py</files>
  <read_first>
    - backend/pyproject.toml (current dep pin style `pkg==X.Y.*`, existing `[tool.pytest.ini_options]`)
    - backend/app/config.py (Settings class + `@lru_cache get_settings` pattern to extend)
    - backend/app/schemas.py (Pydantic conventions: one-line docstrings citing requirement IDs, `X | None = None`, ConfigDict usage)
    - backend/app/deps.py (canonical `BPCategory` Literal with spaces, canonical `"AM"/"PM"` values — the server-side targets of the token maps)
    - .planning/phases/03-agent-via-text-input/03-RESEARCH.md Code Example 2 (the exact Claude-facing union design) and Pitfalls 2/3
  </read_first>
  <action>
    1. `backend/pyproject.toml`: add `"anthropic==0.117.*"` and `"slowapi==0.1.*"` to `[project] dependencies` in the existing pin style (both slopcheck-approved in RESEARCH's Package Legitimacy Audit). Extend `[tool.pytest.ini_options]` with `markers = ["live: hits the real Anthropic API (needs ANTHROPIC_API_KEY)"]` and `addopts = "-m 'not live'"`. Then run `cd backend && .venv/bin/pip install -e ".[dev]"` (or `pip install -e ".[dev]"` inside the venv) to install.
    2. `backend/app/config.py`: add `anthropic_api_key: str = ""` to `Settings` (empty default — RESEARCH Pitfall 9: keyless boot must work, SEC-02) and add `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")` (import `SettingsConfigDict` from pydantic_settings) so `backend/.env` is read when present. Comment the field citing SEC-02 and Pitfall 9.
    3. `backend/app/agent/__init__.py`: empty package init (match `backend/app/routers/__init__.py`).
    4. `backend/app/agent/schemas.py` — two model families, per RESEARCH Code Example 2 and PATTERNS conventions:
       - **Claude-facing (constraint-free, lowercase snake tokens, plain Literal-tagged union — NO Field(ge/le), NO min_length, NO Field(discriminator=))**: `ChartToken = Literal["bp_timeline","pulse_trend","bp_categories","am_pm_comparison"]` (must equal frontend `ChartId` verbatim); `BPCategoryToken = Literal["all","hypotension","normal","elevated","stage_1","stage_2","hypertensive_crisis"]`; `MonthToken` (twelve lowercase month names); `PresetRange {kind: Literal["preset"], preset: Literal["7d","30d","90d","all"]}`; `LastNDays {kind: Literal["last_n_days"], n: int}` (no bounds — clamped in service per Pitfall 3); `MonthRange {kind: Literal["month_range"], start_month: MonthToken, end_month: MonthToken | None = None, year: int | None = None}`; `AbsoluteRange {kind: Literal["absolute"], from_date: str | None = None, to_date: str | None = None}`; `DateRange` = union of the four; `DashboardCommand {action: Literal["command"], chart: ChartToken | None, date_range: DateRange | None, am_pm: Literal["all","am","pm"] | None, bp_category: BPCategoryToken | None, reset: bool = False}` (None = unchanged, D-13; closed absolute vocabulary only, D-14); `DataQuestion {action: Literal["data_question"], chart: ChartToken | None}` (D-16); `Clarification {action: Literal["clarify"], question: str}` (D-09); `MedicalRefusal {action: Literal["refuse_medical"], chart: ChartToken | None}` (D-10); `Unintelligible {action: Literal["unclear"]}` (D-11); `AgentOutput {result: <union of the five>}`.
       - Add a `mode="before"` normalizer on `AgentOutput.result` that lowercases every string token value recursively (including nested `date_range` dicts) EXCEPT the `question` field — RESEARCH Pitfall 2 belt-and-suspenders against enum-capitalization drift.
       - **API-facing (ordinary Pydantic v2, constraints allowed)**: `ClarifyContext {original_text: str, question: str}` (max_length ~500 each); `AgentRequest {text: str (min_length=1, max_length=500), context: ClarifyContext | None = None}`; `AppliedFilters {activeChart: ChartToken | None = None, datePreset: Literal["7d","30d","90d","all"] | None = None, customRange: CustomRange | None = None, amPm: Literal["all","AM","PM"] | None = None, bpCategory: Literal["all","Hypotension","Normal","Elevated","Stage 1","Stage 2","Hypertensive Crisis"] | None = None, reset: bool = False}` where `CustomRange` has fields `from_` (with `Field(alias="from", serialization_alias="from")` since `from` is a Python keyword — set `model_config = ConfigDict(populate_by_name=True)`) and `to: str`, both "YYYY-MM-DD"; `AgentReply {kind: Literal["applied","clarify","refuse","unclear"], filters: AppliedFilters | None = None, message: str = "", context: ClarifyContext | None = None}`.
       - **Token→canonical maps (module constants, single translation point per PATTERNS "Canonical labels vs. wire tokens")**: `BP_TOKEN_TO_LABEL = {"all": "all", "hypotension": "Hypotension", "normal": "Normal", "elevated": "Elevated", "stage_1": "Stage 1", "stage_2": "Stage 2", "hypertensive_crisis": "Hypertensive Crisis"}` (values verbatim from `app/deps.py` BPCategory) and `AMPM_TOKEN_TO_LABEL = {"all": "all", "am": "AM", "pm": "PM"}`. Chart tokens pass through unmapped.
       - Module docstring cites API-04, D-13, D-14, Pitfalls 2/3.
    5. `backend/tests/test_agent_schemas.py` (style: `backend/tests/test_api_readings.py` — flat test functions, parametrize for token tables): parse a full `AgentOutput` dict for each of the five result variants; assert mixed-case drift normalizes (`{"action": "Command", "chart": "Pulse_Trend"}`-style input parses — parametrize over cases like "AM"/"Stage_1"/"Hypertensive_Crisis" in nested date_range and top-level fields); assert `Clarification.question` is NOT lowercased; assert `LastNDays(n=1000)` parses (no bounds at schema level); assert `AgentRequest` rejects text over 500 chars and empty text (ValidationError); assert `CustomRange` serializes with JSON key `"from"` via `model_dump(by_alias=True)`; assert `BP_TOKEN_TO_LABEL` values exactly match the `BPCategory` Literal args imported from `app.deps`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_agent_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `anthropic==0.117.*`, `slowapi==0.1.*`, a `markers` list with a `live:` entry, and `addopts = "-m 'not live'"`
    - `python -c "import anthropic, slowapi"` exits 0 in the backend venv
    - `backend/app/config.py` contains `anthropic_api_key: str = ""` and `env_file=".env"`
    - `cd backend && python -m pytest tests/test_agent_schemas.py -x` exits 0
    - `cd backend && python -m pytest -q` exits 0 with NO `ANTHROPIC_API_KEY` in the environment (keyless boot, SEC-02/Pitfall 9)
    - `grep -c "ge=\|le=\|discriminator" backend/app/agent/schemas.py` finds no matches on Claude-facing models (grep for `discriminator=` returns 0 matches in the file)
  </acceptance_criteria>
  <done>All five AgentOutput variants round-trip through Pydantic with case-drift normalization; API models enforce bounds; both new deps import; full existing suite still green keyless.</done>
</task>

<task type="auto">
  <name>Task 2: Symbolic date resolver with frontend parity tests</name>
  <files>backend/app/agent/resolver.py, backend/tests/test_agent_resolver.py</files>
  <read_first>
    - frontend/src/lib/dates.ts (the `resolveFilters` arithmetic to mirror EXACTLY: anchor = latest reading's date part, N-day range = anchor − (N−1) through anchor, inclusive end, "all" when no anchor)
    - frontend/src/lib/dates.test.ts (the parity expectations to port: anchor "2025-06-13T09:21:00" → 30d = 2025-05-15..2025-06-13, 7d = 2025-06-07..2025-06-13, 90d = 2025-03-16..2025-06-13)
    - backend/app/agent/schemas.py (the DateRange union members this resolves, from Task 1)
    - backend/app/deps.py lines 10-14 and 68-71 (inclusive end_date semantics the emitted strings feed)
  </read_first>
  <action>
    Create `backend/app/agent/resolver.py` (stdlib only: `datetime.date`, `calendar.monthrange`). Module docstring cites API-05, D-15, RESEARCH Pitfall 4, and pins the invariant: NEVER call `date.today()` or `datetime.now()` — data ends 2025-06-13; today-anchoring would blank the dashboard forever.

    Public surface:
    - `n_day_range(latest: date, n: int) -> tuple[date, date]` — returns `(latest - timedelta(days=n-1), latest)`; exposed separately so parity tests hit the exact arithmetic.
    - `class ResolvedDates` (plain dataclass or Pydantic): either `date_preset: str | None` (one of "7d","30d","90d","all") or `custom_from: str / custom_to: str` ("YYYY-MM-DD") — plus a module-level sentinel/exception `InvalidRange` for garbage that must become an `unclear` reply.
    - `resolve_date_range(dr: DateRange | None, earliest: date | None, latest: date | None) -> ResolvedDates | None` with these locked rules:
      * `None` input → `None` (no date change — D-13 carry-over).
      * `PresetRange` → pass `preset` through as `date_preset`.
      * `LastNDays`: `n < 1` → `InvalidRange`; `n in {7, 30, 90}` → the matching preset token; `latest is None` → `date_preset="all"`; otherwise compute `n_day_range(latest, n)` — if `earliest` is not None and the computed start ≤ `earliest`, emit `date_preset="all"` (range covers everything; honest confirmation), else emit customRange strings via `date.isoformat()`.
      * `MonthRange`: `latest is None` → `"all"`. Year inference (RESEARCH A5): `year = mr.year or latest.year`; build `start = date(year, start_month_number, 1)`; if `start > latest`, roll back one year. End: `end_month is None` → `end = latest` ("since June" semantics); else `end = date(end_year, end_month_number, monthrange(end_year, end_month_number)[1])` where `end_year = start.year + 1 if end_month_number < start_month_number else start.year` (handles "November through February"). Emit customRange. Month-name→number via a dict from the `MonthToken` values.
      * `AbsoluteRange`: both fields None → `None`. Parse present fields with `date.fromisoformat` — any parse failure → `InvalidRange`. Missing `from_date` → use `earliest` (if `earliest` None → `date_preset="all"`); missing `to_date` → use `latest` (same fallback). If `from > to` → `InvalidRange`. Emit customRange.
    Create `backend/tests/test_agent_resolver.py` porting the frontend parity expectations with `LATEST = date(2025, 6, 13)`, `EARLIEST = date(2025, 2, 22)`:
    - `n_day_range(LATEST, 30) == (date(2025,5,15), date(2025,6,13))`; same for 7 → 2025-06-07 and 90 → 2025-03-16 (exact strings from dates.test.ts — if these fail the resolver has drifted, Pitfall 4).
    - `LastNDays(n=30)` → preset "30d"; `n=14` → custom 2025-05-31..2025-06-13; `n=500` → preset "all" (start precedes earliest); `n=0` → InvalidRange; `latest=None` → "all".
    - `MonthRange(february..april)` → 2025-02-01..2025-04-30; `MonthRange(june, end=None)` → 2025-06-01..2025-06-13 ("since June", D-15); `MonthRange(december, end=december)` → 2024-12-01..2024-12-31 (year rollback); `MonthRange(november..february)` → cross-year end.
    - `AbsoluteRange(from="2025-03-01", to="2025-04-15")` → passthrough; `to` only → from=EARLIEST; malformed "March 1st" → InvalidRange; from > to → InvalidRange.
    - Source-hygiene test: read `inspect.getsource(resolver_module)` and assert `"date.today" not in source and "datetime.now" not in source`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_agent_resolver.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/agent/resolver.py` contains `def resolve_date_range(` and `def n_day_range(`
    - `cd backend && python -m pytest tests/test_agent_resolver.py -x` exits 0
    - Parity assertions match dates.test.ts exactly: 30d→(2025-05-15, 2025-06-13), 7d→(2025-06-07, 2025-06-13), 90d→(2025-03-16, 2025-06-13)
    - Test asserting `date.today`/`datetime.now` absent from resolver source passes
  </acceptance_criteria>
  <done>Python resolver reproduces the frontend resolveFilters arithmetic for every symbolic form (API-05, D-15); invalid inputs surface as InvalidRange for the service to translate into friendly copy.</done>
</task>

<task type="auto">
  <name>Task 3: System prompt, one-turn message builder, and fixed copy templates</name>
  <files>backend/app/agent/prompt.py, backend/app/agent/copy.py, backend/tests/test_agent_schemas.py</files>
  <read_first>
    - .planning/phases/03-agent-via-text-input/03-RESEARCH.md Code Example 6 (build_messages shape + settled system-prompt content list)
    - backend/app/agent/schemas.py (ClarifyContext + the exact vocabulary tokens the prompt must enumerate, from Task 1)
    - backend/app/routers/stats.py lines 26-29 (module-constant style with provenance comment — copy.py follows this)
    - frontend/src/App.tsx lines 59-84 (the established friendly, non-technical copy register)
  </read_first>
  <action>
    1. `backend/app/agent/prompt.py`:
       - `SYSTEM_PROMPT` (module constant, static — user text NEVER interpolated into it, prompt-injection hygiene per RESEARCH anti-patterns). Content must cover, in plain instruction prose: role = interpreter for a fixed health-dashboard command vocabulary, output only the structured schema; input may be a speech transcript with recognition errors — when one reading is clearly most likely, choose it (D-09 confident-guess; garbled "sho me blod preshure" → blood pressure); enumerate the chart vocabulary with synonyms ("blood pressure"/"BP" → bp_timeline, "pulse"/"heart rate" → pulse_trend, "categories" → bp_categories, "morning vs evening comparison" → am_pm_comparison), AM/PM synonyms ("mornings" → am, "evenings"/"afternoons"/"nights" → pm), BP category names, and the symbolic date forms; NEVER compute absolute dates — emit presets, last_n_days, month tokens, or from/to strings only (API-05); a single named month ("show me May") → start_month = end_month = that month; "since <month>" → end_month omitted; partial commands set ONLY the fields the user mentioned, leave everything else null (D-13); "show all data"/"start over"/"reset" → reset=true; requests for medical interpretation, danger assessment, diagnosis, or reassurance → refuse_medical with the most relevant chart (D-10, VOICE-09); questions about averages/statistics → data_question with the relevant chart (D-16); genuinely ambiguous commands → clarify with ONE short, non-technical question (D-09); relative adjustments ("go back further", "zoom out") are NOT supported → treat as unclear or ask to rephrase (D-14); pure gibberish → unclear (D-11).
       - `build_messages(text: str, context: ClarifyContext | None) -> list[dict]` exactly per RESEARCH Code Example 6: no context → single user message; with context → `[{"role":"user","content":context.original_text}, {"role":"assistant","content":context.question}, {"role":"user","content":text}]` (D-12 one-turn memory).
    2. `backend/app/agent/copy.py` (UPPER_CASE module constants with provenance comments, register matching App.tsx friendliness; cites VOICE-07/VOICE-09/D-10/D-11/D-16):
       - `EXAMPLE_COMMANDS = ["show my pulse", "last 30 days, mornings only", "show all data"]`
       - `UNCLEAR_MESSAGE` — didn't-catch copy that MUST embed 2–3 examples, e.g. "Sorry, I didn't catch that. Try: 'show my pulse' or 'last 30 days, mornings only'." (D-11 — every failure teaches)
       - `UNAVAILABLE_MESSAGE` — keyless/agent-down copy, e.g. "The assistant isn't connected right now. The buttons below still work." (Pitfall 9 graceful degradation)
       - `MEDICAL_REFUSAL_WITH_CHART` / `MEDICAL_REFUSAL_PLAIN` — templates pairing the boundary with a useful action, non-alarming, pointing to the care team, e.g. "I can't interpret readings — that's a question for the care team. Here's the {chart_phrase} data." (D-10, VOICE-09); include a `CHART_PHRASES` dict mapping the four chart tokens to human phrases ("blood pressure", "pulse", "BP categories", "AM vs PM comparison").
       - `DATA_QUESTION_MESSAGE = "Your averages are in the stats bar below."` (D-16 verbatim from CONTEXT)
       Exact wording is Claude's-discretion per CONTEXT — keep it non-technical, non-alarming, large-text friendly; the STRUCTURE above (examples embedded, action paired with refusal) is locked.
    3. Append to `backend/tests/test_agent_schemas.py`: `build_messages("show pulse", None)` returns one user message with that text; `build_messages("mornings", ClarifyContext(original_text="show me the mornings one", question="Which chart?"))` returns the exact 3-message [user, assistant, user] structure with contents in that order (D-12); `UNCLEAR_MESSAGE` contains at least two entries of `EXAMPLE_COMMANDS` (substring assertions); `SYSTEM_PROMPT` is a non-empty str containing the four chart tokens and the word "never" near date computation (assert `"bp_timeline" in SYSTEM_PROMPT` etc.); assert user text placeholder-free: `"{" not in SYSTEM_PROMPT` is too strict if templates used — instead assert `SYSTEM_PROMPT` does not change between two calls (it is a constant, not a function).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_agent_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/agent/prompt.py` contains `SYSTEM_PROMPT` and `def build_messages(`
    - `backend/app/agent/copy.py` contains `UNCLEAR_MESSAGE`, `UNAVAILABLE_MESSAGE`, `DATA_QUESTION_MESSAGE`, and a `CHART_PHRASES` dict with all four chart tokens as keys
    - Test asserts build_messages with context returns exactly 3 messages ordered [user, assistant, user] (D-12)
    - Test asserts UNCLEAR_MESSAGE contains ≥2 example commands (D-11)
    - `cd backend && python -m pytest -q` exits 0
  </acceptance_criteria>
  <done>Prompt and copy modules exist with locked structure; one-turn memory assembly is unit-proven; all safety copy is fixed server-side template text (VOICE-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text → Claude prompt | untrusted command/transcript text enters model context |
| Claude output → backend | model output is untrusted input (API-04) |
| env → Settings | API key custody (SEC-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (prompt injection) | prompt.py build_messages | mitigate | user text confined to `user` role messages; SYSTEM_PROMPT is a static constant, never interpolated; output constrained to closed AgentOutput schema — worst case is a wrong filter, visible via D-07 echo |
| T-03-02 | Tampering (schema escape) | schemas.py AgentOutput | mitigate | closed Literal token unions, constraint-free wire schema + service-layer clamps; lowercase normalizer prevents drift-induced parse bypass |
| T-03-03 | Information disclosure (key) | config.py Settings | mitigate | `anthropic_api_key` via pydantic-settings env/.env only; empty default; never serialized in any response model (SEC-02) |
| T-03-04 | DoS (oversized input) | AgentRequest | mitigate | `max_length=500` on text bounds prompt cost (ASVS V5) |
| T-03-05 | Domain safety (medical harm copy) | copy.py | mitigate | refusal/didn't-catch copy are fixed templates, never model prose (VOICE-09) |
| T-03-SC | Tampering (supply chain) | pip install anthropic/slowapi | accept | both packages [OK]-approved in RESEARCH Package Legitimacy Audit (official vendor SDK; long-established limiter) — no [ASSUMED]/[SUS] installs in this phase |
</threat_model>

<verification>
- `cd backend && python -m pytest -q` — entire backend suite green with no ANTHROPIC_API_KEY set
- `cd backend && python -m pytest -m live -q --collect-only` — collects nothing yet (live marker registered, no live tests until 03-03)
- Resolver parity trio (7/30/90) exactly matches frontend dates.test.ts strings
</verification>

<success_criteria>
- All 9 files created/modified; both test files green; full suite green keyless
- Claude-facing schema is provably constraint-free and closed-vocabulary (API-04, D-14)
- Resolver mirrors frontend arithmetic with inclusive ends and latest-reading anchor (API-05, D-15)
- Prompt encodes D-09/D-10/D-11/D-12/D-13/D-16 routing; copy templates are fixed and example-bearing
</success_criteria>

<output>
Create `.planning/phases/03-agent-via-text-input/03-01-SUMMARY.md` when done
</output>
