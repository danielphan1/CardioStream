---
phase: 03-agent-via-text-input
plan: 04
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - frontend/src/components/CommandBar.tsx
  - frontend/src/components/CommandBar.test.tsx
  - frontend/src/App.tsx
  - frontend/src/components/FilterBar.tsx
autonomous: true
requirements: [VOICE-06, VOICE-07, VOICE-08]
must_haves:
  truths:
    - "A full-width command bar sits between the header and the filter bar — the primary control gets top billing, ready to host Phase 4's mic button (D-01)"
    - "The input teaches its own vocabulary via a rotating example placeholder at ≥18px high contrast, with a real accessible name independent of the placeholder (D-02)"
    - "Submitted text stays visible with a spinner + bar highlight during the Claude round-trip and clears only when a command is applied (D-03)"
    - "Submission works via a ≥48px labeled Send button AND the Enter key (D-04)"
    - "The Working state resolves into the confirmation/clarification/refusal message in the bar itself, in an aria-live region, persisting until the next command (D-05, D-06, VOICE-06)"
    - "Applied commands show the frontend-composed full-state echo, with the D-16 stats-bar message appended when present (D-07)"
    - "Clarify replies store a one-turn context that is resent with the next submit (D-12)"
    - "429 and network errors render fixed friendly copy with example commands — never a raw error (VOICE-07, D-11)"
    - "Filter controls affected by an agent command pulse briefly, gated behind motion-safe (D-08)"
  artifacts:
    - path: "frontend/src/components/CommandBar.tsx"
      provides: "command input + state machine idle/working/confirmed/clarify/error"
      min_lines: 100
    - path: "frontend/src/components/CommandBar.test.tsx"
      provides: "state-machine, store-effect, context-resend, and error-copy tests"
      min_lines: 80
    - path: "frontend/src/App.tsx"
      provides: "CommandBar mounted between Header and main content"
      contains: "CommandBar"
    - path: "frontend/src/components/FilterBar.tsx"
      provides: "D-08 motion-safe pulse on agent-touched control groups"
      contains: "useAgentPulse"
  key_links:
    - from: "frontend/src/components/CommandBar.tsx"
      to: "frontend/src/lib/agent.ts"
      via: "applyAgentFilters + composeConfirmation on applied replies"
      pattern: "composeConfirmation"
    - from: "frontend/src/components/CommandBar.tsx"
      to: "frontend/src/hooks/useAgent.ts"
      via: "useAgent mutation"
      pattern: "useAgent"
    - from: "frontend/src/components/FilterBar.tsx"
      to: "frontend/src/lib/agent.ts"
      via: "useAgentPulse subscription"
      pattern: "useAgentPulse"
---

<objective>
Build the CommandBar (D-01..D-07, D-12 client memory), mount it in App between Header and the dashboard content, and add the D-08 motion-safe pulse to FilterBar — completing the visible half of the text→agent→dashboard loop (VOICE-06/07/08).

Purpose: This is the surface Chris and caregivers actually use; Phase 4 adds a mic button and live transcript to this exact bar without restructuring.
Output: CommandBar component + tests, App/FilterBar integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-via-text-input/03-CONTEXT.md
@.planning/phases/03-agent-via-text-input/03-PATTERNS.md
@.planning/phases/03-agent-via-text-input/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommandBar component + state-machine tests</name>
  <files>frontend/src/components/CommandBar.tsx, frontend/src/components/CommandBar.test.tsx</files>
  <read_first>
    - frontend/src/components/FilterBar.tsx (control class recipes: `min-h-12 rounded-lg px-4 text-[20px] font-bold` + CSS-variable palette; section wrapper `bg-[var(--color-sky)] p-4`; aria-live precedent lines 160-163; decision-ID header comment convention)
    - frontend/src/App.tsx lines 73-80 (primary accent button recipe for Send: `min-h-12 rounded-lg bg-[var(--color-accent)] px-6 text-xl font-bold text-[var(--color-accent-text)]`) and lines 59-84 (friendly error copy register)
    - frontend/src/lib/agent.ts (applyAgentFilters, composeConfirmation, useAgentPulse — from 03-02)
    - frontend/src/hooks/useAgent.ts and frontend/src/api/types.ts (AgentReply/ClarifyContext shapes)
    - frontend/src/components/ReadingsTable.test.tsx (component test style: header contract comment, testing-library render/screen/fireEvent, role-based queries)
  </read_first>
  <action>
    Create `frontend/src/components/CommandBar.tsx` (header comment citing D-01..D-07, D-12, VOICE-06/07/08):
    - Props: `{ latestReading: string | null }`.
    - Local state (useState — NOT the zustand store, per PATTERNS): `text: string`, `status: "idle" | "working" | "confirmed" | "clarify" | "error"`, `message: string`, `clarifyContext: ClarifyContext | null`, `exampleIdx: number`.
    - Rotating placeholder (D-02): `const EXAMPLES = ['show my pulse', 'last 30 days, mornings only', 'show blood pressure', 'show all data']`; a useEffect interval (~6s, cleared on unmount) advances exampleIdx only while status is "idle" and text is empty; placeholder = `Try: "${EXAMPLES[exampleIdx]}"`. Input has `aria-label="Type a dashboard command"` — the placeholder is NOT the accessible name (Pitfall 8).
    - Form submit (Enter via form onSubmit + Send button type="submit", D-04): trimmed-empty → no-op; set status "working" (text REMAINS in the input, input+button disabled, spinner visible, bar highlight via accent border/ring — D-03); call `mutate({ text: trimmed, context: clarifyContext })` from `useAgent()`.
    - onSuccess by `reply.kind`:
      * "applied" → `applyAgentFilters(reply.filters ?? {})`; `msg = composeConfirmation(useFilters.getState(), latestReading)` then append `" " + reply.message` when reply.message non-empty (D-16 stats-bar pointer); set message, status "confirmed", CLEAR text (D-03), clear clarifyContext.
      * "clarify" → message = reply.message, status "clarify", store `reply.context` in clarifyContext (D-12 — resent on next submit), clear text so the answer can be typed.
      * "refuse" → if reply.filters present `applyAgentFilters(reply.filters)` (D-10 useful-action chart switch), message = reply.message, status "confirmed", clear text and context.
      * "unclear" → message = reply.message, status "error" (visually calm, not red-alarm), keep text so the user can edit it, clear context.
    - onError (ApiError): status "error"; message = fixed local copy — status 429 → "One moment — a lot of commands at once. Try again in a few seconds."; anything else → "Couldn't reach the assistant. The buttons below still work. Try: 'show my pulse'." NEVER render error.message (client.ts rule, VOICE-07).
    - Message region: `<p aria-live="polite" className="... text-[18px] ...">` rendering `message` as a text node only; persists until next submit (D-05/D-06); prefix a subtle visual state marker (e.g. check mark char for confirmed) — no color-only signaling.
    - Layout: `<section aria-label="Command bar" className="bg-[var(--color-sky)] p-4">` containing a form row: input (`min-h-12 text-[18px]` min, full flex-grow, high-contrast border 2px ink) + Send button (App.tsx accent recipe, visible text "Send", ≥48px). Spinner = existing Tailwind `animate-spin` pattern or text "Working…" at ≥18px — both shown during working.

    Create `frontend/src/components/CommandBar.test.tsx` — mock `../api/client`'s `postAgent` with `vi.mock` (real useAgent + real QueryClientProvider wrapper, real zustand stores reset in beforeEach):
    - Typing + Enter submits: postAgent called with `{text: "show my pulse", context: null}`.
    - Working: pending promise → input disabled, submitted text still in input, "Working" indicator present (D-03).
    - Applied: resolve `{kind: "applied", filters: {activeChart: "pulse_trend"}, message: "", context: null}` → store activeChart becomes "pulse_trend", aria-live region shows a string starting with "Showing pulse" (composeConfirmation output), input cleared.
    - Applied + message: reply message "Your averages are in the stats bar below." → confirmation ends with that sentence (D-16).
    - Clarify then follow-up: resolve clarify with context `{original_text: "show me the numbers", question: "Which chart?"}` → question shown; type "pulse" + submit → SECOND postAgent call body includes that exact context (D-12).
    - Unclear: message rendered verbatim; input NOT cleared.
    - 429: postAgent rejects with `new ApiError(429, "/agent")` → friendly rate copy shown, no raw status text; network `ApiError(0)` → offline copy containing an example command.
    - Accessibility: input has accessible name "Type a dashboard command"; button role with name "Send"; message container has `aria-live="polite"`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/CommandBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/CommandBar.test.tsx` exits 0 with ≥9 passing tests
    - Test proves clarify context round-trip on the second submit (D-12)
    - Test proves input keeps text during working and clears only on applied (D-03)
    - `grep -c "dangerouslySetInnerHTML" frontend/src/components/CommandBar.tsx` returns 0
    - CommandBar.tsx contains `aria-live="polite"`, `aria-label`, and `min-h-12` on both input and Send button classes
  </acceptance_criteria>
  <done>CommandBar implements the full D-01..D-07 + D-12 interaction contract with deterministic tests and no backend dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Mount in App + FilterBar D-08 pulse</name>
  <files>frontend/src/App.tsx, frontend/src/components/FilterBar.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current layout: `<Header />` then `<main className="mx-auto flex max-w-[1280px] ...">`; `latestReading` already computed from `stats.data?.latest_reading ?? null` around line 51)
    - frontend/src/components/FilterBar.tsx (the three role="group" control sections — date presets, AM/PM, category — where the pulse attaches; existing class recipes)
    - frontend/src/lib/agent.ts (useAgentPulse: `{seq, fields: PulseField[]}` — fields are "chart" | "dateRange" | "amPm" | "bpCategory")
  </read_first>
  <action>
    1. `frontend/src/App.tsx`: render `<CommandBar latestReading={latestReading} />` between `<Header />` and `<main>` inside a full-width wrapper so the bar spans the viewport (D-01): `<section>` with `bg-[var(--color-sky)]` full-width, inner `<div className="mx-auto max-w-[1280px] px-4 md:px-8 xl:px-16">` matching main's gutters (CommandBar's own section styling may fold into this wrapper — keep exactly one bg layer). `latestReading` is already computed in App before the return — pass it down. No other App changes; data fetching stays at App level (Phase 2 invariant).
    2. `frontend/src/components/FilterBar.tsx` (D-08): subscribe `const pulseSeq = useAgentPulse((s) => s.seq)` and `const pulseFields = useAgentPulse((s) => s.fields)`; local `const [pulsing, setPulsing] = useState<PulseField[]>([])`; `useEffect` on `pulseSeq`: if seq > 0 set `pulsing` to `pulseFields`, then `setTimeout(() => setPulsing([]), 1500)` (clear timeout on cleanup). On each of the three control-group wrappers, conditionally append `motion-safe:animate-pulse` (Tailwind built-in; `motion-safe:` gates it behind prefers-reduced-motion — reduced-motion users see no animation, D-08 requirement) plus a non-animated fallback highlight `ring-2 ring-[var(--color-accent)]` while its field ("dateRange" for both preset and custom-range groups, "amPm", "bpCategory") is in `pulsing` — the ring shows for reduced-motion users too, so the change is still perceivable without motion. Do NOT add pulse handling for "chart" here — ChartDeck's existing keyed mount-fade already covers agent-driven chart switches (CONTEXT).
    3. Run the full frontend suite; fix any FilterBar test fallout (pulse must not alter existing DOM roles/names).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/App.tsx` renders `<CommandBar` positioned before `<main` in JSX source order (source assertion)
    - `frontend/src/components/FilterBar.tsx` contains `useAgentPulse` and `motion-safe:animate-pulse`
    - `npx tsc --noEmit` exits 0 and `npx vitest run` exits 0 (entire frontend suite)
    - `grep -c "setTimeout" frontend/src/components/FilterBar.tsx` ≥ 1 (pulse self-clears)
  </acceptance_criteria>
  <done>Command bar is live in the layout between header and dashboard (D-01); agent-touched filter groups pulse motion-safely with a static ring fallback (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /agent reply → DOM | server strings (incl. one Claude-authored clarify question) render in the bar |
| /agent reply → filter store | validated delta mutates dashboard state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering (XSS via clarify question) | CommandBar message region | mitigate | message rendered exclusively as React text node inside aria-live `<p>`; grep gate: no dangerouslySetInnerHTML |
| T-03-17 | Information disclosure (raw errors) | onError handler | mitigate | fixed local copy for 429/0/anything; error.message never rendered (VOICE-07) |
| T-03-18 | DoS (rapid resubmission cost) | submit handler | mitigate | input + Send disabled during "working"; backend slowapi is the authoritative limit, 429 mapped to friendly copy |
| T-03-19 | Spoofing (n/a) | — | accept | no auth UI this phase; Bearer gate lands Phase 5 per roadmap |
| T-03-SC | Tampering (supply chain) | npm installs | accept | no new npm packages in this plan |
</threat_model>

<verification>
- `cd frontend && npx vitest run` — full suite green including CommandBar + existing FilterBar/store tests
- `npx tsc --noEmit` — clean typecheck
</verification>

<success_criteria>
- CommandBar delivers D-01 placement, D-02 rotating placeholder + accessible name, D-03 working state, D-04 ≥48px Send + Enter, D-05/D-06 persistent in-bar resolution, D-07 full-state echo, D-12 context resend
- VOICE-07 friendly copy for every client-visible failure (429, network, unclear)
- D-08 pulse gated motion-safe with non-motion ring fallback
</success_criteria>

<output>
Create `.planning/phases/03-agent-via-text-input/03-04-SUMMARY.md` when done
</output>
