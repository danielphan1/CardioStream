---
phase: 04-voice-capture
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - frontend/src/components/CommandBar.tsx
  - frontend/src/components/CommandBar.test.tsx
  - .planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md
autonomous: false
requirements: [VOICE-01, VOICE-02, VOICE-03, VOICE-04]
must_haves:
  truths:
    - "A caregiver can tap one ≥48px mic button to open a hands-free session and tap again to stop"
    - "The command bar shows listening / working / stopped / paused states with color + word + icon together, legible across a room"
    - "A live transcript of the recognized command (trigger word stripped) streams in green while listening"
    - "The three animated states honor prefers-reduced-motion with a static fallback and the text input stays usable in the paused state"
    - "The restart-on-onend loop and a 10-minute continuous session are verified on a real iOS device"
  artifacts:
    - path: "frontend/src/components/CommandBar.tsx"
      provides: "Mic button + voice state indicator + live transcript on the existing bar (no new component surface)"
      contains: "useVoiceCommand"
      min_lines: 230
    - path: ".planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md"
      provides: "Manual real-device verification script (restart loop + 10-min session + cross-browser)"
      min_lines: 25
  key_links:
    - from: "frontend/src/components/CommandBar.tsx"
      to: "frontend/src/hooks/useVoiceCommand.ts"
      via: "mic tap → start()/stop(); renders state/interim/message"
      pattern: "useVoiceCommand"
    - from: "CommandBar mic button onClick"
      to: "useVoiceCommand.start()"
      via: "synchronous call in the tap handler (D-01 user gesture)"
      pattern: "start\\(\\)"
---

<objective>
Mount the voice layer onto the existing `CommandBar` (D-06 — one element, no second component): a ≥48px mic button, the three-state indicator (color + word + icon/motion, D-07), and the live green transcript (D-04/D-10/D-11), then verify the restart loop and a 10-minute continuous session on a real iOS device (the phase's #1 documented risk).

Implements D-06 (bar transforms), D-07 (color+word+icon triad, never color alone), D-08 (visual only), D-09 (motion-safe + static reduced-motion fallback), D-10/D-11 (armed hint → streaming interim → WORKING → confirmation replaces transcript in one spot), D-13 (explicit stop), D-14 (paused copy + text input stays usable). Consumes `useVoiceCommand` from Wave 2 and every design value from 04-UI-SPEC.md (zero new tokens/colors/fonts).

Purpose: deliver the hands-free experience Chris uses and prove it works on the device that matters.
Output: extended `CommandBar.tsx` (+ `CommandBar.test.tsx`), the iOS test checklist artifact, and a passed on-device checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voice-capture/04-UI-SPEC.md
@.planning/phases/04-voice-capture/04-PATTERNS.md
@.planning/phases/04-voice-capture/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CommandBar with mic button, 3-state indicator, and live transcript</name>
  <files>frontend/src/components/CommandBar.tsx, frontend/src/components/CommandBar.test.tsx</files>
  <read_first>
    - frontend/src/components/CommandBar.tsx (whole file — the state machine, form row lines 158-175, working ring lines 153-157, working spinner lines 179-187, aria-live region lines 193-205, fixed-copy constants lines 46-50, MARKER lines 55-61 — extend IN PLACE, do not add a second component)
    - frontend/src/hooks/useVoiceCommand.ts (the Wave-2 contract: { supported, state, interim, message, start, stop })
    - frontend/src/components/FilterBar.tsx (lines 60-78 pulseClass — the motion-safe:animate-pulse + static ring-2 reduced-motion precedent to copy for the LISTENING ring, D-09)
    - .planning/phases/04-voice-capture/04-UI-SPEC.md §Color (the state→token table: --cat-normal green LISTENING, --cat-elevated amber WORKING, Mic/MicOff glyphs), §Typography (state word 20px/700), §Spacing (mic ≥48px min-h-12 min-w-12), §Copywriting (aria-labels, armed hint, D-14 paused copy)
    - frontend/src/components/CommandBar.test.tsx (whole file — extend the existing suite; reuse renderBar/mock-postAgent harness)
  </read_first>
  <behavior>
    - A mic button with a state-dependent aria-label ("Start voice control" when stopped, "Stop voice control" when a session is open) renders in the existing form row at ≥48px (min-h-12 min-w-12); clicking it calls useVoiceCommand.start() (session open, D-01) / stop()
    - When voice state is "listening" and untriggered, the bar shows the armed hint text interpolating WAKE_WORD: `LISTENING — say "dashboard…"` and a green (--cat-normal) motion-safe:animate-pulse ring with a static ring-2 fallback (D-10/D-09)
    - When state is "triggered", the live interim (already stripped of the wake word) renders in green in the aria-live region (D-10/D-11)
    - When state is "working", the existing amber working ring + spinner + `WORKING…` word show; reduced motion collapses the spin to a static glyph (D-09)
    - On a resolved command, the confirmation `message` REPLACES the transcript in the same aria-live spot (D-11); charts switch via the store
    - When state is "paused", the bar shows `Voice paused — tap to resume` with a MicOff glyph, and the text input + Send button remain enabled and submit through the existing text path (D-14 / VOICE-08)
    - Firefox (supported=false): the mic button is hidden/absent; the text input works unchanged (VOICE-08). No color-only signaling anywhere — every state pairs word + icon + color (D-07)
  </behavior>
  <action>
    Extend `CommandBar.tsx` in place (D-06). Call `useVoiceCommand({ latestReading })` and destructure `{ supported, state, interim, message: voiceMessage, start, stop }`. Add a mic `<button type="button">` into the existing `flex flex-wrap items-center gap-3` form row (planner discretion on left/right placement; keep the inherited `gap-3`), styled per UI-SPEC: `min-h-12 min-w-12` (56px `h-14 w-14` permitted), ink-bordered `--color-sky`/`--color-foam` surface (NOT accent-filled), a `lucide-react` `Mic` glyph when stopped/listening and `MicOff` when paused, and a state-dependent `aria-label` (`"Start voice control"` / `"Stop voice control"`) — never placeholder-derived. `onClick` calls `start()` synchronously when stopped (D-01 user gesture) and `stop()` when a session is open. Render the mic button only when `supported` (Firefox → text-only, VOICE-08). Drive the bar ring/word/transcript from voice `state`: reuse the existing `ring-2 ring-[var(--color-accent)]` for WORKING; ADD a green LISTENING ring using `var(--cat-normal)` with `motion-safe:animate-pulse` + static `ring-2` fallback, copying the FilterBar `pulseClass` structure (D-09). Route the state WORD (`LISTENING — say "…"` with WAKE_WORD interpolated / `WORKING…` / `Voice paused — tap to resume`) and the green interim transcript through the EXISTING `aria-live="polite"` region; keep glyphs `aria-hidden` (color+word+icon triad, D-07). The confirmation `voiceMessage` replaces the transcript in that one spot (D-11). Add the D-14 paused copy as a peer fixed constant next to `RATE_LIMIT_COPY`/`OFFLINE_COPY`; raw recognizer error strings never render (VOICE-07). Keep the text input + Send fully enabled whenever a voice session is not actively `working` so the paused/unsupported fallback is real (VOICE-08). Use only existing `var(--…)` tokens — zero new colors/hex/fonts. Extend `CommandBar.test.tsx` (reusing its postAgent mock + renderBar harness) with cases for each behavior above, driving voice via the FakeRecognition install from Wave 1; assert the mic aria-label state swap, the wake-word gate (no confirmation on room speech), the streamed stripped transcript, the WORKING word, the confirmation replacing the transcript, and the paused state keeping the input enabled. Assert no color-only signaling by checking the state WORD text is present for each state.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/CommandBar.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/CommandBar.test.tsx` green including the new voice cases (mic aria-label swap, wake-word gate, stripped transcript, WORKING word, confirmation replaces transcript, paused keeps input enabled)
    - `grep -q 'useVoiceCommand' frontend/src/components/CommandBar.tsx` and `grep -q 'min-h-12' frontend/src/components/CommandBar.tsx` succeed (hook wired, ≥48px target)
    - `grep -q 'var(--cat-normal)' frontend/src/components/CommandBar.tsx` and `grep -q 'motion-safe:animate-pulse' frontend/src/components/CommandBar.tsx` succeed (D-07 green LISTENING + D-09 reduced-motion); `grep -Ei '#[0-9a-f]{3,6}' frontend/src/components/CommandBar.tsx` returns nothing (no hardcoded hex — token reuse only)
    - `cd frontend && npx vitest run` full suite green; `npx tsc -b` no errors
  </acceptance_criteria>
  <done>The existing bar now carries a ≥48px mic button, a color+word+icon 3-state indicator with reduced-motion fallback, and a live stripped green transcript — all on one surface, tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the real-device (iOS + cross-browser) manual test checklist</name>
  <files>.planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/04-voice-capture/04-RESEARCH.md §"Open Questions" (the tap → command → 60s silence → command spike; the 10-min session), §"Environment Availability" (real iPhone, no CI substitute), §"Common Pitfalls 1-3" (iOS silence auto-stop, background death, Android beep)
    - .planning/ROADMAP.md §"Phase 4" Success Criteria 1 & 5 (real-iOS restart loop + 10-min continuous session)
  </read_first>
  <action>
    Create `.planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md` — a concrete, numbered manual verification script the checkpoint in Task 3 follows. Cover: (1) permission flow — first mic tap prompts once, calm copy, granting opens the session; (2) restart-loop spike (RESEARCH Open Q1) — tap mic, say "dashboard show pulse" (chart switches), stay SILENT 60s, then say "dashboard last 30 days" and confirm it still applies WITHOUT re-tapping (D-12/SC1); (3) 10-minute continuous session with intermittent long silences — the indicator stays LISTENING throughout, no caregiver re-tap needed (SC5); (4) trigger-gating — room chatter without "dashboard" never changes the dashboard (D-02); (5) hard-failure — deny/revoke mic permission → bar shows "Voice paused — tap to resume" and the text input still works (D-14/VOICE-08); (6) cross-browser matrix — repeat the core flow on desktop Chrome/Edge (continuous=true path) and Safari/iOS (restart-loop path); note the Chrome-Android restart beep as an expected caveat if an Android device is used. Each item has an explicit expected result and a pass/fail checkbox. Head the doc with the exact device/OS/browser versions to record and a line to capture the final `WAKE_WORD` false-trigger impression (D-04 UAT knob).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md && grep -Eqi '10[- ]?min' .planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md && grep -qi 'silen' .planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The checklist file exists and contains numbered steps for: permission flow, restart-loop spike (60s silence), 10-minute session, trigger-gating, hard-failure fallback, and a Chrome/Edge + Safari/iOS cross-browser matrix
    - Each step states an explicit expected result and has a pass/fail marker
    - `grep -Eqi '10[- ]?min' && grep -qi 'silen'` succeeds (10-minute + silence criteria present)
  </acceptance_criteria>
  <done>A single, executable device-test script exists that maps ROADMAP SC1 + SC5 to concrete on-device actions for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device verification — iOS restart loop + 10-minute session + cross-browser</name>
  <files>.planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md (the script to execute)
  </read_first>
  <what-built>
    A hands-free voice layer on the existing command bar: tap the mic once to open a continuous session, say "dashboard, …" to issue commands, watch a live green transcript and a listening/working/paused indicator, tap again to stop. The recognizer auto-restarts invisibly through silence (Safari/iOS) and the text input stays usable if voice fails. All logic is unit-tested against a fake recognizer; this checkpoint verifies the one thing CI cannot — real-device behavior (the phase's #1 documented risk, STATE.md blocker).
  </what-built>
  <action>
    HUMAN VERIFICATION (no code change). Run the frontend against the backend (`cd frontend && npm run dev`) and open it on a REAL iPhone in Safari, then separately on desktop Chrome/Edge. Execute every step of `.planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md`, recording device/OS/browser versions. Confirm the five critical passes: (a) restart-loop spike — after 60s of silence a new "dashboard …" command still applies WITHOUT re-tapping (SC1/D-12); (b) a full 10-minute session with long silences keeps LISTENING with no caregiver intervention (SC5); (c) room speech without the wake word never mutates the dashboard (D-02); (d) denying mic permission shows "Voice paused — tap to resume" and the text input still works (D-14); (e) the core flow also works on desktop Chrome/Edge. Record the `"dashboard"` false-trigger impression for the UAT tuning knob (D-04). Report any failure with the exact step + observed behavior so a `--gaps` plan can target it.
  </action>
  <how-to-verify>
    1. Run the frontend against the backend (`cd frontend && npm run dev`) and open it on a REAL iPhone in Safari (and separately on desktop Chrome/Edge).
    2. Execute `.planning/phases/04-voice-capture/04-IOS-TEST-CHECKLIST.md` end to end, recording device/OS/browser versions.
    3. Critical passes required: (a) restart-loop spike — after 60s of silence a new "dashboard …" command still applies without re-tapping (SC1/D-12); (b) a full 10-minute session with long silences keeps LISTENING with no caregiver intervention (SC5); (c) room speech without the wake word never mutates the dashboard (D-02); (d) denying mic permission shows "Voice paused — tap to resume" and the text input still works (D-14); (e) the core flow also works on desktop Chrome/Edge.
    4. Note the `"dashboard"` false-trigger impression for the UAT tuning record (D-04).
  </how-to-verify>
  <verify>
    <human-check>Execute 04-IOS-TEST-CHECKLIST.md on a real iPhone (Safari) + desktop Chrome/Edge; restart-loop spike and 10-minute continuous session both pass without a caregiver re-tap.</human-check>
  </verify>
  <acceptance_criteria>
    - The checklist is executed on a real iPhone (Safari) and on desktop Chrome/Edge, with versions recorded
    - The restart-loop spike and the 10-minute continuous session both pass without a caregiver re-tap
    - The paused-state fallback and the wake-word gate behave as specified
    - Any failure is reported with the exact step + observed behavior so a `--gaps` plan can target it
  </acceptance_criteria>
  <done>Real-iOS restart loop + 10-minute continuous session verified on device (SC1/SC5); core flow confirmed on Chrome/Edge; result recorded (approved or failing steps captured for gap closure).</done>
  <resume-signal>Type "approved" if the restart loop and 10-minute session pass on real iOS (and the core flow passes on Chrome/Edge), or describe the exact failing step + device/OS/browser so gap closure can target it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mic permission grant → open session | The caregiver tap authorizes an always-listening session; the visible LISTENING indicator + explicit stop bound it (no covert listening) |
| recognizer/hook state → rendered UI | Only fixed friendly copy and the stripped transcript render; raw error strings never reach the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Info Disclosure | bar copy / DOM | mitigate | Only fixed copy renders (RATE_LIMIT/OFFLINE/D-14 paused); raw `ApiError`/recognizer `error` strings never surface (VOICE-07); the transcript is display-only and not logged (SEC-03). |
| T-04-05 | Info Disclosure | always-hot mic | mitigate | The LISTENING state is unmissable (color+word+icon, D-07), explicit stop is always available (D-13), and the checkpoint verifies no covert continued listening after stop. |
| T-04-03 | DoS / cost | rapid spoken commands | mitigate | Backoff + seq guard (Wave 2) plus server 429 handling bound request volume; the checkpoint confirms rapid-command behavior on device. |
| T-04-SC | Tampering (supply chain) | npm installs | accept | No new packages added (lucide-react already an audited Phase 2 dependency; native recognizer). |
</threat_model>

<verification>
- `cd frontend && npx vitest run` — full frontend suite green including the extended CommandBar voice cases
- `cd frontend && npx tsc -b` — no type errors
- Manual: `04-IOS-TEST-CHECKLIST.md` executed on a real iPhone (Safari) + desktop Chrome/Edge — restart loop and 10-minute session pass (checkpoint)
</verification>

<success_criteria>
- One ≥48px mic tap opens a hands-free session; a second tap stops it (VOICE-02/D-01/D-13)
- The bar shows listening/working/stopped/paused via color + word + icon, legible across a room, with a reduced-motion fallback (VOICE-03/D-06/D-07/D-09)
- A live stripped green transcript streams while listening and is replaced by the confirmation in one spot (VOICE-04/D-10/D-11)
- The paused state and Firefox both keep the text input usable (D-14/VOICE-08)
- Real-iOS restart loop + 10-minute continuous session verified on device (VOICE-01/SC1/SC5)
</success_criteria>

<output>
Create `.planning/phases/04-voice-capture/04-03-SUMMARY.md` when done
</output>
