---
phase: 15-unified-filter-surface-multi-select-checkboxes-pulse-categor
plan: 07
type: execute
wave: 3
depends_on: ["15-04"]
files_modified:
  - frontend/src/components/EmptyState.tsx
  - frontend/src/App.tsx
  - frontend/src/hooks/useStats.ts
  - frontend/src/lib/voiceCommands.ts
  - frontend/src/lib/voiceCommands.test.ts
  - frontend/src/components/GuideOverlay.tsx
autonomous: true
requirements: [PH15-05]

must_haves:
  truths:
    - "The zero-result empty state correctly describes which of the (now three, not two) filter groups produced no matches"
    - "The stats query and the empty-state copy both read from the store's new multi-select shape, not a stale amPm/bpCategory scalar"
    - "The site guide's 'What Can I Say' list and Filters help text describe time-of-day and pulse-category filtering instead of a retired AM/PM binary"
  artifacts:
    - path: frontend/src/components/EmptyState.tsx
      provides: "empty-state copy built from the three selection maps (timeOfDay/bpCategory/pulseCategory), no amPm prop"
      contains: "pulseCategoryClause"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/EmptyState.tsx"
      via: "props: timeOfDay, bpCategory, pulseCategory (no amPm)"
      pattern: "pulseCategory="
    - from: "frontend/src/hooks/useStats.ts useResolvedFilters"
      to: "frontend/src/lib/dates.ts resolveFilters"
      via: "store subscription -> resolved query params"
      pattern: "pulseCategory"
---

<objective>
Wire the read-only consumers of the filter store to the new multi-select shape — `EmptyState.tsx`'s zero-result copy, `App.tsx`'s call site, and `useStats.ts`'s `useResolvedFilters` bridge — and correct the two places the Guide's "What Can I Say" content still describes the now-retired AM/PM binary instead of the new Time of Day and Pulse Category filters.

Purpose: These are the remaining read-side consumers of the store shape converted in plan 15-04; none of them own the store or the FilterBar, so they run in parallel with plans 15-05/15-06 with zero file overlap. The Guide-copy correction is required — CLAUDE.md's core value is that every feature is voice-operable AND discoverable, and shipping help text describing a feature (AM/PM filtering) that no longer exists would be a real functional regression in the guide, not a cosmetic gap.

Output: EmptyState/App/useStats correctly consuming the v3 store shape; Guide copy accurately describing the new filter surface, its own test suite updated to match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EmptyState + App.tsx call site + useStats.ts</name>
  <files>frontend/src/components/EmptyState.tsx, frontend/src/App.tsx, frontend/src/hooks/useStats.ts</files>
  <read_first>frontend/src/components/EmptyState.tsx, frontend/src/App.tsx, frontend/src/hooks/useStats.ts, frontend/src/lib/dates.ts</read_first>
  <behavior>
    - With Morning checked in `timeOfDay` and nothing else filtered, the empty-state body reads "There are no morning readings in {presetLabel}. ..." (lowercase, per UI-SPEC §8's `timeOfDayPrefix` example).
    - With Stage 1 + Stage 2 checked in `bpCategory` and Tachycardia checked in `pulseCategory`, the body reads "There are no readings in {presetLabel} in Stage 1 and Stage 2 with Tachycardia pulse. ...".
    - Zero-or-all in every group produces the unfiltered "There are no readings in {presetLabel}. ..." sentence with no clauses.
    - `useResolvedFilters()` correctly includes `pulse_category`/`time_of_day` in its resolved output when those groups have a strict-subset selection, and omits them under zero-or-all.
  </behavior>
  <action>
    In `frontend/src/components/EmptyState.tsx`: change `EmptyStateProps` — delete `amPm: "all" | "AM" | "PM";`, change `bpCategory: string;` to `bpCategory: Record<BPCategory, boolean>;`, add `pulseCategory: Record<PulseCategory, boolean>;` and `timeOfDay: Record<TimeOfDayBucket, boolean>;` (import these three types plus `CLINICAL_ORDER`/`PULSE_CLINICAL_ORDER` from `../lib/palette`, `TIME_OF_DAY_ORDER` from `../lib/dates`, and `joinWithAnd` from `../lib/showSentence`). Delete `const amPmSegment = amPm === "all" ? "" : \`${amPm} \`;`. Add a `timeOfDayPrefix` built the same zero-or-all-collapse way as plan 15-06's suffix builders: `""` under zero-or-all, else `\`${joinWithAnd(selected.map((b) => b.toLowerCase()))} \`` per UI-SPEC §8's exact table (e.g. `"morning and evening "`). Change `const categoryClause = bpCategory === "all" ? "" : \` in ${bpCategory}\`;` to a `bpCategoryClause` built the same way: `""` under zero-or-all, else `\` in ${joinWithAnd(selected)}\``. Add a `pulseCategoryClause`: `""` under zero-or-all, else `\` with ${joinWithAnd(selected)} pulse\``. Update the returned template string from `` `There are no ${amPmSegment}readings in ${presetLabel}${categoryClause}.${newestSentence}` `` to `` `There are no ${timeOfDayPrefix}readings in ${presetLabel}${bpCategoryClause}${pulseCategoryClause}.${newestSentence}` `` (UI-SPEC §8's exact locked template).

    In `frontend/src/App.tsx`: change the `const amPm = useFilters((s) => s.amPm);` line to `const timeOfDay = useFilters((s) => s.timeOfDay);`, keep `const bpCategory = useFilters((s) => s.bpCategory);` (now a map, same subscription line), add `const pulseCategory = useFilters((s) => s.pulseCategory);`. Update the `<EmptyState .../>` call site's props: remove `amPm={amPm}`, add `timeOfDay={timeOfDay}` and `pulseCategory={pulseCategory}`, keep `bpCategory={bpCategory}` and `presetLabel={presetLabel(datePreset)}` unchanged.

    In `frontend/src/hooks/useStats.ts`'s `useResolvedFilters`: delete `const amPm = useFilters((s) => s.amPm);`, add `const pulseCategory = useFilters((s) => s.pulseCategory);` and `const timeOfDay = useFilters((s) => s.timeOfDay);` (alongside the existing `bpCategory` subscription, unchanged). Update the `resolveFilters({ datePreset, customRange, amPm, bpCategory }, ...)` call to `resolveFilters({ datePreset, customRange, bpCategory, pulseCategory, timeOfDay }, ...)`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>EmptyState, App.tsx, and useStats.ts all consume the v3 store shape with zero remaining amPm references; the project type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guide copy — retire AM/PM wording, add time-of-day + pulse-category</name>
  <files>frontend/src/lib/voiceCommands.ts, frontend/src/lib/voiceCommands.test.ts, frontend/src/components/GuideOverlay.tsx</files>
  <read_first>frontend/src/lib/voiceCommands.ts, frontend/src/lib/voiceCommands.test.ts, frontend/src/components/GuideOverlay.tsx</read_first>
  <behavior>
    - `VOICE_COMMAND_CATEGORIES` no longer has an entry literally titled "Filtering by AM or PM" — it describes time-of-day filtering instead, under a renamed `id`.
    - `VOICE_COMMAND_CATEGORIES` includes a new entry for pulse-category filtering, discoverable the same way BP-category filtering already is — 10 entries total, not 9.
    - `EXAMPLES` (derived from `VOICE_COMMAND_CATEGORIES`) still has exactly one example phrase per category — unchanged derivation mechanism, now length 10.
    - The Guide's Filters section body text no longer says "by morning (AM) or evening (PM)" — it describes time of day, BP category, and pulse category.
  </behavior>
  <action>
    In `frontend/src/lib/voiceCommands.ts`: change the object `{ id: "am-pm", label: "Filtering by AM or PM", example: "mornings only" }` to `{ id: "time-of-day", label: "Filtering by time of day", example: "mornings only" }` (the example phrase itself stays valid — it now routes to the `time_of_day` field per plan 15-02's prompt rewrite, not `am_pm`; only the id/label describing it change). Add a new entry immediately after the existing `bp-category` entry: `{ id: "pulse-category", label: "Filtering by pulse category", example: "show tachycardia readings" }`. Do not touch `EXAMPLES` or `SIMILAR_PHRASINGS_NOTE` — both derive/stay correct automatically from the array change.

    In `frontend/src/lib/voiceCommands.test.ts`: update `"has exactly 9 entries"` to assert `10`. Update `"has the exact locked id sequence, in order"`'s expected array — replace `"am-pm"` with `"time-of-day"` in its existing position (4th), and insert `"pulse-category"` immediately after `"bp-category"` (making the sequence: `datasets, show-only, date-range, time-of-day, bp-category, pulse-category, chart-view, reset, speech, guide`). Update the `describe("EXAMPLES", ...)` block's `"has length 9"` test to assert `10`.

    In `frontend/src/components/GuideOverlay.tsx`'s `id="filters"` section: change the body paragraph "Filters narrow down which readings are shown — by date range, by morning (AM) or evening (PM), or by blood pressure category." to "Filters narrow down which readings are shown — by date range, by time of day, by blood pressure category, or by pulse category." Update the "By click" example from `Tap a filter chip (like "Last 30 Days" or "Mornings") to turn it on or off.` to reference the actual new checkbox label casing — "Morning" (capitalized, matching the exact FilterBar checkbox label from plan 15-05, not "Mornings"): `Tap a filter checkbox (like "Last 30 Days" or "Morning") to turn it on or off.` Leave the "By voice" example line ("Say a filter phrase, like 'last 30 days, mornings only' or 'show stage 2 readings.'") unchanged — it is still a valid, correctly-routed utterance. Do NOT touch the separate `id="charts"` section's stale "Four charts are available: Blood Pressure Timeline, Pulse Trend..." text — that is a pre-existing Phase-14-era inaccuracy unrelated to this phase's scope; leave it as-is.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/voiceCommands.test.ts</automated>
  </verify>
  <done>The Guide's "What Can I Say" list and Filters help text accurately describe time-of-day and pulse-category filtering with zero remaining "AM or PM" wording; voiceCommands.test.ts passes against the updated 10-entry list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | All three changed components are read-only presentational consumers of already-validated store/query state — no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-15-11 | Information Disclosure | None applicable | accept | Copy-only and read-side wiring changes; no new data path |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — zero type errors
- `cd frontend && npx vitest run src/lib/voiceCommands.test.ts` — passes
- `grep -n "AM or PM\|morning (AM)" frontend/src/lib/voiceCommands.ts frontend/src/components/GuideOverlay.tsx` — zero matches
</verification>

<success_criteria>
- EmptyState/App.tsx/useStats.ts consume the v3 store shape with correct multi-select copy
- The Guide accurately describes time-of-day and pulse-category filtering, no stale AM/PM wording, its own tests updated and passing
</success_criteria>

<output>
Create `.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-07-SUMMARY.md` when done
</output>
