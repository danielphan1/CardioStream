---
phase: 15-unified-filter-surface-multi-select-checkboxes-pulse-categor
plan: 06
type: execute
wave: 3
depends_on: ["15-04", "15-03"]
files_modified:
  - frontend/src/lib/agent.ts
  - frontend/src/lib/agent.test.ts
autonomous: true
requirements: [PH15-07]

must_haves:
  truths:
    - "A server-composed multi-select filter delta (e.g. bpCategory: ['Stage 1', 'Stage 2']) correctly replaces the store's whole BP Category selection"
    - "The spoken/written confirmation sentence names every selected category and time-of-day bucket grammatically ('Stage 1 and Stage 2 blood pressure'), never a raw list dump"
    - "Resetting still marks the correct D-08 pulse groups, now including pulseCategory and timeOfDay instead of amPm"
  artifacts:
    - path: frontend/src/lib/agent.ts
      provides: "applyAgentFilters handling list-typed bpCategory/pulseCategory/timeOfDay deltas; composeConfirmation multi-select suffix builders"
      contains: "pulseCategorySuffix"
  key_links:
    - from: "frontend/src/lib/agent.ts applyAgentFilters"
      to: "frontend/src/store/filters.ts setBpCategory/setPulseCategory/setTimeOfDay"
      via: "AppliedFilters delta application"
      pattern: "s.setBpCategory"
    - from: "frontend/src/lib/agent.ts composeConfirmation"
      to: "frontend/src/lib/showSentence.ts joinWithAnd"
      via: "multi-select suffix grammar"
      pattern: "joinWithAnd"
---

<objective>
Rewire `lib/agent.ts` — the pure, backend-free bridge between a server-composed `AppliedFilters` delta and the zustand store — for the new list-typed `bpCategory`/`pulseCategory`/`timeOfDay` fields (dropping `amPm` entirely), and rewrite `composeConfirmation`'s suffix-building logic to describe a multi-select as a grammatical joined list per group instead of at-most-one scalar value.

Purpose: This is the other half of the frontend/backend wire contract plan 15-02 defined — `AppliedFilters` deltas from the (currently billing-inert, per STATE.md AGENT-01) agent only have somewhere to land once this plan lands.

Output: A working, unit-tested `applyAgentFilters`/`composeConfirmation` pair matching UI-SPEC §9's locked multi-select confirmation template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-UI-SPEC.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyAgentFilters — list-typed category deltas, drop amPm</name>
  <files>frontend/src/lib/agent.ts, frontend/src/lib/agent.test.ts</files>
  <read_first>frontend/src/lib/agent.ts, frontend/src/lib/agent.test.ts, frontend/src/store/filters.ts, frontend/src/api/types.ts</read_first>
  <behavior>
    - `applyAgentFilters({ bpCategory: ["Stage 1", "Stage 2"] })` calls `setBpCategory(["Stage 1", "Stage 2"])` (full replace) and marks `"bpCategory"` as a touched pulse field.
    - `applyAgentFilters({ bpCategory: [] })` (the server's "clear to all" representation, per plan 15-02's `bp_category_all` mapping) ALSO calls `setBpCategory([])`, correctly clearing every key — the guard must be `!= null`, not truthy, since an empty-but-present array is a valid, meaningful delta (this exact `!= null` discipline already governs every other present-value field in this function).
    - The identical two behaviors for `pulseCategory`/`timeOfDay`.
    - `applyAgentFilters({ reset: true })` marks `"timeOfDay"` and `"pulseCategory"` (not `"amPm"`, which no longer exists) as touched pulse fields, alongside the existing `"chart"`/`"dateRange"`/`"bpCategory"`/`"datasets"`.
    - `hasOtherCommand`'s guide-auto-close disjunction correctly reacts to a `bpCategory`/`pulseCategory`/`timeOfDay` delta (previously reacted to `amPm`/`bpCategory`).
  </behavior>
  <action>
    In `frontend/src/lib/agent.ts`: update the `PulseField` union — delete `"amPm"`, add `"timeOfDay"` and `"pulseCategory"` (three category-ish pulse fields now: `"bpCategory"`, `"pulseCategory"`, `"timeOfDay"`).

    In `hasOtherCommand`'s disjunction inside `applyAgentFilters`: delete the `f.amPm != null ||` clause; add `f.pulseCategory != null ||` and `f.timeOfDay != null ||` alongside the existing `f.bpCategory != null ||`.

    In the `if (f.reset)` branch: change `touched.add("amPm");` to `touched.add("timeOfDay"); touched.add("pulseCategory");` (bpCategory's `touched.add("bpCategory")` stays as-is — three category-ish groups reset now, not one amPm group plus bpCategory).

    Delete the `if (f.amPm != null) { s.setAmPm(f.amPm); touched.add("amPm"); }` block entirely. Change `if (f.bpCategory != null) { s.setBpCategory(f.bpCategory); touched.add("bpCategory"); }` — this block's SHAPE stays (the `!= null` guard, the `touched.add` call) but `f.bpCategory` is now `BPCategory[] | null | undefined` and `s.setBpCategory` now takes that array directly (plan 15-04 already made `setBpCategory` accept an array) — no further change needed to this block's body beyond what the type change already provides. Add the identical two blocks for `pulseCategory`/`timeOfDay`: `if (f.pulseCategory != null) { s.setPulseCategory(f.pulseCategory); touched.add("pulseCategory"); }` and `if (f.timeOfDay != null) { s.setTimeOfDay(f.timeOfDay); touched.add("timeOfDay"); }`.

    In `frontend/src/lib/agent.test.ts`: update the `beforeEach`'s `useFilters.setState({...})` seed — drop `amPm: "all",`, replace `bpCategory: "all",` with an all-false 6-key map, add all-false `pulseCategory`/`timeOfDay` maps (read `store/filters.ts`'s final field names first). In `describe("applyAgentFilters", ...)`: update `"reset returns the store to defaults"` and `"applies reset FIRST, then per-field deltas"` — their setup calls `useFilters.setState({ ..., amPm: "AM", bpCategory: "Stage 2" })` and assertions check `s.amPm`/`s.bpCategory` as scalars; change the setup to seed the new map shapes directly (e.g. `bpCategory: { ...allFalse, "Stage 2": true }`) and change assertions to check every key is `false` after reset (not `=== "all"`). Update `"marks a D-08 pulse for exactly the touched groups and bumps seq"` — its `applyAgentFilters({ showOnly: ["pulse"], amPm: "AM" })` call and `["amPm", "datasets"]` assertion — change to `applyAgentFilters({ showOnly: ["pulse"], bpCategory: ["Stage 1"] })` asserting `["bpCategory", "datasets"]`. Update `"reset marks all five pulse groups"` — its expected `["amPm", "bpCategory", "chart", "dateRange", "datasets"]` list — to `["bpCategory", "chart", "dateRange", "datasets", "pulseCategory", "timeOfDay"]` (six groups now, sorted).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/agent.test.ts -t "applyAgentFilters"</automated>
  </verify>
  <done>applyAgentFilters correctly applies list-typed bpCategory/pulseCategory/timeOfDay deltas including the explicit empty-array clear case, with amPm fully removed and reset marking the correct three groups; the applyAgentFilters describe block passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: composeConfirmation multi-select suffix grammar + its tests</name>
  <files>frontend/src/lib/agent.ts, frontend/src/lib/agent.test.ts</files>
  <read_first>frontend/src/lib/agent.ts, frontend/src/lib/agent.test.ts, frontend/src/lib/showSentence.ts, frontend/src/lib/palette.ts, frontend/src/lib/dates.ts</read_first>
  <behavior>
    - `composeConfirmation` with only `bpCategory` having Stage 1 and Stage 2 checked produces the suffix `", Stage 1 and Stage 2 blood pressure"`.
    - With only Tachycardia checked in `pulseCategory`, produces `", Tachycardia pulse"`.
    - With Morning checked in `timeOfDay`, produces `", mornings"` (singular-bucket wording preserved verbatim from today's AM/PM suffix text, per UI-SPEC §9's table); with Morning AND Evening both checked, produces `", mornings and evenings"`.
    - Zero-or-all in any group collapses that suffix to `""` entirely (no trailing comma, no empty clause).
    - The worked UI-SPEC §9 example composes correctly end to end: blood pressure + pulse datasets, last 30 days, Morning only, Stage 1 + Stage 2 BP, Tachycardia pulse -> `"Showing blood pressure and pulse, last 30 days, mornings, Stage 1 and Stage 2 blood pressure, Tachycardia pulse"`.
  </behavior>
  <action>
    In `frontend/src/lib/agent.ts`: `composeConfirmation`'s `state` parameter type currently includes `amPm: "all" | "AM" | "PM"; bpCategory: "all" | BPCategory;` — change to `bpCategory: Record<BPCategory, boolean>; pulseCategory: Record<PulseCategory, boolean>; timeOfDay: Record<TimeOfDayBucket, boolean>;` (drop `amPm` entirely; add `import type { PulseCategory, TimeOfDayBucket } from "../api/types";`, `import { CLINICAL_ORDER, PULSE_CLINICAL_ORDER } from "./palette";`, and `import { TIME_OF_DAY_ORDER } from "./dates";` — extend/add alongside the existing `joinWithAnd` import from `./showSentence`, already present in this file). Delete the `const ampmSuffix = state.amPm === "AM" ? ... : state.amPm === "PM" ? ... : "";` line. Replace `const categorySuffix = state.bpCategory !== "all" ? \`, ${state.bpCategory} readings only\` : "";` with a `bpCategorySuffix` built via a local `selected(m)` helper (`Object.keys(m).filter((k) => m[k])`, scoped to this function — a 1-line local helper, no new cross-file import needed for this) that returns `""` when the selected count is `0` or equals `CLINICAL_ORDER.length`, else `\`, ${joinWithAnd(selected(state.bpCategory))} blood pressure\``. Add a `pulseCategorySuffix` with the identical shape over `PULSE_CLINICAL_ORDER`/`state.pulseCategory`, template `\`, ${joinWithAnd(...)} pulse\``. Add a `timeOfDaySuffix` over `TIME_OF_DAY_ORDER`/`state.timeOfDay` — for the SINGLE-selected case preserve the exact legacy wording per UI-SPEC §9's table (Morning-only -> `", mornings"`, Evening-only -> `", evenings"`; for Afternoon/Night-only or any 2+ selection, use the general `", {joinWithAnd(pluralized lowercase labels)}"` form — e.g. `", mornings and evenings"`, `", afternoons"`, `", nights"` — pluralize each selected label by lowercasing and appending `"s"`, which is correct English for all four bucket names). Update the final return template string from `` `Showing ${chartPhrase}, ${rangePhrase}${ampmSuffix}${categorySuffix}` `` to `` `Showing ${chartPhrase}, ${rangePhrase}${timeOfDaySuffix}${bpCategorySuffix}${pulseCategorySuffix}` `` (UI-SPEC §9's locked clause order: time of day, then BP category, then pulse category).

    In `frontend/src/lib/agent.test.ts`: update the `confState()` helper's `ConfState` type and default object — drop `amPm: "all" | "AM" | "PM";`, add `bpCategory: Record<BPCategory, boolean>; pulseCategory: Record<PulseCategory, boolean>; timeOfDay: Record<TimeOfDayBucket, boolean>;` with all-false defaults (importing `PulseCategory`/`TimeOfDayBucket` from `../api/types`). Rewrite `describe("composeConfirmation", ...)`'s three existing tests to use the new map shapes: `"emits the VOICE-06/D-07 canonical string exactly"` — seed `timeOfDay: { ...allFalse, Morning: true }` instead of `amPm: "AM"`, same expected output string `"Showing blood pressure, last 30 days, mornings"`. `"composes pulse + PM + category suffix from all data"` — seed `timeOfDay: { ...allFalse, Evening: true }` instead of `amPm: "PM"` and `bpCategory: { ...allFalse, "Stage 2": true }` instead of `bpCategory: "Stage 2"`; update the expected string from `"Showing pulse, all data, evenings, Stage 2 readings only"` to `"Showing pulse, all data, evenings, Stage 2 blood pressure"` (new suffix wording — no more "readings only", per the new `bpCategorySuffix` template). Add a new test asserting the UI-SPEC §9 worked example verbatim: both vitals visible, `datePreset: "30d"`, `timeOfDay: { Morning: true, rest false }`, `bpCategory: { "Stage 1": true, "Stage 2": true, rest false }`, `pulseCategory: { Tachycardia: true, rest false }` -> `"Showing blood pressure and pulse, last 30 days, mornings, Stage 1 and Stage 2 blood pressure, Tachycardia pulse"`. Add a zero-or-all collapse test: every key `true` (or every key `false`) in all three maps at once produces `"Showing blood pressure, last 30 days"` with no trailing suffixes at all.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/agent.test.ts</automated>
  </verify>
  <done>composeConfirmation produces the exact UI-SPEC §9 worked-example string and correctly collapses each of the three new/converted suffixes under zero-or-all; the full agent.test.ts file passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server-composed `AppliedFilters` (closed TS union) -> store mutation | Only closed-union fields reach store actions here (T-03-07, unchanged) — nothing model-authored is ever executed, only fixed enum/array values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-15-10 | Tampering | `applyAgentFilters`'s list-typed field handling | accept | `AppliedFilters` fields are closed TS unions matching the backend's own closed Pydantic schema (plan 15-02) — no free-form string ever reaches a store setter through this path |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/agent.test.ts` — passes
- `cd frontend && npx tsc --noEmit` — zero type errors
</verification>

<success_criteria>
- applyAgentFilters correctly handles list-typed bpCategory/pulseCategory/timeOfDay deltas including explicit-empty-array clears
- composeConfirmation matches UI-SPEC §9's locked template and worked example exactly
- amPm is fully removed from lib/agent.ts and lib/agent.test.ts
</success_criteria>

<output>
Create `.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-06-SUMMARY.md` when done
</output>
