---
phase: 15-unified-filter-surface-multi-select-checkboxes-pulse-categor
plan: 05
type: execute
wave: 3
depends_on: ["15-04", "15-03"]
files_modified:
  - frontend/src/components/FilterBar.tsx
  - frontend/src/components/FilterBar.test.tsx
autonomous: true
requirements: [PH15-01]

must_haves:
  truths:
    - "A caregiver can check both 'Stage 1' and 'Stage 2' BP-category boxes at once and see both apply"
    - "A caregiver can filter by Pulse Category, a control that did not exist before this phase"
    - "A caregiver can filter by Time of Day (Morning/Afternoon/Evening/Night), replacing the old AM/PM binary"
    - "Every checkbox is a real <input type=checkbox>, ≥48px target, reachable by Tab, toggleable by Space"
  artifacts:
    - path: frontend/src/components/FilterBar.tsx
      provides: "5-group filter bar: Date (unchanged buttons), Time of Day (new checkboxes), BP Category (checkboxes), Pulse Category (new checkboxes)"
      contains: "Pulse Category"
    - path: frontend/src/components/FilterBar.test.tsx
      provides: "component tests for every checkbox group, the 48px floor, and the zero-or-all sentence collapse"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/FilterBar.tsx"
      to: "frontend/src/store/filters.ts toggleBpCategory/togglePulseCategory/toggleTimeOfDay"
      via: "checkbox onChange"
      pattern: "toggleBpCategory"
    - from: "frontend/src/components/FilterBar.tsx sentence"
      to: "frontend/src/lib/dates.ts selectedOrAll"
      via: "zero-or-all collapse per segment"
      pattern: "selectedOrAll"
---

<objective>
Convert `FilterBar.tsx`'s AM/PM and BP Category groups from single-select `aria-pressed` buttons to real multi-select checkboxes (matching `ShowPanel.tsx`'s proven control language verbatim), replace the AM/PM group with the new four-bucket Time of Day group, and add the brand-new Pulse Category group — the two filters (per UI-SPEC §3) that make "Stage 1 AND Stage 2" expressible for the first time. Create `FilterBar.test.tsx`, which does not exist today.

Purpose: This is the user-facing control surface change the whole phase exists to ship — everything in Waves 1-2 exists to support this component.

Output: A working, tested 4-group FilterBar (Date unchanged, Time of Day, BP Category, Pulse Category) with a correct zero-or-all live sentence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-UI-SPEC.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert FilterBar to the 4-group checkbox surface</name>
  <files>frontend/src/components/FilterBar.tsx</files>
  <read_first>frontend/src/components/FilterBar.tsx, frontend/src/components/ShowPanel.tsx, frontend/src/store/filters.ts, frontend/src/lib/dates.ts, frontend/src/lib/palette.ts, frontend/src/lib/showSentence.ts</read_first>
  <behavior>
    - Group order left to right: Date (unchanged exclusive buttons), Time of Day (new plain checkboxes), BP Category (checkboxes, colored chips), Pulse Category (new checkboxes, colored chips).
    - The live sentence collapses each segment to "All …" under zero-or-all, and joins a strict subset with `joinWithAnd` (e.g. "Morning and Evening", "Stage 1 and Stage 2").
    - D-08 agent-pulse ring/flash still works per group, now keyed on `"timeOfDay"`/`"bpCategory"`/`"pulseCategory"` (no `"amPm"` group anymore).
  </behavior>
  <action>
    In `frontend/src/components/FilterBar.tsx`: replace the `import { categoryColor, CHIP_TEXT, CLINICAL_ORDER } from "../lib/palette";` line with one also importing `PULSE_CLINICAL_ORDER, pulseCategoryColor`. Add `import { TIME_OF_DAY_ORDER, selectedOrAll } from "../lib/dates";` and `import { joinWithAnd } from "../lib/showSentence";`. Delete the `AM_PM_OPTIONS` constant entirely.

    Replace the store subscriptions: delete `const amPm = useFilters((s) => s.amPm);` and `const setAmPm = useFilters((s) => s.setAmPm);`; change `const bpCategory = useFilters((s) => s.bpCategory);` to subscribe to the new map shape (same line, same name, now a `Record<BPCategory, boolean>`); change `const setBpCategory = useFilters((s) => s.setBpCategory);` to `const toggleBpCategory = useFilters((s) => s.toggleBpCategory);`; add `const pulseCategory = useFilters((s) => s.pulseCategory);`, `const togglePulseCategory = useFilters((s) => s.togglePulseCategory);`, `const timeOfDay = useFilters((s) => s.timeOfDay);`, `const toggleTimeOfDay = useFilters((s) => s.toggleTimeOfDay);`.

    Replace the AM/PM `<div role="group" aria-label="Time of day" ...>` block (the one rendering `AM_PM_OPTIONS` as `aria-pressed` buttons) with a NEW Time of Day checkbox group using `ShowPanel.tsx`'s exact plain-checkbox markup (`boxClass` string: `"min-h-12 flex items-center gap-3 rounded-xl py-2 pl-3.5 pr-4 text-label bg-[var(--color-mist)] text-[var(--color-depth)] border-2 border-[var(--color-depth)] shadow-[var(--shadow-elevation)] cursor-pointer"` — define this as a local const, same string `ShowPanel.tsx` uses), mapping over `TIME_OF_DAY_ORDER`, one `<label>` per bucket wrapping a real `<input type="checkbox" checked={timeOfDay[bucket]} onChange={() => toggleTimeOfDay(bucket, !timeOfDay[bucket])} className="h-[26px] w-[26px] flex-none cursor-pointer accent-[var(--color-brass)]" />` followed by the bucket name as visible text. Keep `role="group"` on the wrapping `<div>`; per UI-SPEC §3's table, this group's `aria-label` is `"Time of day"` (the new group claims that label — the OLD am/pm group no longer exists to conflict with it) and add a small visible `<span>` heading `"Time of Day:"` immediately before the group per UI-SPEC §3's "visible label prefix" instruction (mirrors `ShowPanel.tsx`'s own `"Show:"` prefix `<span>`). Keep the existing `pulseClass("dateRange")`-style wrapping for the pulse ring, but key this group's ring on `"timeOfDay"`.

    Convert the BP Category `<button aria-pressed>` chip group to real checkboxes per UI-SPEC §3's exact target markup: each `<label>` (not `<button>`) with `backgroundColor: categoryColor(cat)` and `color: CHIP_TEXT` inline style (UNCHANGED — fill is always solid regardless of checked state, D-14), wrapping a real `<input type="checkbox" checked={bpCategory[cat]} onChange={() => toggleBpCategory(cat, !bpCategory[cat])} className="h-[26px] w-[26px] flex-none cursor-pointer" style={{ accentColor: CHIP_TEXT }} />` followed by the category name. The checked-state ring (`boxShadow: "0 0 0 3px var(--color-depth)"`) moves from the button's `aria-pressed`-driven condition to `bpCategory[cat]` on the wrapping `<label>`'s style. Add a visible `"BP Category:"` heading span before this group, matching the Time of Day group's new prefix pattern. Update this group's `aria-label` per UI-SPEC §3's table to `"Blood pressure category"`.

    Add a brand-new Pulse Category group immediately after BP Category, structurally identical to the just-converted BP Category group but mapping over `PULSE_CLINICAL_ORDER` with `pulseCategoryColor(cat)`, `pulseCategory[cat]`/`togglePulseCategory(cat, !pulseCategory[cat])`, `aria-label="Pulse category"`, visible heading `"Pulse Category:"`, and pulse-ring keyed on `"pulseCategory"`.

    Rewrite the live-sentence assembly (`sentenceParts` array): keep the date-preset segment unchanged (index 0 and the conditional `to {date}` segment). Replace the old `sentenceParts.push(amPm === "all" ? "All times" : amPm)` and `sentenceParts.push(bpCategory === "all" ? "All categories" : bpCategory)` lines with three segments in this order — Time of Day, BP Category, Pulse Category — each built the same way: call `selectedOrAll(map, allLabel)` (e.g. `selectedOrAll(timeOfDay, "All times of day")`, `selectedOrAll(bpCategory, "All categories")`, `selectedOrAll(pulseCategory, "All pulse categories")`), and when the result is an array (not the all-label string), pass it through `joinWithAnd(...)` before pushing to `sentenceParts`; when it's the string, push it as-is. The AM/PM segment's old label ("All times") becomes the Time of Day segment's zero-or-all label — do not keep a separate "All times" wording for a group that no longer exists.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>FilterBar renders 4 groups (Date unchanged, Time of Day, BP Category, Pulse Category) all as real checkboxes except Date; the live sentence correctly collapses zero-or-all per segment and joins strict subsets with joinWithAnd; the file type-checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FilterBar.test.tsx (new file)</name>
  <files>frontend/src/components/FilterBar.test.tsx</files>
  <read_first>frontend/src/components/ShowPanel.test.tsx, frontend/src/components/FilterBar.tsx, frontend/src/store/filters.ts</read_first>
  <behavior>
    - Every checkbox group renders the correct count of real `role="checkbox"` elements with the correct accessible names (Time of Day: 4, BP Category: 6, Pulse Category: 3).
    - Checking a box in one group mutates only that group's map, leaving siblings and the other two groups untouched.
    - Checking a second box in the same group (e.g. Stage 1 then Stage 2) results in BOTH being checked — the multi-select capability this phase exists to add.
    - 0 selected and ALL selected in a group both render the group's "All …" sentence segment identically.
    - Every checkbox-bearing `<label>` carries the `min-h-12` class (48px floor).
  </behavior>
  <action>
    Create `frontend/src/components/FilterBar.test.tsx` mirroring `ShowPanel.test.tsx`'s structure (imports: `act, fireEvent, render, screen` from `@testing-library/react`; `beforeEach, describe, expect, it` from `vitest`; `useFilters` from `../store/filters`; `FilterBar` from `./FilterBar`). Seed a `beforeEach` resetting `useFilters.setState({...})` to a full default state (date `all`, all-false `bpCategory`/`pulseCategory`/`timeOfDay` maps, default `visibleDatasets`) — read `store/filters.ts`'s actual field names/defaults first rather than guessing. Render with `latestReading={null}` (the required prop).

    Write a `describe("Time of Day checkbox group", ...)` block: renders exactly 4 checkboxes named `Morning`/`Afternoon`/`Evening`/`Night`; clicking `Morning` sets `useFilters.getState().timeOfDay.Morning` to `true` and leaves the other 3 keys `false`; clicking both `Morning` and `Evening` leaves both `true` (the multi-select proof).

    Write a `describe("BP Category checkbox group", ...)` block: renders exactly 6 checkboxes named after `CLINICAL_ORDER`'s labels; clicking `Stage 1` then `Stage 2` leaves BOTH `true` in `useFilters.getState().bpCategory` (this is the phase's headline capability — "Stage 1 AND Stage 2" — write this test with that comment).

    Write a `describe("Pulse Category checkbox group", ...)` block: renders exactly 3 checkboxes named `Bradycardia`/`Normal`/`Tachycardia`; clicking `Tachycardia` sets only that key `true`.

    Write a `describe("live sentence — zero-or-all convention", ...)` block: with everything at its all-false default, the rendered text includes `"All times of day"`, `"All categories"`, and `"All pulse categories"`; after ticking every one of the 4 Time of Day boxes (via `act` + repeated `fireEvent.click`), the sentence STILL reads `"All times of day"` (all-selected collapses identically to none-selected); after ticking exactly `Morning` and `Evening`, the sentence includes `"Morning and Evening"` (via `joinWithAnd`, no Oxford comma).

    Write a `describe("48px accessibility floor", ...)` block mirroring `ShowPanel.test.tsx`'s "every label meets the 48px target floor" test: every `<label>` rendered by the component contains `min-h-12` in its `className`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/FilterBar.test.tsx</automated>
  </verify>
  <done>FilterBar.test.tsx exists and passes, covering multi-select behavior in all three new/converted groups, the zero-or-all sentence collapse, and the 48px floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User click/keyboard -> zustand store mutation | Local UI state only, no server trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-15-09 | Information Disclosure | None applicable — filter selections are not sensitive and never leave the browser except as already-validated query params (plan 15-01's backend guards) | accept | No new trust boundary introduced by a checkbox UI change |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/FilterBar.test.tsx` — passes
- `cd frontend && npx tsc --noEmit` — zero type errors
- Manual: `cd frontend && npm run dev`, Tab through the FilterBar, confirm every checkbox is keyboard-reachable and Space-toggleable, and visible focus rings exist in both themes (per VALIDATION.md's Manual-Only accessibility item — deferred to the phase-level human-verify checkpoint, not this plan's automated gate)
</verification>

<success_criteria>
- FilterBar renders Date (unchanged), Time of Day, BP Category, Pulse Category as real multi-select checkboxes
- Two categories can be selected simultaneously in the same group and both apply
- The live sentence correctly collapses zero-or-all and joins strict subsets
</success_criteria>

<output>
Create `.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-05-SUMMARY.md` when done
</output>
