---
phase: 15-unified-filter-surface-multi-select-checkboxes-pulse-categor
plan: 08
type: execute
wave: 4
depends_on: ["15-04", "15-06", "15-02"]
files_modified:
  - frontend/src/components/ChartDeck.test.tsx
  - frontend/src/components/CommandBar.test.tsx
  - frontend/src/hooks/useVoiceCommand.test.ts
  - frontend/src/components/ShowPanel.test.tsx
  - frontend/src/lib/agent-parity.test.ts
autonomous: true
requirements: [PH15-01, PH15-06, PH15-07]

must_haves:
  truths:
    - "The full frontend test suite compiles and passes against the final v3 store shape — no test file still constructs a scalar amPm/bpCategory fixture"
    - "The ACC-03 parity suite proves every new list-typed filter field (bpCategory/pulseCategory/timeOfDay) is voice-reachable, and that the frontend/backend token vocabularies still match byte-for-byte"
  artifacts:
    - path: frontend/src/lib/agent-parity.test.ts
      provides: "updated enumeration-reachability + store-action-surface + frontend/backend token parity checks for the v3 schema"
      contains: "toggleBpCategory"
  key_links:
    - from: "frontend/src/lib/agent-parity.test.ts"
      to: "backend/app/agent/schemas.py"
      via: "readFileSync + regex — must match plan 15-02's final list[Literal[...]] shape"
      pattern: "list\\[Literal"
---

<objective>
Repair every remaining frontend test fixture that still constructs the OLD scalar `amPm`/`bpCategory` filter-store shape (a real gap PATTERNS.md flagged beyond RESEARCH.md's own file list — these fixtures fail to type-check, not just fail assertions, once plan 15-04 lands), and fully rewrite `lib/agent-parity.test.ts` — the ACC-03/D-15 lockstep test that enumerates every store action and cross-checks the frontend's token vocabulary against the backend's actual `schemas.py` file on disk via regex.

Purpose: This is the last plan in the phase specifically because `agent-parity.test.ts` reads the REAL `backend/app/agent/schemas.py` file at test time and asserts its exact `Literal[...]` text shape — it cannot be written correctly until plan 15-02's backend schema and plan 15-06's frontend `applyAgentFilters`/store-action surface have both actually landed, not just been designed.

Output: A fully green frontend test suite with no lingering pre-Phase-15 fixture shape anywhere, and a parity suite that proves the new list-typed filters are voice-reachable end to end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the four scalar-fixture test files</name>
  <files>frontend/src/components/ChartDeck.test.tsx, frontend/src/components/CommandBar.test.tsx, frontend/src/hooks/useVoiceCommand.test.ts, frontend/src/components/ShowPanel.test.tsx</files>
  <read_first>frontend/src/components/ChartDeck.test.tsx, frontend/src/components/CommandBar.test.tsx, frontend/src/hooks/useVoiceCommand.test.ts, frontend/src/components/ShowPanel.test.tsx, frontend/src/store/filters.ts</read_first>
  <behavior>
    - All four files compile and their existing test suites (unrelated to filters — chart routing, command bar submission, voice session handling, dataset checkboxes) pass unchanged in behavior, only their filter-store seed fixtures are updated to the new shape.
  </behavior>
  <action>
    Read `frontend/src/store/filters.ts` first to confirm the exact final field names or `FilterState`'s default values. In EACH of the four files, find the `useFilters.setState({...})` (or the module-level `INITIAL`/`INITIAL_FILTERS` constant used in a `beforeEach`) that currently includes `amPm: "all" as const,` (or `amPm: "all",`) and `bpCategory: "all" as const,` (or `bpCategory: "all",`): DELETE the `amPm` line entirely, and replace the `bpCategory` line with an all-false `Record<BPCategory, boolean>` literal (all six `CLINICAL_ORDER` labels as keys, each `false`) plus two new sibling lines for all-false `pulseCategory` (three keys) and `timeOfDay` (four keys) maps — write the literal object inline in each file exactly as `INITIAL`/`INITIAL_FILTERS` already writes `visibleDatasets` (a plain object literal, no shared constant import needed, matching each file's existing self-contained fixture style). `ChartDeck.test.tsx` and `ShowPanel.test.tsx` use a module-level `const INITIAL = {...}` / `const INITIAL_FILTERS = {...}`; `CommandBar.test.tsx` and `useVoiceCommand.test.ts` seed directly inside `beforeEach(() => { ... useFilters.setState({...}) ... })` — apply the same edit in whichever form each file uses, do not restructure the file's existing pattern.

    Separately, in `frontend/src/components/ShowPanel.test.tsx`'s `describe("agent pulse parity (D-08)", ...)` block, the `"does not ring for an unrelated field"` test calls `useAgentPulse.getState().mark(["amPm"])` — this is a distinct `PulseField` literal, unrelated to the `INITIAL_FILTERS` fixture edit above, and `"amPm"` is removed from the `PulseField` union by plan 15-06 (which converts the pulse-parity field set to `"timeOfDay"`/`"bpCategory"`/`"pulseCategory"`, no `"amPm"` member). Change this call to `mark(["bpCategory"])` (any field unrelated to whatever group the test is NOT checking still proves the assertion) so the file continues to type-check under the v3 `PulseField` union.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ChartDeck.test.tsx src/components/CommandBar.test.tsx src/hooks/useVoiceCommand.test.ts src/components/ShowPanel.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>All four files compile against the v3 FilterState shape and their full existing test suites pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite lib/agent-parity.test.ts for the v3 schema</name>
  <files>frontend/src/lib/agent-parity.test.ts</files>
  <read_first>frontend/src/lib/agent-parity.test.ts, frontend/src/store/filters.ts, frontend/src/lib/agent.ts, backend/app/agent/schemas.py</read_first>
  <behavior>
    - Every concrete value of `BPCategory`×6, `PulseCategory`×3, `TimeOfDayBucket`×4 is provable reachable through `applyAgentFilters` and correctly mutates the matching store map.
    - The full, real mutating-action surface of the store (`toggleBpCategory`, `setBpCategory`, `togglePulseCategory`, `setPulseCategory`, `toggleTimeOfDay`, `setTimeOfDay`, plus the pre-existing `setChartView`/`setDatePreset`/`setCustomRange`/`showAllData`/`setDataset`/`showOnlyDatasets`) is enumerated and cross-checked against the store's actual function-typed keys — with the three `toggle*` category actions EXCLUDED from the "must be voice-reachable" list (same exclusion status as the existing `initFilters` bootstrap exclusion: UI-only, no `AppliedFilters` field ever drives a single-key category toggle from the agent).
    - The backend `bpCategory: list[Literal[...]] | None` field's token set (read live from `backend/app/agent/schemas.py`) equals the frontend `BPCategory` union exactly, with no `"all"` sentinel on either side anymore.
    - New parity checks exist for `pulseCategory`/`timeOfDay` mirroring the existing `bpCategory` parity check's shape.
    - The `CASES` array in `describe("store↔command 1:1 mapping...", ...)` ends up with EXACTLY ONE entry tagged `action: "setBpCategory"` — never two.
  </behavior>
  <action>
    In `frontend/src/lib/agent-parity.test.ts`: delete the `AMPM` constant and its `it.each(AMPM)("applyAgentFilters({ amPm: '%s' }) mutates the store", ...)` block entirely. Add `PULSE_CATEGORIES` (mirroring `BP_CATEGORIES`'s `as const satisfies readonly PulseCategory[]` shape, importing `PulseCategory` from `../api/types`) and `TIME_OF_DAY_BUCKETS` (same shape, `TimeOfDayBucket`, 4 values).

    Update the `beforeEach`'s `useFilters.setState({...})` seed to the v3 shape (drop `amPm`, all-false `bpCategory`/`pulseCategory`/`timeOfDay` maps — read `store/filters.ts` first for exact field names).

    In `describe("enumeration reachability...", ...)`: replace the `it.each(["all", ...BP_CATEGORIES] as const)("applyAgentFilters({ bpCategory: '%s' }) mutates the store", ...)` block — since `bpCategory` is now list-typed, not scalar — with `it.each(BP_CATEGORIES)("applyAgentFilters({ bpCategory: ['%s'] }) mutates the store", (category) => { applyAgentFilters({ bpCategory: [category] }); expect(useFilters.getState().bpCategory[category]).toBe(true); })`. Add the identical two `it.each` blocks for `PULSE_CATEGORIES`/`pulseCategory` and `TIME_OF_DAY_BUCKETS`/`timeOfDay`. Update `"applyAgentFilters({ reset: true }) returns the store to defaults"` — its setup `useFilters.setState({ datePreset: "30d", amPm: "AM", bpCategory: "Stage 2" })` — to seed the v3 shape instead (e.g. `bpCategory: { ...allFalse, "Stage 2": true }`), and its assertions (`expect(s.amPm).toBe("all")`) to instead assert every key in all three maps is `false`.

    Update `STORE_ACTIONS`: remove `"setAmPm"`, change `"setBpCategory"` to stay (now the list-replace variant), and add ONLY `"setPulseCategory"` and `"setTimeOfDay"` — do NOT add `"toggleBpCategory"`, `"togglePulseCategory"`, or `"toggleTimeOfDay"` to `STORE_ACTIONS` (that list is specifically "the agent-reachable command surface", and per this plan's objective all three `toggle*` category actions are UI-only). This yields 9 entries in `STORE_ACTIONS` (today's 8, minus `setAmPm`, plus `setPulseCategory` and `setTimeOfDay`) — matching the 9-item `actualActions` the same test computes after excluding all three `toggle*` category actions below.

    In `describe("store↔command 1:1 mapping...", ...)`'s `CASES` array: the array currently contains TWO separate objects touching BP category — one tagged `action: "setAmPm"` (`apply: () => applyAgentFilters({ amPm: "PM" })`, `assert: () => expect(useFilters.getState().amPm).toBe("PM")`), and a SEPARATE, already-existing object tagged `action: "setBpCategory"` using the OLD scalar form (`apply: () => applyAgentFilters({ bpCategory: "Stage 2" })`, `assert: () => expect(useFilters.getState().bpCategory).toBe("Stage 2")`). These are two distinct objects in the array, not one — handle BOTH explicitly:
    1. Convert the `setAmPm` object IN PLACE to the new list-replace `setBpCategory` form: `apply: () => applyAgentFilters({ bpCategory: ["Stage 2"] })`, `assert: () => expect(useFilters.getState().bpCategory["Stage 2"]).toBe(true)`.
    2. DELETE the separate, pre-existing `setBpCategory` object entirely (the one still using `apply: () => applyAgentFilters({ bpCategory: "Stage 2" })` / `assert: () => expect(useFilters.getState().bpCategory).toBe("Stage 2")`) — the object converted in step 1 now fully covers the `setBpCategory` action, and leaving both would (a) fail `npx tsc --noEmit` since the old object still passes a bare string where `bpCategory` is now `Record<BPCategory, boolean>`, and (b) produce two `CASES` entries tagged with the same action, which is itself wrong even before considering the type error.
    Confirm afterward that the `CASES` array has exactly ONE object tagged `action: "setBpCategory"` — the converted one — not two.
    Then add two new cases for `setPulseCategory`/`setTimeOfDay` mirroring the same list-replace shape (only these two additional `set*` actions are agent-reachable; `toggleBpCategory` is intentionally absent from `CASES` too).

    In the final `"the covered actions equal the store's full mutating-action surface"` test: update its `.filter(([key, value]) => typeof value === "function" && key !== "initFilters")` line to ALSO exclude `"toggleBpCategory"`, `"togglePulseCategory"`, `"toggleTimeOfDay"` — add a code comment next to this filter explaining these three are UI-only checkbox-click actions with no `AppliedFilters` field ever driving a single-key toggle, the same exclusion rationale already documented for `initFilters`.

    In `describe("frontend voice vocabulary matches backend tokens (D-15)", ...)`: the regex `schemaText.match(/bpCategory: Literal\[([^\]]*)\]/)` no longer matches — plan 15-02 changed this field to `bpCategory: list[Literal[...]] | None = None`. Update the regex to `/bpCategory: list\[Literal\[([^\]]*)\]\]/` and update the assertion's expected value from `["all", ...BP_CATEGORIES].sort()` to `[...BP_CATEGORIES].sort()` (no more `"all"` token on either side). Add two new parity tests mirroring this one exactly: `pulseCategory: list[Literal[...]]` against `PULSE_CATEGORIES`, and `timeOfDay: list[Literal[...]]` against `TIME_OF_DAY_BUCKETS`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/agent-parity.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>agent-parity.test.ts fully passes against the v3 schema, proving every BPCategory/PulseCategory/TimeOfDayBucket value is voice-reachable, the store's mutating-action surface is exhaustively accounted for (toggle* category actions correctly excluded as UI-only), the CASES array has exactly one setBpCategory entry (the pre-existing duplicate removed), and the frontend/backend token vocabularies match byte-for-byte with zero "all" sentinel remaining on either side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Test-only changes; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-15-12 | Tampering | `agent-parity.test.ts`'s regex-based cross-file token check | accept | This is a build-time correctness guard, not a runtime trust boundary — a regex drift here fails the build loudly rather than silently shipping a mismatch, which is the intended failure mode |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` — full frontend suite green
- `cd frontend && npx tsc --noEmit` — zero type errors
- `grep -rn "amPm\|\"all\"" frontend/src/lib/agent-parity.test.ts` — no remaining scalar-shape or "all"-sentinel references
</verification>

<success_criteria>
- Every frontend test file compiles and passes against the final v3 store/schema shape
- agent-parity.test.ts proves full voice-reachability for the three new/converted category groups and byte-for-byte frontend/backend token parity
</success_criteria>

<output>
Create `.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-08-SUMMARY.md` when done
</output>
</output>
