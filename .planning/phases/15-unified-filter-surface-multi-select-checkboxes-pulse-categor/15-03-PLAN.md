---
phase: 15-unified-filter-surface-multi-select-checkboxes-pulse-categor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/lib/palette.ts
  - frontend/src/api/client.ts
  - frontend/src/lib/dates.ts
  - frontend/src/lib/dates.test.ts
autonomous: true
requirements: [PH15-05]

must_haves:
  truths:
    - "A multi-value filter (e.g. two BP categories) serializes as repeated query params, not a comma-joined string or the last value only"
    - "Selecting zero or every value in a filter group resolves to an omitted query key, matching the backend's zero-or-all convention"
  artifacts:
    - path: frontend/src/api/types.ts
      provides: "PulseCategory/TimeOfDayBucket unions; list-typed ResolvedFilters/AppliedFilters; Reading.pulse_category tightened"
      contains: "export type TimeOfDayBucket"
    - path: frontend/src/lib/dates.ts
      provides: "list-aware resolveFilters + selectedKeys/selectedOrOmit/selectedOrAll shared helpers + TIME_OF_DAY_ORDER"
      contains: "selectedOrOmit"
    - path: frontend/src/api/client.ts
      provides: "getJson support for string[] query param values via URLSearchParams.append"
      contains: "Array.isArray(value)"
  key_links:
    - from: "frontend/src/lib/dates.ts resolveFilters"
      to: "frontend/src/api/client.ts getJson"
      via: "ResolvedFilters passed straight through to getReadings/getStatsSummary"
      pattern: "search.append"
---

<objective>
Lay the shared frontend contracts and utilities every later Phase 15 plan (store, FilterBar, agent bridge) builds against: the new `PulseCategory`/`TimeOfDayBucket` TS unions and widened `ResolvedFilters`/`AppliedFilters` types (`api/types.ts`), the Pulse Category color palette (`lib/palette.ts`), multi-value query-param support in the HTTP client (`api/client.ts` — a real gap PATTERNS.md found beyond RESEARCH.md's own file list), and the list-aware `resolveFilters` plus the shared zero-or-all selection helpers (`lib/dates.ts`).

Purpose: Interface-first ordering — every other frontend plan in this phase imports from these five files. Landing the contracts first means later plans build against a fixed target instead of guessing.

Output: Compiling, tested type/utility layer with no UI or store changes yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-RESEARCH.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-PATTERNS.md
@.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type unions + AppliedFilters/ResolvedFilters widening + Pulse Category palette</name>
  <files>frontend/src/api/types.ts, frontend/src/lib/palette.ts</files>
  <read_first>frontend/src/api/types.ts, frontend/src/lib/palette.ts, frontend/src/index.css</read_first>
  <behavior>
    - `PulseCategory`/`TimeOfDayBucket` are exported unions matching the backend's `Literal` vocabularies exactly (spaces included, no snake_case).
    - `pulseCategoryColor("Bradycardia")` returns `var(--ref-bradycardia)`; `pulseCategoryColor("Normal")` returns `var(--cat-normal)`; `pulseCategoryColor("Tachycardia")` returns `var(--cat-elevated)` — zero new CSS custom properties, verified they already exist in `index.css`.
  </behavior>
  <action>
    In `frontend/src/api/types.ts`: add `export type PulseCategory = "Bradycardia" | "Normal" | "Tachycardia";` and `export type TimeOfDayBucket = "Morning" | "Afternoon" | "Evening" | "Night";` immediately after the existing `BPCategory` export. Change `Reading.pulse_category: string` to `pulse_category: PulseCategory`. Change `ResolvedFilters` to drop `am_pm?: "AM" | "PM";`, widen `bp_category?: BPCategory;` to `bp_category?: BPCategory[];`, and add `pulse_category?: PulseCategory[];` and `time_of_day?: TimeOfDayBucket[];`. Change `AppliedFilters` to drop `amPm?: "all" | "AM" | "PM" | null;`, widen `bpCategory?: "all" | BPCategory | null;` to `bpCategory?: BPCategory[] | null;`, and add `pulseCategory?: PulseCategory[] | null;` and `timeOfDay?: TimeOfDayBucket[] | null;` — these three follow the exact optional-array-or-null shape already used by `datasetsOn?: SeriesDataset[] | null;` a few lines below in the same type.

    In `frontend/src/lib/palette.ts`: add `import type { PulseCategory } from "../api/types";` alongside the existing `BPCategory` import (combine into one `import type { BPCategory, PulseCategory } from "../api/types";` line). Add `export const PULSE_CLINICAL_ORDER: PulseCategory[] = ["Bradycardia", "Normal", "Tachycardia"];`, a `const PULSE_CATEGORY_VARS: Record<PulseCategory, string> = { Bradycardia: "var(--ref-bradycardia)", Normal: "var(--cat-normal)", Tachycardia: "var(--cat-elevated)" };`, and `export function pulseCategoryColor(cat: PulseCategory): string { return PULSE_CATEGORY_VARS[cat]; }` — mirroring `CLINICAL_ORDER`/`CATEGORY_VARS`/`categoryColor()` exactly, same file, no new import of `CHIP_TEXT` needed (already exported, reused by the Pulse Category chips in a later plan).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>PulseCategory/TimeOfDayBucket exist and are used consistently; ResolvedFilters/AppliedFilters no longer reference amPm anywhere and carry list-typed bp_category/pulse_category/time_of_day; pulseCategoryColor returns the three UI-SPEC-pinned existing CSS vars; the project type-checks with zero errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-value query params in getJson + list-aware resolveFilters</name>
  <files>frontend/src/api/client.ts, frontend/src/lib/dates.ts, frontend/src/lib/dates.test.ts</files>
  <read_first>frontend/src/api/client.ts, frontend/src/lib/dates.ts, frontend/src/lib/dates.test.ts, frontend/src/lib/showSentence.ts</read_first>
  <behavior>
    - `getJson("/readings", { bp_category: ["Stage 1", "Stage 2"] })` builds the query string `?bp_category=Stage+1&bp_category=Stage+2` (repeated key, via `.append()`), never `.set()` (which would overwrite to the last value only) and never a comma-joined single value (FastAPI does not parse that as a list for this parameter style).
    - `resolveFilters` with a `bpCategory` map where 0 keys or all 6 keys are `true` omits `bp_category` from the resolved output entirely; with a strict subset (e.g. 2 of 6 true) it sets `bp_category` to the array of the `true` keys.
    - Same zero-or-all behavior for `pulseCategory`/`timeOfDay`.
  </behavior>
  <action>
    In `frontend/src/api/client.ts`: widen `getJson`'s `params` parameter type from `Record<string, string | undefined>` to `Record<string, string | string[] | undefined>`. In the loop building `search`, add an `Array.isArray(value)` branch before the existing `search.set(key, value)` call: when the value is an array, loop over it and call `search.append(key, v)` for each item; the existing scalar branch keeps using `search.set(key, value)`. Keep the `if (value !== undefined)` outer guard (an empty array `[]` should still be skipped entirely by callers per the zero-or-all convention — this file does not need its own empty-array special case since `resolveFilters` below never emits an empty array, only `undefined` or a non-empty array).

    In `frontend/src/lib/dates.ts`: add `import { CLINICAL_ORDER, PULSE_CLINICAL_ORDER } from "./palette";` and `import type { PulseCategory, TimeOfDayBucket } from "../api/types";` (extend the existing `import type { BPCategory, ResolvedFilters } from "../api/types";` line). Add `export const TIME_OF_DAY_ORDER: TimeOfDayBucket[] = ["Morning", "Afternoon", "Evening", "Night"];`. Change `FilterDateState` to drop `amPm: "all" | "AM" | "PM";`, widen `bpCategory: "all" | BPCategory;` to `bpCategory: Record<BPCategory, boolean>;`, and add `pulseCategory: Record<PulseCategory, boolean>;` and `timeOfDay: Record<TimeOfDayBucket, boolean>;`. Add three exported helpers before `resolveFilters`: `selectedKeys<K extends string>(m: Record<K, boolean>): K[]` returning `(Object.keys(m) as K[]).filter((k) => m[k])`; `selectedOrOmit<K extends string>(m: Record<K, boolean>, totalKeys: number): K[] | undefined` returning `undefined` when `selectedKeys(m).length` is `0` or equals `totalKeys`, else the selected array; `selectedOrAll<K extends string>(m: Record<K, boolean>, allLabel: string): K[] | string` returning `allLabel` under the same zero-or-all condition, else the raw selected-keys array (callers join it with `joinWithAnd` themselves — this helper does not import `joinWithAnd`, keeping `dates.ts` free of a `showSentence.ts` dependency). In `resolveFilters`, delete the two lines `if (state.amPm !== "all") resolved.am_pm = state.amPm;` and `if (state.bpCategory !== "all") resolved.bp_category = state.bpCategory;`, replacing them with three blocks using `selectedOrOmit`: `const bpCategory = selectedOrOmit(state.bpCategory, CLINICAL_ORDER.length); if (bpCategory) resolved.bp_category = bpCategory;` and the identical pattern for `pulseCategory`/`PULSE_CLINICAL_ORDER.length` and `timeOfDay`/`TIME_OF_DAY_ORDER.length`.

    In `frontend/src/lib/dates.test.ts`: update the `state()` helper to drop `amPm: "all" as const,` and replace `bpCategory: "all" as const,` with an all-false 6-key `Record<BPCategory, boolean>` object (using `CLINICAL_ORDER`'s six labels as keys), and add all-false `pulseCategory`/`timeOfDay` maps (3 and 4 keys respectively). Replace the test `'amPm "AM" adds am_pm; bpCategory "Stage 1" adds the verbatim label'` with tests proving: (a) a single `true` key in `bpCategory` resolves to `bp_category: ["Stage 1"]`; (b) two `true` keys resolve to both elements in the array; (c) all keys `true` (or none) resolves to `bp_category` being absent from the result entirely; (d) the identical zero-or-all/strict-subset behavior for `timeOfDay` (e.g. `Morning`+`Evening` true resolves to `time_of_day: ["Morning", "Evening"]`).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/dates.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>getJson serializes array-valued params as repeated query keys; resolveFilters is list-aware with correct zero-or-all omission for bpCategory/pulseCategory/timeOfDay; dates.test.ts passes against the new FilterDateState shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `GET /readings`/`GET /stats/summary` query string construction | Client-side only; the actual trust boundary (server-side validation) is unchanged and lives in plan 15-01's backend `Literal` guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-15-06 | Tampering | `getJson`'s array-param serialization | accept | Client-side query-string construction only; any malformed/forged value is still re-validated server-side by plan 15-01's `Literal` query-param types — no new trust is placed in this layer |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — zero type errors
- `cd frontend && npx vitest run src/lib/dates.test.ts` — passes
</verification>

<success_criteria>
- `api/types.ts` exposes `PulseCategory`/`TimeOfDayBucket` and list-typed `ResolvedFilters`/`AppliedFilters` with `amPm` fully removed
- `getJson` correctly serializes array-valued params as repeated query keys
- `resolveFilters` omits a filter key under the zero-or-all convention and returns the selected array otherwise
</success_criteria>

<output>
Create `.planning/phases/15-unified-filter-surface-multi-select-checkboxes-pulse-categor/15-03-SUMMARY.md` when done
</output>
