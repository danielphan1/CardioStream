---
phase: 14-unified-show-panel-and-combined-timeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/tests/contrast.test.ts
autonomous: true
requirements: [D-03]

must_haves:
  truths:
    - "Pulse has its own colour token distinct from --line-systolic in BOTH themes, so the two series are distinguishable when drawn on one chart"
    - "The chart line colours are contrast-tested for the first time — the suite previously covered brass/hazard/panel only"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "--line-pulse token, light and dark"
      contains: "--line-pulse"
    - path: "frontend/src/tests/contrast.test.ts"
      provides: "3:1 non-text UI floor coverage for all three vitals line colours in both themes"
      contains: "line-pulse"
---

<objective>
Add a dedicated `--line-pulse` colour token and bring the vitals line colours under contrast test.

Purpose: `PulseTrend.tsx:121` currently strokes the pulse line with `var(--line-systolic)`. That is
invisible today because the BP and pulse charts never coexist — on Phase 14's combined chart the two
series would be literally the same colour. This is the blocking prerequisite for `CombinedTimeline`.
Output: token defined in both themes, contrast-verified, with systolic/diastolic covered while the
harness is open.
</objective>

<task name="add_line_pulse_token">
  <read_first>
    - frontend/src/index.css (both `:root` and the dark block — see the existing `--line-systolic` / `--line-diastolic` pairs)
    - .planning/phases/14-unified-show-panel-and-combined-timeline/14-UI-SPEC.md (§6 New colour token)
  </read_first>
  <action>
    Add `--line-pulse: #9E4A24;` to the light palette immediately after `--line-diastolic`, and
    `--line-pulse: #E3A07C;` at the matching position in the dark block.

    Rust is chosen to avoid four existing collisions: `--line-systolic` navy, `--line-diastolic`
    teal, `--color-brass` (reserved accent, One Signal Rule), and the three `--overlay-*` hues. It
    is deliberately warmer and lighter than `--color-hazard` so it does not read as an alarm.
  </action>
  <acceptance_criteria>
    - `frontend/src/index.css` contains `--line-pulse: #9E4A24;` inside the light `:root` block
    - `frontend/src/index.css` contains `--line-pulse: #E3A07C;` inside the dark block
    - Both declarations sit adjacent to the existing `--line-systolic` / `--line-diastolic` pair
  </acceptance_criteria>
</task>

<task name="contrast_cases">
  <read_first>
    - frontend/src/tests/contrast.test.ts (the existing brass/hazard/panel describe blocks and the local ratio helper)
  </read_first>
  <action>
    Add two `describe` blocks — `light theme — vitals line contrast floors` and
    `dark theme — vitals line contrast floors` — asserting the 3:1 WCAG 1.4.11 non-text UI floor for
    `--line-pulse`, `--line-systolic`, and `--line-diastolic`, each against both `--color-deck` and
    `--color-mist` for that theme. Reuse the file's existing ratio helper and hex constants pattern;
    do not introduce a colour library.

    A line drawn on a chart is a non-text UI component, so 3:1 is the correct floor — not 4.5:1.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/tests/contrast.test.ts` exits 0
    - The file references `line-pulse`, `line-systolic`, and `line-diastolic`
    - Six new assertions minimum (3 colours × 2 grounds) per theme
    - If rust fails 3:1, adjust lightness along the same hue and record the final hex in this plan's SUMMARY — do not switch hue family
  </acceptance_criteria>
</task>

<verification>
- `npx vitest run` from `frontend/` exits 0 with no regression against the 383-test baseline
- `grep -c "line-pulse" frontend/src/index.css` returns 2
</verification>
