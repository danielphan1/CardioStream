---
phase: 14-unified-show-panel-and-combined-timeline
plan: 06
type: execute
wave: 3
depends_on: ["14-02", "14-03", "14-04", "14-05"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/ChartDeck.tsx
  - frontend/src/components/OverlayEventsList.tsx
  - frontend/src/api/types.ts
  - frontend/src/lib/agent.ts
  - frontend/src/lib/agent.test.ts
  - frontend/src/lib/agent-parity.test.ts
  - frontend/src/lib/voiceCommands.ts
  - frontend/src/components/GuideOverlay.tsx
  - frontend/src/components/charts/CategoryBars.tsx
  - frontend/src/components/charts/AmPmComparison.tsx
autonomous: true
requirements: [D-01, D-05, D-06, D-08, D-09, D-10]

must_haves:
  truths:
    - "The dashboard renders the combined timeline, the events-only list, or a summary view purely from store state — no chart rotation remains"
    - "A voice show_only command produces the same end state as ticking the same boxes by hand — voice and click are one system, the project's core constraint"
    - "The guide teaches the new vocabulary, so the feature is discoverable without sight of this plan"
  artifacts:
    - path: "frontend/src/components/ChartDeck.tsx"
      provides: "View router: timeline | events-only list | nothing-selected prompt | summary charts"
      contains: "EventTimelineList"
    - path: "frontend/src/lib/agent.ts"
      provides: "applyAgentFilters handling chartView, dataset toggles, and showOnly"
      contains: "showOnly"
---

<objective>
Wire everything together: rebuild `ChartDeck` as a view router, repoint `App.tsx`'s data gating at
`visibleDatasets`, and bring the frontend agent mirror, voice vocabulary, and guide up to the new
command set.

Purpose: closes the loop. Without this plan the new components exist but nothing renders them and
voice still speaks the old vocabulary.
Output: a working dashboard matching the client-approved contract, by voice and by click.
</objective>

<task name="chart_deck_router">
  <read_first>
    - frontend/src/components/ChartDeck.tsx (CHART_REGISTRY, FadeSwap, hero/mini layout)
    - .planning/phases/14-unified-show-panel-and-combined-timeline/14-UI-SPEC.md (§4 empty states)
  </read_first>
  <action>
    Replace the registry + hero/mini rotation with a router on `chartView`:

    - `"bp_categories"` → `<CategoryBars stats={stats} />`
    - `"am_pm_comparison"` → `<AmPmComparison readings={readings} />`
    - `"timeline"` → branch on the vitals:
      - either vital on → `<CombinedTimeline showBP showPulse … />`
      - no vitals, ≥1 event type on → `<EventTimelineList … />`
      - nothing on → prompt card: `Nothing selected` + a `Show everything` button calling `showAllData()`

    Keep `FadeSwap` and key it on the resolved view so swaps still animate, and keep
    `motion-reduce:transition-none`. Drop the `variant` prop from `CategoryBars`/`AmPmComparison`
    and delete their mini branches — there are no mini cards any more.

    Render `ChartViewSwitcher` above the chart region.
  </action>
  <acceptance_criteria>
    - With `chartView: "timeline"` and both vitals off but incidents on, `EventTimelineList` renders and no Recharts chart mounts
    - With all five datasets off, the prompt card renders and its button calls `showAllData()`
    - With `chartView: "bp_categories"`, `CategoryBars` renders regardless of `visibleDatasets`
    - No `mini` or `CHART_REGISTRY` reference remains
  </acceptance_criteria>
</task>

<task name="app_wiring">
  <read_first>
    - frontend/src/App.tsx (overlay fetch gating, the `enabled: false` cache-clearing comment, OverlayEventsList props)
  </read_first>
  <action>
    Repoint the three overlay hooks and the three `useMemo` event arrays from `overlayDatasets.*` to
    `visibleDatasets.*`. **Keep the short-circuit-to-`[]` pattern exactly** — the comment explains it
    guards a real bug (TanStack Query's `enabled: false` stops fetches but does not clear cached
    `data`), and that hazard is unchanged by this phase.

    Keep the narrow `{ start_date, end_date }` query key — the overlay endpoints still have no
    server-side effect for `am_pm`/`bp_category` (T-09-06).

    Swap `<OverlayToggle />` for `<ShowPanel />` in the controls cluster and update
    `OverlayEventsList`'s `enabled` props.
  </action>
  <acceptance_criteria>
    - `grep -n "overlayDatasets" frontend/src/App.tsx` returns no hits
    - The `useMemo` short-circuit on the toggle flag is still present for all three event types
    - The overlay query key still contains only `start_date` and `end_date`
  </acceptance_criteria>
</task>

<task name="agent_mirror">
  <read_first>
    - frontend/src/lib/agent.ts (`applyAgentFilters`, `hasOtherCommand`, `composeConfirmation`, `CHART_PHRASE`)
    - frontend/src/api/types.ts (`AppliedFilters`)
    - backend/app/agent/schemas.py (the authoritative wire shape from 14-03)
  </read_first>
  <action>
    Mirror the backend byte-for-byte: `AppliedFilters` gains `chartView?: ChartView | null` and
    `showOnly?: SeriesDataset[] | null`, loses `activeChart`, and widens `overlayDataset` to
    `SeriesDataset`.

    In `applyAgentFilters`:
    - `f.chartView != null` → `setChartView`, touch `"chart"`
    - `f.overlayDataset != null && f.overlayState != null` → `setDataset`, touch `"datasets"`
    - `f.showOnly != null && f.showOnly.length > 0` → `showOnlyDatasets`, touch `"datasets"`
    - Add both new fields to the `hasOtherCommand` enumeration. **Keep it explicitly enumerated** —
      the existing comment records that FastAPI serializes every key (mostly `null`), so an
      `Object.keys` presence check would always be true and break guide auto-close (T-11-06).

    Update `PulseField`: `"overlay"` → `"datasets"`.

    `composeConfirmation` currently opens with `CHART_PHRASE[activeChart]`. Rewrite it to lead with
    the visible datasets — e.g. `"Showing blood pressure and pulse, last 30 days, mornings"` — and
    to describe the summary views when `chartView` is not `timeline`. This string is spoken aloud by
    TTS, so it must match what is actually on screen.
  </action>
  <acceptance_criteria>
    - `applyAgentFilters({ showOnly: ["pulse"] })` leaves only `pulse` true in the store
    - A `showOnly` delta closes an open guide (appears in `hasOtherCommand`)
    - `applyAgentFilters({ guideOpen: "open" })` alone still does NOT trip `hasOtherCommand`
    - `composeConfirmation` output names the visible datasets, not a chart id
    - `agent-parity.test.ts` passes against the 14-03 wire shape
  </acceptance_criteria>
</task>

<task name="vocabulary_and_guide">
  <read_first>
    - frontend/src/lib/voiceCommands.ts (VOICE_COMMAND_CATEGORIES — EXAMPLES is derived, never independently authored)
    - frontend/src/components/GuideOverlay.tsx (the "What Can I Say" section)
  </read_first>
  <action>
    Update `VOICE_COMMAND_CATEGORIES`: replace the `charts` category example with a dataset toggle
    (`"show my pulse"`), add a `show-only` category (`"only blood pressure and pulse"`), and update
    the `overlay` category label to cover all five datasets. Add a `chart-view` category
    (`"show categories"`).

    Do not touch `EXAMPLES` — it is derived from the categories by design so the placeholder
    rotation and the guide can never drift.
  </action>
  <acceptance_criteria>
    - A `show-only` category exists with an example containing the word `only`
    - `EXAMPLES` is still `VOICE_COMMAND_CATEGORIES.map(c => c.example)` with no hand-authored entries
    - `npx vitest run src/lib/voiceCommands.test.ts src/components/GuideOverlay.test.tsx` exits 0
  </acceptance_criteria>
</task>

<verification>
- `npx vitest run` exits 0 with no test count regression against the 383 baseline
- `npx tsc -b --noEmit` exits 0 with zero errors
- `npx oxlint` reports no new findings
- `cd backend && python -m pytest` still exits 0
</verification>

<manual_checkpoint>
Cross-check against the client-approved contract at both themes and two viewport widths:
tick each box in turn, confirm the events-only list appears when both vitals are off, confirm the
not-applicable note appears on BP Categories without disabling the boxes, and confirm pulse is
visually distinct from systolic. The agent remains unfunded (AGENT-01), so voice commands cannot be
exercised live — the `show_only` path is covered by unit tests only.
</manual_checkpoint>
