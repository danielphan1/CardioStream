---
phase: 14-unified-show-panel-and-combined-timeline
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - frontend/src/components/ShowPanel.tsx
  - frontend/src/components/ShowPanel.test.tsx
  - frontend/src/components/ChartViewSwitcher.tsx
  - frontend/src/components/EventTimelineList.tsx
  - frontend/src/components/EventTimelineList.test.tsx
  - frontend/src/lib/datasetMeta.ts
  - frontend/src/lib/showSentence.ts
  - frontend/src/lib/showSentence.test.ts
  - frontend/src/components/OverlayToggle.tsx
  - frontend/src/components/OverlayToggle.test.tsx
autonomous: true
requirements: [D-01, D-02, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "Every dataset is a real checkbox in a >=48px target — the control model Chris described, replacing aria-pressed buttons"
    - "The panel doubles as the chart legend: each box carries the exact mark the chart draws, so colour is never the only channel"
    - "The boxes never disable on a non-timeline view — the note is a visible indicator, never a functional gate (D-01 carry-over)"
  artifacts:
    - path: "frontend/src/components/ShowPanel.tsx"
      provides: "Five-checkbox dataset panel with live sentence and not-applicable note"
      contains: "type=\"checkbox\""
    - path: "frontend/src/lib/datasetMeta.ts"
      provides: "Single source of label/glyph/colour/stroke for all five datasets, extending the OVERLAY_META pattern"
      contains: "blood_pressure"
    - path: "frontend/src/components/EventTimelineList.tsx"
      provides: "Dated event list rendered in the chart slot when no vitals are checked"
      contains: "EventTimelineList"
---

<objective>
Build the Show panel, the chart view switcher, and the events-only list.

Purpose: this is the surface the client actually asked for — "any data sets were clickable… like a
check box". `OverlayToggle` is deleted, not adapted: its three-item `aria-pressed` button row is the
wrong control model for the merged five-dataset set.
Output: three new components, a shared dataset metadata module, a tested sentence builder.
</objective>

<task name="dataset_meta">
  <read_first>
    - frontend/src/lib/overlayMeta.ts (the OVERLAY_META/OVERLAY_ORDER pattern this extends)
    - .planning/phases/14-unified-show-panel-and-combined-timeline/14-UI-SPEC.md (§2 series key table)
  </read_first>
  <action>
    Create `lib/datasetMeta.ts` with `DATASET_ORDER: SeriesDataset[]` =
    `["blood_pressure", "pulse", "labs", "incidents", "procedures"]` and `DATASET_META` giving each
    entry `label`, `kind: "vital" | "event"`, `color` (CSS var string), and for vitals a
    `dash: string | undefined` matching the chart stroke.

    Keep `overlayMeta.ts` — the three event hooks and `OverlayEventsList` legitimately key off the
    event subset. `datasetMeta` re-exports the event colours FROM `OVERLAY_META` rather than
    redeclaring them, so the two can never drift (the exact failure `overlayMeta.ts`'s own header
    comment was written to prevent).
  </action>
  <acceptance_criteria>
    - `DATASET_ORDER` has exactly five entries in the UI-SPEC order
    - Event colours in `DATASET_META` are read from `OVERLAY_META`, not literal duplicates
    - `DATASET_META.pulse.color` is `"var(--line-pulse)"` and `DATASET_META.pulse.dash` is `"9 5"`
  </acceptance_criteria>
</task>

<task name="show_sentence">
  <read_first>
    - frontend/src/lib/overlayEvents.ts (`buildOverlaySentence`, `joinWithOr` — the precedent)
  </read_first>
  <action>
    Create `lib/showSentence.ts` exporting `buildShowSentence(visible, hasVitals)` returning the
    three locked strings from UI-SPEC §2. Pure function, no React — same constraint as
    `overlayEvents.ts` (these must be unit-testable without jsdom Recharts).
    Join with `and` (not `or`): `a and b`, `a, b and c`.
  </action>
  <acceptance_criteria>
    - All five off → `"Nothing selected — pick a dataset to see it."`
    - Only incidents → `"Showing incidents — no vitals selected, so these are listed by date."`
    - BP + pulse + incidents → `"Showing blood pressure, pulse and incidents."`
    - Output order always follows `DATASET_ORDER`, never selection order
  </acceptance_criteria>
</task>

<task name="show_panel">
  <read_first>
    - frontend/src/components/OverlayToggle.tsx (the section shell, agent-pulse effect, not-applicable note — all carry forward)
    - frontend/src/components/FilterBar.tsx (activeClass/inactiveClass contract)
    - .planning/phases/14-unified-show-panel-and-combined-timeline/14-UI-SPEC.md (§2)
  </read_first>
  <action>
    Create `ShowPanel.tsx`. Five `<label>`-wrapped real `<input type="checkbox">` controls in
    `DATASET_ORDER`, reading `visibleDatasets` and calling `setDataset`. Heading `Show`.

    Carry forward from `OverlayToggle` verbatim: the `<section className="bg-[var(--color-mist)] p-4">`
    shell, the `useAgentPulse` seq/fields effect with the 1500ms timeout, and the not-applicable note
    — now gated on `chartView !== "timeline"` with the new copy, still `aria-live="polite"`, still
    never disabling the controls.

    Use `PulseField` `"datasets"`. Style per UI-SPEC: `accent-[var(--color-brass)]` on a visible
    26px checkbox, key swatch solid when checked / hollow when unchecked, and **no opacity dimming
    on the group** (quick-task `260827-kir` removed exactly that anti-pattern).

    Render the live sentence from `buildShowSentence` in an `aria-live="polite"` paragraph.
  </action>
  <acceptance_criteria>
    - Five `input[type=checkbox]` render, labelled Blood Pressure / Pulse / Labs / Incidents / Procedures
    - Clicking a label calls `setDataset` with that dataset and the negated state
    - Every label has `min-h-12`
    - With `chartView: "bp_categories"` the note renders AND all five inputs remain enabled
    - No `opacity-` class is applied to the checkbox group under any state
    - Glyph/swatch spans are `aria-hidden="true"`
  </acceptance_criteria>
</task>

<task name="view_switcher">
  <read_first>
    - frontend/src/components/FilterBar.tsx (exact activeClass/inactiveClass strings)
  </read_first>
  <action>
    Create `ChartViewSwitcher.tsx`: three `aria-pressed` buttons (`Timeline`, `BP Categories`,
    `AM vs PM`) in `role="group" aria-label="Chart view"`, reusing FilterBar's class pair verbatim
    and pulsing on `PulseField` `"chart"`.
  </action>
  <acceptance_criteria>
    - Three buttons, each `min-h-12`, exactly one with `aria-pressed="true"`
    - Clicking a button calls `setChartView` with the matching `ChartView` token
  </acceptance_criteria>
</task>

<task name="event_timeline_list">
  <read_first>
    - frontend/src/components/OverlayEventsList.tsx (row markup, empty/error copy)
    - frontend/src/lib/overlayEvents.ts (`OverlayEvent` shape)
    - .planning/phases/14-unified-show-panel-and-combined-timeline/14-UI-SPEC.md (§5)
  </read_first>
  <action>
    Create `EventTimelineList.tsx` taking `events: OverlayEvent[]` and `enabledTypes: OverlayDataset[]`.
    Renders newest-first rows of `dateCell · glyph · whatHappened`, min-height 48px, grid
    `96px 24px 1fr auto` collapsing below 640px per UI-SPEC. Empty state reuses `buildEmptyMessage`.
    Non-interactive — marker detail panels are OVERLAY-07 (v2).
  </action>
  <acceptance_criteria>
    - Renders one row per event in descending `ts` order
    - Zero events with incidents enabled renders `buildEmptyMessage(["incidents"])` output
    - Glyphs are `aria-hidden="true"`; the type is conveyed by visible text
  </acceptance_criteria>
</task>

<task name="delete_overlay_toggle">
  <action>
    Delete `OverlayToggle.tsx` and `OverlayToggle.test.tsx`. Port any still-relevant assertions from
    the old test file into `ShowPanel.test.tsx` — specifically the "controls never disable on a
    non-applicable chart" case, which encodes a real regression (`260827-kir`) and must not be lost.
  </action>
  <acceptance_criteria>
    - Both `OverlayToggle` files are gone
    - `ShowPanel.test.tsx` contains a never-disabled assertion
    - `grep -rn "OverlayToggle" frontend/src` returns no hits
  </acceptance_criteria>
</task>

<verification>
- `npx vitest run src/components/ShowPanel.test.tsx src/components/EventTimelineList.test.tsx src/lib/showSentence.test.ts` exits 0
</verification>
