---
phase: 01-data-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - backend/app/etl.py
  - backend/tests/conftest.py
  - backend/tests/test_etl.py
autonomous: true
requirements: [DATA-01, DATA-05, DATA-07]
tags: [python, pandas, etl, openpyxl]

must_haves:
  truths:
    - "parse_omron reads a real-format OMRON .xlsx into a normalized DataFrame with a naive datetime column"
    - "transform computes all five derived columns via app.derivations, dedupes intra-file datetime collisions last-wins (D-07), and rejects bad rows with per-row reasons (D-08) without aborting the file"
    - "No timezone-aware value exists anywhere in the pipeline output (DATA-05)"
  artifacts:
    - path: "backend/app/etl.py"
      provides: "parse_omron + transform + RejectedRow — the pure ETL shared by seeder and Phase 5 upload"
      exports: ["parse_omron", "transform", "RejectedRow"]
      min_lines: 80
    - path: "backend/tests/test_etl.py"
      provides: "Parse, dedupe, rejection, and naive-dtype tests on synthetic frames/files"
      min_lines: 60
    - path: "backend/tests/conftest.py"
      provides: "OMRON-shaped DataFrame and tmp xlsx builder fixtures"
  key_links:
    - from: "backend/app/etl.py"
      to: "backend/app/derivations.py"
      via: "transform imports the five derivation functions"
      pattern: "from app.derivations import|from .derivations import"
    - from: "backend/tests/test_etl.py"
      to: "backend/app/etl.py"
      via: "direct import"
      pattern: "from app.etl import"
---

<objective>
Build the file-facing half of the ETL as pure functions: `parse_omron(path_or_buffer, max_rows=...) -> raw_df` and `transform(raw_df) -> (clean_df, rejected)` — pinned to the REAL OMRON export format inspected first-hand (files landed in plan 01-01), computing derived columns exclusively via `app.derivations` (DATA-01), with D-07 last-wins intra-file dedupe and D-08 per-row rejection.

Purpose: this exact code path is reused by the seeder (01-07) and the Phase 5 upload route — the CLAUDE.md-mandated "pure function raw_df -> clean_df shared by CLI seeder and upload route" pattern. Format verification here retires the STATE.md blocker (A1/A2 assumptions).
Output: `backend/app/etl.py` (parse + transform), conftest fixtures, green test_etl.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@backend/app/derivations.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect the real OMRON export and pin the parser contract (retires blocker A1/A2)</name>
  <files>backend/tests/conftest.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-01-SUMMARY.md (did real files land, or was the checkpoint skipped?)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Open Questions 1 — what to inspect; §Assumptions Log A1/A2)
  </read_first>
  <action>
    If `data/*.xlsx` exists: inspect it with a throwaway script — `pd.read_excel(path, engine="openpyxl", nrows=8)` plus `df.dtypes` — and record: exact header row position, exact column names (expected: Date, Time, Systolic, Diastolic, Pulse, Symptoms, Consumed, Notes per A1), whether Date/Time arrive as datetime/time cells or text (A2), and any blank-row noise. Also `pd.read_csv("data/bp_data_cleaned.csv", nrows=5)` to record the CSV's exact category label spellings and MAP decimal places (feeds plan 01-07's label map — Open Question 2). Write the findings as a "REAL FORMAT (inspected <date>)" section in the module docstring of `backend/app/etl.py` when Task 2 creates it, and in the 01-04 SUMMARY.

    If `data/` is still empty (checkpoint skipped): proceed against the A1/A2 assumed format and note in the SUMMARY that the blocker remains open — parse_omron must then keep BOTH branches (native datetime cells AND text via `pd.to_datetime(..., errors="coerce")`), which Task 2 builds regardless per RESEARCH recommendation.

    Create `backend/tests/conftest.py` with two fixtures usable by this plan and later ones: `omron_df()` — builds an OMRON-shaped raw DataFrame (matching the inspected/assumed 8 columns) from a list of row dicts; `omron_xlsx(tmp_path)` — writes such a frame to a tmp .xlsx via `df.to_excel(path, index=False)` and returns the path. No DB fixtures yet (plan 01-06 adds them).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "
import sys; sys.path.insert(0, 'tests')
from pathlib import Path
assert Path('tests/conftest.py').read_text().count('def ') >= 2
print('conftest fixtures present')
" && .venv/bin/python -c "
from pathlib import Path
xl = sorted(Path('../data').glob('*.xlsx')) if Path('../data').exists() else []
print('real export present:' , bool(xl))
"</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py contains fixtures `omron_df` and `omron_xlsx` producing the 8 OMRON columns (Date, Time, Systolic, Diastolic, Pulse, Symptoms, Consumed, Notes)
    - If real files exist: the 01-04 SUMMARY records header position, column names, cell types for Date/Time, and the CSV's category label spellings + MAP decimals
    - If real files absent: SUMMARY explicitly states the A1/A2 blocker remains open and the defensive parse branch is mandatory
  </acceptance_criteria>
  <done>Parser contract pinned to observed reality (or explicitly to defended assumptions); shared fixtures exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parse_omron — xlsx to normalized raw DataFrame with naive datetimes</name>
  <files>backend/app/etl.py, backend/tests/test_etl.py</files>
  <read_first>
    - backend/tests/conftest.py (Task 1 fixtures)
    - backend/app/derivations.py (import surface — do not duplicate any logic from it)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Don't Hand-Roll — pd.read_excel, no manual serial-date math; §Common Pitfalls 4 and 5)
  </read_first>
  <behavior>
    - Reading an `omron_xlsx` fixture file returns a DataFrame with columns `datetime` (dtype datetime64[ns], NO tz), `systolic`, `diastolic`, `pulse`, `notes` — Date+Time combined into one naive datetime; Symptoms/Consumed dropped or folded into notes handling
    - Date/Time as text strings (e.g. "2025-03-01", "8:05 AM") still parse via the `pd.to_datetime(..., errors="coerce")` fallback branch; unparseable values become NaT (transform rejects them later — NOT parse_omron's job to drop rows)
    - Blank trailing rows are dropped
    - A file with more than `max_rows` (default 10_000) data rows raises a ValueError naming the limit (DoS guard, becomes load-bearing at the Phase 5 upload boundary)
    - Output dtype check: `df["datetime"].dt.tz is None` (DATA-05)
  </behavior>
  <action>
    Create `backend/app/etl.py` with `parse_omron(path_or_buffer, max_rows: int = 10_000) -> pd.DataFrame` using `pd.read_excel(..., engine="openpyxl")`. Normalize column names to lowercase snake_case, combine Date + Time into a single naive `datetime` column (native datetime/time cells path AND text fallback with `errors="coerce"` — never manual Excel-serial math per Don't Hand-Roll), keep `notes` as pandas 3.0 `str` dtype with NaN preserved. pandas 3.0 idioms only: `df.loc[mask, col] = ...`, no chained assignment, no `inplace` (Pitfall 5). Accepts a path or file-like buffer so the Phase 5 upload route can pass `UploadFile.file` unchanged.

    Write test_etl.py parse tests first against the behavior list (they fail, module absent), then implement to green. Commits: `test(01-04): failing parse_omron tests` then `feat(01-04): implement parse_omron`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_etl.py -q -k "parse"</automated>
  </verify>
  <acceptance_criteria>
    - `app/etl.py` contains `def parse_omron(` with a `max_rows` parameter defaulting to 10000
    - test_etl.py asserts `df["datetime"].dt.tz is None` on parse output
    - A test feeds text-typed Date/Time values and asserts coerce-to-NaT behavior (defensive branch exercised)
    - A test asserts ValueError when row count exceeds max_rows
    - `pytest tests/test_etl.py -k parse -q` exits 0
  </acceptance_criteria>
  <done>parse_omron reads real-format OMRON files (path or buffer) into a normalized naive-datetime frame with the DoS guard in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: transform — derive five columns, D-07 dedupe, D-08 rejection</name>
  <files>backend/app/etl.py, backend/tests/test_etl.py</files>
  <read_first>
    - backend/app/etl.py (parse_omron from Task 2 — transform consumes its output shape)
    - backend/app/derivations.py (the ONLY permitted source of category/MAP/AM-PM logic — DATA-01)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-07 last-wins dedupe, D-08 skip-and-report)
  </read_first>
  <behavior>
    - `transform(raw_df) -> tuple[pd.DataFrame, list[RejectedRow]]` where clean_df has columns: datetime, systolic, diastolic, pulse, am_pm, bp_category, pulse_category, map, pulse_pressure, notes — derived values computed by calling app.derivations functions row-wise (single source of truth, DATA-01)
    - D-08: a row with NaT datetime, or missing/non-numeric/non-positive systolic/diastolic/pulse, is EXCLUDED from clean_df and appears in the rejected list as `RejectedRow(row_index, reason)` — reason names the field and problem (e.g. "pulse: not a number") but does NOT echo other health values from the row (log-hygiene, V8); one bad row never aborts the rest
    - D-07: two input rows with the same minute-granularity datetime → only the LAST in file order survives in clean_df; the displaced one appears in rejected with reason "intra-file duplicate datetime: superseded by later row" (surfaced, never silent)
    - notes: NaN normalized to None before output (DB-ready); pandas 3.0 str dtype respected
    - clean_df datetime remains naive (assert `.dt.tz is None`)
    - Known-value spot check: a row (2025-03-01 08:05, 120, 80, 55) yields am_pm "AM", bp_category "Elevated", pulse_category "Bradycardia", map 93.3, pulse_pressure 40
  </behavior>
  <action>
    Extend `backend/app/etl.py` with a `RejectedRow` dataclass or Pydantic model (`row_index: int`, `reason: str`) and `def transform(raw_df)`. Validate rows first (build rejected list), dedupe with `df.drop_duplicates(subset=["datetime"], keep="last")` recording displaced indices BEFORE dropping, then compute the five derived columns via `app.derivations` (apply per-row or vectorize the arithmetic — but category logic must call `classify_bp`/`classify_pulse`, never re-implement thresholds in pandas expressions). Tests first (failing), then implement; commits `test(01-04):` / `feat(01-04):`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_etl.py tests/test_categories.py tests/test_derivations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/etl.py` contains `def transform(` and `class RejectedRow`
    - `grep -v '^#' backend/app/etl.py | grep -c "def classify_bp\|def classify_pulse"` outputs 0 (no threshold re-implementation — imports only)
    - A test asserts last-wins on duplicate datetimes AND that the displaced row is in rejected (D-07)
    - A test asserts a row with a missing pulse is rejected with a reason string containing the field name, while remaining rows survive (D-08)
    - A test asserts rejected reasons do not contain the row's systolic/diastolic values (log hygiene)
    - Full test command exits 0
  </acceptance_criteria>
  <done>transform produces DB-ready clean frames with locked D-07/D-08 semantics, deriving exclusively through app.derivations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded/local xlsx file → pandas/openpyxl | File content is untrusted input (Phase 5 exposes this same code to caregiver uploads) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Tampering / DoS | parse_omron | mitigate | `max_rows` guard (ValueError beyond 10k rows); pandas/openpyxl parsing only — no eval/exec of any cell content; unparseable cells coerce to NaT/NaN and route to D-08 rejection instead of raising |
| T-1-07 | Tampering (integrity) | transform | mitigate | Per-row validation with explicit rejection reasons (D-08); intra-file dedupe surfaced in output, never silent (D-07); category logic imported from the tested derivations module only |
| T-1-04 | Information Disclosure | rejection reasons / logs | mitigate | RejectedRow.reason names field + problem only, never echoes other health values from the row; asserted by test |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests -q` green
- No `tz_localize`, `tz_convert`, or `timezone.utc` anywhere under backend/app/ (`grep -rn "tz_localize\|tz_convert\|timezone.utc" backend/app/` empty)
</verification>

<success_criteria>
- Real OMRON format verified and documented (blocker A1/A2 retired or explicitly still open)
- DATA-01 derivation path complete: file → parse → transform → clean_df with all five derived columns
- D-07/D-08 semantics pinned by tests (DATA-07 partial)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-04-SUMMARY.md` when done. Record the inspected OMRON format facts and the CSV label spellings/MAP decimals for plan 01-07's golden master.
</output>
