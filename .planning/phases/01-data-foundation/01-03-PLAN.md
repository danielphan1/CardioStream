---
phase: 01-data-foundation
plan: 03
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - backend/app/derivations.py
  - backend/tests/test_categories.py
  - backend/tests/test_derivations.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-07]
tags: [python, etl, medical-derivations, tdd]

must_haves:
  truths:
    - "classify_bp implements the verified AHA table with the D-02 hypotension gate first and D-03 severity-max — every boundary in the RESEARCH matrix passes"
    - "classify_pulse returns Bradycardia <60, Normal 60-100 inclusive, Tachycardia >100 (D-04)"
    - "MAP, pulse pressure, and AM/PM derivations match the pinned formulas at the pinned rounding"
  artifacts:
    - path: "backend/app/derivations.py"
      provides: "Single source of truth for all five derivations (DATA-01)"
      exports: ["classify_bp", "classify_pulse", "compute_map", "compute_pulse_pressure", "derive_am_pm"]
      min_lines: 40
    - path: "backend/tests/test_categories.py"
      provides: "Full boundary matrix for BP + pulse categories"
      min_lines: 40
    - path: "backend/tests/test_derivations.py"
      provides: "MAP, pulse pressure, AM/PM tests"
  key_links:
    - from: "backend/tests/test_categories.py"
      to: "backend/app/derivations.py"
      via: "direct import, no DB, no files"
      pattern: "from app.derivations import"
---

<objective>
Build the medical derivation module test-first: BP classification (verified AHA table + D-02 hypotension gate + D-03 severity-max), pulse classification (D-04), MAP, pulse pressure, and AM/PM. These pure functions are the single source of truth DATA-01 mandates — the transform (01-04), sample generator (01-05), and golden-master (01-07) all import them; nothing else may ever recompute a category.

Purpose: DATA-02 correctness is the medical heart of the project — Chris's data genuinely spans Hypotension to Hypertensive Crisis (systolic 60–211), so every boundary is load-bearing. TDD because the I/O contract is fully specified by the RESEARCH boundary matrix before any code exists.
Output: `backend/app/derivations.py` + green boundary test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@backend/pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing boundary tests for all five derivations</name>
  <files>backend/tests/test_categories.py, backend/tests/test_derivations.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Verified AHA Classification — exact thresholds and Crisis strictness; §Boundary test matrix — copy it completely; §Common Pitfalls 2 and 3)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-01..D-04 — locked category rules)
    - backend/pyproject.toml (pytest config — tests live in backend/tests)
  </read_first>
  <behavior>
    Canonical labels (pinned now, surface in Phase 2 charts/API; golden-master label-map handles any CSV spelling drift per D-01): "Hypotension", "Normal", "Elevated", "Stage 1", "Stage 2", "Hypertensive Crisis" for BP; "Bradycardia", "Normal", "Tachycardia" for pulse.

    test_categories.py — classify_bp(systolic, diastolic):
    - D-02 hypotension gate FIRST: (89, 70) → "Hypotension"; (200, 55) → "Hypotension" (precedence pin from Pitfall 3 — diastolic <60 wins over Crisis-range systolic); (90, 60) → "Normal" (gate not triggered at exactly 90/60)
    - Systolic ladder at dia=70: (119,70) → "Normal"; (120,70) → "Elevated"; (129,70) → "Elevated"; (130,70) → "Stage 1"; (139,70) → "Stage 1"; (140,70) → "Stage 2"
    - Diastolic drives via D-03 severity-max at sys=115: (115,80) → "Stage 1"; (115,89) → "Stage 1"; (115,90) → "Stage 2"
    - Crisis is STRICTLY greater than 180/120 (Pitfall 2): (180,100) → "Stage 2"; (181,100) → "Hypertensive Crisis"; (150,120) → "Stage 2"; (150,121) → "Hypertensive Crisis"
    - Mixed-category severity-max: (125,85) → "Stage 1"; (125,75) → "Elevated"

    test_categories.py — classify_pulse(pulse) per D-04:
    - 59 → "Bradycardia"; 60 → "Normal"; 100 → "Normal"; 101 → "Tachycardia"

    test_derivations.py:
    - compute_map(120, 80) == pytest.approx(93.3, abs=0.05); compute_map(90, 60) == pytest.approx(70.0, abs=0.05) — formula (sbp + 2*dbp)/3 rounded to 1 decimal, returned as float
    - compute_pulse_pressure(120, 80) == 40 and is an int
    - derive_am_pm on naive datetimes: 00:00 → "AM"; 11:59 → "AM"; 12:00 → "PM" (noon exactly is PM); 23:59 → "PM"; result datetime inputs must be naive — include one assertion that the function is exercised with tzinfo-free datetimes only (DATA-05 convention)
  </behavior>
  <action>
    Write both test files importing `from app.derivations import classify_bp, classify_pulse, compute_map, compute_pulse_pressure, derive_am_pm`. Use `pytest.mark.parametrize` for the matrices so failures name the exact boundary. Run `python -m pytest tests/test_categories.py tests/test_derivations.py -q` and confirm they FAIL with ImportError/ModuleNotFoundError (app.derivations does not exist yet). Commit the red state: `test(01-03): add failing boundary tests for BP/pulse/MAP/AM-PM derivations`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_categories.py tests/test_derivations.py -q; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - test_categories.py contains parametrized cases for every tuple listed in behavior, including (200, 55) → "Hypotension" and (181,100)/(150,121) → "Hypertensive Crisis"
    - test_derivations.py contains `pytest.approx` MAP assertions and the four AM/PM boundary times
    - The suite currently fails (exit != 0) because app/derivations.py does not exist
    - A commit exists with message starting `test(01-03):`
  </acceptance_criteria>
  <done>Complete failing boundary suite committed — the spec is executable before implementation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement app/derivations.py to the locked rules</name>
  <files>backend/app/derivations.py</files>
  <read_first>
    - backend/tests/test_categories.py, backend/tests/test_derivations.py (Task 1 output — the executable spec)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Code Examples: "BP classifier implementing D-02/D-03 + verified AHA table" — reference pattern; adjust labels to the canonical set)
  </read_first>
  <behavior>
    All Task 1 tests pass; no test is modified to make them pass. Any test change requires a documented reason (spec error), not implementation convenience.
  </behavior>
  <action>
    Create `backend/app/derivations.py` with five pure functions (no pandas, no DB, no I/O — plain ints/datetimes in, str/float/int out):
    - `classify_bp(systolic: int, diastolic: int) -> str`: first the D-02 gate `systolic < 90 or diastolic < 60 → "Hypotension"`; then classify systolic and diastolic independently on the AHA ladder (Crisis strictly >180 / >120; Stage 2 ≥140 / ≥90; Stage 1 ≥130 / ≥80; Elevated ≥120 systolic only; else Normal) and return the more severe via an ordered SEVERITY list (D-03)
    - `classify_pulse(pulse: int) -> str`: <60 Bradycardia, >100 Tachycardia, else Normal (D-04)
    - `compute_map(systolic: int, diastolic: int) -> float`: `round((systolic + 2 * diastolic) / 3, 1)` — float, 1 decimal (discretion pinned; golden-master diffs with atol=0.05)
    - `compute_pulse_pressure(systolic: int, diastolic: int) -> int`: systolic - diastolic
    - `derive_am_pm(dt: datetime) -> str`: `"AM" if dt.hour < 12 else "PM"`
    Module docstring must state: canonical labels list, "categories are computed here and ONLY here (DATA-01)", and cite the AHA source + D-01..D-04. Run the full suite; commit `feat(01-03): implement BP/pulse/MAP/AM-PM derivations`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_categories.py tests/test_derivations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_categories.py tests/test_derivations.py -q` exits 0
    - `app/derivations.py` contains `def classify_bp(`, `def classify_pulse(`, `def compute_map(`, `def compute_pulse_pressure(`, `def derive_am_pm(`
    - `grep -c "import pandas\|import sqlalchemy" backend/app/derivations.py` outputs 0 (pure module)
    - `git diff HEAD~1 --name-only` for the GREEN commit does not include test files
  </acceptance_criteria>
  <done>Derivation module green against the full boundary matrix — single source of truth established for DATA-01/DATA-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-process functions; no input crosses a trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Tampering (integrity) | classify_bp / classify_pulse | mitigate | Full boundary test matrix pins every threshold (off-by-one on Crisis or the hypotension gate silently mislabels real medical data); tests committed before implementation so regressions are structurally detectable |
| T-1-04 | Information Disclosure | test output | accept | Tests use synthetic literal values from the published AHA table, never Chris's real readings — no PHI can appear in CI/test logs |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests -q` green
- Two commits: RED (`test(01-03):`) then GREEN (`feat(01-03):`)
</verification>

<success_criteria>
- Every value in the RESEARCH boundary matrix asserted and passing (ROADMAP success criterion 3, DATA-07 partial)
- Categories exist in exactly one module (DATA-01 single source of truth)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-03-SUMMARY.md` when done. Record the canonical label strings and the MAP rounding convention — plans 01-04, 01-05, 01-07 and Phase 2 consume them.
</output>
