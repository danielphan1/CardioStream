---
phase: 01-data-foundation
plan: 07
type: execute
wave: 5
depends_on: [01-05, 01-06]
files_modified:
  - backend/app/seed.py
  - backend/tests/test_golden_master.py
  - README.md
autonomous: true
requirements: [DATA-01, DATA-04, DATA-08]
tags: [python, seeding, golden-master, privacy]

must_haves:
  truths:
    - "python -m app.seed on the dev machine loads all 132 real readings through the full ETL pipeline (DATA-04); on a fresh clone it seeds the synthetic sample instead (D-12)"
    - "Every derived column the ETL produces matches bp_data_cleaned.csv in a golden-master diff, with any deliberate divergence investigated and documented (D-01)"
    - "Git history contains no real health data; README documents the seed command and the privacy design"
  artifacts:
    - path: "backend/app/seed.py"
      provides: "python -m app.seed entry point: pick source -> parse -> transform -> merge -> print summary (D-16)"
      contains: "__main__"
    - path: "backend/tests/test_golden_master.py"
      provides: "Skipif-guarded diff of derived columns vs bp_data_cleaned.csv"
      contains: "skipif"
    - path: "README.md"
      provides: "Setup + seeding docs (D-16) and privacy statement"
      contains: "python -m app.seed"
  key_links:
    - from: "backend/app/seed.py"
      to: "backend/app/etl.py"
      via: "seeder is a thin wrapper over parse_omron/transform/merge_readings"
      pattern: "merge_readings"
    - from: "backend/tests/test_golden_master.py"
      to: "data/bp_data_cleaned.csv"
      via: "golden-master fixture read (never loaded into DB, D-14)"
      pattern: "bp_data_cleaned"
---

<objective>
Ship the phase's end state: the `python -m app.seed` entry point (D-16) that ingests the real OMRON export through the full ETL when present, else the synthetic sample (D-12); the golden-master test diffing every derived column against `bp_data_cleaned.csv` (D-01, D-14); and the final privacy audit + README.

Purpose: DATA-04 (132 real readings seeded), ROADMAP success criteria 1 and 4, and closure of the phase goal — "Chris's real data lives in a correctly-derived, duplicate-proof database, and never in the public repo."
Output: seeded dev DB, green golden-master (or documented skip), README, clean privacy audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-04-SUMMARY.md
@.planning/phases/01-data-foundation/01-05-SUMMARY.md
@.planning/phases/01-data-foundation/01-06-SUMMARY.md
@backend/app/etl.py
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: python -m app.seed — source selection, full-ETL seeding, summary output (D-12, D-16)</name>
  <files>backend/app/seed.py</files>
  <read_first>
    - backend/app/etl.py (parse_omron, transform, merge_readings, IngestSummary — the seeder composes these, adds no logic)
    - backend/app/config.py (Settings.database_url — seeder uses the same engine/session as the app)
    - .planning/phases/01-data-foundation/01-05-SUMMARY.md (sample row count for the fallback assertion)
  </read_first>
  <action>
    Create `backend/app/seed.py` with a `main()` guarded by `if __name__ == "__main__":` so `python -m app.seed` works from `backend/` (D-16). Behavior:
    1. Resolve source per D-12: `sorted(Path("../data").glob("*.xlsx"))` relative to repo root (resolve robustly from the module's location, not cwd) — if any real export exists use the first, else `backend/sample_data/omron_sample.xlsx`. NEVER read `bp_data_cleaned.csv` — it is a test fixture only, loading it into the DB is forbidden (D-14).
    2. Run the FULL pipeline: `parse_omron(source)` → `transform(raw)` → `merge_readings(session, clean, rejected)` — the same code path Phase 5 uploads will use (D-14).
    3. Print: which source file was used (path only), then the IngestSummary as labeled lines (added / updated / unchanged / rejected count with reasons / total / latest reading date). Do not print reading values (log hygiene).
    4. Exit code 1 if total == 0 (a silent empty seed is a failure).
    The seeder assumes a migrated DB — it must NOT call `create_all`; if tables are missing, let the error surface and document in README that `alembic upgrade head` runs first.

    Then execute for real: `cd backend && .venv/bin/alembic upgrade head && .venv/bin/python -m app.seed`, run it TWICE, and capture both summaries: with real data present, first run must report added=132 (or the actual export row count minus rejects — investigate any rejects), second run added=0, updated=0. On a no-real-data machine the counts reflect the sample instead.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/alembic upgrade head && .venv/bin/python -m app.seed && .venv/bin/python -m app.seed && .venv/bin/python -c "
from sqlalchemy import create_engine, text
from app.config import get_settings
e = create_engine(get_settings().database_url)
with e.connect() as c:
    n = c.execute(text('select count(*) from readings')).scalar()
assert n > 0, 'empty seed'
print('readings in DB:', n)
"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/seed.py` contains `if __name__ == "__main__":` and calls parse_omron, transform, merge_readings (grep confirms all three)
    - `grep -c "bp_data_cleaned" backend/app/seed.py` outputs 0 (D-14 — CSV never touches the seeder)
    - Running `python -m app.seed` twice leaves the readings count unchanged between runs (idempotency observed end-to-end)
    - With real data present: readings count == 132 and the SUMMARY records the exact first-run counts; any rejected rows are listed with reasons and investigated
    - Seeder output contains added/updated/unchanged/total/latest lines and no systolic/diastolic/pulse values beyond the counts
  </acceptance_criteria>
  <done>One zero-config seed command works on the dev machine, fresh clones, and CI, through the exact pipeline future uploads use — DATA-04 satisfied on the dev machine.</done>
</task>

<task type="auto">
  <name>Task 2: Golden-master test — ETL output vs bp_data_cleaned.csv (D-01, D-14)</name>
  <files>backend/tests/test_golden_master.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-04-SUMMARY.md (inspected CSV label spellings + MAP decimals — the label map source)
    - backend/app/etl.py (transform output column names)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Pattern 4 skipif; §Common Pitfalls 3 and 8 — hypotension-gate rows and column-set discipline)
  </read_first>
  <action>
    Create `backend/tests/test_golden_master.py`, module-level guarded with `pytest.mark.skipif(not <repo-root>/data/bp_data_cleaned.csv exists, reason="real data not present (gitignored)")` — resolve the repo root from `Path(__file__)`, never cwd. The test: run the real export (`data/*.xlsx`) through `parse_omron` + `transform`, load the CSV with `parse_dates=["DateTime"]`, align both frames on datetime, and diff ONLY these columns (Pitfall 8 — the CSV's DayOfWeek/WeekNumber/Month are out of scope): DateTime, AM_PM, BP_Category, Pulse_Category, MAP, Pulse_Pressure. Compare with `pandas.testing.assert_series_equal` per column; MAP with `check_exact=False, atol=0.05` until the CSV's rounding is pinned, then tighten.

    Map CSV label spellings → canonical labels via an explicit `LABEL_MAP` dict built from the 01-04 SUMMARY findings (e.g. "Hypertensive Crisis" may already match; "Stage 1"/"Stage 1 Hypertension" normalize). Per D-01 the CSV is a regression check, NOT the source of truth: if any row disagrees after label mapping, do not bend the classifier — investigate the row (expect candidates among wide-pulse-pressure hypotension-gate rows like 200/55 per Pitfall 3), decide which side is right against the locked D-02/D-03 rules, and encode any deliberate divergence as an explicit documented exclusion list in the test (row datetime + reason comment), recorded in the SUMMARY.

    Also assert row count 132 and that both frames' datetimes are tz-naive. Run the full suite including this test on the dev machine; confirm it executes (not skipped) when data/ is populated.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_golden_master.py -v -q; .venv/bin/python -m pytest tests -q</automated>
  </verify>
  <acceptance_criteria>
    - test_golden_master.py contains `skipif`, a `LABEL_MAP` dict, and diffs exactly the six columns listed (no DayOfWeek/WeekNumber/Month)
    - With real data present: the test RUNS (pytest -v output shows PASSED, not SKIPPED) and passes; any divergences appear as documented exclusions with datetime + reason
    - Without real data: the test reports SKIPPED with reason "real data not present"
    - `grep -c "to_sql\|merge_readings\|session" backend/tests/test_golden_master.py` outputs 0 (CSV never loaded into the DB — D-14; pure-frame comparison)
    - Full suite `pytest tests -q` exits 0
  </acceptance_criteria>
  <done>ROADMAP success criterion 1 proven: every derived field matches the golden master (or divergence is deliberate and documented per D-01).</done>
</task>

<task type="auto">
  <name>Task 3: README + final privacy audit (DATA-08 closure)</name>
  <files>README.md</files>
  <read_first>
    - README.md (stub from plan 01-01 — extend, don't clobber the Privacy section)
    - .planning/phases/01-data-foundation/01-05-SUMMARY.md (sample regeneration command to document)
  </read_first>
  <action>
    Extend `README.md` with: `## Setup` (Python 3.12 venv, `pip install` from backend/pyproject.toml, `alembic upgrade head`), `## Seeding` (D-16: `cd backend && python -m app.seed`; explains D-12 source selection — real export in `data/` if present, else the committed synthetic sample — and shows the added/updated/unchanged summary shape), `## Synthetic sample` (what `backend/sample_data/omron_sample.xlsx` is, that it is generated by `python scripts/generate_sample.py`, and that it contains NO real health data), and expand `## Privacy` (real files live only in gitignored `data/`; the DB file is gitignored; derived medical categories follow the 2025 AHA/ACC guideline — cite heart.org — with the documented Hypotension extension).

    Then run the final privacy audit across the ENTIRE phase history:
    1. `git log --all --name-only --format="" | sort -u | grep -Ei "^data/|bp_data_cleaned|omron" ` — the ONLY permitted matches are `backend/sample_data/omron_sample.xlsx` and source files referencing names (no file under `data/` may ever appear)
    2. `git check-ignore -q data/bp_data_cleaned.csv` (or a probe if absent) exits 0
    3. `git status --porcelain` contains no `data/`, `.venv`, or `.db` entries
    Record all three outputs in the SUMMARY as the DATA-08 evidence. If check 1 ever matches a real file, STOP — do not push, report to the user immediately (history rewrite decision is theirs).
  </action>
  <verify>
    <automated>test -z "$(git log --all --name-only --format='' | sort -u | grep -Ei '^data/')" && git status --porcelain | grep -cE 'data/|\.db' | grep -qx 0 && grep -q "python -m app.seed" README.md && grep -q "## Privacy" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Setup`, `## Seeding`, `python -m app.seed`, `alembic upgrade head`, and a `## Privacy` section mentioning `data/`
    - `git log --all --name-only` shows no path beginning with `data/` in the entire history
    - `git status --porcelain` shows no data/, .venv, or .db entries
    - SUMMARY records the three audit command outputs verbatim
  </acceptance_criteria>
  <done>Phase goal's privacy clause proven against full git history; a new contributor can clone, install, migrate, and seed from README alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree (contains PHI in data/ and dev.db) → public git history | Final audit gate for the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | git history | mitigate | Task 3 full-history audit (`git log --all --name-only`) with hard stop on any data/ match; gitignore probes re-verified |
| T-1-04 | Information Disclosure | seeder stdout / test output | mitigate | Seeder prints counts, reasons, dates — never reading values; golden-master failures show derived-column diffs locally only (dev machine, gitignored data) — acceptable because that output never leaves the machine |
| T-1-09 | Tampering (integrity) | golden-master divergences | mitigate | D-01 protocol: investigate, decide against locked D-02/D-03 rules, document exclusions in-test — no silent bending of the classifier to match a possibly-buggy CSV |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests -q` fully green (golden-master included on the dev machine)
- `python -m app.seed` twice: second run added=0
- Full-history privacy audit clean
</verification>

<success_criteria>
- ROADMAP success criteria 1, 2 (observed end-to-end), and 4 all TRUE
- DATA-04: 132 real readings in the dev DB with derived fields matching the golden master
- Phase 1 goal complete: correctly-derived, duplicate-proof, never-public data foundation
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-07-SUMMARY.md` when done. Record: seed counts (both runs), golden-master result incl. any documented divergences (D-01), and the privacy-audit evidence.
</output>
