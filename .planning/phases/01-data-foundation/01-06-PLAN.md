---
phase: 01-data-foundation
plan: 06
type: execute
wave: 4
depends_on: [01-02, 01-04]
files_modified:
  - backend/app/etl.py
  - backend/tests/conftest.py
  - backend/tests/test_idempotency.py
autonomous: true
requirements: [DATA-03, DATA-05, DATA-07]
tags: [python, sqlalchemy, idempotency, etl]

must_haves:
  truths:
    - "Ingesting the same clean_df twice adds zero rows the second time — added=0, updated=0, unchanged=N (DATA-03)"
    - "A re-appearing datetime with changed values updates the row in place and counts as updated (D-05)"
    - "merge_readings returns the D-06 summary shape: added/updated/unchanged + rejected + total + latest reading date"
  artifacts:
    - path: "backend/app/etl.py"
      provides: "merge_readings(session, clean_df, rejected) -> IngestSummary"
      exports: ["merge_readings", "IngestSummary"]
    - path: "backend/tests/test_idempotency.py"
      provides: "Double-ingest, upsert, unchanged-count, constraint-safety-net tests"
      min_lines: 60
  key_links:
    - from: "backend/app/etl.py"
      to: "backend/app/models.py"
      via: "merge_readings queries/creates Reading rows keyed by datetime"
      pattern: "from app.models import|from .models import"
    - from: "backend/tests/test_idempotency.py"
      to: "backend/app/etl.py"
      via: "parse/transform/merge pipeline exercised end-to-end on tmp SQLite"
      pattern: "merge_readings"
---

<objective>
Complete the ETL with the DB half: `merge_readings(session, clean_df, rejected) -> IngestSummary` implementing the Python-level idempotent merge (RESEARCH Pattern 2 — not ON CONFLICT, which cannot report added-vs-updated-vs-unchanged), with D-05 upsert semantics and the D-06 summary shape that becomes the Phase 5 `POST /upload` response.

Purpose: DATA-03 — re-running a cumulative OMRON export must never duplicate rows; the unique constraint (plan 01-02) is the safety net, this merge is the semantics. Plan 01-07's seeder is a thin wrapper over this function.
Output: `merge_readings` + `IngestSummary` in `app/etl.py`, DB fixtures in conftest, green `test_idempotency.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@backend/app/etl.py
@backend/app/models.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IngestSummary model + merge_readings with D-05/D-06 semantics</name>
  <files>backend/app/etl.py, backend/tests/conftest.py</files>
  <read_first>
    - backend/app/etl.py (current parse/transform + RejectedRow from plan 01-04 — merge extends this module; clean_df column shape)
    - backend/app/models.py (Reading attribute names: datetime_, map_value — the merge writes these)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Pattern 2: Idempotent merge; §Anti-Patterns — session.merge() is forbidden, it keys on the id PK not the datetime natural key)
  </read_first>
  <behavior>
    - `IngestSummary` (Pydantic model, D-06 shape): `added: int`, `updated: int`, `unchanged: int`, `rejected: list[RejectedRow]`, `total: int` (readings in DB after merge), `latest: datetime | None` (naive). This exact shape becomes the Phase 5 API-03 upload response — do not deviate.
    - New datetime → row inserted, counted `added`
    - Existing datetime with any of systolic/diastolic/pulse/notes differing → row updated in place (incoming file is truth, D-05), derived fields recomputed values from clean_df applied too, counted `updated`
    - Existing datetime, all input fields identical → untouched, counted `unchanged`
    - All within ONE transaction; `latest` is the max reading datetime in the DB after commit; stored datetimes round-trip with `tzinfo is None` (DATA-05)
  </behavior>
  <action>
    Extend `backend/app/etl.py`: define `IngestSummary(BaseModel)` and `merge_readings(session, clean_df, rejected: list[RejectedRow]) -> IngestSummary`. Implementation per RESEARCH Pattern 2: load existing rows once into a dict keyed by `Reading.datetime_`, iterate `clean_df.itertuples(index=False)`, branch add/update/unchanged comparing systolic/diastolic/pulse/notes (derived columns follow inputs deterministically). Explicitly do NOT use `session.merge()` (anti-pattern — wrong key). Single `session.commit()` at the end.

    Extend `backend/tests/conftest.py` with DB fixtures: `engine` (function-scoped, `create_engine("sqlite://")` in-memory with `Base.metadata.create_all` — tests use create_all, migrations are covered by test_migrations.py per RESEARCH), and `session` yielding a Session bound to it.

    Write the behavior tests in test_idempotency.py FIRST (fail: merge_readings absent), then implement. Commits: `test(01-06): failing merge/idempotency tests`, `feat(01-06): implement idempotent merge with D-06 counts`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_idempotency.py -q -k "summary or added or updated or unchanged"</automated>
  </verify>
  <acceptance_criteria>
    - `app/etl.py` contains `class IngestSummary(BaseModel)` with fields added, updated, unchanged, rejected, total, latest
    - `app/etl.py` contains `def merge_readings(` and does NOT contain `session.merge(`
    - conftest.py contains `engine` and `session` fixtures using in-memory SQLite + `Base.metadata.create_all`
    - Tests assert each branch's counts: fresh ingest (added=N), value-change re-ingest (updated=1), identical re-ingest (unchanged=N)
  </acceptance_criteria>
  <done>Merge implements the locked D-05 upsert and returns the locked D-06 summary shape inside one transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotency proof — double-ingest, overlap, constraint safety net, naive round-trip</name>
  <files>backend/tests/test_idempotency.py</files>
  <read_first>
    - backend/app/etl.py (full pipeline: parse_omron, transform, merge_readings)
    - backend/tests/conftest.py (omron_xlsx + DB fixtures)
    - .planning/ROADMAP.md (Phase 1 success criterion 2 — the exact behavior being proven)
  </read_first>
  <behavior>
    - Full-pipeline double ingest: build an OMRON xlsx via the `omron_xlsx` fixture, run parse→transform→merge twice into the same session/engine; second summary has added=0, updated=0, unchanged == row count; `SELECT count(*)` unchanged between runs (ROADMAP criterion 2)
    - Cumulative-overlap ingest: second file = first file's rows PLUS 3 new datetimes PLUS 1 existing datetime with a changed systolic → summary reports added=3, updated=1, unchanged=rest; total reflects the union
    - Constraint safety net: bypass merge_readings and `session.add` two Reading objects with identical datetime_ → commit raises IntegrityError (proves DATA-03 holds even if merge logic regresses)
    - Naive round-trip (DATA-05): after merge, every stored `Reading.datetime_` has `tzinfo is None`, and clean_df datetimes equal stored datetimes exactly (no shift across the AM/PM boundary)
  </behavior>
  <action>
    Complete `backend/tests/test_idempotency.py` with the four behavior groups above, using the shared fixtures. Keep each test independent (fresh in-memory engine per test via the function-scoped fixture). These tests are the DATA-03/DATA-07 phase-gate evidence: name them descriptively (test_double_ingest_adds_zero_rows, test_overlapping_export_upserts, test_unique_constraint_safety_net, test_datetimes_naive_roundtrip).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_idempotency.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_idempotency.py contains the four named tests listed in action
    - `pytest tests/test_idempotency.py -x -q` exits 0
    - The double-ingest test asserts both the summary counts AND a direct DB row count equality
    - The safety-net test expects `IntegrityError` via `pytest.raises`
    - Full suite `pytest tests -q` exits 0 (no regression in plans 01-02..01-05 tests)
  </acceptance_criteria>
  <done>ROADMAP success criterion 2 proven by automated tests: overlapping cumulative ingests add zero duplicates, with the DB constraint as backstop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clean_df (derived from untrusted file) → database | Last hop before persistence; integrity semantics enforced here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Tampering (integrity) | merge_readings | mitigate | Deterministic add/update/unchanged branching inside one transaction; DB `uq_readings_datetime` constraint as safety net proven by dedicated test; ORM-only writes (T-1-03 continuation) |
| T-1-04 | Information Disclosure | IngestSummary / seeder output | mitigate | Summary carries counts, reasons, and dates — not blood-pressure values; RejectedRow hygiene already asserted in plan 01-04 |
| T-1-06 | DoS | merge loop | accept | Dict-load of existing rows is O(n) on a dataset that is ~132 rows growing by dozens per upload; capacity risk is negligible for a single-user DB, and parse_omron's max_rows caps input |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests -q` fully green
- `grep -n "session.merge(" backend/app/etl.py` empty
</verification>

<success_criteria>
- DATA-03 satisfied with D-05/D-06/D-07/D-08 semantics all pinned by tests
- IngestSummary shape frozen for Phase 5 API-03 reuse
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-06-SUMMARY.md` when done. Record the IngestSummary field names verbatim — Phase 5's upload endpoint must reuse them.
</output>
