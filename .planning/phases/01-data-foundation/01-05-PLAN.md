---
phase: 01-data-foundation
plan: 05
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - backend/scripts/generate_sample.py
  - backend/sample_data/omron_sample.xlsx
  - backend/tests/test_sample.py
autonomous: true
requirements: [DATA-08]
tags: [python, synthetic-data, privacy, openpyxl]

must_haves:
  truths:
    - "A committed synthetic OMRON-format .xlsx exists that fresh clones and CI can develop against with zero real health data (DATA-08)"
    - "The sample matches the real data's character: ~130 rows, Feb-Jun 2025, ~88% bradycardic, systolic spanning ~60-211, every BP category represented (D-10)"
    - "Regenerating with the same seed reproduces the identical file (D-09)"
  artifacts:
    - path: "backend/scripts/generate_sample.py"
      provides: "Seeded-random generator (committed portfolio evidence, D-09)"
      contains: "SEED"
    - path: "backend/sample_data/omron_sample.xlsx"
      provides: "Committed OMRON-format sample — seeder fallback (D-12) and Phase 5 upload demo (D-11)"
    - path: "backend/tests/test_sample.py"
      provides: "Sample character assertions (columns, coverage, bradycardia share)"
  key_links:
    - from: "backend/scripts/generate_sample.py"
      to: "backend/app/derivations.py"
      via: "classify_bp/classify_pulse used to assert category coverage — no threshold duplication"
      pattern: "from app.derivations import"
---

<objective>
Create the seeded-random synthetic sample generator and its committed output: an OMRON-format `.xlsx` (D-11) with the real dataset's statistical character (D-10) that serves as the seeder fallback (D-12), the CI/dev dataset, and the Phase 5 upload demo file.

Purpose: DATA-08's second half — the repo must be developable without real health data. Committing both script and output (D-09) makes the sample regenerable and doubles as portfolio evidence.
Output: `scripts/generate_sample.py`, `sample_data/omron_sample.xlsx` (committed), green `test_sample.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@backend/app/derivations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded generator script matching the real data's character (D-09, D-10, D-11)</name>
  <files>backend/scripts/generate_sample.py</files>
  <read_first>
    - backend/app/derivations.py (classify_bp/classify_pulse — used for coverage assertions; canonical labels)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-09..D-11 — locked sample design)
    - .planning/PROJECT.md (§Existing data — real data characteristics being mimicked)
  </read_first>
  <action>
    Create `backend/scripts/generate_sample.py`, runnable as `python scripts/generate_sample.py` from `backend/`. Use `random.Random(SEED)` with a module-level `SEED = 20250222` constant — NEVER the global random state (D-09: byte-reproducible output).

    Generate ~130 rows (fixed count, e.g. 132 to mirror the real dataset) spanning 2025-02-22 through 2025-06-13 (D-10), 1-2 readings/day at plausible morning/evening times → mixed AM/PM. Draw pulse so ~88% of rows are <60 bpm (bradycardic character) with the rest spanning Normal and a few >100 Tachycardia. Draw systolic/diastolic from mixture distributions spanning systolic ~60-211 such that EVERY BP category gets at least one row; then force-include a handful of hand-picked edge rows guaranteeing coverage: at least one Hypotension (e.g. 85/55), one Normal, one Elevated, one Stage 1, one Stage 2, one Hypertensive Crisis (e.g. 195/125), and one wide-pulse-pressure hypotension-gate row (e.g. 200/55 per D-02). After generation, assert coverage in-script by mapping rows through `app.derivations.classify_bp` and `classify_pulse` — the script fails loudly if any category is missing (imports the single source of truth; no threshold copies).

    Output columns exactly: `Date`, `Time`, `Systolic`, `Diastolic`, `Pulse`, `Symptoms`, `Consumed`, `Notes` with the last three empty (D-11 — must exercise the same parse path as the real export; match the format facts recorded in 01-04's SUMMARY if available, else A1 assumptions). Write with `df.to_excel("sample_data/omron_sample.xlsx", index=False)` (Don't Hand-Roll — no manual openpyxl assembly). Print a small character report (row count, bradycardia %, category histogram) on completion.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python scripts/generate_sample.py && .venv/bin/python scripts/generate_sample.py && git status --porcelain sample_data/ | grep -qv '^??' ; test -f sample_data/omron_sample.xlsx</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate_sample.py` contains `SEED` and `random.Random(SEED)` (or numpy Generator seeded equivalently); no bare `random.random()`/`random.randint()` on the global state
    - Script imports `from app.derivations import classify_bp` (coverage check uses the real classifier)
    - Running the script twice produces byte-identical `omron_sample.xlsx` (compare hashes: `shasum` equal across two runs)
    - `sample_data/omron_sample.xlsx` exists with exactly the 8 OMRON columns
  </acceptance_criteria>
  <done>Deterministic generator committed; sample file has the D-10 character and D-11 format.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the sample + character regression test</name>
  <files>backend/sample_data/omron_sample.xlsx, backend/tests/test_sample.py</files>
  <read_first>
    - backend/scripts/generate_sample.py (Task 1 output — the character contract to assert)
    - backend/app/derivations.py (canonical labels for coverage assertions)
  </read_first>
  <action>
    Commit `backend/sample_data/omron_sample.xlsx` (this file is INTENDED to be committed — verify it is NOT matched by any .gitignore rule; it lives under backend/sample_data/, not data/).

    Create `backend/tests/test_sample.py` reading the committed file directly with `pd.read_excel(..., engine="openpyxl")` (no dependency on app.etl — plan 01-04 runs in parallel this wave). Assert: (a) columns == ["Date", "Time", "Systolic", "Diastolic", "Pulse", "Symptoms", "Consumed", "Notes"]; (b) row count between 120 and 140; (c) all dates within 2025-02-22..2025-06-13; (d) bradycardia share (pulse < 60) between 0.80 and 0.95; (e) mapping (Systolic, Diastolic) through `classify_bp` covers all six canonical categories: Hypotension, Normal, Elevated, Stage 1, Stage 2, Hypertensive Crisis; (f) Symptoms/Consumed/Notes columns are entirely empty; (g) both AM and PM times present. This test also serves as the demo-file guarantee for the Phase 5 upload flow.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_sample.py -x -q && git ls-files --error-unmatch sample_data/omron_sample.xlsx</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch backend/sample_data/omron_sample.xlsx` exits 0 (file is tracked/committed)
    - `pytest tests/test_sample.py -x -q` exits 0
    - test_sample.py contains assertions for all six canonical BP category labels and the 0.80-0.95 bradycardia share
    - `git check-ignore backend/sample_data/omron_sample.xlsx` exits 1 (NOT ignored)
  </acceptance_criteria>
  <done>Synthetic sample committed and its D-10 character pinned by a regression test — DATA-08's development-data half satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator output → public git repo | The one xlsx that IS committed must contain zero real values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | sample_data/omron_sample.xlsx | mitigate | File is generated purely from `random.Random(SEED)` draws and hand-picked textbook boundary values — the script never reads anything under `data/`; acceptance criterion greps the script for `data/` reads |
| T-1-08 | Spoofing (data provenance) | synthetic sample | mitigate | README (plan 01-07) and script docstring label the file as synthetic demo data so it can never be mistaken for Chris's real readings |
</threat_model>

<verification>
- `grep -n "data/" backend/scripts/generate_sample.py` returns nothing (generator provably independent of real data)
- `cd backend && .venv/bin/python -m pytest tests -q` green
</verification>

<success_criteria>
- ROADMAP success criterion 4 (second half): committed synthetic sample with same schema and outlier character works for development
- Sample regenerable deterministically (D-09); exercises the real openpyxl ingest path (D-11)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-05-SUMMARY.md` when done. Record the sample's exact row count and category histogram — plan 01-07 uses it for the fallback-seed count check.
</output>
