---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/app/models.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/
  - backend/tests/test_migrations.py
autonomous: true
requirements: [DATA-03, DATA-05, DATA-06]
tags: [sqlalchemy, alembic, schema, migrations]

must_haves:
  truths:
    - "alembic upgrade head on an empty database creates readings, lab_results, incidents, procedures"
    - "Inserting two readings with the same datetime raises an IntegrityError (DATA-03 hard guarantee)"
    - "The readings datetime column stores naive local time — no timezone anywhere in the schema"
  artifacts:
    - path: "backend/app/models.py"
      provides: "Typed declarative Base with naming convention; Reading + 3 empty future tables"
      contains: "UniqueConstraint"
      min_lines: 40
    - path: "backend/alembic/env.py"
      provides: "Alembic wired to Base.metadata with render_as_batch=True"
      contains: "render_as_batch=True"
    - path: "backend/tests/test_migrations.py"
      provides: "Migration smoke test: upgrade head creates all 4 tables and matches models"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/models.py"
      via: "target_metadata = Base.metadata"
      pattern: "target_metadata\\s*=\\s*Base\\.metadata"
    - from: "backend/alembic/env.py"
      to: "backend/app/config.py"
      via: "sqlalchemy.url from Settings.database_url"
      pattern: "get_settings|database_url"
---

<objective>
Define the SQLAlchemy 2.0 typed declarative schema (Reading with unique naive datetime, plus empty lab_results/incidents/procedures per DATA-06) and manage it with Alembic: batch mode + naming convention from migration #1, two autogenerated migrations (readings, then future tables — RESEARCH.md discretion recommendation for portfolio legibility), and a smoke test proving migrations create exactly the model tables.

Purpose: DATA-03 (unique datetime constraint), DATA-05 (naive local time schema), DATA-06 (future-table migrations). The merge loader (plan 01-06) and seeder (01-07) build on these models.
Output: `backend/app/models.py`, working Alembic environment with two migrations, `tests/test_migrations.py` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@CLAUDE.md
@backend/app/config.py
@backend/app/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reading model on a naming-convention Base (DATA-03, DATA-05)</name>
  <files>backend/app/models.py</files>
  <read_first>
    - backend/app/db.py (engine/session — models must import cleanly beside it)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Code Examples: "Declarative base with naming convention" — use the exact convention dict; §Common Pitfalls 4 and 6)
    - .planning/PROJECT.md (§Database schema sketch — column names are the contract for Phase 2 API)
  </read_first>
  <action>
    Create `backend/app/models.py`. Define `Base(DeclarativeBase)` with `metadata = MetaData(naming_convention=convention)` using the exact 5-key convention dict from RESEARCH.md (ix/uq/ck/fk/pk) — required so SQLite batch migrations can drop named constraints later.

    Define `Reading` (`__tablename__ = "readings"`) with SQLAlchemy 2.0 `Mapped[]`/`mapped_column` typed declarative ONLY (no legacy Column API): `id` int primary key; `datetime_: Mapped[datetime]` mapped to column name `"datetime"` via `mapped_column("datetime", DateTime, nullable=False)` — plain `DateTime`, never `DateTime(timezone=True)` (DATA-05: naive local time); `systolic`, `diastolic`, `pulse` as `Mapped[int]`; `am_pm`, `bp_category`, `pulse_category` as `Mapped[str]` on `Text`; `map_value: Mapped[float]` mapped to column name `"map"` with `Numeric(5, 1, asdecimal=False)` (RESEARCH Pitfall 6 — floats consistently on SQLite and Postgres); `pulse_pressure: Mapped[int]`; `notes: Mapped[str | None]` on `Text`. Add `__table_args__ = (UniqueConstraint("datetime"),)` — this is DATA-03's hard guarantee.

    Use only portable types (`Integer`, `Numeric`, `Text`, `DateTime`) per CLAUDE.md so the same models run on SQLite dev and Postgres prod. Attribute names `datetime_` and `map_value` avoid shadowing the `datetime` module and the `map` builtin while keeping the DB column names from the PROJECT.md sketch.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "
from sqlalchemy import create_engine
from app.models import Base, Reading
from datetime import datetime as dt
e = create_engine('sqlite://')
Base.metadata.create_all(e)
from sqlalchemy.orm import Session
from sqlalchemy.exc import IntegrityError
with Session(e) as s:
    s.add(Reading(datetime_=dt(2025,3,1,8,0), systolic=120, diastolic=80, pulse=55, am_pm='AM', bp_category='Elevated', pulse_category='Bradycardia', map_value=93.3, pulse_pressure=40))
    s.commit()
    s.add(Reading(datetime_=dt(2025,3,1,8,0), systolic=121, diastolic=81, pulse=56, am_pm='AM', bp_category='Elevated', pulse_category='Bradycardia', map_value=94.3, pulse_pressure=40))
    try:
        s.commit(); raise SystemExit('duplicate datetime accepted — constraint missing')
    except IntegrityError:
        print('unique constraint OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/models.py` contains `class Base(DeclarativeBase)`, `naming_convention`, `mapped_column("datetime", DateTime`, `Numeric(5, 1, asdecimal=False)`, and `UniqueConstraint("datetime")`
    - No occurrence of `timezone=True`, `tz_localize`, or `timezone.utc` in backend/app/models.py
    - Duplicate-datetime insert raises IntegrityError (verify command prints "unique constraint OK")
    - Table name is `readings`; column names include `datetime` and `map` (not `datetime_`/`map_value`) when inspected via `Reading.__table__.columns.keys()`
  </acceptance_criteria>
  <done>Reading model exists with the DATA-03 unique constraint and DATA-05 naive DateTime, on a naming-convention Base.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic environment + migration 0001 (readings)</name>
  <files>backend/alembic.ini, backend/alembic/env.py, backend/alembic/script.py.mako, backend/alembic/versions/</files>
  <read_first>
    - backend/app/models.py (Task 1 output — target_metadata source)
    - backend/app/config.py (Settings.database_url — env.py must use it, not a hardcoded URL)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Pattern 3: batch mode + naming convention; §Code Examples: env.py batch mode)
  </read_first>
  <action>
    Run `alembic init alembic` inside `backend/` (venv's alembic). Edit `backend/alembic/env.py`: import `Base` from `app.models`, set `target_metadata = Base.metadata`, and add `render_as_batch=True` to BOTH `context.configure()` calls (offline and online) — Alembic docs state batch mode is safe in all cases and a no-op on Postgres. Set the database URL from `app.config.get_settings().database_url` (override `config.set_main_option("sqlalchemy.url", ...)` in env.py) so `DATABASE_URL` env var controls migrations exactly like the app; remove/neutralize the static `sqlalchemy.url` in `alembic.ini`.

    Generate migration 0001: `cd backend && .venv/bin/alembic revision --autogenerate -m "create readings table"` then `.venv/bin/alembic upgrade head` against the default dev DB. At this point models.py contains ONLY Reading (future tables arrive in Task 3), so 0001 contains exactly the readings table with its named unique constraint (`uq_readings_datetime` via the convention).

    Note: `dev.db` is gitignored — never commit it. The `alembic/versions/*.py` files ARE committed.
  </action>
  <verify>
    <automated>cd backend && rm -f /tmp/mig1.db && DATABASE_URL=sqlite:////tmp/mig1.db .venv/bin/alembic upgrade head && .venv/bin/python -c "
from sqlalchemy import create_engine, inspect
i = inspect(create_engine('sqlite:////tmp/mig1.db'))
assert 'readings' in i.get_table_names(), i.get_table_names()
uqs = [u['name'] for u in i.get_unique_constraints('readings')]
assert 'uq_readings_datetime' in uqs, uqs
print('migration 0001 OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/alembic/env.py` contains `target_metadata = Base.metadata` and `render_as_batch=True` (both configure calls)
    - `backend/alembic/env.py` references `get_settings` (URL comes from env, not alembic.ini)
    - Exactly one file exists in `backend/alembic/versions/` after this task, and it creates table `readings` with unique constraint `uq_readings_datetime`
    - `DATABASE_URL=sqlite:////tmp/mig1.db alembic upgrade head` exits 0 and the verify assertions pass
  </acceptance_criteria>
  <done>Alembic runs against any DATABASE_URL with batch mode on; migration 0001 creates readings with the named unique constraint.</done>
</task>

<task type="auto">
  <name>Task 3: Future-table models + migration 0002 + migration smoke test (DATA-06)</name>
  <files>backend/app/models.py, backend/alembic/versions/, backend/tests/test_migrations.py</files>
  <read_first>
    - backend/app/models.py (current state after Task 1)
    - .planning/PROJECT.md (§Database schema sketch — future tables: lab_results, incidents, procedures column lists)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Common Pitfalls 7 — models/migrations drift; §Alternatives: create_all in tests, migrations verified separately)
  </read_first>
  <action>
    Extend `backend/app/models.py` with three typed-declarative models per the PROJECT.md sketch, all with int primary keys, portable types, and naive `DateTime`/`Date` where dates appear (DATA-05): `LabResult` (`lab_results`: date, test_name, result, unit, range_low, range_high, notes — Numeric for result/range values, Text otherwise, all nullable except date/test_name); `Incident` (`incidents`: datetime naive, incident_type, duration, notes); `Procedure` (`procedures`: date, procedure_name, location, outcome, notes). Tables stay EMPTY in v1 (DATA-06) — no seed data, no API.

    Generate migration 0002: `alembic revision --autogenerate -m "create future tables lab_results incidents procedures"` and upgrade head. Two-migration granularity is the RESEARCH.md discretion recommendation (readings first, future tables second) — document this in the migration docstring.

    Create `backend/tests/test_migrations.py`: a test that (a) runs `alembic upgrade head` programmatically (alembic.config.Config + alembic.command.upgrade) against a `tmp_path` SQLite URL via `DATABASE_URL` monkeypatch/env, (b) asserts the reflected table set equals `set(Base.metadata.tables.keys())` == {"readings", "lab_results", "incidents", "procedures"} (+ alembic_version), and (c) asserts the `datetime` column on readings reflects with no timezone. This is the drift guard from Pitfall 7 — unit tests elsewhere use `create_all`, so this is the ONLY place migrations are exercised.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_migrations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/models.py` contains classes `LabResult`, `Incident`, `Procedure` with `__tablename__` values `lab_results`, `incidents`, `procedures`
    - Exactly two files exist in `backend/alembic/versions/`
    - `pytest tests/test_migrations.py -x -q` exits 0
    - test_migrations.py contains an assertion comparing reflected table names to `Base.metadata.tables.keys()`
  </acceptance_criteria>
  <done>All four tables come from migrations, the smoke test pins models-vs-migrations parity, and future tables exist empty per DATA-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → database | All reads/writes cross the ORM layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Tampering | DB access layer | mitigate | ORM-only access via typed declarative models (no raw SQL anywhere — CLAUDE.md lock); Alembic autogenerate produces DDL, never hand-written strings interpolating input |
| T-1-04 | Tampering (integrity) | readings.datetime | mitigate | `UniqueConstraint("datetime")` at the schema level — duplicate health readings are structurally impossible even if application merge logic regresses (DATA-03) |
| T-1-02 | Information Disclosure | dev.db / /tmp migration DBs | accept | Local-only SQLite files already covered by `.gitignore` (`*.db`); /tmp scratch DBs contain only synthetic verify rows, no PHI |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests -q` green (test_migrations plus any earlier tests)
- Fresh DB via `alembic upgrade head` contains exactly the 4 model tables
</verification>

<success_criteria>
- ROADMAP success criterion 5 satisfied: migrations create readings + empty lab_results/incidents/procedures, all timestamps naive
- DATA-03 unique constraint proven by IntegrityError test
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-02-SUMMARY.md` when done. Record the exact model attribute names (`datetime_`, `map_value`) and canonical table/column names — plans 01-06 and 01-07 and Phase 2 API consume them.
</output>
