---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - README.md
  - backend/pyproject.toml
  - backend/app/__init__.py
  - backend/app/config.py
  - backend/app/db.py
autonomous: false
requirements: [DATA-08]
tags: [python, sqlalchemy, privacy, scaffolding]

must_haves:
  truths:
    - "A file placed under data/ is invisible to git (check-ignore passes) BEFORE any real data can land"
    - "The backend package imports cleanly with pinned dependencies installed in a Python 3.12 venv"
    - "Real OMRON export and bp_data_cleaned.csv exist in data/ locally (or the user has explicitly deferred them)"
  artifacts:
    - path: ".gitignore"
      provides: "Privacy gate: data/, *.db, .venv, caches ignored"
      contains: "data/"
    - path: "backend/pyproject.toml"
      provides: "Pinned dependencies + pytest + ruff config"
      contains: "[tool.pytest.ini_options]"
    - path: "backend/app/config.py"
      provides: "pydantic-settings Settings with DATABASE_URL default sqlite:///./dev.db"
    - path: "backend/app/db.py"
      provides: "Sync SQLAlchemy engine + session factory"
  key_links:
    - from: "backend/app/db.py"
      to: "backend/app/config.py"
      via: "engine built from Settings.database_url"
      pattern: "create_engine"
---

<objective>
Create the privacy-safe monorepo skeleton: `.gitignore` covering `data/` committed BEFORE anything else (the irreversible privacy step), the `backend/` package layout per D-15, pinned Python 3.12 dependencies, and pydantic-settings config. Then pause for the user to drop Chris's real OMRON export + `bp_data_cleaned.csv` into `data/` per D-13 so all later ETL work targets the real file format.

Purpose: DATA-08 — real health data must never enter the public repo; privacy is a first-commit problem, not a code problem (RESEARCH.md Pitfall 1). Everything downstream (models, ETL, seeding) builds on this scaffold.
Output: committed `.gitignore` + `backend/` scaffold; real data files present locally and ignored by git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit .gitignore alone, then create the monorepo layout (D-13, D-15)</name>
  <files>.gitignore, README.md</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Common Pitfalls: Pitfall 1 — privacy ordering; §Recommended Project Structure)
    - CLAUDE.md (Privacy constraints)
  </read_first>
  <action>
    Create `.gitignore` at repo root with at minimum these patterns: `data/`, `*.db`, `.venv/`, `__pycache__/`, `.pytest_cache/`, `.ruff_cache/`, `.env`, `.DS_Store`. Commit `.gitignore` ALONE as the very first commit of this plan — before any other file in this task or plan is staged (D-13: real files may land at any moment; git history is irreversible in a public portfolio repo).

    Then create the monorepo layout per D-15: `mkdir -p data backend/app` at repo root. Do NOT create `frontend/` (reserved for Phase 2 per D-15) and do NOT put any placeholder file inside `data/` that would be committed — `data/` stays untracked by design. Create a root `README.md` stub with the project title "Health Visualizer — Chris's Health Dashboard", a one-paragraph description, and a "## Privacy" section stating that real health data lives in the gitignored `data/` directory and only a synthetic sample is committed. Full setup/seeding docs are added in plan 01-07.

    Verify the ignore rule with a probe: `touch data/probe.txt && git check-ignore -q data/probe.txt && rm data/probe.txt`.
  </action>
  <verify>
    <automated>git log --oneline -- .gitignore | grep -q . && touch data/probe.txt && git check-ignore -q data/probe.txt && rm data/probe.txt && test -z "$(git status --porcelain | grep 'data/')"</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains lines `data/`, `*.db`, `.venv/`, `__pycache__/`
    - The commit introducing `.gitignore` contains no file under `data/` (verify: `git show --name-only $(git log --format=%H --reverse -- .gitignore | head -1)` lists no data/ paths)
    - `git check-ignore -q data/probe.txt` exits 0 for a probe file
    - `README.md` contains `## Privacy` and the string `data/`
    - Directories `data/` and `backend/app/` exist; `frontend/` does NOT exist
  </acceptance_criteria>
  <done>Gitignore is committed before any other artifact, data/ is provably invisible to git, and the D-15 layout exists.</done>
</task>

<task type="auto">
  <name>Task 2: Backend Python project — pinned deps, pytest/ruff config, settings, engine</name>
  <files>backend/pyproject.toml, backend/app/__init__.py, backend/app/config.py, backend/app/db.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (§Standard Stack — exact pinned versions; §Package Legitimacy Audit — approved list)
    - CLAUDE.md (Technology Stack table — SQLAlchemy sync, pydantic-settings pattern)
    - .gitignore (created in Task 1 — confirm .venv/ and *.db are ignored before creating them)
  </read_first>
  <action>
    Create `backend/pyproject.toml` declaring project metadata (name `health-visualizer-backend`, requires-python `>=3.12`) and ONLY the packages from the approved audit table in 01-RESEARCH.md, pinned: `pandas==3.0.*`, `openpyxl==3.1.*`, `sqlalchemy==2.0.*`, `alembic==1.18.*`, `pydantic==2.13.*`, `pydantic-settings==2.14.*`, and dev deps `pytest==9.*`, `ruff==0.15.*`. Do NOT install fastapi/httpx/psycopg this phase (deferred per RESEARCH — no API surface, SQLite dev). Add `[tool.pytest.ini_options]` with `testpaths = ["tests"]` and `[tool.ruff]` with `line-length = 100`, target-version `py312`.

    Create the venv with the 3.12 interpreter found at /usr/local/bin/python3.12 (`python3.12 -m venv backend/.venv`) and install: `backend/.venv/bin/pip install -e "backend[dev]"` or `pip install` the pinned list directly — whichever matches the pyproject layout chosen (optional-dependencies group `dev` recommended).

    Create `backend/app/__init__.py` (empty). Create `backend/app/config.py`: a pydantic-settings `Settings(BaseSettings)` class with a single field `database_url: str = "sqlite:///./dev.db"` read from env var `DATABASE_URL`, plus a module-level `get_settings()` accessor (plain function returning a cached instance). Create `backend/app/db.py`: sync `create_engine(get_settings().database_url)` and a `sessionmaker` named `SessionLocal` (SQLAlchemy 2.0 style, no async — CLAUDE.md locks sync). No timezone configuration anywhere — naive datetimes end-to-end (DATA-05 groundwork).

    Create `backend/tests/` directory with an empty `backend/tests/__init__.py` so the Wave-0 test tree exists (test modules arrive in later plans; pytest exits 5 with no tests collected — that is expected until plan 01-02).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "import pandas, openpyxl, sqlalchemy, alembic, pydantic_settings; from app.config import get_settings; from app.db import engine, SessionLocal; assert get_settings().database_url.startswith('sqlite'); print(pandas.__version__, sqlalchemy.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` contains `pandas==3.0.`, `sqlalchemy==2.0.`, `alembic==1.18.`, `pytest==9.` pins and `[tool.pytest.ini_options]`
    - `backend/pyproject.toml` does NOT contain `fastapi`, `httpx`, or `psycopg`
    - `backend/app/config.py` contains `database_url` with default `sqlite:///./dev.db` and reads env var `DATABASE_URL`
    - `backend/app/db.py` contains `create_engine` and `SessionLocal`; no `create_async_engine` anywhere
    - The verify import command exits 0 and prints a pandas 3.0.x and sqlalchemy 2.0.x version
    - `git status --porcelain` shows no `.venv` or `dev.db` entries
  </acceptance_criteria>
  <done>Pinned, audited dependencies install cleanly in a 3.12 venv; app package imports; config/engine follow the locked sync + naive-datetime stack.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Add real data files to data/ (D-13)</name>
  <files>data/ (local only — never committed)</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-CONTEXT.md (D-13: user adds files before ETL work; canonical_refs — expected file shapes)
  </read_first>
  <action>
    Claude cannot obtain Chris's health data. The user must place two files into the repo-root `data/` directory (created in Task 1, gitignored):
    1. The raw OMRON export `.xlsx` (expected columns: Date, Time, Systolic, Diastolic, Pulse, Symptoms, Consumed, Notes)
    2. `bp_data_cleaned.csv` (golden-master fixture with DateTime, AM_PM, DayOfWeek, WeekNumber, Month, BP_Category, Pulse_Category, MAP, Pulse_Pressure)
  </action>
  <instructions>
    1. Copy the OMRON export .xlsx into `data/` (any filename ending .xlsx)
    2. Copy `bp_data_cleaned.csv` into `data/bp_data_cleaned.csv`
    3. Reply "done" (or "skip" to proceed without real files — ETL will be built against the assumed format, and the golden-master + real-data seed will auto-skip until files appear)
  </instructions>
  <acceptance_criteria>
    - On "done": `ls data/*.xlsx` and `ls data/bp_data_cleaned.csv` succeed, AND `git status --porcelain` contains no `data/` entries, AND `git check-ignore -q data/bp_data_cleaned.csv` exits 0
    - On "skip": the executor records in the SUMMARY that real files are absent and downstream real-data tasks will skip (STATE.md blocker stays open)
  </acceptance_criteria>
  <verify>
    <automated>ls data/*.xlsx data/bp_data_cleaned.csv 2>/dev/null; git status --porcelain | grep -c 'data/' | grep -qx 0</automated>
  </verify>
  <resume-signal>Reply "done" after copying the files, or "skip" to defer real data</resume-signal>
  <done>Real data files exist locally and are provably ignored by git — or the user has explicitly deferred them and the blocker is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → public git repo | Real health files (PHI) exist beside the working tree; a single wrong `git add` publishes them permanently |
| PyPI → local environment | Third-party packages execute at install/import time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | git history / data/ | mitigate | `.gitignore` with `data/` committed alone as first commit; `git check-ignore` probe in Task 1 verify; checkpoint Task 3 re-verifies with real files present; never `git add -f` under data/ |
| T-1-02 | Information Disclosure | dev.db (contains PHI after seeding) | mitigate | `*.db` in `.gitignore` before any DB file exists |
| T-1-SC | Tampering | pip installs | mitigate | Only the 8 packages rated [OK] in RESEARCH.md Package Legitimacy Audit, version-pinned; no [ASSUMED]/[SUS] packages exist, so no legitimacy checkpoint required |
</threat_model>

<verification>
- `git check-ignore -q data/probe` passes; `git status` never lists data/ content
- `backend/.venv/bin/python -c "from app.db import engine"` exits 0
- First commit of the plan touches only `.gitignore`
</verification>

<success_criteria>
- Privacy gate provably active before real files can land (DATA-08)
- backend/ scaffold matches D-15; deps pinned to the audited versions
- Real data files present locally (or explicitly deferred) and invisible to git
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done. Record whether real data files landed ("done") or were deferred ("skip") — plans 01-04 and 01-07 read this.
</output>
