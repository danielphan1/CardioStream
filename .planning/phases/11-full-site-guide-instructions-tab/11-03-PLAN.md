---
phase: 11-full-site-guide-instructions-tab
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/voiceCommands.ts
  - frontend/src/lib/voiceCommands.test.ts
  - frontend/src/components/CommandBar.tsx
autonomous: true
requirements: [GUIDE-02]

must_haves:
  truths:
    - "VOICE_COMMAND_CATEGORIES exposes exactly the 8 categories, in the fixed order, locked by 11-UI-SPEC.md's Copywriting Contract (Switching charts, Filtering by date, Filtering by AM or PM, Filtering by blood pressure category, Showing labs/incidents/procedures, Starting over, Voice replies, Opening this guide)"
    - "EXAMPLES (CommandBar's placeholder-rotation list) is DERIVED from VOICE_COMMAND_CATEGORIES's example phrases, not independently authored, so the two can never drift again (D-08)"
    - "CommandBar's placeholder rotation behavior is unchanged after the swap — same array length, same rotation mechanics, same rendered 'Try: \"...\"' format"
  artifacts:
    - path: frontend/src/lib/voiceCommands.ts
      provides: "VOICE_COMMAND_CATEGORIES, SIMILAR_PHRASINGS_NOTE, EXAMPLES — the single shared source for both CommandBar's rotation and the guide's What Can I Say section"
      exports: ["VOICE_COMMAND_CATEGORIES", "SIMILAR_PHRASINGS_NOTE", "EXAMPLES", "VoiceCommandCategory"]
  key_links:
    - from: frontend/src/components/CommandBar.tsx
      to: frontend/src/lib/voiceCommands.ts
      via: "import { EXAMPLES } from \"../lib/voiceCommands\""
      pattern: "from \"\\.\\./lib/voiceCommands\""
---

<objective>
Extract `CommandBar.tsx`'s inline `EXAMPLES` array into a new shared, exported module,
`frontend/src/lib/voiceCommands.ts`, per D-08: today's array is 4 short placeholder-rotation
items with an incomplete vocabulary (missing overlay toggles, TTS mute, category filters, and the
new guide open/close command). This plan builds the single source of truth — grouped by category
per D-09, one canonical example phrase per category with the fixed "similar phrasings work too"
note per D-10, in the exact category order `11-UI-SPEC.md` locks — and rewires `CommandBar.tsx`
to import its rotation list FROM this module rather than authoring it inline. Plan 11-04's
`GuideOverlay` "What Can I Say" section imports the same `VOICE_COMMAND_CATEGORIES` constant, so
the two surfaces can never drift again — this is what makes GUIDE-02's "reuse the existing
centralized command-copy source, not a second divergent list" literally satisfiable.

Purpose: GUIDE-02 requires the guide's voice-command reference reuse ONE shared source, not a
second list. Today's `EXAMPLES` array isn't actually complete or reusable as-is — this plan fixes
that at the source before Plan 11-04 consumes it.
Output: `frontend/src/lib/voiceCommands.ts` exporting the categorized reference + a derived flat
`EXAMPLES` list; `CommandBar.tsx` importing `EXAMPLES` instead of authoring it inline, with zero
behavior change to the rotation itself.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-full-site-guide-instructions-tab/11-CONTEXT.md
@.planning/phases/11-full-site-guide-instructions-tab/11-UI-SPEC.md
@.planning/phases/11-full-site-guide-instructions-tab/11-PATTERNS.md
@frontend/src/components/CommandBar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/voiceCommands.ts + voiceCommands.test.ts</name>
  <files>frontend/src/lib/voiceCommands.ts, frontend/src/lib/voiceCommands.test.ts</files>
  <read_first>frontend/src/components/CommandBar.tsx (lines 41-48 — the `EXAMPLES` array is the direct extraction source), backend/app/agent/copy.py (lines 12-16 — the backend's own separate `EXAMPLE_COMMANDS` list; read for awareness only, do NOT import across the Python/TypeScript boundary and do NOT modify this file), .planning/phases/11-full-site-guide-instructions-tab/11-UI-SPEC.md (the "Voice-command category headings (D-09)" row of the Copywriting Contract table — the authoritative, locked category order and labels)</read_first>
  <behavior>
    - `VOICE_COMMAND_CATEGORIES` is an array of exactly 8 objects, each with `id`, `label`, `example` string fields, in this EXACT order: `charts`/"Switching charts", `date-range`/"Filtering by date", `am-pm`/"Filtering by AM or PM", `bp-category`/"Filtering by blood pressure category", `overlay`/"Showing labs, incidents, and procedures", `reset`/"Starting over", `speech`/"Voice replies", `guide`/"Opening this guide".
    - `SIMILAR_PHRASINGS_NOTE` is the single fixed string `"Similar phrasings work too — you don't need the exact words."`
    - `EXAMPLES` is a flat array DERIVED from `VOICE_COMMAND_CATEGORIES.map((c) => c.example)` — never independently authored — so its length always equals 8 and always equals the category list's example phrases in the same order.
  </behavior>
  <action>
Create `frontend/src/lib/voiceCommands.ts` with a file-level doc comment citing D-08/D-09/D-10:
this is the SINGLE source both `CommandBar`'s placeholder rotation and `GuideOverlay`'s "What Can
I Say" section (Plan 11-04) import from; fixed category order per `11-UI-SPEC.md`; one canonical
example phrase per category plus the fixed "similar phrasings" note.

Export `type VoiceCommandCategory = { id: string; label: string; example: string }`.

Export `const VOICE_COMMAND_CATEGORIES: VoiceCommandCategory[]` implementing the `&lt;behavior&gt;`
above. Exact phrase wording per category is Claude's Discretion per 11-CONTEXT.md — write short,
concrete, illustrative phrases (e.g. `"show my pulse"` for charts, `"last 30 days"` for
date-range, `"mornings only"` for am-pm, `"show stage 2 readings"` for bp-category, `"show
incidents"` for overlay, `"show all data"` for reset, `"mute the voice replies"` for speech,
`"open the guide"` for guide) — the category `id`/`label` order is LOCKED, the exact example text
is not.

Export `const SIMILAR_PHRASINGS_NOTE = "Similar phrasings work too — you don't need the exact
words.";` verbatim (this exact string is locked by `11-UI-SPEC.md`'s Copywriting Contract).

Export `const EXAMPLES = VOICE_COMMAND_CATEGORIES.map((c) => c.example);` — the derived flat list
Task 2 wires into `CommandBar.tsx`.

Create `frontend/src/lib/voiceCommands.test.ts` (mirrors the direct-assertion style of
`frontend/src/lib/palette.test.ts` or `frontend/src/lib/overlayEvents.test.ts` — a plain data
module, no store/render needed) covering: `VOICE_COMMAND_CATEGORIES` has exactly 8 entries; the
`id` sequence equals `["charts", "date-range", "am-pm", "bp-category", "overlay", "reset",
"speech", "guide"]` in that exact order; every `label` is non-empty; `EXAMPLES` has length 8 and
equals `VOICE_COMMAND_CATEGORIES.map((c) => c.example)` (referential derivation, not a coincidence
of independently-typed duplicate data); `SIMILAR_PHRASINGS_NOTE` equals the exact locked string.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/voiceCommands.ts` exports `VOICE_COMMAND_CATEGORIES`, `SIMILAR_PHRASINGS_NOTE`, `EXAMPLES`, `VoiceCommandCategory`
    - `grep -c '"charts"\|"date-range"\|"am-pm"\|"bp-category"\|"overlay"\|"reset"\|"speech"\|"guide"' frontend/src/lib/voiceCommands.ts` is at least 8
    - `cd frontend && npx vitest run src/lib/voiceCommands.test.ts` passes with zero failures
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/voiceCommands.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>voiceCommands.ts exports the 8-category reference, the fixed similar-phrasings note, and a derived EXAMPLES list, all covered by voiceCommands.test.ts.</done>
</task>

<task type="auto">
  <name>Task 2: CommandBar.tsx — replace inline EXAMPLES with the shared import</name>
  <files>frontend/src/components/CommandBar.tsx</files>
  <read_first>frontend/src/components/CommandBar.tsx (full file — the import block at lines 22-33, the inline `const EXAMPLES = [...]` block at lines 41-48, the rotation effect at lines 102-108, and the `placeholder` usage at line 194 are the direct edit targets), frontend/src/lib/voiceCommands.ts (the new module created in Task 1 of this same plan)</read_first>
  <action>
Remove the inline `const EXAMPLES = [...]` block (currently lines 41-48, including its leading
comment) from CommandBar.tsx. Add `import { EXAMPLES } from "../lib/voiceCommands";` to the
existing import block, positioned alphabetically near the existing `import { applyAgentFilters,
composeConfirmation } from "../lib/agent";` line (both are `../lib/*` imports). No other
CommandBar logic changes — the `exampleIdx`/rotation `useEffect` (lines 102-108) and the
`placeholder` template literal (line 194) consume the imported `EXAMPLES` exactly as they
consumed the inline array before; do not rename `EXAMPLES` at the call sites.
  </action>
  <acceptance_criteria>
    - `grep -c "^const EXAMPLES" frontend/src/components/CommandBar.tsx` equals 0 (inline array removed)
    - `grep -c 'import { EXAMPLES } from "../lib/voiceCommands"' frontend/src/components/CommandBar.tsx` equals 1
    - `cd frontend && npx tsc --noEmit` passes
    - `cd frontend && npx vitest run src/components/CommandBar.test.tsx` passes with zero failures (regression check — this file has no test that asserts on placeholder/EXAMPLES content, so a green run here confirms no behavior broke)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/components/CommandBar.test.tsx</automated>
  </verify>
  <done>CommandBar.tsx's placeholder rotation now sources its examples from the shared voiceCommands.ts module, with zero behavior change and a fully green CommandBar.test.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | `voiceCommands.ts` is a static, compile-time-authored data module — no user input, no network call, no external data source anywhere in this file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | `voiceCommands.ts` content | accept | Every string in this module is a hardcoded literal authored at build time — there is no dynamic, user-controlled, or model-controlled input anywhere in this file, so there is no injection surface to mitigate. The subsequent `GuideOverlay` consumer (Plan 11-04) renders these strings as plain JSX text nodes (React auto-escapes), never `dangerouslySetInnerHTML`. |

No new backend routes, no new dependency, no new attack surface — this plan only moves an
existing hardcoded array to a new file and adds a derived one alongside it.
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit && npx vitest run src/lib/voiceCommands.test.ts src/components/CommandBar.test.tsx`
</verification>

<success_criteria>
- `voiceCommands.ts` exposes the 8-category reference in the exact locked order, plus a derived
  `EXAMPLES` list that can never drift from it
- `CommandBar.tsx`'s placeholder rotation is unchanged in behavior, now sourced from the shared
  module
- `cd frontend && npx vitest run src/lib/voiceCommands.test.ts src/components/CommandBar.test.tsx`
  is fully green
</success_criteria>

<output>
Create `.planning/phases/11-full-site-guide-instructions-tab/11-03-SUMMARY.md` when done
</output>
