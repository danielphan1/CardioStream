---
phase: 11-full-site-guide-instructions-tab
plan: 04
type: execute
wave: 2
depends_on: ["11-02", "11-03"]
files_modified:
  - frontend/src/components/GuideOverlay.tsx
  - frontend/src/components/GuideOverlay.test.tsx
  - frontend/src/components/Header.tsx
autonomous: true
requirements: [GUIDE-01, GUIDE-02, GUIDE-04]

must_haves:
  truths:
    - "GuideOverlay renders nothing (returns null) when useGuide's open is false, and the full guide content when open is true — a single scrollable page with a jump-to-section table of contents (D-11), never an accordion or tabs, and text-only with no screenshots or diagrams (D-12)"
    - "Every one of the 7 fixed-format sections (Command Bar, Filters, Charts, Overlay, Voice Replies, Upload, Add a Record) has an h2 heading, a 'what it does' paragraph, and a 'By click:' paragraph; only Upload and Add a Record omit the 'By voice:' paragraph (D-13 — those two flows have no voice equivalent today)"
    - "The What Can I Say section renders all 8 voiceCommands.ts categories, each showing its canonical example phrase and the shared SIMILAR_PHRASINGS_NOTE (D-09/D-10) — imported from lib/voiceCommands.ts, never re-authored"
    - "Escape key and the Close button both call useGuide's setOpen(false); the overlay uses role=\"region\" (not role=\"dialog\"), has no aria-modal attribute, and traps no focus — nothing outside the guide is excluded from the tab order (D-04)"
    - "The Header shows an icon+text Guide button immediately after the Voice Replies toggle, styled identically (min-h-12, border-2 border-[var(--color-ink)], bg-[var(--color-sky)], text-[20px] font-bold, never accent-filled), with aria-pressed reflecting useGuide's open state, on every view Header mounts on (D-02)"
  artifacts:
    - path: frontend/src/components/GuideOverlay.tsx
      provides: "The full-screen site guide: h1, table of contents, 7 fixed-format sections, What Can I Say section, About This Guide section, sticky Close button, Escape-to-close"
      min_lines: 150
    - path: frontend/src/components/GuideOverlay.test.tsx
      provides: "Behavior coverage: closed→null, open→content, Escape/Close→setOpen(false), no dialog/focus-trap semantics, all 9 h2 headings present, Upload/Add-a-Record omit the By-voice line"
    - path: frontend/src/components/Header.tsx
      provides: "Guide button (BookOpen icon + 'Guide' text, aria-pressed, useGuide-wired)"
  key_links:
    - from: frontend/src/components/GuideOverlay.tsx
      to: frontend/src/store/guide.ts
      via: "useGuide((s) => s.open) / useGuide((s) => s.setOpen)"
      pattern: "useGuide\\(\\(s\\) => s\\.open\\)"
    - from: frontend/src/components/GuideOverlay.tsx
      to: frontend/src/lib/voiceCommands.ts
      via: "VOICE_COMMAND_CATEGORIES.map(...) rendering the What Can I Say section"
      pattern: "VOICE_COMMAND_CATEGORIES\\.map"
    - from: frontend/src/components/Header.tsx
      to: frontend/src/store/guide.ts
      via: "onClick={toggleGuide} where toggleGuide = useGuide((s) => s.toggleOpen)"
      pattern: "useGuide\\(\\(s\\) => s\\.toggleOpen\\)"
---

<objective>
Build the guide's actual content surface: `GuideOverlay.tsx`, a new full-screen component
rendering the static, text-only, one-scrollable-page guide locked by D-11/D-12/D-13 and
`11-UI-SPEC.md`'s Phase-Specific Component Contract — a table of contents, 7 sections in the
fixed what-it-does/by-click/by-voice format, a "What Can I Say" section sourced from Plan 11-03's
`voiceCommands.ts`, and an "About This Guide" closing section. The overlay is a landmark
(`role="region"`), NOT a modal (`LogoutConfirmDialog`'s `role="dialog"`/`aria-modal`/focus-trap
are explicitly NOT copied here — D-04 requires nothing outside the guide, especially
`CommandBar`, be excluded from the tab order). Also add the Header-right "Guide" button (D-02)
that opens/closes it by click, styled identically to the existing Theme/Voice Replies toggles.

Purpose: GUIDE-01 requires the guide explain "every control, filter, chart, and upload flow";
GUIDE-02 requires its voice-command reference reuse the ONE shared source (Plan 11-03);
GUIDE-04 requires the same accessibility bar as the rest of the site (≥48px targets, ≥18px text,
high contrast, keyboard + voice navigable) — this plan is where all three requirements become an
actual renderable surface for the first time.
Output: A fully self-contained `GuideOverlay` component (mount point deferred to Plan 11-05) and
a working Header click-to-open/close button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-full-site-guide-instructions-tab/11-CONTEXT.md
@.planning/phases/11-full-site-guide-instructions-tab/11-UI-SPEC.md
@.planning/phases/11-full-site-guide-instructions-tab/11-PATTERNS.md
@frontend/src/store/guide.ts
@frontend/src/lib/voiceCommands.ts
@frontend/src/components/Header.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GuideOverlay.tsx + GuideOverlay.test.tsx</name>
  <files>frontend/src/components/GuideOverlay.tsx, frontend/src/components/GuideOverlay.test.tsx</files>
  <read_first>frontend/src/components/Header.tsx (lines 37-119 — `LogoutConfirmDialog`'s Escape-handling `onKeyDown` pattern is the partial template; its `role="dialog"`/`aria-modal="true"`/Tab-trap/dimmed backdrop at lines 77, 84-85, 59-71 must NOT be copied), frontend/src/components/OverlayToggle.tsx (lines 24-30 — the chip styling class strings referenced by 11-UI-SPEC.md's ToC snippet), frontend/src/store/guide.ts (the useGuide contract — open/setOpen, from Plan 11-02), frontend/src/lib/voiceCommands.ts (VOICE_COMMAND_CATEGORIES/SIMILAR_PHRASINGS_NOTE, from Plan 11-03), .planning/phases/11-full-site-guide-instructions-tab/11-UI-SPEC.md (the "Overlay stacking/layering", "Guide close control", "Table of contents", and "Per-section fixed format" sections — the authoritative, binding markup/class contract for this task), frontend/src/index.css (lines 21-28 — the `--color-foam`/`--color-sky`/`--color-ink`/`--color-accent` token definitions this component must use verbatim, never a hex literal)</read_first>
  <behavior>
    - `GuideOverlay` reads `useGuide((s) => s.open)`; when `open` is `false` the component returns `null` — nothing renders, nothing is in the DOM.
    - When `open` is `true`, the component renders: an `<h1>` reading exactly `"Site Guide"`; a `<nav aria-label="Jump to a section">` table of contents with one chip-style anchor link per section (`href="#{id}"`); 7 sections in the fixed order Command Bar → Filters → Charts → Overlay → Voice Replies → Upload → Add a Record, each with an `<h2>`, a "what it does" paragraph, and a "By click:" paragraph; Upload and Add a Record omit the "By voice:" paragraph, every other section (Command Bar, Filters, Charts, Overlay, Voice Replies) includes one; a "What Can I Say" section with one `<h3>` + quoted example + `SIMILAR_PHRASINGS_NOTE` block per `VOICE_COMMAND_CATEGORIES` entry (8 total); an "About This Guide" section; a Close button.
    - A `keydown` listener is attached to `window` ONLY while `open` is `true` (added in a `useEffect` with `[open, setOpen]` deps, removed on cleanup/close) — pressing `Escape` calls `setOpen(false)`.
    - Clicking the Close button calls `setOpen(false)`.
    - The outer container has `role="region"` and `aria-label="Site guide"` — it does NOT have `role="dialog"`, does NOT have `aria-modal`, and no element inside sets `tabIndex={-1}`/`inert` on anything OUTSIDE itself (nothing outside this component's own returned tree is ever touched).
  </behavior>
  <action>
Create `frontend/src/components/GuideOverlay.tsx`. File-level doc comment citing D-01..D-13 and
11-UI-SPEC.md's stacking contract: this is a landmark region rendered as an always-mounted
component that returns `null` when closed, NOT a modal — no focus trap, no `aria-modal`, because
`CommandBar` must stay reachable while this is open (D-03/D-04).

Imports: `useEffect` from `"react"`; `X` from `"lucide-react"`; `useGuide` from
`"../store/guide"`; `VOICE_COMMAND_CATEGORIES, SIMILAR_PHRASINGS_NOTE` from
`"../lib/voiceCommands"`.

Component body: `const open = useGuide((s) => s.open); const setOpen = useGuide((s) =>
s.setOpen);`. A `useEffect` that, when `open` is true, adds a `window` `"keydown"` listener
calling `setOpen(false)` on `e.key === "Escape"`, and removes it on cleanup (mirrors
`LogoutConfirmDialog`'s Escape branch, but as a `window`-level listener since there is no local
`onKeyDown`-bearing dialog element to attach to — this component has no focus trap). Immediately
after: `if (!open) return null;`.

Outer element: a `<div role="region" aria-label="Site guide" className="fixed inset-0 z-50
overflow-y-auto bg-[var(--color-foam)]">` — opaque background (never `bg-black/40` or any dimmed
value; 11-UI-SPEC.md D-02 explicitly forbids a dimmed backdrop here), `z-50` (lower than the
CommandBar band's `z-[60]` promotion, wired in Plan 11-05).

Inside it, first a sticky Close-button strip: `<div className="sticky top-0 z-10 flex justify-end
bg-[var(--color-foam)] px-4 py-2 md:px-8">` containing the Close button using the exact markup
locked in 11-UI-SPEC.md's "Guide close control" section: `<button type="button"
onClick={() => setOpen(false)} className="flex min-h-12 items-center gap-2 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-sky)] px-6 text-[20px] font-bold
text-[var(--color-ink)]"><X aria-hidden="true" size={24} />Close</button>`.

Then a content wrapper: `<div className="mx-auto flex max-w-[1280px] flex-col gap-8 px-4 pt-24
pb-16 md:px-8 xl:px-16">` — the `pt-24` (96px) top clearance is a pragmatic estimate of the
CommandBar band's typical rendered height (its own `py-4` wrapping a `min-h-12` row ≈ 80px, plus
buffer); Plan 11-05's manual verification checkpoint explicitly re-checks this value against the
band's real height (including its taller voice-state sub-lines) and may need to increase it —
leave a short inline comment noting this.

Inside the content wrapper, define a local `const SECTIONS = [{ id: "command-bar", label:
"Command Bar" }, { id: "filters", label: "Filters" }, { id: "charts", label: "Charts" }, { id:
"overlay", label: "Overlay" }, { id: "voice-replies", label: "Voice Replies" }, { id: "upload",
label: "Upload" }, { id: "add-a-record", label: "Add a Record" }, { id: "what-can-i-say", label:
"What Can I Say" }, { id: "about-this-guide", label: "About This Guide" }];` (all 9 sections, so
the ToC can jump anywhere on the page) — declare this above the returned JSX, not inline.

Render, in order: `<h1 className="text-[32px] font-bold leading-tight
text-[var(--color-ink)]">Site Guide</h1>`; the table of contents using the exact snippet locked in
11-UI-SPEC.md's "Table of contents" section (a `<nav aria-label="Jump to a section">` with a
`<p>Jump to a section</p>` lead-in and a `<ul className="mt-2 flex flex-wrap gap-2">` mapping
`SECTIONS` to `<li><a href={\`#${s.id}\`} className="flex min-h-12 items-center rounded-lg
border-2 border-[var(--color-ink)] bg-[var(--color-sky)] px-4 text-[20px] font-bold
text-[var(--color-ink)]">{s.label}</a></li>`).

Then 7 `<section id="{matching SECTIONS id}">` blocks, each following D-13's fixed format
EXACTLY: an `<h2 className="text-2xl leading-tight font-bold text-[var(--color-ink)]">` with the
section's title; one `<p className="text-lg text-[var(--color-ink)]">` paragraph describing what
the control/chart/flow does; one `<p>` prefixed with a bold `"By click:"` lead-in (e.g. `<p
className="text-lg text-[var(--color-ink)]"><strong>By click:</strong> ...</p>`) describing the
click/tap interaction; one `<p>` prefixed with a bold `"By voice:"` lead-in describing the voice
phrasing — OMIT this third paragraph entirely for the Upload and Add a Record sections (neither
has a voice equivalent today — Upload has none per D-13's own example, and Add a Record has none
because AGENT-02 voice/text data entry is v2-deferred, REQUIREMENTS.md). Content-writing detail
(exact prose) is Claude's Discretion per 11-CONTEXT.md; concrete anchors for what each section
must describe: Command Bar (the text input + Send button + mic button, by click = type and press
Send/Enter, by voice = tap the mic and speak a command); Filters (the date/AM-PM/BP-category
filter chips, by click = tap a chip, by voice = say a filter phrase e.g. "last 30 days, mornings
only"); Charts (the 4 mini chart-picker cards for BP Timeline/Pulse Trend/BP Categories/AM vs PM,
by click = tap a card, by voice = say a chart name e.g. "show my pulse"); Overlay (the 3
Labs/Incidents/Procedures toggle buttons, by click = tap a button, by voice = say "show incidents"
or "hide labs"); Voice Replies (the Header mute/quiet toggle, by click = tap the Header button, by
voice = say "mute the voice replies"); Upload (the Upload page for OMRON `.xlsx` files, by click =
tap the Header Upload button then choose a file — no voice line); Add a Record (the Add Record
page's Lab/Incident/Procedure forms, by click = tap the Header Add Record button then fill in a
form — no voice line).

Then the "What Can I Say" `<section id="what-can-i-say">`: an `<h2>` reading `"What Can I Say"`,
then `VOICE_COMMAND_CATEGORIES.map((c) => ...)` rendering, per category, an `<h3
className="text-[20px] font-bold text-[var(--color-ink)]">{c.label}</h3>`, a bold-quoted example
`<p className="text-lg font-bold text-[var(--color-ink)]">"{c.example}"</p>`, and the fixed note
`<p className="text-lg text-[var(--color-ink)]">{SIMILAR_PHRASINGS_NOTE}</p>` — one shared note
string imported, never re-typed per category (D-10). Use `c.id` as the React `key`.

Finally the "About This Guide" `<section id="about-this-guide">`: an `<h2>` reading `"About This
Guide"` and one short closing `<p>` (content-writing detail, Claude's Discretion) noting the guide
can be reopened any time via the Header "Guide" button, and closed via this Close button or
Escape.

Create `frontend/src/components/GuideOverlay.test.tsx` (mirrors `CommandBar.test.tsx`'s
`render`/`screen`/`fireEvent` style, no `QueryClientProvider` needed — this component makes no
network calls). `beforeEach` must reset `useGuide.setState({ open: false })`. Cover: (1) renders
nothing when closed — `render(&lt;GuideOverlay /&gt;)` then `expect(screen.queryByRole("region",
{ name: "Site guide" })).not.toBeInTheDocument()`; (2) renders the region and `<h1>Site
Guide&lt;/h1&gt;` when `useGuide.setState({ open: true })` before render; (3) all 9 section
headings present — query `getByRole("heading", { name: "Command Bar" })` etc. for all 7 fixed
sections plus `"What Can I Say"` and `"About This Guide"`; (4) pressing Escape
(`fireEvent.keyDown(window, { key: "Escape" })`) sets `useGuide.getState().open` to `false`; (5)
clicking the Close button (`screen.getByRole("button", { name: /close/i })`) sets
`useGuide.getState().open` to `false`; (6) `screen.queryByRole("dialog")` is null and the region
container has no `aria-modal` attribute; (7) the Upload section's text content does NOT contain
`"By voice:"` and the Add a Record section's text content does NOT contain `"By voice:"` (query
via `container.querySelector("#upload")!.textContent` / `#add-a-record`); (8) all 8
`VOICE_COMMAND_CATEGORIES` example phrases render inside the "What Can I Say" section (loop
`VOICE_COMMAND_CATEGORIES` and assert `screen.getByText(\`"${c.example}"\`)` for each).
  </action>
  <acceptance_criteria>
    - `grep -c 'role="dialog"\|aria-modal' frontend/src/components/GuideOverlay.tsx` equals 0
    - `grep -c 'role="region"' frontend/src/components/GuideOverlay.tsx` equals 1
    - `grep -c 'bg-black' frontend/src/components/GuideOverlay.tsx` equals 0 (no dimmed backdrop)
    - `grep -c 'VOICE_COMMAND_CATEGORIES' frontend/src/components/GuideOverlay.tsx` is at least 1
    - `grep -c '<section id=' frontend/src/components/GuideOverlay.tsx` equals 9
    - `cd frontend && npx vitest run src/components/GuideOverlay.test.tsx` passes with zero failures, including all 8 behaviors above
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/GuideOverlay.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>GuideOverlay.tsx renders the full text-only guide per D-11/D-12/D-13 and 11-UI-SPEC.md's contract, with zero focus-trap/modal semantics, sourcing its voice-command reference from the shared voiceCommands.ts module — fully covered by GuideOverlay.test.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Header.tsx — "Guide" button</name>
  <files>frontend/src/components/Header.tsx</files>
  <read_first>frontend/src/components/Header.tsx (full file — the Voice Replies toggle button at lines 184-200 and its store-read pattern at lines 126-127 are the direct templates; the "View toggle" comment block at line 202 marks the exact insertion boundary), frontend/src/store/guide.ts (the useGuide contract this button reads/writes, from Plan 11-02), .planning/phases/11-full-site-guide-instructions-tab/11-UI-SPEC.md (the "Header — new 'Guide' button (D-02)" section — the authoritative, binding markup)</read_first>
  <action>
Add `BookOpen` to the existing `import { ClipboardPlus, LogOut, Moon, Sailboat, Sun, Upload,
Volume2, VolumeX } from "lucide-react";` line, inserted alphabetically (→ `BookOpen,
ClipboardPlus, LogOut, Moon, Sailboat, Sun, Upload, Volume2, VolumeX`). Add `import { useGuide }
from "../store/guide";` to the store-import block, positioned alphabetically between `import {
useAuth } from "../store/auth";` and `import { useSpeech } from "../store/speech";`.

Inside the `Header()` function, immediately after the existing `const toggleSpeech = useSpeech((s)
=> s.toggleEnabled);` line, add: `const guideOpen = useGuide((s) => s.open);` and `const
toggleGuide = useGuide((s) => s.toggleOpen);`.

Add a new button immediately after the Voice Replies toggle button's closing `</button>` tag
(currently line 200) and before the existing "View toggle" comment block (currently line 202),
using the exact markup locked in 11-UI-SPEC.md's "Header — new 'Guide' button" section: `<button
type="button" onClick={toggleGuide} aria-pressed={guideOpen} className="flex min-h-12 items-center
gap-2 rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-sky)] px-4 text-[20px]
font-bold text-[var(--color-ink)]"><BookOpen aria-hidden="true" size={24} />Guide</button>`. The
label text stays `"Guide"` in both open and closed states — it does NOT swap to "Close Guide" the
way Theme/Voice Replies swap their label text (11-UI-SPEC.md's Copywriting Contract: the
dedicated Close/X control inside the overlay already owns that verb; `aria-pressed` alone
communicates state here). No separate `aria-label` — the visible text is the accessible name,
matching the Theme/Voice Replies toggles' own convention.
  </action>
  <acceptance_criteria>
    - `grep -c '>Guide<' frontend/src/components/Header.tsx` equals 1
    - `grep -c 'aria-pressed={guideOpen}' frontend/src/components/Header.tsx` equals 1
    - The new button's line number is greater than the Voice Replies toggle button's closing `</button>` line and less than the "View toggle" comment's line
    - The button's className string is byte-identical to the Voice Replies toggle's className string
    - `grep -c '"Close Guide"' frontend/src/components/Header.tsx` equals 0 (label never swaps)
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Header.tsx renders a Guide toggle immediately after Voice Replies, styled identically (icon + text, aria-pressed, >=48px, bordered sky surface, never accent-filled, fixed "Guide" label), reading and writing the Plan 11-02 guide store, present on every view Header mounts on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `GuideOverlay`'s rendered content | 100% static, build-time-authored text (this component's own JSX literals + `voiceCommands.ts`'s exported constants) — no user-supplied, database-supplied, or model-supplied string is ever rendered here |
| Header click → `useGuide` | Purely local UI-visibility toggle, no network, no new attack surface |
| Guide overlay ↔ auth gate | `GuideOverlay`/`Header` only ever mount inside the already-authenticated tree (`App.tsx`'s `token === null` gate returns `LoginGate` before any of `Dashboard`/`UploadView`/`RecordsView` — and therefore before `Header` — ever mounts); the guide introduces no new route, no new URL, and no new auth surface of its own |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Tampering / Cross-Site Scripting | `GuideOverlay.tsx` content, including `voiceCommands.ts`-sourced text | mitigate | Every string rendered is a JSX text-node literal (React auto-escapes text children) — `dangerouslySetInnerHTML` is never used anywhere in this component; `voiceCommands.ts`'s content is itself a static, hardcoded array (Plan 11-03), not user/model input, so there is no dynamic string anywhere in this render path that could carry injected markup. |
| T-11-09 | Elevation of Privilege (session/auth) | New overlay "screen" | accept — not applicable | `GuideOverlay` is not a route: no `react-router`, no URL change, and it deliberately does NOT use `useView`'s hard-swap pattern (D-01) — it is a plain client-side visibility boolean rendered inside the already-authenticated component tree, inheriting the existing Bearer-token gate with zero new exposed surface. |
| T-11-10 | Denial of Service (focus/keyboard trap) | Escape-to-close `window` keydown listener | mitigate | The listener is added only while `open` is `true` and removed on cleanup/close (React `useEffect` cleanup) — it cannot leak an active global listener after the guide closes or the component unmounts. |
| T-11-11 | Elevation of Privilege (accessibility bypass) | No focus trap (D-04) | accept | Deliberate, locked design decision — unlike `LogoutConfirmDialog`, nothing outside `GuideOverlay`'s own tree is marked `inert` or removed from tab order, because `CommandBar` must stay fully operable (keyboard and voice) while the guide is open; this is a requirement (GUIDE-03/D-03), not an oversight. |

No new backend routes, no new dependency (only the already-installed `lucide-react` `BookOpen`/`X`
icons), no destructive action — opening/closing the guide is instantaneous and fully reversible.
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit && npx vitest run src/components/GuideOverlay.test.tsx src/components/CommandBar.test.tsx`
</verification>

<success_criteria>
- `GuideOverlay` renders the complete, text-only, D-11/D-12/D-13-compliant guide when open, and
  nothing when closed
- The "What Can I Say" section is sourced entirely from `voiceCommands.ts` — zero re-authored
  category/example content inside `GuideOverlay.tsx` itself
- No modal/focus-trap semantics anywhere in the component (`role="region"`, no `aria-modal`, no
  `role="dialog"`, nothing outside the guide marked `inert`)
- Header renders a fully functional Guide click toggle, styled identically to the Theme/Voice
  Replies toggles, present on every Header-mounting view
- `cd frontend && npx vitest run src/components/GuideOverlay.test.tsx` is fully green
</success_criteria>

<output>
Create `.planning/phases/11-full-site-guide-instructions-tab/11-04-SUMMARY.md` when done
</output>
