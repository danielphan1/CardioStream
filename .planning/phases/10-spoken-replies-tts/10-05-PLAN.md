---
phase: 10-spoken-replies-tts
plan: 05
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/lib/agent.ts
  - frontend/src/lib/agent.test.ts
autonomous: true
requirements: [TTS-02]

must_haves:
  truths:
    - "The Header shows an icon+text Voice Replies toggle immediately after the Theme toggle, with aria-pressed reflecting useSpeech's current enabled state (D-02, TTS-02)"
    - "The toggle button is >=48px tall, bordered (not accent-filled), matching the Theme toggle's exact contract"
    - "Clicking the toggle flips useSpeech's enabled state and persists it (via the Plan 10-02 store)"
    - "A server-composed AppliedFilters.speechEnabled delta (from a toggle_speech voice/agent command) reaches useSpeech.setEnabled through the single applyAgentFilters fan-out surface, without touching the FilterBar pulse"
  artifacts:
    - path: frontend/src/components/Header.tsx
      provides: "Voice Replies toggle button (Volume2/VolumeX icon + text label, aria-pressed, min-h-12, border-2 border-[var(--color-ink)] bg-[var(--color-sky)])"
    - path: frontend/src/lib/agent.ts
      provides: "applyAgentFilters's speechEnabled != null branch calling useSpeech.getState().setEnabled(...)"
  key_links:
    - from: frontend/src/components/Header.tsx
      to: frontend/src/store/speech.ts
      via: "onClick={toggleSpeech} where toggleSpeech = useSpeech((s) => s.toggleEnabled)"
      pattern: "useSpeech\\(\\(s\\) => s\\.toggleEnabled\\)"
    - from: frontend/src/lib/agent.ts
      to: frontend/src/store/speech.ts
      via: "useSpeech.getState().setEnabled(f.speechEnabled === \"on\")"
      pattern: "setEnabled\\(f\\.speechEnabled"
---

<objective>
Deliver the click half of the mute/quiet toggle (D-02): a new Header-right button, styled and
behaviorally identical to the existing Theme toggle, that reads/writes the `useSpeech` store
built in Plan 10-02. Also close the loop for the voice half (D-01, Plan 10-01's backend
`toggle_speech` schema): extend `lib/agent.ts`'s single `applyAgentFilters` fan-out point with a
`speechEnabled` branch, so a server-composed `AppliedFilters.speechEnabled` delta — whether it
originated from a voice command or (once live) a text command — reaches `useSpeech.setEnabled`
exactly the way every other `AppliedFilters` field already reaches its target store.

Purpose: TTS-02 requires a "prominent, persisted, voice-reachable mute/quiet toggle." The click
path must be fully functional today; the voice path is wired identically to every other voice
command (inert until the Anthropic account has billing/credits, an accepted project-wide
limitation, not a Phase 10 blocker per 10-UI-SPEC.md's Voice scope note).
Output: A working Header click toggle, and `applyAgentFilters` routing `speechEnabled` to the
speech store from either input path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-spoken-replies-tts/10-CONTEXT.md
@.planning/phases/10-spoken-replies-tts/10-UI-SPEC.md
@frontend/src/components/Header.tsx
@frontend/src/lib/agent.ts
@frontend/src/store/speech.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header.tsx — Voice Replies toggle button</name>
  <files>frontend/src/components/Header.tsx</files>
  <read_first>frontend/src/components/Header.tsx (full file — the Theme toggle button at lines 157-170 and its store-read pattern at 113-115 are the direct templates), frontend/src/store/speech.ts (the useSpeech contract this button reads/writes)</read_first>
  <action>
Add `Volume2, VolumeX` to the existing `import { ClipboardPlus, LogOut, Moon, Sailboat, Sun,
Upload } from "lucide-react";` line, appended alphabetically at the end (→ `ClipboardPlus,
LogOut, Moon, Sailboat, Sun, Upload, Volume2, VolumeX`). Add `import { useSpeech } from
"../store/speech";` to the store-import block, positioned alphabetically between `import {
useAuth } from "../store/auth";` and `import { useTheme } from "../store/theme";`.

Inside the `Header()` function, immediately after the existing `const isDark = theme ===
"dark";` line, add: `const speechEnabled = useSpeech((s) => s.enabled);` and `const toggleSpeech
= useSpeech((s) => s.toggleEnabled);`.

Add a new button immediately after the Theme toggle button's closing `</button>` tag and before
the existing "View toggle" comment block, using the exact markup locked in 10-UI-SPEC.md's
"Header.tsx — new Voice Replies toggle" section: `<button type="button" onClick={toggleSpeech}
aria-pressed={speechEnabled} className="flex min-h-12 items-center gap-2 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-sky)] px-4 text-[20px] font-bold
text-[var(--color-ink)]">` containing a conditional `<Volume2 aria-hidden="true" size={24} />` /
`<VolumeX aria-hidden="true" size={24} />` (on when `speechEnabled`, off otherwise) followed by
the conditional text `"Voice Replies: On"` / `"Voice Replies: Off"`. No separate `aria-label` —
the visible text is the accessible name, matching the Theme toggle's own convention. Call the
store action `toggleEnabled` (not `toggle`) — this is the name Plan 10-02's store actually
exports; 10-UI-SPEC.md's illustrative snippet uses a shorthand name that does not match the
locked store contract.
  </action>
  <acceptance_criteria>
    - `grep -c 'Voice Replies: On' frontend/src/components/Header.tsx` equals 1 and `grep -c 'Voice Replies: Off' frontend/src/components/Header.tsx` equals 1
    - `grep -c 'aria-pressed={speechEnabled}' frontend/src/components/Header.tsx` equals 1
    - The new button's line number is greater than the Theme toggle button's closing `</button>` line and less than the "View toggle" comment's line (button placed immediately after Theme, before the view-toggle block)
    - The button's className string is byte-identical to the Theme toggle's className string
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Header.tsx renders a Voice Replies toggle immediately after the Theme toggle, styled identically (icon + text, aria-pressed, >=48px, bordered sky surface, never accent-filled), reading and writing the Plan 10-02 speech store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/agent.ts speechEnabled fan-out branch + lib/agent.test.ts</name>
  <files>frontend/src/lib/agent.ts, frontend/src/lib/agent.test.ts</files>
  <read_first>frontend/src/lib/agent.ts (full file — the `!= null` present-value-delta branch style at lines 71-82, and the import block at 8-13, are the direct edit targets), frontend/src/lib/agent.test.ts (full file — the beforeEach and the "overlayDataset + overlayState" test at lines 130-135 are the direct templates), frontend/src/store/speech.ts (the useSpeech contract this branch calls)</read_first>
  <behavior>
    - `applyAgentFilters({ speechEnabled: "off" })` calls `useSpeech.getState().setEnabled(false)`; `applyAgentFilters({ speechEnabled: "on" })` calls `useSpeech.getState().setEnabled(true)`.
    - This branch does NOT add anything to the `touched`/`PulseField` set — Voice Replies is not one of FilterBar's five highlighted groups (RESEARCH: no PulseField exists for it), so `applyAgentFilters({ speechEnabled: "off" })`'s return value is an empty array when no other field is present in the same call.
    - `applyAgentFilters({})` (no speechEnabled key) never calls `useSpeech.getState().setEnabled` at all.
  </behavior>
  <action>
In lib/agent.ts, add `import { useSpeech } from "../store/speech";` to the import block,
positioned after `import { useFilters } from "../store/filters";`. Inside `applyAgentFilters`,
add a new field-check block after the existing `overlayDataset`/`overlayState` check and before
`const fields = [...touched];`: `if (f.speechEnabled != null) { useSpeech.getState().setEnabled(
f.speechEnabled === "on"); }` — no `touched.add(...)` call, per `&lt;behavior&gt;` above.

In lib/agent.test.ts, add `import { useSpeech } from "../store/speech";`. In the `beforeEach`,
add `useSpeech.setState({ enabled: true, isSpeaking: false, primed: false });` alongside the
existing `useFilters.setState(...)`/`useAgentPulse.setState(...)` resets. Add a new test inside
the `describe("applyAgentFilters", ...)` block: "speechEnabled reaches useSpeech.setEnabled
without touching the pulse (no PulseField for it)" — call `applyAgentFilters({ speechEnabled:
"off" })`, assert `useSpeech.getState().enabled` is `false` and the returned `fields` array is
`[]` (`toEqual([])`).
  </action>
  <acceptance_criteria>
    - `grep -c 'useSpeech.getState().setEnabled(f.speechEnabled' frontend/src/lib/agent.ts` equals 1
    - The new field-check block does not call `touched.add`
    - `cd frontend && npx vitest run src/lib/agent.test.ts` passes with zero failures, including the new speechEnabled test
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/agent.test.ts</automated>
  </verify>
  <done>A server-composed speechEnabled delta reaches useSpeech.setEnabled through the single applyAgentFilters fan-out surface, verified without disturbing the existing FilterBar pulse mechanism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Header click → useSpeech | Purely local UI preference toggle, no network, no new attack surface |
| Server-composed AppliedFilters.speechEnabled → useSpeech | Same trust boundary already assessed in Plan 10-01 (closed Literal enum, structured-outputs-constrained) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | `applyAgentFilters`'s speechEnabled branch | accept | Reuses the identical `!= null` present-value-delta discipline already applied to every other `AppliedFilters` field on this single fan-out surface; the value's only possible origin is an already-validated backend `Literal["on","off"]` enum (Plan 10-01) — no new validation logic needed here. |

No new backend routes, no new dependency, no destructive action (this toggle is instantaneous
and reversible, matching the Theme toggle's own disposition per 10-UI-SPEC.md).
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit && npx vitest run src/lib/agent.test.ts`
</verification>

<success_criteria>
- Header renders a fully functional Voice Replies click toggle, styled identically to the Theme
  toggle (D-02)
- `applyAgentFilters` routes `speechEnabled` to `useSpeech.setEnabled` from either input path,
  without touching the FilterBar pulse
- `cd frontend && npx vitest run src/lib/agent.test.ts` is fully green
</success_criteria>

<output>
Create `.planning/phases/10-spoken-replies-tts/10-05-SUMMARY.md` when done
</output>
