---
phase: 16-trend-clarity-and-chart-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/charts/CategoryBars.tsx
  - frontend/src/components/charts/AmPmComparison.tsx
autonomous: true
requirements: [D-06]

must_haves:
  truths:
    - "On a narrow (<480px) chart container, CategoryBars' longest label ('Hypertensive Crisis — NN readings (NN%)') still renders in full, not clipped or truncated, because the reserved right margin and label font shrink together"
    - "On a narrow (<480px) chart container, AmPmComparison's two side-by-side panels reclaim horizontal space via a smaller gap instead of clipping bar value labels"
    - "Neither fix uses window.innerWidth or a CSS media query — both use the existing useElementWidth hook measuring the chart's actual rendered container width, since viewport width and rendered chart width diverge inside the max-w-[1280px] content column"
  artifacts:
    - path: frontend/src/components/charts/CategoryBars.tsx
      provides: "responsive right margin (300->160) and label font (18->16) below 480px container width"
      contains: "useElementWidth"
    - path: frontend/src/components/charts/AmPmComparison.tsx
      provides: "responsive gap (gap-8->gap-4) below 480px container width"
      contains: "useElementWidth"
  key_links:
    - from: "frontend/src/components/charts/CategoryBars.tsx"
      to: "frontend/src/hooks/useElementWidth.ts"
      via: "const { ref, width: containerWidth } = useElementWidth<HTMLDivElement>()"
      pattern: "useElementWidth"
    - from: "frontend/src/components/charts/AmPmComparison.tsx"
      to: "frontend/src/hooks/useElementWidth.ts"
      via: "const { ref, width: containerWidth } = useElementWidth<HTMLDivElement>()"
      pattern: "useElementWidth"
---

<objective>
D-06 readability pass: fix `CategoryBars.tsx`'s fixed 300px right margin (squeezes bars to unreadable slivers below roughly 480px container width) and `AmPmComparison.tsx`'s fixed 32px inter-panel gap, both via the codebase's established `useElementWidth` responsive-width hook, per `16-UI-SPEC.md`'s Readability Pass section — the two concrete, code-grounded issues found by reading these components directly.

Purpose: These are independent of the rolling-average work (Plan 16-01) and the CombinedTimeline trend-line work (Plan 16-03) — no file overlap, no shared logic — so this plan runs fully in parallel, wave 1.

Output: `CategoryBars.tsx` and `AmPmComparison.tsx` both measure their own live container width and branch their Recharts geometry/Tailwind classes below a 480px threshold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-CONTEXT.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-UI-SPEC.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CategoryBars.tsx responsive right margin and label font</name>
  <files>frontend/src/components/charts/CategoryBars.tsx</files>
  <read_first>frontend/src/components/charts/CategoryBars.tsx, frontend/src/components/charts/CombinedTimeline.tsx, frontend/src/hooks/useElementWidth.ts</read_first>
  <action>
    In `frontend/src/components/charts/CategoryBars.tsx`, add `import { useElementWidth } from "../../hooks/useElementWidth";` to the import block. Inside the `CategoryBars` component body, before the `barLabel` glyph function is defined, add `const { ref, width: containerWidth } = useElementWidth<HTMLDivElement>();` and `const narrow = containerWidth > 0 && containerWidth < 480;` (alias the hook's `width` to `containerWidth` — do NOT rename `barLabel`'s own destructured `width` parameter, which is the bar's pixel width in a separate function scope and is a different value).

    Wrap the existing `<ResponsiveContainer width="100%" height="100%">...</ResponsiveContainer>` return value in a new outer `<div ref={ref} className="h-full w-full">` (mirrors `CombinedTimeline.tsx`'s own outer wrapping div, minus its `onKeyDown` handler which does not apply here).

    In the `<BarChart>`'s `margin` prop, change the literal `right: 300` to `right: narrow ? 160 : 300` (keep `top: 8, bottom: 8, left: 8` unchanged). Inside `barLabel`'s returned `<text>` element, change the literal `fontSize={18}` to `fontSize={narrow ? 16 : 18}`. Do not truncate `row.label` — the full "Stage 1 — 34 readings (26%)" string must still render at the smaller font size, per the No-Off-Screen-Rule spirit (shrink, don't clip).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <human-check>jsdom's ResizeObserver stub (frontend/src/tests/setup.ts) never fires, so containerWidth stays 0 in any automated test — the narrow branch only exercises live. Run `cd frontend && npm run dev`, open the dashboard's BP Categories chart at a ~375px viewport width (browser devtools device toolbar), confirm the full "Hypertensive Crisis — NN readings (NN%)" label renders without clipping and the bars themselves remain visibly wider than a sliver.</human-check>
  </verify>
  <done>CategoryBars.tsx imports and calls useElementWidth, computes narrow from containerWidth < 480, both margin.right and the label fontSize branch on narrow, and the file type-checks with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: AmPmComparison.tsx responsive inter-panel gap</name>
  <files>frontend/src/components/charts/AmPmComparison.tsx</files>
  <read_first>frontend/src/components/charts/AmPmComparison.tsx, frontend/src/hooks/useElementWidth.ts</read_first>
  <action>
    In `frontend/src/components/charts/AmPmComparison.tsx`, add `import { useElementWidth } from "../../hooks/useElementWidth";` to the import block. Inside the `AmPmComparison` component body, before the `return`, add `const { ref, width: containerWidth } = useElementWidth<HTMLDivElement>();` and `const narrow = containerWidth > 0 && containerWidth < 480;`.

    On the existing outer `<div className="flex h-full w-full gap-8">`, add `ref={ref}` and change the className to the template literal `` `flex h-full w-full ${narrow ? "gap-4" : "gap-8"}` ``. Do not add `flex-col` or any vertical-stacking layout — UI-SPEC explicitly rules this out for this phase (it would need a taller container than `ChartDeck.tsx`'s fixed `h-[420px]` wrapper provides, and `ChartDeck.tsx` is outside this phase's file scope).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <human-check>Same ~375px viewport check as Task 1. Confirm the AM/PM comparison's two panels sit with a visibly tighter (16px) gap and neither panel's bar value labels are clipped at their edges. If it still reads as cramped at 375px, do not expand this plan's file scope — note the ChartDeck.tsx fixed-height coupling as a follow-up instead.</human-check>
  </verify>
  <done>AmPmComparison.tsx imports and calls useElementWidth, the outer flex div carries ref and a narrow-branched gap class, and the file type-checks with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Already-authenticated `/stats/summary` and `/readings` responses -> client-side layout | No new input surface; both components already receive fully-validated data pre-phase, this plan only changes how their existing data is laid out at different container widths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-04 | Information Disclosure | CategoryBars.tsx / AmPmComparison.tsx responsive rendering | accept | No new data displayed; purely a layout/geometry change reacting to already-rendered container width |
| T-16-05 | Denial of Service | ResizeObserver callback (via useElementWidth, already-shipped hook) | accept | Same hook already in production use on CombinedTimeline.tsx since Phase 13; no new resource consumption pattern introduced |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — zero type errors
- Manual: `cd frontend && npm run dev`, verify both charts at a ~375px viewport width per each task's human-check
</verification>

<success_criteria>
- CategoryBars' full category labels never clip at narrow container widths
- AmPmComparison's two panels keep bar value labels legible at narrow container widths
- Both fixes reuse the existing useElementWidth hook, no viewport media query introduced
</success_criteria>

<output>
Create `.planning/phases/16-trend-clarity-and-chart-polish/16-02-SUMMARY.md` when done
</output>
