---
phase: 16-trend-clarity-and-chart-polish
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - frontend/src/components/charts/CombinedTimeline.tsx
  - frontend/src/components/charts/CombinedTimeline.test.tsx
autonomous: true
requirements: [D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "When 7 or more readings are plotted, each visible series (systolic, diastolic, pulse — respecting the existing showBP/showPulse toggles) draws a bold 4px trend line in its own existing color token, on top of a dimmed 2px/0.85-opacity raw line (D-03/D-04/D-05)"
    - "When fewer than 7 readings are plotted, no trend line is drawn at all and the raw lines render exactly as they did before this phase — full 3px, opacity 1, own end-label pill — never a misleading partial average"
    - "The pulse trend line keeps its dashed stroke pattern — the dash, not hue, is what separates pulse from systolic under greyscale/colour-vision deficiency"
    - "The end-of-line label pill moves from the raw line to the trend line once a trend line exists for that series, and never renders on both at once"
    - "Clicking any point still opens the tooltip showing that reading's real systolic/diastolic/pulse/notes values — unaffected by trend-line rendering, since both raw and trend lines read off the same underlying points array"
    - "A visible 18px caption states whether trend lines are showing (>=7 points) or explains why not, including the exact reading count when there are fewer than 7"
  artifacts:
    - path: frontend/src/components/charts/CombinedTimeline.tsx
      provides: "trend <Line> elements for systolic/diastolic/pulse, conditionally-dimmed raw lines, caption text, flex-col layout wrap"
      contains: "rollingAverage"
    - path: frontend/src/components/charts/CombinedTimeline.test.tsx
      provides: "trend-line rendering assertions on a new >=7-point fixture, with every pre-existing <7-point assertion left unmodified"
      contains: "trend lines"
  key_links:
    - from: "frontend/src/components/charts/CombinedTimeline.tsx"
      to: "frontend/src/lib/chartData.ts rollingAverage"
      via: 'import { rollingAverage } from "../../lib/chartData"'
      pattern: 'rollingAverage\(points'
    - from: "frontend/src/components/charts/CombinedTimeline.tsx trend <Line>"
      to: "trendPoints merged render data"
      via: 'dataKey="systolicTrend" / "diastolicTrend" / "pulseTrend"'
      pattern: 'Trend"'
---

<objective>
Add bold rolling-average trend lines for systolic, diastolic, and pulse to `CombinedTimeline.tsx` (D-01–D-05), dim the existing raw lines to a supporting role only when a trend line is actually being drawn, move the end-label pill to whichever line is now the series' visual endpoint, and wrap the chart in the caption + flex layout `16-UI-SPEC.md` specifies — then extend `CombinedTimeline.test.tsx` to prove all of it without changing a single pre-existing assertion.

Purpose: This is the phase's payload — "which direction is my health moving" is unanswerable today because ~1,500 raw points on the combined timeline have no visible trend. Plans 16-01 (rollingAverage) and 16-02 (readability, independent) exist to support or parallel this change.

Output: `CombinedTimeline` renders bold trend lines over dimmed raw data whenever 7+ readings are visible, with a working caption, and zero regressions to the existing dual-axis/band/marker/tooltip behavior proven by the file's pre-existing test suite passing unmodified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-CONTEXT.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-UI-SPEC.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-PATTERNS.md
@frontend/src/lib/chartData.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trend lines, conditional dimming, and caption in CombinedTimeline.tsx</name>
  <files>frontend/src/components/charts/CombinedTimeline.tsx</files>
  <read_first>frontend/src/components/charts/CombinedTimeline.tsx, frontend/src/lib/chartData.ts, frontend/src/components/charts/AmPmComparison.tsx</read_first>
  <behavior>
    - `hasTrend = points.length >= 7` gates everything new in this task: the three trend `<Line>` elements, the raw-line dimming, and which line carries the end-label pill. This keeps the pre-Phase-16 `<7`-point behavior byte-for-byte unchanged (verified in Task 2 against the file's existing 4-point fixture).
    - Trend lines read off `dataKey="systolicTrend" | "diastolicTrend" | "pulseTrend"`, values supplied by three `rollingAverage()` calls merged onto a `trendPoints` array that replaces `points` as the `<LineChart data={...}>` source (a strict superset — bands/axes/markers/tooltip are unaffected since they never read the new keys).
    - Raw lines dim (`strokeWidth` 3->2, add `strokeOpacity` 1->0.85) ONLY when `hasTrend` is true — dimming a raw line when nothing bold exists on top of it would contradict the `<7`-point caption's own claim ("Showing individual readings only").
  </behavior>
  <action>
    Add `rollingAverage` to the existing `../../lib/chartData` named-import block (alphabetical position: after `resolveLabelY`, before `toTimePoints`).

    Below the existing `type BandLabelChipProps = {...}` declaration, add a local (unexported) type: `type TrendPoint = TimePoint & { systolicTrend?: number; diastolicTrend?: number; pulseTrend?: number };`. `frontend/tsconfig.app.json` has `verbatimModuleSyntax: true`, so `TimePoint` (a type-only export) must NOT be added to the existing plain value-import list from `../../lib/chartData` — add a new, separate line `import type { TimePoint } from "../../lib/chartData";` directly below that import, matching the established convention already used in `ChartTooltip.tsx`.

    Directly below the existing `const { ref, width } = useElementWidth<HTMLDivElement>();` line inside the component body, add `const hasTrend = points.length >= 7;`. Below the existing `const crowded = isDotCrowded(width, points.length);` line, build the merged render data: `const systolicTrend = rollingAverage(points, "systolic"); const diastolicTrend = rollingAverage(points, "diastolic"); const pulseTrend = rollingAverage(points, "pulse"); const trendPoints: TrendPoint[] = points.map((p, i) => ({ ...p, systolicTrend: systolicTrend[i], diastolicTrend: diastolicTrend[i], pulseTrend: pulseTrend[i] }));`. Change the `<LineChart data={points} ...>` prop to `<LineChart data={trendPoints} ...>`.

    On each of the three existing raw `<Line>` elements (systolic, diastolic, pulse): change `strokeWidth={3}` to `strokeWidth={hasTrend ? 2 : 3}`; add `strokeOpacity={hasTrend ? 0.85 : 1}`; wrap the existing `<LabelList content={makeEndLabel(...)} />` child in `{!hasTrend && (...)}` so the raw line's own pill only renders when there is no trend line to carry it instead. Do not touch `dot`, `activeDot`, `dataKey`, `stroke`, or `strokeDasharray` (pulse) — dimming is width/opacity only, per the D-04/D-05 contract table.

    Immediately after the pulse raw `<Line>` block closes and before the `{/* Markers bind to the VISIBLE axis */}` comment / `overlayEvents?.map` block, insert three new trend `<Line>` elements, each additionally gated on `hasTrend`:
    - `{showBP && hasTrend && (<Line yAxisId={MMHG} dataKey="systolicTrend" stroke="var(--line-systolic)" strokeWidth={4} dot={false} activeDot={false} isAnimationActive={animate}><LabelList content={makeEndLabel(lastIndex, "Systolic", "var(--line-systolic)", endLabelYs)} /></Line>)}`
    - the diastolic equivalent: `dataKey="diastolicTrend"`, `stroke="var(--line-diastolic)"`, label text `"Diastolic"`
    - `{showPulse && hasTrend && (<Line yAxisId={BPM} dataKey="pulseTrend" stroke="var(--line-pulse)" strokeWidth={4} strokeDasharray="9 5" dot={false} activeDot={false} isAnimationActive={animate}><LabelList content={makeEndLabel(lastIndex, "Pulse", "var(--line-pulse)", endLabelYs)} /></Line>)}` — keeps `strokeDasharray="9 5"` unchanged (D-04's locked dash-pattern row).
    None of the three trend lines set `strokeOpacity` (defaults to 1, matching the contract table's "Trend: 1, unchanged, full opacity" row).

    Wrap the caption + layout per the Copywriting Contract: change the outermost `<div ref={ref} className="h-full w-full" onKeyDown={...}>` to `<div ref={ref} className="flex h-full w-full flex-col gap-2" onKeyDown={...}>` (same `ref`, same `onKeyDown` body — only the className changes). Immediately inside, before the existing `<ResponsiveContainer>`, add a caption: `<p className="m-0 shrink-0" style={{ fontSize: 18, color: "var(--color-depth)" }}>{hasTrend ? "Bold lines show a 7-reading rolling average. Lighter lines show each individual reading." : \`Trend line needs at least 7 readings to show — you have ${points.length} here. Showing individual readings only.\`}</p>`. Wrap the existing `<ResponsiveContainer width="100%" height="100%">...</ResponsiveContainer>` block in a new `<div className="min-h-0 flex-1">` — the exact `min-h-0 flex-1` idiom already used twice in `AmPmComparison.tsx` for the identical "fixed-height parent, flexible chart body" problem.

    Add one new bullet to the file's header docstring (after the existing "Elevated and Stage 1 never get chips" bullet) documenting: trend lines only mount when `hasTrend` (`points.length >= 7` — D-02); raw-line dimming is gated on the SAME `hasTrend`, since dimming a raw line only makes sense when a bold line exists to support — never dim with nothing on top; the end-label pill moves from raw to trend line via the same gate and must never render on both.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>File type-checks with zero errors. Trend lines render only when hasTrend is true; raw lines dim only when hasTrend is true; the pulse trend line stays dashed; the caption branches on hasTrend with the exact reading count in the &lt;7 case; the chart is wrapped in the flex-col/min-h-0 layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CombinedTimeline.test.tsx — trend-line coverage, zero regressions</name>
  <files>frontend/src/components/charts/CombinedTimeline.test.tsx</files>
  <read_first>frontend/src/components/charts/CombinedTimeline.test.tsx, frontend/src/components/charts/CombinedTimeline.tsx</read_first>
  <behavior>
    - A new >=7-point fixture renders exactly 6 `.recharts-line-curve` elements when `showBP` and `showPulse` are both on (3 raw + 3 trend).
    - JSX render order determines DOM order: index 0/1/2 are the raw systolic/diastolic/pulse lines, index 3/4/5 are the trend systolic/diastolic/pulse lines (trend block is inserted after all three raw lines in Task 1).
    - Raw lines on the >=7-point fixture carry `stroke-width="2"` and `stroke-opacity="0.85"`; trend lines carry `stroke-width="4"` and no `stroke-opacity` attribute (`null`).
    - The pulse trend line (last of the six) still carries `stroke-dasharray="9 5"`.
    - Exactly 3 end-label pill `rect`s exist (one per series) even with 6 lines present — proves the pill moved, not duplicated.
    - The >=7-point fixture's caption text is "Bold lines show a 7-reading rolling average."; the original 4-point `READINGS` fixture's caption text includes "you have 4 here".
    - Every pre-existing `it`/`describe` block in this file, which all use the original 4-point `READINGS` fixture, is left completely unmodified and continues to pass (hasTrend is false for that fixture, so Task 1's changes are no-ops there).
  </behavior>
  <action>
    Add a new fixture array directly below the existing `READINGS` constant, built with the file's own `reading(id, datetime, systolic, diastolic, pulse)` helper (do not add a second factory), using 8 entries: `reading(5, "2025-01-05T08:00:00", 118, 76, 66)`, `reading(6, "2025-02-05T08:00:00", 122, 78, 70)`, `reading(7, "2025-03-05T08:00:00", 130, 82, 68)`, `reading(8, "2025-04-05T08:00:00", 128, 80, 74)`, `reading(9, "2025-05-05T08:00:00", 135, 84, 71)`, `reading(10, "2025-06-05T08:00:00", 140, 86, 69)`, `reading(11, "2025-07-05T08:00:00", 132, 81, 73)`, `reading(12, "2025-08-05T08:00:00", 138, 85, 70)`. Name it `TREND_READINGS: Reading[]`.

    Add a new `describe("CombinedTimeline trend lines (Phase 16, D-01–D-05)", ...)` block with these `it` cases:
    1. Render `<CombinedTimeline readings={TREND_READINGS} showBP showPulse />`; assert `lines(container)` has length 6.
    2. Same render; assert `lines(container)[0].getAttribute("stroke-width")` is `"2"` and `lines(container)[0].getAttribute("stroke-opacity")` is `"0.85"`; assert `lines(container)[3].getAttribute("stroke-width")` is `"4"` and `lines(container)[3].getAttribute("stroke-opacity")` is `null`.
    3. Same render; assert `lines(container)[5].getAttribute("stroke-dasharray")` is `"9 5"` (the pulse trend line).
    4. Same render; reusing this file's existing pill-rect query pattern (`querySelectorAll("rect")` filtered to fills matching the three series color vars), assert the count is exactly `3` — proves no duplicate pill now that each series has two `<Line>`s.
    5. Same render; assert `container.textContent` contains `"Bold lines show a 7-reading rolling average."`.
    6. Render `<CombinedTimeline readings={READINGS} showBP showPulse />` (the original 4-point fixture, unchanged); assert `container.textContent` contains `"you have 4 here"`.

    Do not modify any existing `it`/`describe` block in this file — every pre-existing assertion (line counts, axis mounting, band rendering, stroke checks, end-label pill checks) must remain byte-for-byte unchanged and continue passing, since they all use the original 4-point `READINGS` fixture where `hasTrend` is false and Task 1's new branches are inert.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/charts/CombinedTimeline.test.tsx</automated>
  </verify>
  <done>All pre-existing tests in this file still pass, unmodified. Six new tests pass, covering the 6-line render, the dim/bold attribute split, the dashed pulse trend line, the no-duplicate-pill invariant, and both caption variants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Already-authenticated `/readings` response -> client-side chart rendering | No new input surface; trend lines are a client-side aggregation of data that already crossed the Phase 5 auth gate and backend Pydantic validation before reaching this component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-06 | Tampering | `trendPoints` merge (spreads `rollingAverage()` output onto existing points) | accept | Purely a client-side render-data transform of already-fetched, already-typed `Reading[]`; no new external input, no `dangerouslySetInnerHTML`, no raw HTML/SVG string injection |
| T-16-07 | Information Disclosure | Trend `<Line>` / caption text | accept | No new data displayed — the trend line is a deterministic function of readings already on screen; the caption states a count already visible in the reading list |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — zero type errors
- `cd frontend && npx vitest run src/components/charts/CombinedTimeline.test.tsx` — full suite (pre-existing + new) passes with zero failures
- `cd frontend && npx vitest run` — full frontend suite passes (confirms no cross-file regression from the chartData.ts import change)
</verification>

<success_criteria>
- CombinedTimeline draws bold rolling-average trend lines for every visible series once 7+ readings exist
- Raw lines dim to a supporting role only when a trend line exists to support
- Tooltip/inspect-a-reading behavior is unchanged
- All pre-existing CombinedTimeline.test.tsx assertions pass without modification
</success_criteria>

<output>
Create `.planning/phases/16-trend-clarity-and-chart-polish/16-03-SUMMARY.md` when done
</output>
