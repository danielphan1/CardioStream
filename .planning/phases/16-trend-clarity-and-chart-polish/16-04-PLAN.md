---
phase: 16-trend-clarity-and-chart-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/chartData.ts
  - frontend/src/lib/chartData.test.ts
  - frontend/src/components/charts/CategoryBars.tsx
autonomous: true
requirements: [D-06]
gap_closure: true

must_haves:
  truths:
    - "On a narrow (<480px) CategoryBars chart container, the longest real D-10 label ('Hypertensive Crisis — NN readings (NN%)') renders in full — margin.right is computed from the actual label set via estimateChipWidth(), not the old static 160px guess, and always covers the widest label's estimated width plus padding"
    - "At the wide (>=480px) breakpoint, margin.right is likewise derived from the label set at the 18px font, not the old static 300px guess"
    - "A chartData.test.ts regression test locks this invariant — it fails if the computed margin ever drops below the longest label's estimated width at either breakpoint"
  artifacts:
    - path: frontend/src/lib/chartData.ts
      provides: "categoryBarRightMargin(rows, fontSize) pure function deriving CategoryBars' BarChart margin.right from the actual rendered label set via the existing estimateChipWidth() helper"
      exports: ["categoryBarRightMargin"]
    - path: frontend/src/lib/chartData.test.ts
      provides: "describe(\"categoryBarRightMargin\", ...) regression block with exact-value assertions at 16px/18px plus an empty-input degenerate case"
      contains: "describe(\"categoryBarRightMargin\""
    - path: frontend/src/components/charts/CategoryBars.tsx
      provides: "margin.right sourced from categoryBarRightMargin(rows, labelFontSize) instead of the static narrow ? 160 : 300 guess"
      contains: "categoryBarRightMargin(rows, labelFontSize)"
  key_links:
    - from: "frontend/src/components/charts/CategoryBars.tsx"
      to: "frontend/src/lib/chartData.ts"
      via: "const rightMargin = categoryBarRightMargin(rows, labelFontSize);"
      pattern: "categoryBarRightMargin\\(rows"
    - from: "frontend/src/lib/chartData.ts categoryBarRightMargin"
      to: "frontend/src/lib/chartData.ts estimateChipWidth"
      via: "Math.max(...rows.map((r) => estimateChipWidth(r.label, fontSize)))"
      pattern: "estimateChipWidth\\(r\\.label"
---

<objective>
Close the one BLOCKER gap `16-VERIFICATION.md` found (score 11/13, independently confirmed by `16-REVIEW.md`'s CR-01): `CategoryBars.tsx`'s narrow-viewport responsive fix from Plan 16-02 reserves a static `margin.right` (160px narrow / 300px wide) that is roughly half of what the actual longest D-10 label ("Hypertensive Crisis — NN readings (NN%)", ~37 chars) needs at either font size (~367-397px narrow / ~413-446px wide) — the label clips invisibly (Recharts SVGs default to `overflow: hidden`), defeating this component's stated sole purpose ("the labels ARE the values").

Purpose: `CategoryBars.tsx`'s primary content must never clip on any real viewport width this app supports (CLAUDE.md accessibility floor + DESIGN.md's No-Off-Screen Rule). The fix must derive the reserved margin from the actual label set, not another static guess, and lock that invariant with a regression test that would have caught this bug.

Output: a new pure function `categoryBarRightMargin()` in `frontend/src/lib/chartData.ts` (reusing the already-exported `estimateChipWidth()`), a `chartData.test.ts` regression block that locks the computed margin to cover the longest real label at both breakpoints, and `CategoryBars.tsx` wired to call it instead of its old static ternary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-CONTEXT.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-VERIFICATION.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-REVIEW.md
@frontend/src/components/charts/CategoryBars.tsx
@frontend/src/lib/chartData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: categoryBarRightMargin() pure function + regression tests</name>
  <files>frontend/src/lib/chartData.ts, frontend/src/lib/chartData.test.ts</files>
  <read_first>frontend/src/lib/chartData.ts (full file — specifically `estimateChipWidth` at lines 160-177, its `CHIP_CHAR_WIDTH_FACTOR` constant, and the `CategoryBarRow` type at lines 32-38, which is what the new function will accept), frontend/src/lib/chartData.test.ts (full file — the `describe("estimateChipWidth", ...)` block at lines 221-233 is the exact-value assertion style to copy, and the import block at lines 5-18), .planning/phases/16-trend-clarity-and-chart-polish/16-REVIEW.md (CR-01, lines 57-123 — the exact numbers this fix must satisfy: 37-char label, 16px/18px fonts, ~367px/~413px needed), .planning/phases/16-trend-clarity-and-chart-polish/16-VERIFICATION.md (frontmatter `gaps:` block — the authoritative "missing" list this task closes item 1 and 2 of)</read_first>
  <action>
    In `frontend/src/lib/chartData.ts`, immediately after the `estimateChipWidth` function's closing brace (currently ending at line 177, before the `END_LABEL_HEIGHT` comment block), add a new module-level constant `const CATEGORY_LABEL_MARGIN_PADDING = 16;` with a one-line comment explaining it covers `CategoryBars.tsx`'s own 8px label-offset (`barLabel`'s `x + width + 8`) plus a small buffer, citing this as the Phase 16 gap-closure fix for CR-01/16-VERIFICATION.md's clipping finding (the static 160px/300px guess covered roughly half of what the longest real D-10 label needs).

    Directly below that constant, add and export a new function `categoryBarRightMargin(rows: CategoryBarRow[], fontSize: number): number`. Body: if `rows.length === 0`, return `0` (mirrors `estimateChipWidth`'s own empty-string-returns-0 degenerate case). Otherwise compute `Math.max(...rows.map((r) => estimateChipWidth(r.label, fontSize)))` and return that value plus `CATEGORY_LABEL_MARGIN_PADDING`. Add a JSDoc block stating this is the `BarChart margin.right` CategoryBars needs to draw its D-10 labels in full without SVG clipping, derived from the actual label set instead of a static guess — reference CR-01/16-VERIFICATION.md by name so a future reader understands why this function exists instead of a literal margin value.

    In `frontend/src/lib/chartData.test.ts`: add `categoryBarRightMargin` to the existing named-import block from `"./chartData"` (alphabetically, between `categoryBarData` and `estimateChipWidth`). Add a new top-level `import type { CategoryBarRow } from "./chartData";` line directly below the existing `import type { Reading, StatsSummary } from "../api/types";` line.

    Add a new `describe("categoryBarRightMargin", ...)` block, placed directly after the existing `describe("estimateChipWidth", ...)` block (after its closing `});` at line 233). Inside, declare a local `const rows: CategoryBarRow[]` array literal with two entries: one `{ category: "Hypertensive Crisis", count: 6, percent: 4.5, label: "Hypertensive Crisis — 6 readings (5%)" }` (the exact 37-char longest-label fixture `16-REVIEW.md`'s CR-01 and the existing `categoryBarData` describe block both use) and one shorter `{ category: "Normal", count: 28, percent: 21.2, label: "Normal — 28 readings (21%)" }` to prove the function picks the max across rows, not just the first. Write four `it` cases: (1) at `fontSize=16`, `categoryBarRightMargin(rows, 16)` equals `383` (estimateChipWidth's 367 + the 16px padding constant) — comment the expected value's derivation; (2) at `fontSize=18`, equals `429` (413 + 16); (3) an invariant check that `categoryBarRightMargin(rows, 16)` is `>=` `Math.max(...rows.map((r) => estimateChipWidth(r.label, 16)))` (so the test still catches a regression even if the exact padding constant is later retuned); (4) `categoryBarRightMargin([], 16)` returns `0` for the degenerate empty-rows case, mirroring the `isDotCrowded`/`estimateChipWidth` empty-input style already established in this file.
  </action>
  <acceptance_criteria>
    - `grep -c "export function categoryBarRightMargin" frontend/src/lib/chartData.ts` returns 1
    - `grep -c "CATEGORY_LABEL_MARGIN_PADDING" frontend/src/lib/chartData.ts` returns 2 (the constant declaration plus its one use inside `categoryBarRightMargin`)
    - `grep -c "describe(\"categoryBarRightMargin\"" frontend/src/lib/chartData.test.ts` returns 1
    - `cd frontend && npx vitest run src/lib/chartData.test.ts` passes, including the 4 new `categoryBarRightMargin` cases (exact-value at 16px, exact-value at 18px, invariant check, empty-degenerate)
    - `cd frontend && npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run src/lib/chartData.test.ts</automated>
  </verify>
  <done>chartData.ts exports categoryBarRightMargin(rows, fontSize), deriving the required margin from the actual label set via estimateChipWidth() with a documented padding constant; chartData.test.ts locks this behavior with exact-value assertions at both CategoryBars breakpoints (16px/18px) plus an invariant check and an empty-input degenerate case, all passing.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CategoryBars.tsx to the derived margin, removing the static 160/300 guess</name>
  <files>frontend/src/components/charts/CategoryBars.tsx</files>
  <read_first>frontend/src/components/charts/CategoryBars.tsx (full file — the current static `margin={{ top: 8, right: narrow ? 160 : 300, bottom: 8, left: 8 }}` at line 85, the `barLabel` glyph's `fontSize={narrow ? 16 : 18}` at line 69, and the stale doc comment at lines 48-52 describing the old static-margin approach, all need updating), frontend/src/lib/chartData.ts (Task 1's new `categoryBarRightMargin` export and its signature), .planning/phases/16-trend-clarity-and-chart-polish/16-REVIEW.md (CR-01's suggested fix, lines 106-117 — the exact wiring shape this task implements)</read_first>
  <action>
    In `frontend/src/components/charts/CategoryBars.tsx`'s import from `"../../lib/chartData"`, add `categoryBarRightMargin` to the existing `{ categoryBarData, prefersReducedMotion }` list (alphabetical: `categoryBarData, categoryBarRightMargin, prefersReducedMotion`).

    In the component body, directly after the existing `const narrow = containerWidth > 0 && containerWidth < 480;` line, add `const labelFontSize = narrow ? 16 : 18;` and `const rightMargin = categoryBarRightMargin(rows, labelFontSize);` (both single-source-of-truth values computed once, replacing the two separate inline `narrow ? 16 : 18` / `narrow ? 160 : 300` ternaries that previously existed at two different call sites).

    Inside the `barLabel` glyph function's returned `<text>` element, change `fontSize={narrow ? 16 : 18}` to `fontSize={labelFontSize}` (reads the component-level variable instead of re-deriving it).

    On the `<BarChart>`'s `margin` prop, change `right: narrow ? 160 : 300` to `right: rightMargin` (keep `top: 8, bottom: 8, left: 8` unchanged).

    Update the stale doc comment at lines 48-52 (currently describing "The right margin below reserves room for the longest label... even on the widest bar; it shrinks alongside the font on narrow containers rather than clipping the label") to instead state the margin is now derived from the actual rendered label set via `categoryBarRightMargin()`/`estimateChipWidth()` rather than a fixed guess — this was Phase 16's gap-closure fix for a real clipping regression (CR-01/16-VERIFICATION.md) where the prior static 160px/300px values covered roughly half of what the longest label needed.
  </action>
  <acceptance_criteria>
    - `grep -c "right: narrow ? 160 : 300" frontend/src/components/charts/CategoryBars.tsx` returns 0 (old static guess fully removed)
    - `grep -c "categoryBarRightMargin(rows, labelFontSize)" frontend/src/components/charts/CategoryBars.tsx` returns 1
    - `grep -c "labelFontSize" frontend/src/components/charts/CategoryBars.tsx` returns 3 (the `const labelFontSize` declaration, its use inside `barLabel`'s `fontSize={labelFontSize}`, and its use in `categoryBarRightMargin(rows, labelFontSize)`)
    - `cd frontend && npx tsc --noEmit` passes with zero errors
    - `cd frontend && npx vitest run` — full suite passes, zero regressions to any pre-existing test (this task touches no test files; `CategoryBars.tsx` has no dedicated component test today per `16-REVIEW.md`'s own finding, and jsdom's `ResizeObserver` stub cannot exercise the `narrow` branch regardless — Task 1's `chartData.test.ts` regression already locks the underlying margin math)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run</automated>
    <human-check>jsdom's ResizeObserver stub never fires, so containerWidth stays 0 in any automated test — the narrow branch and the derived-margin value only exercise live, with real text layout. Run `cd frontend && npm run dev`, open the dashboard's BP Categories chart at a ~320-414px viewport width (browser devtools device toolbar, e.g. iPhone SE at 375px), and confirm the full "Hypertensive Crisis — NN readings (NN%)" label renders without clipping at either edge, and the bars themselves remain visibly wider than a sliver. Repeat once at a wide (>=480px) viewport to confirm the wide-breakpoint margin also doesn't clip.</human-check>
  </verify>
  <done>CategoryBars.tsx's BarChart margin.right is sourced from categoryBarRightMargin(rows, labelFontSize) — a value derived from the actual label set — instead of the old static narrow ? 160 : 300 guess; the label fontSize ternary is deduplicated into the same labelFontSize variable; the file type-checks with zero errors and the full frontend suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This is a narrow gap-closure fix: it replaces one static layout-geometry guess (`margin.right`) with a value computed from data already rendered pre-phase (`rows` from `categoryBarData(stats)`, already-authenticated `/stats/summary` response). No new input surface, no new component boundary, no new package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-08 | Information Disclosure | `categoryBarRightMargin()` / `CategoryBars.tsx` margin computation | accept | Purely client-side layout math over data the authenticated user is already authorized to see and that is already fully rendered elsewhere on the same chart; no new data is displayed, only how much horizontal space its label reserves. |
| T-16-09 | Denial of Service | `categoryBarRightMargin()`'s `Math.max(...rows.map(...))` | accept | `rows` is always exactly the 6 fixed BP-category rows from `/stats/summary` (never user-controlled length or content) — no unbounded growth, same bound as the pre-existing `estimateChipWidth()` calls this codebase already ships. |

No new auth/session/data-flow surface introduced; unaffected by this plan.
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — zero type errors
- `cd frontend && npx vitest run` — full suite passes, including Task 1's 4 new `categoryBarRightMargin` regression cases, zero regressions elsewhere
- `cd frontend && npm run lint` — oxlint clean
- Manual: `cd frontend && npm run dev`, verify the BP Categories chart at both a ~320-414px viewport and a >=480px viewport per Task 2's human-check
</verification>

<success_criteria>
- `CategoryBars.tsx`'s longest real D-10 label never clips at any viewport width this app supports — margin.right is derived from the actual label set, not a static guess
- A chartData.test.ts regression test locks the computed margin to cover the longest label at both the narrow (16px) and wide (18px) breakpoints, and would fail if the margin ever again drops below what the label needs
- Live browser verification at ~320-414px and >=480px both confirm no clipping (jsdom cannot exercise this — human-check required per project convention)
- `16-VERIFICATION.md`'s one failed truth ("CategoryBars' longest label still renders in full... on a narrow container") is now achievable
</success_criteria>

<output>
Create `.planning/phases/16-trend-clarity-and-chart-polish/16-04-SUMMARY.md` when done
</output>
