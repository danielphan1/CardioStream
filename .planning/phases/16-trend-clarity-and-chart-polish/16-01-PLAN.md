---
phase: 16-trend-clarity-and-chart-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/chartData.ts
  - frontend/src/lib/chartData.test.ts
  - frontend/src/tests/contrast.test.ts
autonomous: true
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "rollingAverage() returns undefined for every index before a full 7-reading window exists (D-02) — never a fabricated partial average"
    - "rollingAverage() is a pure, count-based window over the last 7 readings, indifferent to the size of the time gap between them (D-01)"
    - "The same rollingAverage() function correctly averages whichever key (systolic, diastolic, or pulse) it is called with, so Plan 16-03 can reuse it identically for all three series (D-03)"
    - "The raw-line dimming opacity (0.85) Plan 16-03 will apply is proven, by an automated contrast test against literal hex values, to still clear the WCAG 1.4.11 3:1 non-text floor for all three vitals lines, in both themes, against both the deck and mist backgrounds"
  artifacts:
    - path: frontend/src/lib/chartData.ts
      provides: "rollingAverage(points, key, window=7) exported pure function, sibling to toTimePoints/groupAmPm"
      contains: "export function rollingAverage"
    - path: frontend/src/lib/chartData.test.ts
      provides: "describe(\"rollingAverage\", ...) covering partial-window, exact-window, sliding-window, gap-indifference, empty-input, and per-key cases"
      contains: 'describe("rollingAverage"'
    - path: frontend/src/tests/contrast.test.ts
      provides: "12-value dimmed-raw-line contrast regression block (3 series x 2 backgrounds x 2 themes) at 0.85 opacity"
      contains: "DimmedVsDeck"
  key_links:
    - from: "frontend/src/lib/chartData.ts rollingAverage"
      to: "frontend/src/components/charts/CombinedTimeline.tsx (Plan 16-03, wave 2)"
      via: 'import { rollingAverage } from "../../lib/chartData"'
      pattern: "rollingAverage"
---

<objective>
Implement the count-based 7-reading rolling-average window function (D-01/D-02/D-03) that Plan 16-03's trend lines will consume, with full unit-test coverage per CLAUDE.md's Quality constraint (this is a derived medical-adjacent value, same bar as BP category boundaries/MAP). Also lock in a contrast-floor regression guard for the 0.85 raw-line dimming opacity Plan 16-03 will apply (D-04/D-05), so a future opacity change that breaks contrast fails a test instead of shipping.

Purpose: This plan is pure foundation — Plan 16-03 (wave 2) depends on `rollingAverage` existing and exported before it can wire trend lines into `CombinedTimeline.tsx`. The contrast task is independent verification that can run in parallel with Plan 16-02's readability fixes.

Output: `rollingAverage()` exported from `lib/chartData.ts`, fully unit-tested; `contrast.test.ts` extended with a 12-assertion regression block for the dimmed-line opacity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-CONTEXT.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-UI-SPEC.md
@.planning/phases/16-trend-clarity-and-chart-polish/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rollingAverage() pure function + unit tests</name>
  <files>frontend/src/lib/chartData.ts, frontend/src/lib/chartData.test.ts</files>
  <read_first>frontend/src/lib/chartData.ts, frontend/src/lib/chartData.test.ts</read_first>
  <behavior>
    All cases operate on `TimePoint[]` built via the existing `toTimePoints()` (or constructed directly). Use the existing `reading()` factory in `chartData.test.ts` (accepts `Partial<Reading>` overrides).
    - Case A — partial window (D-02): 6 readings with `systolic` values `[100,101,103,104,106,107]` -> `rollingAverage(points, "systolic")` is `[undefined,undefined,undefined,undefined,undefined,undefined]` (length 6, every entry undefined — no full window yet).
    - Case B — exact window, rounded to 1 decimal: 7 readings with `systolic` values `[100,101,103,104,106,107,109]` (sum 730, avg 104.285714...) -> index 6 is `104.3` (via the existing `round1()` helper), indices 0-5 are `undefined`.
    - Case C — sliding window (8th point drops the earliest of the prior 7): append an 8th reading with `systolic: 112` to Case B's 7 -> index 6 stays `104.3` (unchanged), index 7 is the average of `[101,103,104,106,107,109,112]` = `106` exactly (742/7).
    - Case D — count-based, gap-indifferent (D-01): reuse Case B's 7 systolic values but give the readings wildly uneven `datetime` values (e.g. years apart, in any order that still produces ascending `ts` when mapped through `toTimePoints`) -> the result at index 6 is still `104.3`, proving the window is purely count-based and never reads `ts`.
    - Case E — empty input: `rollingAverage([], "systolic")` returns `[]`.
    - Case F — per-key correctness (D-03): 7 readings with `diastolic` values `[60,61,63,64,66,67,69]` (sum 450, avg exactly 64.285714 -> rounds to `64.3`) -> `rollingAverage(points, "diastolic")` index 6 is `64.3`; confirms the function reads the `key` argument, not a hardcoded field.
  </behavior>
  <action>
    In `frontend/src/lib/chartData.ts`, add directly below `groupAmPm` (and above `formatCategoryLabel`) a new exported function with this exact signature: `export function rollingAverage(points: TimePoint[], key: "systolic" | "diastolic" | "pulse", window = 7): (number | undefined)[]`. Implementation: `points.map((_, i) => { if (i < window - 1) return undefined; let sum = 0; for (let j = i - window + 1; j <= i; j++) sum += points[j][key]; return round1(sum / window); })` — reuse the file's existing `round1()` helper (already defined above `groupAmPm`), do not write a second rounding helper. Add a JSDoc comment above it, in this file's established style (see `groupAmPm`'s docstring), explicitly citing D-01 (count-based window, not calendar-day — readings are not daily) and D-02 (no partial-window averaging; `undefined` for the first `window - 1` indices, mirroring `groupAmPm`'s own "no data -> skip/omit, don't fabricate" instinct). No new imports needed — `TimePoint` is already defined in this file.

    In `frontend/src/lib/chartData.test.ts`: add `rollingAverage` to the existing named-import block from `./chartData` (alphabetically, after `resolveLabelY` and before `toTimePoints`). Add a new `describe("rollingAverage", ...)` block implementing the six cases (A-F) from `<behavior>` above as six `it(...)` blocks, each with a name that states which decision/edge-case it proves (mirroring the file's existing `resolveLabelY`/`isDotCrowded` describe blocks, which name the specific boundary condition in each `it` string).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/chartData.test.ts</automated>
  </verify>
  <done>`rollingAverage` is exported from `chartData.ts` with the exact signature above; all six new tests pass; the full `chartData.test.ts` suite (existing + new) passes with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Contrast regression for the 0.85 dimmed-raw-line opacity</name>
  <files>frontend/src/tests/contrast.test.ts</files>
  <read_first>frontend/src/tests/contrast.test.ts</read_first>
  <action>
    In `frontend/src/tests/contrast.test.ts`, add six new keys to the `LIGHT` object and six to the `DARK` object (alpha=0.85 raw line over the 0.15-weighted background, per-channel `round(fg*0.85 + bg*0.15)`, computed already — paste these exact hex literals, do not recompute):

    `LIGHT` additions: `lineSystolicDimmedVsDeck: "#3E5676"`, `lineSystolicDimmedVsMist: "#3C5574"`, `lineDiastolicDimmedVsDeck: "#3F8D81"`, `lineDiastolicDimmedVsMist: "#3C8B7F"`, `linePulseDimmedVsDeck: "#AB6444"`, `linePulseDimmedVsMist: "#A86242"`.

    `DARK` additions: `lineSystolicDimmedVsDeck: "#87A5C3"`, `lineSystolicDimmedVsMist: "#88A7C6"`, `lineDiastolicDimmedVsDeck: "#6DB9AB"`, `lineDiastolicDimmedVsMist: "#6EBAAE"`, `linePulseDimmedVsDeck: "#C28B6E"`, `linePulseDimmedVsMist: "#C38C71"`.

    Directly below the existing `VITALS_LINES` constant, add: `const DIMMED_LINE_PAIRS = [["lineSystolicDimmedVsDeck", "deck"], ["lineDiastolicDimmedVsDeck", "deck"], ["linePulseDimmedVsDeck", "deck"], ["lineSystolicDimmedVsMist", "mist"], ["lineDiastolicDimmedVsMist", "mist"], ["linePulseDimmedVsMist", "mist"]] as const;` (tuple array: `[tokenKey, backgroundKey]`).

    Add two new `describe` blocks, one for light one for dark, following this file's existing `it.each` structure exactly (see `describe("light theme — vitals line contrast floors", ...)` as the direct template): `describe("light theme — dimmed raw vitals line contrast floors (Phase 16, D-04 @ 0.85 opacity)", () => { it.each(DIMMED_LINE_PAIRS)("%s clears the non-text UI floor (3:1, WCAG 1.4.11)", (token, bgKey) => { expect(hex(LIGHT[token], LIGHT[bgKey])).toBeGreaterThanOrEqual(3); }); });` and the same shape for `DARK`. Add a one-line comment above the light block naming Phase 16 and D-04 as the reason this block exists (matching the file's existing Phase-14-regression-guard comment style at the bottom of the file).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/tests/contrast.test.ts</automated>
  </verify>
  <done>12 new assertions pass (3 series x 2 backgrounds x 2 themes), all clearing the 3:1 WCAG 1.4.11 non-text floor; the full `contrast.test.ts` suite (existing + new) passes with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Already-authenticated `/readings` response -> client-side chart math | No new input surface; `rollingAverage()` only ever sees `TimePoint[]` derived from readings that already crossed the Phase 5 auth gate and backend Pydantic validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-16-01 | Tampering | `rollingAverage()` inputs | accept | Window size defaults to a hardcoded 7, never taken from a URL param, form field, or other user-controlled input; the `points` array is data already fetched and rendered pre-phase |
| T-16-02 | Denial of Service | `rollingAverage()` loop | accept | O(n × window) over an already date-range-bounded readings set (dashboard queries are filtered server-side); no unbounded growth introduced by this function |
| T-16-03 | Information Disclosure | Contrast test literals | accept | Test-only file; hex color tokens are already public in `index.css`, not secret |
</threat_model>

<verification>
- `cd frontend && npx vitest run src/lib/chartData.test.ts` — passes, includes 6 new `rollingAverage` tests
- `cd frontend && npx vitest run src/tests/contrast.test.ts` — passes, includes 12 new dimmed-line contrast assertions
- `cd frontend && npx tsc --noEmit` — zero type errors
</verification>

<success_criteria>
- `rollingAverage()` is exported, matches the D-01/D-02/D-03 contract, and is unit-tested for partial-window, full-window, sliding-window, gap-indifference, empty-input, and per-key correctness
- The 0.85 dimming opacity Plan 16-03 will apply is proven to clear the 3:1 WCAG floor for all 3 series, both themes, both backgrounds
</success_criteria>

<output>
Create `.planning/phases/16-trend-clarity-and-chart-polish/16-01-SUMMARY.md` when done
</output>
