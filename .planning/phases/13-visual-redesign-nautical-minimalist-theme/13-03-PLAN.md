---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/components/GuideOverlay.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-06, D-09]

must_haves:
  truths:
    - "Every Header utility control (theme/voice-replies/guide toggles, upload/add-record/log-out buttons) is >=48px tall, including the 4 that previously used the removed 'exempt from 48px' carve-out"
    - "Header and GuideOverlay render with the new deck/mist/depth/brass token names — zero remaining Phase 12 token references"
  artifacts:
    - path: "frontend/src/components/Header.tsx"
      provides: "Site header, LogoutConfirmDialog, wave-curve divider — re-skinned to Phase 13 tokens with the 48px floor applied unconditionally"
      contains: "color-depth"
    - path: "frontend/src/components/GuideOverlay.tsx"
      provides: "Full-site guide overlay — re-skinned to Phase 13 tokens"
      contains: "color-deck"
  key_links:
    - from: "frontend/src/components/Header.tsx"
      to: "frontend/src/index.css"
      via: "var(--color-*) Tailwind arbitrary-value classes"
      pattern: "var\\(--color-(deck|mist|depth|brass|brass-text)\\)"
---

<objective>
Re-skin `Header.tsx` (site header, theme/voice/guide toggles, caregiver view-switch + log-out controls, `LogoutConfirmDialog`, wave-curve divider) and `GuideOverlay.tsx` (full-site guide, its backdrop, sticky Close bar, section jump-nav) to the Phase 13 token system, and close the accessibility-floor gap PATTERNS.md flagged: 4 Header utility buttons that currently rely on the removed "exempt from 48px" carve-out.

Purpose: Header and GuideOverlay are the two dialog/chrome archetypes in the codebase (`LogoutConfirmDialog` = true modal, `GuideOverlay` = non-modal always-mounted region) — both must carry the new palette and the "no sub-floor carve-out for any header/utility control" rule (`13-UI-SPEC.md` Spacing Scale) without losing their existing focus-trap/Escape/backdrop-click behavior.
Output: both files fully re-skinned; zero Phase 12 token names remain in either file; every Header button is >=48px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin Header.tsx and close the 48px gap on 4 utility buttons</name>
  <files>frontend/src/components/Header.tsx</files>
  <read_first>frontend/src/components/Header.tsx (full 301-line file — token map applies to every `var(--color-ink)`/`var(--color-sky)`/`var(--color-foam)`/`var(--color-accent)`/`var(--color-accent-text)` occurrence)</read_first>
  <action>
    Apply the token rename everywhere in this file: `--color-ink`→`--color-depth`, `--color-sky`→`--color-mist`, `--color-foam`→`--color-deck`, `--color-accent`→`--color-brass`, `--color-accent-text`→`--color-brass-text`. `rounded-xl`/`rounded-lg` class names stay unchanged (their pixel values now come from the Plan 13-01 `--radius-xl` theme override automatically).

    Typography rename: `text-h1 font-bold` (site title, line 163) → `text-heading` (drop `font-bold` — the token already carries weight 600; per UI-SPEC's exactly-4-sizes rule the site H1 uses the Heading role, not Display, since Display is reserved exclusively for StatsStrip's numeric values). `text-h2 font-bold` (LogoutConfirmDialog's "Log out?" heading, line 95) → `text-heading`. `text-control font-bold` (LogoutConfirmDialog's Cancel/Log out buttons, lines 107/114) → `text-label`. `text-[20px] font-bold` (theme/voice-replies/guide toggle buttons, lines 178/196/226) → `text-label`.

    Close the 48px gap: the 4 buttons at lines 241, 249, 259, 273 (Upload, Add Record, Back-to-dashboard, Log out) currently read `className="flex items-center gap-2 rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-sky)] px-4 py-2 text-[20px] font-bold text-[var(--color-ink)]"`. Change each to `className="flex min-h-12 items-center gap-2 rounded-lg border-2 border-[var(--color-depth)] bg-[var(--color-mist)] px-4 text-label text-[var(--color-depth)]"` — add `min-h-12`, drop `py-2` (min-h-12 + `flex items-center` already centers content vertically), replace `text-[20px] font-bold` with `text-label`.

    Wave-curve divider (line ~291): `fill="var(--color-sky)"` → `fill="var(--color-mist)"`. Keep it as abstract line art — do not literalize into a boat/anchor glyph (D-03).

    LogoutConfirmDialog body text (`text-lg text-[var(--color-ink)]`, line 99): rename token only, `text-lg` (18px Body) stays as-is.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <done>Header.tsx contains zero Phase-12 token/type-scale names and every one of its 7 header-right controls is >=48px.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-ink\|color-sky\|color-foam\|color-accent\b\|color-accent-text" frontend/src/components/Header.tsx returns 0</criterion>
    <criterion type="source">grep -c "min-h-12" frontend/src/components/Header.tsx is at least 7 (3 pre-existing toggle buttons + 4 newly-floored utility buttons)</criterion>
    <criterion type="source">grep -c "text-control\|text-h1\|text-h2\|text-\[20px\] font-bold" frontend/src/components/Header.tsx returns 0</criterion>
    <criterion type="behavior">cd frontend && npx vitest run 2>&1 | grep -i header (existing Header-adjacent tests, if any, still pass — Header.tsx has no dedicated .test.tsx file, so this is a build/typecheck sanity check via `npx tsc -b --noEmit`)</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin GuideOverlay.tsx</name>
  <files>frontend/src/components/GuideOverlay.tsx</files>
  <read_first>frontend/src/components/GuideOverlay.tsx (full 399-line file — 3 separate `bg-[var(--color-foam)]` occurrences: backdrop div line 187, scrollable region line 194, sticky Close bar line 199)</read_first>
  <action>
    Apply the token rename everywhere: `--color-foam`→`--color-deck` (all 3 backdrop/region/close-bar occurrences), `--color-ink`→`--color-depth`, `--color-sky`→`--color-mist`.

    Typography rename: `h2Class` constant (line 30, `"text-h2 leading-tight font-bold text-[var(--color-ink)]"`) → `"text-heading leading-tight text-[var(--color-depth)]"` (drop `font-bold`). `bodyClass` constant (line 31) → rename token only, keep `text-lg`. Close button (line 204, `text-control font-bold`) → `text-label`. "Site Guide" `<h1>` (line 239, `text-h1 font-bold leading-tight`) → `text-heading leading-tight` (Heading role, not Display — Display is StatsStrip-exclusive). "Jump to a section" label (line 244, `text-control font-bold`) → `text-label`. Section jump-nav chip links (line 252, `text-control font-bold`) → `text-label`. "What Can I Say" category headings (line 375, `text-control font-bold`) → `text-label`. Example-quote paragraph (line 378, `text-lg font-bold`) → `text-lg font-semibold` (standalone `font-bold` not co-located with a named type-scale token — Two-Weight Rule caps weight at 600, so it becomes `font-semibold`, keeping the 18px size).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/GuideOverlay.test.tsx</automated>
  </verify>
  <done>GuideOverlay.tsx contains zero Phase-12 token/type-scale names and zero 700-weight classes; existing tests still pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-foam\|color-ink\|color-sky" frontend/src/components/GuideOverlay.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-control\|text-h1\|text-h2" frontend/src/components/GuideOverlay.tsx returns 0</criterion>
    <criterion type="source">grep -c "font-bold" frontend/src/components/GuideOverlay.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/GuideOverlay.test.tsx exits 0 (existing focus-management/Escape/backdrop-click behavior unchanged by a pure token/class rename)</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — pure presentational/CSS-class and typography-class changes to two already-existing, already-accessibility-verified components. No new inputs, endpoints, or data handling.

## STRIDE Threat Register

No new attack surface. Header's LogoutConfirmDialog and GuideOverlay's focus/Escape/backdrop-click handling are unchanged behaviorally — only className token strings and a font-weight utility change. No entries required.
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/GuideOverlay.test.tsx` passes; manual spot-check that all 7 Header-right controls (theme, voice replies, guide, upload, add record, back-to-dashboard/log out) are visibly >=48px tall in the browser.
</verification>

<success_criteria>
Header.tsx and GuideOverlay.tsx contain zero Phase-12 token names and zero old type-scale token names; every Header interactive control is >=48px with no remaining "exempt" language or missing `min-h-12`.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-03-SUMMARY.md` when done
</output>
