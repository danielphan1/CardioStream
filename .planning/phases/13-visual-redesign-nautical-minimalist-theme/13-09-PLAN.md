---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 09
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/components/AddRecordPage.tsx
  - frontend/src/components/records/LabFields.tsx
  - frontend/src/components/records/IncidentFields.tsx
  - frontend/src/components/records/ProcedureFields.tsx
  - frontend/src/components/records/SingleDateField.tsx
autonomous: true
requirements: [D-01, D-06, D-09]

must_haves:
  truths:
    - "The Lab/Incident/Procedure type-switcher, submit-button disabled/enabled states, and the FadeSwap mount-fade transition all behave identically — only token names and typography classes change"
    - "All four records/*Fields.tsx files keep their independently-declared (not shared-import) inputClass/labelClass constants, now pointing at the new tokens"
  artifacts:
    - path: "frontend/src/components/AddRecordPage.tsx"
      provides: "Add-a-record container re-skinned to Phase 13 tokens"
      contains: "color-brass"
    - path: "frontend/src/components/records/LabFields.tsx"
      provides: "Lab result field-set re-skinned to Phase 13 tokens"
      contains: "color-depth"
  key_links:
    - from: "frontend/src/components/records/LabFields.tsx"
      to: "frontend/src/components/records/SingleDateField.tsx"
      via: "shared inputClass/labelClass value convention (independently declared per file, not imported)"
      pattern: "color-depth.*color-deck"
---

<objective>
Re-skin `AddRecordPage.tsx` and all four `records/*.tsx` field-set files to the Phase 13 token system. The four field-set files (`LabFields.tsx`, `IncidentFields.tsx`, `ProcedureFields.tsx`, `SingleDateField.tsx`) independently duplicate the exact same `inputClass`/`labelClass` two-constant pattern (per `13-PATTERNS.md`, no shared constants file exists) — the same textual edit applies to each.

Purpose: these five files form the "Add a Record" surface's complete implementation; none of the field-validation logic (numeric-field guards, date/time combining, canSubmit gating) changes — only className token strings and typography classes.
Output: all five files re-skinned; zero Phase-12 token references remain in any of them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin the four records/*.tsx field-set files (identical pattern, applied per file)</name>
  <files>frontend/src/components/records/LabFields.tsx, frontend/src/components/records/IncidentFields.tsx, frontend/src/components/records/ProcedureFields.tsx, frontend/src/components/records/SingleDateField.tsx</files>
  <read_first>
    frontend/src/components/records/LabFields.tsx (full 151-line file — `inputClass`/`labelClass` lines 21-23, `<h3>` heading line 71)
    frontend/src/components/records/IncidentFields.tsx (full 120-line file — same constants lines 26-28, `<h3>` line 66)
    frontend/src/components/records/ProcedureFields.tsx (full 106-line file — same constants lines 18-20, `<h3>` line 50)
    frontend/src/components/records/SingleDateField.tsx (full 61-line file — `rdpSizing` lines 20-27, `inputClass` line 29-30, `<label>` line 37)
  </read_first>
  <action>
    In LabFields.tsx, IncidentFields.tsx, and ProcedureFields.tsx, apply this identical edit to each file's own copy of the constants: `inputClass` → `"min-h-12 rounded-xl border-2 border-[var(--color-depth)] bg-[var(--color-deck)] px-3 text-[18px] text-[var(--color-depth)]"`; `labelClass` → `"flex flex-col gap-1 text-label text-[var(--color-depth)]"` (drop `font-bold` — `text-label` carries weight 600). Each file's own `<h3>` icon-heading (`text-[20px] font-bold text-[var(--color-ink)]`) → `text-label text-[var(--color-depth)]` (consolidate the arbitrary 20px size into the Label role, per the exactly-4-sizes rule).

    In SingleDateField.tsx: `rdpSizing`'s `"--rdp-accent-color": "var(--color-accent)"` → `"var(--color-brass)"`; `"--rdp-accent-background-color": "var(--color-sky)"` → `"var(--color-mist)"`. `inputClass` (line 29-30) → same replacement as above. `<label>` className (line 37) → `"flex flex-col gap-1 text-label text-[var(--color-depth)]"` (drop `font-bold`). DayPicker wrapper div (line 50) → `text-[var(--color-depth)]`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/records/LabFields.test.tsx src/components/records/IncidentFields.test.tsx src/components/records/ProcedureFields.test.tsx</automated>
  </verify>
  <done>All four records/*.tsx files contain zero Phase-12 token/type-scale names; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-ink\|color-foam\|color-sky\|color-accent\b" frontend/src/components/records/LabFields.tsx frontend/src/components/records/IncidentFields.tsx frontend/src/components/records/ProcedureFields.tsx frontend/src/components/records/SingleDateField.tsx returns 0 for each file</criterion>
    <criterion type="source">grep -c "text-\[20px\] font-bold" frontend/src/components/records/LabFields.tsx frontend/src/components/records/IncidentFields.tsx frontend/src/components/records/ProcedureFields.tsx returns 0 for each file</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/records/LabFields.test.tsx src/components/records/IncidentFields.test.tsx src/components/records/ProcedureFields.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin AddRecordPage.tsx</name>
  <files>frontend/src/components/AddRecordPage.tsx</files>
  <read_first>frontend/src/components/AddRecordPage.tsx (full 265-line file — `inactiveClass`/`activeClass` lines 55-58, page wrapper line 177, submit button lines 220-229, result panels lines 234-260)</read_first>
  <action>
    `inactiveClass` (line 56): `"min-h-12 rounded-xl px-4 text-control bg-[var(--color-sky)] text-[var(--color-ink)] border-2 border-[var(--color-ink)]"` → `"min-h-12 rounded-xl px-4 text-label bg-[var(--color-mist)] text-[var(--color-depth)] border-2 border-[var(--color-depth)]"`. `activeClass` (line 58) → `"min-h-12 rounded-xl px-4 text-label bg-[var(--color-brass)] text-[var(--color-brass-text)] border-2 border-[var(--color-brass)]"`. Page wrapper (line 177): `bg-[var(--color-foam)]` → `bg-[var(--color-deck)]`. Heading (line 179): `text-h2 leading-tight text-[var(--color-ink)]` → `text-heading leading-tight text-[var(--color-depth)]`. Body paragraph (line 182): rename token only. Submit button (lines 227-228): enabled branch `bg-[var(--color-accent)] ... text-[var(--color-accent-text)]` → `bg-[var(--color-brass)] ... text-[var(--color-brass-text)]`; disabled branch `border-[var(--color-ink)] bg-[var(--color-sky)] ... text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-mist)] ... text-[var(--color-depth)]`; both `text-control` → `text-label`. Success panel (line 238): `border-[var(--color-ink)] bg-[var(--color-sky)] text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-mist)] text-[var(--color-depth)]`; nested paragraph (line 241) rename token only. Error panel (line 251): same border/bg/text rename as success panel.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/AddRecordPage.test.tsx</automated>
  </verify>
  <done>AddRecordPage.tsx contains zero Phase-12 token/type-scale names; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-foam\|color-ink\|color-sky\|color-accent\b\|color-accent-text" frontend/src/components/AddRecordPage.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-h2\|text-control" frontend/src/components/AddRecordPage.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/AddRecordPage.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caregiver form input → backend `POST` | `LabFields`/`IncidentFields`/`ProcedureFields` collect free-text and numeric input, validated client-side (numeric-field guard, date/time format) before `AddRecordPage.tsx` submits via `useCreateLab`/`useCreateIncident`/`useCreateProcedure` — unchanged by this plan |

## STRIDE Threat Register

No new attack surface — pure className token changes. The existing numeric-field validation guard (Pitfall 3: non-empty-but-non-numeric blocks submit, never silently coerces), the async-race guard (CR-01, submittedType snapshot), and the duplicate-submit guard are all untouched. No entries required.
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/AddRecordPage.test.tsx src/components/records/LabFields.test.tsx src/components/records/IncidentFields.test.tsx src/components/records/ProcedureFields.test.tsx` passes. Manually confirm the Lab/Incident/Procedure type-switcher's FadeSwap transition and the submit button's dashed-border disabled state both render correctly under the new palette.
</verification>

<success_criteria>
All five files contain zero Phase-12 token names and zero old type-scale token names; form validation and submit-state behavior are unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-09-SUMMARY.md` when done
</output>
