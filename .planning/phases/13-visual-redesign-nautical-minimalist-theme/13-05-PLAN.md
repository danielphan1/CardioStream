---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 05
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/components/FilterBar.tsx
  - frontend/src/components/OverlayToggle.tsx
  - frontend/src/components/DateRangePicker.tsx
autonomous: true
requirements: [D-01, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "Filter, overlay, and custom-date-range controls keep their aria-pressed / word-plus-icon state signaling exactly as before — only token names and weight/size classes change"
    - "The agent-driven pulse-ring visual (motion-safe/motion-reduce dual path) still fires on filter/overlay changes, now ringed in Brass instead of the old accent color"
  artifacts:
    - path: "frontend/src/components/FilterBar.tsx"
      provides: "Date/AM-PM/category filter controls re-skinned to Phase 13 tokens"
      contains: "color-brass"
    - path: "frontend/src/components/OverlayToggle.tsx"
      provides: "Overlay dataset toggle row re-skinned to Phase 13 tokens"
      contains: "color-mist"
    - path: "frontend/src/components/DateRangePicker.tsx"
      provides: "Custom date-range picker (react-day-picker + typed entry) re-skinned to Phase 13 tokens"
      contains: "color-brass"
  key_links:
    - from: "frontend/src/components/FilterBar.tsx"
      to: "frontend/src/components/OverlayToggle.tsx"
      via: "shared inactiveClass/activeClass/pulseClass pattern, kept in sync by convention (not a shared import)"
      pattern: "min-h-12 rounded-xl px-4 text-label"
---

<objective>
Re-skin the three filter/toggle control surfaces that share the same `inactiveClass`/`activeClass`/`aria-pressed` pattern (per `13-PATTERNS.md`, `FilterBar.tsx` and `OverlayToggle.tsx` explicitly cross-reference each other's styling) plus `DateRangePicker.tsx`, the custom-range disclosure `FilterBar` mounts.

Purpose: these three files are the app's primary manual-control surface (mirrored 1:1 by the voice-command vocabulary) — the `aria-pressed` + word/icon state pattern (D-07) and the `useAgentPulse` motion-safe pulse ring must survive the re-skin exactly, with only token names, radius (auto via Plan 13-01's `--radius-xl` override), and typography classes changing.
Output: all three files re-skinned; zero Phase-12 token or old type-scale references remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin FilterBar.tsx and OverlayToggle.tsx</name>
  <files>frontend/src/components/FilterBar.tsx, frontend/src/components/OverlayToggle.tsx</files>
  <read_first>
    frontend/src/components/FilterBar.tsx (full 205-line file — `inactiveClass`/`activeClass` constants lines 25-28, pulse class line 77, category chip inline style lines 169-177)
    frontend/src/components/OverlayToggle.tsx (full 109-line file — `inactiveClass`/`activeClass` constants lines 27-30, pulse class line 49)
  </read_first>
  <action>
    FilterBar.tsx: `inactiveClass` (line 26) → `"min-h-12 rounded-xl px-4 text-label bg-[var(--color-mist)] text-[var(--color-depth)] border-2 border-[var(--color-depth)]"`. `activeClass` (line 28) → `"min-h-12 rounded-xl px-4 text-label bg-[var(--color-brass)] text-[var(--color-brass-text)] border-2 border-[var(--color-brass)]"`. `pulseClass` (line 77): `ring-[var(--color-accent)]` → `ring-[var(--color-brass)]`. Category-chip active ring (line 175): `boxShadow: isActive ? "0 0 0 3px var(--color-ink)" : undefined` → `"0 0 0 3px var(--color-depth)"`. Filter-state sentence paragraph (line 199): `text-[var(--color-ink)]` → `text-[var(--color-depth)]` (size stays `text-[18px]`, already Body).

    OverlayToggle.tsx: `inactiveClass` (line 28) → `"min-h-12 flex items-center gap-2 rounded-xl px-4 text-label bg-[var(--color-mist)] text-[var(--color-depth)] border-2 border-[var(--color-depth)] shadow-[var(--shadow-elevation)]"`. `activeClass` (line 30) — no color tokens in this constant (the active fill is per-dataset inline style), leave `text-control`→`text-label` rename only: `"min-h-12 flex items-center gap-2 rounded-xl px-4 text-label border-2 shadow-[var(--shadow-elevation)]"`. Pulse ring (line 49): `ring-[var(--color-accent)]` → `ring-[var(--color-brass)]`. "Overlay:" label (line 60): `text-control font-bold text-[var(--color-ink)]` → `text-label text-[var(--color-depth)]`. Both `NOTE_COPY`/sentence paragraphs (lines 98, 104): `text-[var(--color-ink)]` → `text-[var(--color-depth)]`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/OverlayToggle.test.tsx</automated>
  </verify>
  <done>FilterBar.tsx and OverlayToggle.tsx contain zero Phase-12 token/type-scale names; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-ink\|color-sky\|color-accent\b" frontend/src/components/FilterBar.tsx returns 0</criterion>
    <criterion type="source">grep -c "color-ink\|color-sky\|color-accent\b" frontend/src/components/OverlayToggle.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-control" frontend/src/components/FilterBar.tsx frontend/src/components/OverlayToggle.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/OverlayToggle.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin DateRangePicker.tsx</name>
  <files>frontend/src/components/DateRangePicker.tsx</files>
  <read_first>frontend/src/components/DateRangePicker.tsx (full 144-line file — `rdpSizing` CSS-var object lines 21-28, `inputClass` line 77, Apply button lines 129-141)</read_first>
  <action>
    `rdpSizing` object (lines 26-27): `"--rdp-accent-color": "var(--color-accent)"` → `"var(--color-brass)"`; `"--rdp-accent-background-color": "var(--color-sky)"` → `"var(--color-mist)"`. `inputClass` (line 77): `border-[var(--color-ink)] bg-[var(--color-foam)] ... text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-deck)] ... text-[var(--color-depth)]`. Both `<label>` classes (lines 84, 101): `text-control font-bold text-[var(--color-ink)]` → `text-label text-[var(--color-depth)]`. DayPicker wrapper (line 120): `text-[var(--color-ink)]` → `text-[var(--color-depth)]`. Apply button (lines 133-137): active branch `bg-[var(--color-accent)] ... text-[var(--color-accent-text)]` → `bg-[var(--color-brass)] ... text-[var(--color-brass-text)]`, disabled branch `border-[var(--color-ink)] bg-[var(--color-sky)] ... text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-mist)] ... text-[var(--color-depth)]`; both branches' `text-control font-bold` → `text-label`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/DateRangePicker.test.tsx</automated>
  </verify>
  <done>DateRangePicker.tsx contains zero Phase-12 token/type-scale names; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-ink\|color-sky\|color-foam\|color-accent\b\|color-accent-text" frontend/src/components/DateRangePicker.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-control" frontend/src/components/DateRangePicker.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/DateRangePicker.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → filter state | `DateRangePicker`'s typed `YYYY-MM-DD` text fields are user input, validated via `isValidDateText`/`parseDateOnly` before reaching the store — unchanged by this plan |

## STRIDE Threat Register

No new attack surface this plan — pure className/style token changes. `DateRangePicker`'s existing date-text validation path (`isValidDateText`, never the raw `Date` constructor on a date-only string) is untouched. No entries required.
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/OverlayToggle.test.tsx src/components/DateRangePicker.test.tsx` passes. Manually confirm `aria-pressed` active/inactive states are still visually distinct (Brass fill vs Mist card) at every control.
</verification>

<success_criteria>
FilterBar.tsx, OverlayToggle.tsx, and DateRangePicker.tsx contain zero Phase-12 token names and zero old type-scale token names; all `aria-pressed` and pulse-ring behavior is visually unchanged apart from the new palette.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-05-SUMMARY.md` when done
</output>
