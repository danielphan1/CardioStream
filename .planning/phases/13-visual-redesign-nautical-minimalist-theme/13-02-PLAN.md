---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
autonomous: false
requirements: [D-01, D-09]

must_haves:
  truths:
    - "The app loads Inter (400+600) and Space Grotesk (600) as self-hosted fonts, matching the existing @fontsource pattern — no external font CDN request"
  artifacts:
    - path: "frontend/package.json"
      provides: "@fontsource/inter and @fontsource/space-grotesk as new dependencies"
      contains: "@fontsource/inter"
    - path: "frontend/src/main.tsx"
      provides: "Font CSS imports for Inter 400/600 and Space Grotesk 600"
      contains: "@fontsource/inter/400.css"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/index.css --font-sans/--font-display"
      via: "self-hosted @fontsource CSS imports providing the font-family the CSS custom properties name"
      pattern: "@fontsource/(inter|space-grotesk)"
---

<objective>
Add the two new self-hosted font packages (Inter, Space Grotesk) this phase's typography contract requires, behind a package-legitimacy checkpoint since no RESEARCH.md package audit exists this cycle (`13-CONTEXT.md`: research was skipped for Phase 13).

Purpose: `frontend/src/index.css` (Plan 13-01) already declares `--font-sans: "Inter", ...` and `--font-display: "Space Grotesk", ...` — those custom properties are inert until the actual font files are installed and imported, exactly mirroring how `@fontsource/atkinson-hyperlegible` backs the current system.
Output: two new self-hosted font dependencies installed and imported in `main.tsx`, verified safe via `npmjs.com` before install.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @fontsource/inter and @fontsource/space-grotesk package legitimacy</name>
  <action>Nothing is built yet — this is a pre-install package-legitimacy gate. `13-RESEARCH.md` does not exist this cycle (research was explicitly skipped; `13-CONTEXT.md`/`13-UI-SPEC.md` carry the locked design decisions instead), so there is no `## Package Legitimacy Audit` table to satisfy the install gate automatically. Per the planner's fallback policy, `@fontsource/inter` and `@fontsource/space-grotesk` are treated as `[ASSUMED]` and require human confirmation before Task 2 installs them. Confirm both on npmjs.com per the steps below, then respond with the resume-signal.</action>
  <what-built>Nothing yet — this is a pre-install package-legitimacy gate. `13-RESEARCH.md` does not exist this cycle (research was explicitly skipped; `13-CONTEXT.md`/`13-UI-SPEC.md` carry the locked design decisions instead), so there is no `## Package Legitimacy Audit` table to satisfy the install gate automatically. Per the planner's fallback policy, `@fontsource/inter` and `@fontsource/space-grotesk` are treated as `[ASSUMED]` and require human confirmation before Task 2 installs them.</what-built>
  <how-to-verify>
    Visit https://www.npmjs.com/package/@fontsource/inter and https://www.npmjs.com/package/@fontsource/space-grotesk. Confirm both are:
    1. Published under the `@fontsource` npm scope/org (same publisher already trusted in this repo via the existing `@fontsource/atkinson-hyperlegible` dependency in `frontend/package.json`).
    2. Actively maintained (recent publish history, non-trivial weekly download counts consistent with a widely-used self-hosted-font package family).
    3. Free of any postinstall-script or unusual-permission red flags on the npm package page.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install in Task 2, or describe any concern found on the npm package pages.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install and import Inter + Space Grotesk fonts</name>
  <files>frontend/package.json, frontend/src/main.tsx</files>
  <read_first>
    frontend/package.json (current dependencies block — alphabetical ordering, caret-range pinning style e.g. `"@fontsource/atkinson-hyperlegible": "^5.2.8"`)
    frontend/src/main.tsx (current font import lines 5-6, immediately before `import './index.css'`)
  </read_first>
  <action>
    Only proceed after Task 1's "approved" resume-signal.

    Run `cd frontend && npm install @fontsource/inter@latest @fontsource/space-grotesk@latest`. This adds both packages to `dependencies` in `package.json` with caret-range pins matching the existing `^5.2.8`-style formatting already used for `@fontsource/atkinson-hyperlegible` — insert them alphabetically (`@fontsource/atkinson-hyperlegible`, `@fontsource/inter`, `@fontsource/space-grotesk`, then the existing `@tanstack/react-query` entry).

    In `frontend/src/main.tsx`, add three new font CSS imports immediately after the existing two Atkinson Hyperlegible imports (do NOT remove the Atkinson imports — `13-UI-SPEC.md` explicitly defers that removal to a later cleanup task, not this same commit, to avoid breaking anything mid-migration):
    `import '@fontsource/inter/400.css'`
    `import '@fontsource/inter/600.css'`
    `import '@fontsource/space-grotesk/600.css'`
    Update the comment above the font-import block from "Self-hosted font — no CDN requests (SEC-03 discipline)" to note both font families are self-hosted the same way.
  </action>
  <verify>
    <automated>cd frontend && npm ls @fontsource/inter @fontsource/space-grotesk && npm run build</automated>
  </verify>
  <done>Both packages resolve as installed, both are imported in main.tsx alongside the untouched Atkinson imports, and `npm run build` succeeds.</done>
  <acceptance_criteria>
    <criterion type="behavior">cd frontend && npm ls @fontsource/inter @fontsource/space-grotesk exits 0 (both resolve as installed)</criterion>
    <criterion type="source">grep -c "@fontsource/inter/400.css\|@fontsource/inter/600.css\|@fontsource/space-grotesk/600.css" frontend/src/main.tsx returns 3</criterion>
    <criterion type="source">grep -c "@fontsource/atkinson-hyperlegible" frontend/src/main.tsx returns 2 (unchanged — not removed this phase)</criterion>
    <criterion type="behavior">cd frontend && npm run build exits 0 (Vite resolves the new font CSS imports without error)</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build artifact | Two new third-party packages enter the frontend's dependency tree and, transitively, the shipped bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | `npm install @fontsource/inter`, `npm install @fontsource/space-grotesk` | mitigate | Blocking human checkpoint (Task 1) verifying both packages on npmjs.com before install, per the package-legitimacy fallback policy (no RESEARCH.md audit table exists this cycle) |
</threat_model>

<verification>
`cd frontend && npm run build` succeeds; the dev server (`npm run dev`) renders text in a visibly different typeface than the current Atkinson Hyperlegible once `13-01`'s `--font-sans` change is live (spot-check only — full visual confirmation happens at the phase-level checkpoint, plan 13-12).
</verification>

<success_criteria>
`@fontsource/inter` and `@fontsource/space-grotesk` are installed dependencies, imported in `main.tsx` alongside the still-present Atkinson Hyperlegible imports, and the build succeeds — with the install gated behind an explicit human legitimacy check since no automated package audit exists this cycle.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-02-SUMMARY.md` when done
</output>
