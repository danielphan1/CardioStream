---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 07
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/components/charts/BPTimeline.tsx
  - frontend/src/components/charts/ChartTooltip.tsx
  - frontend/src/components/charts/CategoryBars.tsx
  - frontend/src/components/charts/AmPmComparison.tsx
  - frontend/src/components/ChartDeck.tsx
  - frontend/src/lib/chartData.ts
autonomous: true
requirements: [D-01, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "BP Timeline's decorative AHA band tints and label chips still fully occlude any plotted line segment behind them under the new palette (BPTimeline.tsx's chart-band exemption precedent, ~line 46)"
    - "Every SVG-rendered text glyph across these files uses weight 600, not 700 (Two-Weight Rule) — chip labels, bar labels, tooltip text, AM/PM value labels"
    - "ChartDeck's hero/mini chart-picker cards keep their >=48px click target and keyed fade-swap rotation unchanged"
  artifacts:
    - path: "frontend/src/components/charts/BPTimeline.tsx"
      provides: "BP Timeline chart with the Two-Weight Rule applied to its two SVG label-glyph helpers"
      contains: "fontWeight={600}"
    - path: "frontend/src/components/charts/ChartTooltip.tsx"
      provides: "Click-persistent hero tooltip re-skinned to Phase 13 tokens with weight 600"
      contains: "color-mist"
    - path: "frontend/src/components/charts/CategoryBars.tsx"
      provides: "BP Categories bar chart re-skinned (bar-end label fill only)"
      contains: "color-depth"
    - path: "frontend/src/components/charts/AmPmComparison.tsx"
      provides: "AM vs PM grouped-bar chart re-skinned (heading text + bar value labels)"
      contains: "color-depth"
    - path: "frontend/src/components/ChartDeck.tsx"
      provides: "Hero/mini chart-picker deck re-skinned to Phase 13 tokens"
      contains: "color-mist"
  key_links:
    - from: "frontend/src/components/charts/BPTimeline.tsx"
      to: "frontend/src/index.css .chart-band rule"
      via: "className=\"chart-band\" resolving var(--band-opacity), unchanged mechanism"
      pattern: "className=\"chart-band\""
---

<objective>
Apply the Two-Weight Rule (400/600 only, no 700) and the remaining Phase-12 `var(--color-ink)`/`var(--color-sky)`/`var(--color-accent)`/`var(--color-accent-text)` references across the chart-region files that need any change this phase — `BPTimeline.tsx` needs only its two inline SVG `fontWeight={700}` literals corrected (it uses no renamed color tokens); `ChartTooltip.tsx`, `CategoryBars.tsx`, `AmPmComparison.tsx`, and `ChartDeck.tsx` need both the token rename and the weight fix. `frontend/src/lib/chartData.ts` gets one doc-comment-only edit (font-name reference).

Purpose: `PulseTrend.tsx` needs zero changes this phase (confirmed: it references no `--color-*` tokens and sets no explicit `fontWeight`) — do not touch it, and do not re-derive any `--cat-*`/`--line-*`/`--ref-*`/`--overlay-*` hex value inline anywhere in these files; they already resolve to the new Phase 13 hex via Plan 13-01 with zero code change.
Output: all six listed files updated; `PulseTrend.tsx` verified unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix BPTimeline.tsx's two SVG font-weight literals; update chartData.ts's font-name doc comment; verify PulseTrend.tsx needs no changes</name>
  <files>frontend/src/components/charts/BPTimeline.tsx, frontend/src/lib/chartData.ts</files>
  <read_first>frontend/src/components/charts/BPTimeline.tsx (full 352-line file — `makeBandLabelChip`'s `<text fontWeight={700}>` ~line 119, `makeEndLabel`'s `<text fontWeight={700}>` ~line 149)</read_first>
  <action>
    In `makeBandLabelChip` (band-label chip glyph, ~line 119): change `fontWeight={700}` to `fontWeight={600}`. In `makeEndLabel` (line-end "Systolic"/"Diastolic" label, ~line 149): change `fontWeight={700}` to `fontWeight={600}`. No other change to this file — it references no `--color-foam`/`--color-sky`/`--color-ink`/`--color-accent`/`--color-focus` tokens (only unrenamed `--line-*`/`--cat-*`/`--ref-*` via `categoryColor()`/`CHIP_TEXT`, which resolve to new hex automatically). Do not touch `CHIP_FONT_SIZE`/`CHIP_PAD_X`/`CHIP_PAD_Y`/`CHIP_OFFSET` constants.

    Separately, open `frontend/src/lib/chartData.ts` and update ONLY the doc comment above `CHIP_CHAR_WIDTH_FACTOR` (currently references "the bold 14px Atkinson Hyperlegible band-label chip text") to say "Inter" instead of "Atkinson Hyperlegible" — the constant's numeric value (0.62) stays unchanged; it is a deliberately generous overestimate that stays safe (errs toward a wider, not clipped, chip) under the new font. Do not touch `isDotCrowded`/`DOT_DIAMETER_PX`/`estimateChipWidth`'s logic.

    Read `frontend/src/components/charts/PulseTrend.tsx` and confirm it contains no `--color-foam`/`--color-sky`/`--color-ink`/`--color-accent`/`--color-focus` references and no explicit `fontWeight={700}`/`fontWeight: 700` — it needs zero edits this phase. Do not modify it.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <done>BPTimeline.tsx's two SVG font-weight literals are 600; chartData.ts's doc comment references Inter, not Atkinson; PulseTrend.tsx is confirmed untouched.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "fontWeight={700}" frontend/src/components/charts/BPTimeline.tsx returns 0</criterion>
    <criterion type="source">grep -c "fontWeight={600}" frontend/src/components/charts/BPTimeline.tsx returns 2</criterion>
    <criterion type="source">grep -c "Atkinson" frontend/src/lib/chartData.ts returns 0</criterion>
    <criterion type="behavior">git diff --stat frontend/src/components/charts/PulseTrend.tsx shows no output (file untouched)</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin ChartTooltip.tsx, CategoryBars.tsx, and AmPmComparison.tsx</name>
  <files>frontend/src/components/charts/ChartTooltip.tsx, frontend/src/components/charts/CategoryBars.tsx, frontend/src/components/charts/AmPmComparison.tsx</files>
  <read_first>
    frontend/src/components/charts/ChartTooltip.tsx (full 166-line file — dialog style object lines 100-115, title/rows lines 117-134, Close button lines 137-163)
    frontend/src/components/charts/CategoryBars.tsx (full 102-line file — `barLabel` fill line 67)
    frontend/src/components/charts/AmPmComparison.tsx (full 179-line file — `makeBarLabels` fill lines 55/65, two heading `<p style>` blocks lines 125/152)
  </read_first>
  <action>
    ChartTooltip.tsx: dialog wrapper `style` object (lines ~101-103): `background: "var(--color-sky)"` → `"var(--color-mist)"`; `color: "var(--color-ink)"` → `"var(--color-depth)"`; `border: "2px solid var(--color-ink)"` → `"2px solid var(--color-depth)"`. Title `style` (~line 117): `fontWeight: 700` → `600`. Category chip `style` (~lines 124-128, uses `categoryColor`/`CHIP_TEXT` — leave those two calls unchanged, only its `fontWeight: 700` → `600`). Close button `style` (~lines 154-159): `background: "var(--color-accent)"` → `"var(--color-brass)"`; `color: "var(--color-accent-text)"` → `"var(--color-brass-text)"`; `fontWeight: 700` → `600`.

    CategoryBars.tsx: `barLabel`'s `fill="var(--color-ink)"` (~line 67) → `fill="var(--color-depth)"`. No other change — `categoryColor()` calls resolve automatically.

    AmPmComparison.tsx: `makeBarLabels`'s two `fill` values — the period label `fill="var(--color-ink)"` (~line 55) → `fill="var(--color-depth)"` (the value label already uses unrenamed `var(--cat-chip-text)`, leave it). Both heading `<p style>` blocks (~lines 125, 152): `color: "var(--color-ink)"` → `"var(--color-depth)"`; `fontWeight: 700` → `600`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/charts/ChartTooltip.test.tsx</automated>
  </verify>
  <done>ChartTooltip.tsx, CategoryBars.tsx, and AmPmComparison.tsx contain zero Phase-12 token names and zero 700-weight literals; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-sky\|color-ink\|color-accent\b\|color-accent-text" frontend/src/components/charts/ChartTooltip.tsx returns 0</criterion>
    <criterion type="source">grep -c "fontWeight: 700\|fontWeight={700}" frontend/src/components/charts/ChartTooltip.tsx frontend/src/components/charts/AmPmComparison.tsx returns 0</criterion>
    <criterion type="source">grep -c "color-ink" frontend/src/components/charts/CategoryBars.tsx frontend/src/components/charts/AmPmComparison.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/charts/ChartTooltip.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-skin ChartDeck.tsx</name>
  <files>frontend/src/components/ChartDeck.tsx</files>
  <read_first>frontend/src/components/ChartDeck.tsx (full 174-line file — hero heading line 140, mini button line 158, mini label line 160)</read_first>
  <action>
    Hero heading (line 140): `text-h2 leading-tight font-bold text-[var(--color-ink)]` → `text-heading leading-tight text-[var(--color-depth)]`. Mini chart-picker button (line 158): `bg-[var(--color-sky)]` → `bg-[var(--color-mist)]` (radius/shadow class names unchanged — values come from Plan 13-01 automatically; `min-h-12` unchanged). Mini label span (line 160): `text-control font-bold text-[var(--color-ink)]` → `text-label text-[var(--color-depth)]`. `FadeSwap`'s transition classes are untouched — no color tokens there.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <done>ChartDeck.tsx contains zero Phase-12 token/type-scale names; the mini chart-picker buttons remain >=48px.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-ink\|color-sky" frontend/src/components/ChartDeck.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-h2\|text-control" frontend/src/components/ChartDeck.tsx returns 0</criterion>
    <criterion type="source">grep -c "min-h-12" frontend/src/components/ChartDeck.tsx returns 1 (mini button's 48px floor unchanged)</criterion>
    <criterion type="test-command">cd frontend && npx tsc -b --noEmit exits 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — no user input, endpoints, or data handling in any of these files. Chart data renders verbatim from already-validated `Reading`/`StatsSummary` payloads.

## STRIDE Threat Register

No new attack surface — pure inline-style/className token and font-weight literal changes. No entries required.
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/charts/ChartTooltip.test.tsx` passes. Manually confirm in the browser: BP Timeline's 4 band-label chips (Hypotension/Normal/Stage 2/Hypertensive Crisis) still fully occlude the plotted lines behind them under the new palette and Inter font; the click-persistent tooltip is legible against the new Mist background; the 3 mini chart-picker cards in ChartDeck are legible and >=48px.
</verification>

<success_criteria>
BPTimeline.tsx's two font-weight literals are corrected; chartData.ts's doc comment matches the new font; ChartTooltip.tsx/CategoryBars.tsx/AmPmComparison.tsx/ChartDeck.tsx contain zero Phase-12 token names and zero 700-weight literals; PulseTrend.tsx is verified untouched.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-07-SUMMARY.md` when done
</output>
