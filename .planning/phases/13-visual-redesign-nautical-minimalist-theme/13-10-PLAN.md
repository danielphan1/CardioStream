---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 10
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/components/EmptyState.tsx
  - frontend/src/components/LoginGate.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-06, D-09]

must_haves:
  truths:
    - "The Copywriting Contract's verbatim strings ('Show all data', 'No readings match these filters', 'That password didn't work.', etc.) are byte-for-byte unchanged"
    - "LoginGate's password field remains a standard <input type=\"password\">, keyboard-operable, >=18px, high-contrast, exempt from voice per its documented accessibility scope"
  artifacts:
    - path: "frontend/src/components/EmptyState.tsx"
      provides: "Guided empty-state card re-skinned to Phase 13 tokens (source-of-truth for the shared card+CTA shell)"
      contains: "color-brass"
    - path: "frontend/src/components/LoginGate.tsx"
      provides: "Password gate re-skinned to Phase 13 tokens, mirroring EmptyState's card+CTA treatment"
      contains: "color-mist"
  key_links:
    - from: "frontend/src/components/LoginGate.tsx"
      to: "frontend/src/components/EmptyState.tsx"
      via: "shared card+accent-CTA-button shell convention (cited by LoginGate's own header comment)"
      pattern: "rounded-xl.*color-mist.*shadow-\\[var\\(--shadow-elevation\\)\\]"
---

<objective>
Re-skin `EmptyState.tsx` (the guided zero-results state, source of the shared card+CTA shell per `13-PATTERNS.md`) and `LoginGate.tsx` (the full-screen password gate, which explicitly copies EmptyState's card/button styling per its own header comment) to the Phase 13 token system.

Purpose: both are small, self-contained, copy-locked files (the Copywriting Contract in `13-UI-SPEC.md` carries every string in both files over verbatim) — only surface tokens and typography classes change.
Output: both files re-skinned; zero Phase-12 token references remain; zero copy changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin EmptyState.tsx</name>
  <files>frontend/src/components/EmptyState.tsx</files>
  <read_first>frontend/src/components/EmptyState.tsx (full 65-line file — card shell line 45, heading line 49, CTA button line 58)</read_first>
  <action>
    Card `<section>` (line 45): `bg-[var(--color-sky)]` → `bg-[var(--color-mist)]`. Heading (line 49): `text-h2 leading-tight font-bold` → `text-heading leading-tight`. "Show all data" CTA button (line 58): `bg-[var(--color-accent)] ... text-[var(--color-accent-text)]` → `bg-[var(--color-brass)] ... text-[var(--color-brass-text)]`, `text-control font-bold` → `text-label`. The `Sailboat` icon and every string ("No readings match these filters", the dynamic sentence, "Show all data") stay byte-for-byte unchanged — copy-locked by the Copywriting Contract.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <done>EmptyState.tsx contains zero Phase-12 token/type-scale names; all copy strings unchanged.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-sky\|color-accent\b\|color-accent-text" frontend/src/components/EmptyState.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-h2\|text-control" frontend/src/components/EmptyState.tsx returns 0</criterion>
    <criterion type="source">grep -n "Show all data" frontend/src/components/EmptyState.tsx returns 1 match (copy unchanged)</criterion>
    <criterion type="test-command">cd frontend && npx tsc -b --noEmit exits 0</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin LoginGate.tsx</name>
  <files>frontend/src/components/LoginGate.tsx</files>
  <read_first>frontend/src/components/LoginGate.tsx (full 119-line file — main wrapper line 48, form card line 52, headings lines 60/63, password input lines 75-85, rejected-notice panel line 92, submit button line 111)</read_first>
  <action>
    Main wrapper (line 48): `bg-[var(--color-foam)]` → `bg-[var(--color-deck)]`. Form card (line 52): `border-[var(--color-ink)] bg-[var(--color-sky)]` → `border-[var(--color-depth)] bg-[var(--color-mist)]`. Sailboat icon color (line 58): rename token only. Both headings (lines 60, 63): `text-h1 leading-tight text-[var(--color-ink)]` → `text-heading leading-tight text-[var(--color-depth)]` (h1→heading, not display); `text-h2 leading-tight text-[var(--color-ink)]` → `text-heading leading-tight text-[var(--color-depth)]`. Password `<label>` (line 71): `text-control text-[var(--color-ink)]` → `text-label text-[var(--color-depth)]`. Password `<input>` (line 84): `border-[var(--color-ink)] bg-[var(--color-foam)] ... text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-deck)] ... text-[var(--color-depth)]`. Rejected-password panel (line 94): `border-[var(--color-ink)] bg-[var(--color-sky)] ... text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-mist)] ... text-[var(--color-depth)]`. Submit button (line 111): `bg-[var(--color-accent)] ... text-[var(--color-accent-text)]` → `bg-[var(--color-brass)] ... text-[var(--color-brass-text)]`, `text-control` → `text-label`. All strings ("Chris's Health Dashboard", "Enter the password to continue", "Password", "That password didn't work.", "Please try again.", "Enter") stay byte-for-byte unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/LoginGate.test.tsx</automated>
  </verify>
  <done>LoginGate.tsx contains zero Phase-12 token/type-scale names; existing tests pass; all copy strings unchanged.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-foam\|color-ink\|color-sky\|color-accent\b\|color-accent-text" frontend/src/components/LoginGate.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-h1\|text-h2\|text-control" frontend/src/components/LoginGate.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/LoginGate.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caregiver password → `POST /auth` | `LoginGate.tsx`'s password field is a standard `<input type="password">` submitting to the backend; any failure (wrong password, network) collapses to one fixed non-specific notice — unchanged by this plan |

## STRIDE Threat Register

No new attack surface — pure className token changes. The existing "no status-code leak on auth failure" discipline and the pre-auth no-data-fetch invariant (App.tsx renders only LoginGate until a token exists) are untouched. No entries required.
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/LoginGate.test.tsx` passes. Manually confirm the password field remains fully keyboard-operable (tab to field, type, Enter submits) under the new palette.
</verification>

<success_criteria>
EmptyState.tsx and LoginGate.tsx contain zero Phase-12 token names and zero old type-scale token names; every user-visible string is byte-for-byte unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-10-SUMMARY.md` when done
</output>
