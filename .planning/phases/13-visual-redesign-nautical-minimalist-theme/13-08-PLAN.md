---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 08
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/components/UploadPage.tsx
  - frontend/src/components/AgentStatusBanner.tsx
autonomous: true
requirements: [D-01, D-06, D-09]

must_haves:
  truths:
    - "UploadPage remains exempt from the 48px/voice-operable floor per its documented caregiver-only accessibility scope, but its Choose-a-file affordance and result/error panels are still >=18px, high-contrast, and keyboard-operable"
    - "AgentStatusBanner's mount/unmount-a-live-region convention and its exact fixed copy (AGENT_UNAVAILABLE_BANNER_COPY) are unchanged — only surface tokens change"
  artifacts:
    - path: "frontend/src/components/UploadPage.tsx"
      provides: "OMRON upload surface re-skinned to Phase 13 tokens"
      contains: "color-brass"
    - path: "frontend/src/components/AgentStatusBanner.tsx"
      provides: "Persistent agent-unavailable banner re-skinned to Phase 13 tokens"
      contains: "color-depth"
  key_links:
    - from: "frontend/src/components/AgentStatusBanner.tsx"
      to: "frontend/src/lib/copy.ts"
      via: "AGENT_UNAVAILABLE_BANNER_COPY import, unchanged this phase"
      pattern: "AGENT_UNAVAILABLE_BANNER_COPY"
---

<objective>
Re-skin `UploadPage.tsx` (the caregiver-only OMRON `.xlsx` ingest surface, deliberately exempt from the 48px/voice-operable rule but not from contrast/keyboard-operability) and `AgentStatusBanner.tsx` (the persistent, non-dismissible assistant-unavailable indicator) to the Phase 13 token system.

Purpose: both are small, self-contained, low-risk files whose only Phase 13 obligation is the token/typography rename — neither's documented behavioral contract (D-10 error discipline, D-09/LIVE-02 liveness banner) changes.
Output: both files re-skinned; zero Phase-12 token references remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin UploadPage.tsx</name>
  <files>frontend/src/components/UploadPage.tsx</files>
  <read_first>frontend/src/components/UploadPage.tsx (full 198-line file — main wrapper line 86, heading line 88, file-picker label line 101, success/error panels lines 126/145/174)</read_first>
  <action>
    Main wrapper (line 86): `bg-[var(--color-foam)]` → `bg-[var(--color-deck)]`, `text-[var(--color-ink)]` → `text-[var(--color-depth)]`. Heading (line 88): `text-h2 leading-tight text-[var(--color-ink)]` → `text-heading leading-tight text-[var(--color-depth)]`. Body paragraph (line 91): rename token only. "Choose a file" label (line 101): `bg-[var(--color-accent)] ... text-[var(--color-accent-text)]` → `bg-[var(--color-brass)] ... text-[var(--color-brass-text)]`, `text-control` → `text-label`. Filename paragraph (line 112): rename token only. Loading paragraph (line 117): rename token only. Success panel (line 126): `border-[var(--color-ink)] bg-[var(--color-sky)] text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-mist)] text-[var(--color-depth)]`; every nested `text-[var(--color-ink)]` (lines 132) also renamed. "Show which rows" toggle (line 145): `border-[var(--color-ink)] bg-[var(--color-sky)] text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-mist)] text-[var(--color-depth)]`, `text-control` → `text-label`. Rejected-row list items (line 159): rename token only. Error panel (line 174): `border-[var(--color-ink)] bg-[var(--color-sky)] text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-mist)] text-[var(--color-depth)]`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/UploadPage.test.tsx</automated>
  </verify>
  <done>UploadPage.tsx contains zero Phase-12 token/type-scale names; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-foam\|color-ink\|color-sky\|color-accent\b\|color-accent-text" frontend/src/components/UploadPage.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-h2\|text-control" frontend/src/components/UploadPage.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/UploadPage.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin AgentStatusBanner.tsx</name>
  <files>frontend/src/components/AgentStatusBanner.tsx</files>
  <read_first>frontend/src/components/AgentStatusBanner.tsx (full 46-line file — banner div className line 39)</read_first>
  <action>
    Banner `<div>` (line 39): `border-[var(--color-ink)] bg-[var(--color-foam)] ... text-[var(--color-ink)]` → `border-[var(--color-depth)] bg-[var(--color-deck)] ... text-[var(--color-depth)]`. Do not touch `AGENT_UNAVAILABLE_BANNER_COPY`, the `useHealth`/`useAgentStatus` wiring, or the `role="status" aria-live="polite" aria-atomic="true"` contract — none of it changes this phase.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/AgentStatusBanner.test.tsx</automated>
  </verify>
  <done>AgentStatusBanner.tsx contains zero Phase-12 token names; existing tests pass; the fixed copy import is unchanged.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-ink\|color-foam" frontend/src/components/AgentStatusBanner.tsx returns 0</criterion>
    <criterion type="source">grep -c "AGENT_UNAVAILABLE_BANNER_COPY" frontend/src/components/AgentStatusBanner.tsx returns 1 (import unchanged)</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/AgentStatusBanner.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → backend `/upload` | `UploadPage.tsx` sends a caregiver-selected `.xlsx` file to the backend; error classification (400 vs generic) already collapses to fixed UI-authored copy, unchanged by this plan |

## STRIDE Threat Register

No new attack surface — this plan only changes className tokens. The existing D-10/T-05-13 "never render a raw error/status code" discipline in UploadPage.tsx's catch block is untouched. No entries required.
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/UploadPage.test.tsx src/components/AgentStatusBanner.test.tsx` passes.
</verification>

<success_criteria>
UploadPage.tsx and AgentStatusBanner.tsx contain zero Phase-12 token names and zero old type-scale token names; both files' documented behavioral contracts are unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-08-SUMMARY.md` when done
</output>
