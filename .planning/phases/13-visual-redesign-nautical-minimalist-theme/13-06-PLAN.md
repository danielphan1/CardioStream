---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 06
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - frontend/src/components/ReadingsTable.tsx
  - frontend/src/components/OverlayEventsList.tsx
autonomous: true
requirements: [D-01, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "The desktop table and the sub-640px stacked-card layout both render with the new tokens — no regression to the width-driven table/card switch (useElementWidth + shouldUseCardLayout)"
    - "Category chips in both the table and overlay-events list keep using categoryColor()/CHIP_TEXT and overlay dataset colors verbatim — no hardcoded hex introduced"
  artifacts:
    - path: "frontend/src/components/ReadingsTable.tsx"
      provides: "Readings table (desktop + mobile card layout) re-skinned to Phase 13 tokens"
      contains: "color-mist"
    - path: "frontend/src/components/OverlayEventsList.tsx"
      provides: "Overlaid lab/incident/procedure events table re-skinned to Phase 13 tokens"
      contains: "color-depth"
  key_links:
    - from: "frontend/src/components/ReadingsTable.tsx"
      to: "frontend/src/lib/palette.ts"
      via: "categoryColor()/CHIP_TEXT imports, unchanged this phase"
      pattern: "categoryColor\\(r\\.bp_category\\)"
---

<objective>
Re-skin `ReadingsTable.tsx` (the zero-precision data fallback, desktop table + sub-640px card layout) and `OverlayEventsList.tsx` (explicitly templated on ReadingsTable's own accessible-table contract) to the Phase 13 token system.

Purpose: both files render every reading/event verbatim with no client-side math beyond sort/slice — this plan changes only surface classes (background, border, text color, radius via the automatic `--radius-xl` override, typography) and must not touch the `categoryColor()`/`CHIP_TEXT`/`OVERLAY_META` color-lookup calls, which already resolve to the new hex values via Plan 13-01 with zero code change required here.
Output: both files re-skinned; zero Phase-12 token references remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin ReadingsTable.tsx</name>
  <files>frontend/src/components/ReadingsTable.tsx</files>
  <read_first>frontend/src/components/ReadingsTable.tsx (full 225-line file — outer section line 79, table header cells lines 92-109, "Show 20 more" button line 128, row/card border colors lines 141/183)</read_first>
  <action>
    Outer `<section>` (line 79): `bg-[var(--color-sky)]` → `bg-[var(--color-mist)]` (radius/shadow class names unchanged — values come from Plan 13-01 automatically). Table header cells (lines 92-109, 6x `text-xl font-bold`): since `text-xl` (20px) equals the new Label role's size exactly and `font-bold` (700) is banned by the Two-Weight Rule, consolidate each to `text-label` (drop both `text-xl` and `font-bold`). "Show 20 more" button (line 128): `bg-[var(--color-accent)] ... text-[var(--color-accent-text)]` → `bg-[var(--color-brass)] ... text-[var(--color-brass-text)]`, `text-control font-bold` → `text-label`. Row border (line 141) and card border (line 183): `border-[var(--color-foam)]` → `border-[var(--color-deck)]`. The 12 `<dt>`/table-cell `text-lg font-bold` occurrences (lines 186-203, card-layout field labels): rename `font-bold`→`font-semibold` (standalone, not a named type-scale token; Two-Weight Rule caps at 600) — keep `text-lg` (18px Body) unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ReadingsTable.test.tsx</automated>
  </verify>
  <done>ReadingsTable.tsx contains zero Phase-12 token/type-scale names and zero 700-weight classes; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-sky\|color-foam\|color-accent\b\|color-accent-text" frontend/src/components/ReadingsTable.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-xl font-bold\|text-control" frontend/src/components/ReadingsTable.tsx returns 0</criterion>
    <criterion type="source">grep -c "font-bold" frontend/src/components/ReadingsTable.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/ReadingsTable.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin OverlayEventsList.tsx</name>
  <files>frontend/src/components/OverlayEventsList.tsx</files>
  <read_first>frontend/src/components/OverlayEventsList.tsx (full 185-line file — heading line 108, error/empty text lines 112/118, table shell line 121, header cells lines 126-135, "Show 20 more" line 155, row border line 169)</read_first>
  <action>
    "Overlaid events" `<h2>` (line 108): `text-h2 leading-tight font-bold text-[var(--color-ink)]` → `text-heading leading-tight text-[var(--color-depth)]`. Error/empty-state paragraphs (lines 112, 118): `text-[var(--color-ink)]` → `text-[var(--color-depth)]` (size stays `text-[18px]`). Table shell `<section>` (line 121): `bg-[var(--color-sky)]` → `bg-[var(--color-mist)]`. Table header cells (lines 126-135, 4x `text-xl font-bold`): consolidate to `text-label` (matches ReadingsTable's identical treatment — same 20px role). "Show 20 more" button (line 155): `bg-[var(--color-accent)] ... text-[var(--color-accent-text)]` → `bg-[var(--color-brass)] ... text-[var(--color-brass-text)]`, `text-control font-bold` → `text-label`. Row border (line 169): `border-[var(--color-foam)]` → `border-[var(--color-deck)]`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/OverlayEventsList.test.tsx</automated>
  </verify>
  <done>OverlayEventsList.tsx contains zero Phase-12 token/type-scale names; existing tests pass.</done>
  <acceptance_criteria>
    <criterion type="source">grep -c "color-ink\|color-sky\|color-foam\|color-accent\b\|color-accent-text" frontend/src/components/OverlayEventsList.tsx returns 0</criterion>
    <criterion type="source">grep -c "text-h2\|text-control\|text-xl font-bold" frontend/src/components/OverlayEventsList.tsx returns 0</criterion>
    <criterion type="test-command">cd frontend && npx vitest run src/components/OverlayEventsList.test.tsx exits 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — `notes`/free-text fields render exclusively as React text nodes in both files (T-02-08/T-09-08 discipline), unchanged by this plan. No new inputs, endpoints, or data handling.

## STRIDE Threat Register

No new attack surface — pure className/token changes. The existing "no dangerouslySetInnerHTML, plain text node only" free-text rendering discipline is untouched. No entries required.
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/ReadingsTable.test.tsx src/components/OverlayEventsList.test.tsx` passes. Manually confirm the sub-640px card layout (resize the browser or use devtools responsive mode) still reflows correctly with the new tokens.
</verification>

<success_criteria>
ReadingsTable.tsx and OverlayEventsList.tsx contain zero Phase-12 token names, zero old type-scale names, and zero lingering 700-weight utility classes.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-06-SUMMARY.md` when done
</output>
