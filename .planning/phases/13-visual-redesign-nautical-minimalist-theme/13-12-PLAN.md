---
phase: 13-visual-redesign-nautical-minimalist-theme
plan: 12
type: execute
wave: 4
depends_on: ["13-03", "13-04", "13-05", "13-06", "13-07", "13-08", "13-09", "13-10", "13-11"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "Every screen (dashboard, command bar, filter/overlay controls, guide, upload, add-record, login) renders the new Slack Water (light) / Night Watch (dark) palette — zero lingering Phase-12 token names anywhere in frontend/src"
    - "All existing interactions still work identically: voice/text commands, filters, overlay toggles, chart switching, uploads, record submission, guide navigation, login/logout, theme toggle"
    - "The accessibility floor holds under the new system: every interactive control >=48x48px (including the 4 previously-exempt Header utility buttons), body text >=18px, full keyboard operability, no color-alone state signaling"
    - "Typography follows exactly 4 named sizes / 2 weights sitewide — no stray 700-weight class or literal remains anywhere in frontend/src"
    - "The nautical motif reads as restrained/premium (D-03), not cartoonish — confirmed by a human, not inferable from source alone"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Single source of truth for every Phase 13 token, unchanged since Plan 13-01"
      contains: "color-deck"
  key_links:
    - from: "every frontend/src/components/**/*.tsx file"
      to: "frontend/src/index.css"
      via: "var(--color-*) token consumption"
      pattern: "color-deck|color-mist|color-depth|color-brass|color-signal|color-hazard"
---

<objective>
Close out Phase 13 with an automated sitewide regression sweep (old-token leak grep, full test suite, typecheck/build) followed by a human cross-screen, cross-theme visual and functional walkthrough — the goal-backward verification for the phase-level goal: "Every screen gets a completely new visual identity ... with zero regression to existing functionality or the accessibility floor."

Purpose: 11 parallel/sequenced plans touched ~29 files independently; this plan is the single point where the whole assembled site is checked together, the way Phase 12's own final checkpoint worked.
Output: a clean automated sweep plus explicit human sign-off recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-CONTEXT.md
@.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated sitewide regression sweep</name>
  <files></files>
  <read_first>frontend/src/index.css (the Plan 13-01 token source of truth to grep the rest of the tree against)</read_first>
  <action>
    Run each of the following from the repo root and confirm the stated result. Do not proceed to Task 2 until every check passes — if any fails, fix the offending file directly (it means an earlier plan missed an occurrence) and re-run.

    1. `grep -rn "color-foam\|color-sky\|var(--color-ink)\|color-accent\b\|color-accent-text\|color-focus" frontend/src --include="*.tsx" --include="*.ts" --include="*.css"` — expect zero matches (every Phase-12 token name fully retired).
    2. `grep -rn "text-control\|text-h1\|text-h2" frontend/src --include="*.tsx"` — expect zero matches (old type-scale token names fully retired).
    3. `grep -rn "font-bold" frontend/src --include="*.tsx"` — expect zero matches (Two-Weight Rule: no 700-weight utility class remains).
    4. `grep -rn "fontWeight: 700\|fontWeight={700}\|fontWeight={700 as const}" frontend/src --include="*.tsx"` — expect zero matches (no 700-weight inline/SVG literal remains).
    5. `grep -rln "text-display\|font-display" frontend/src --include="*.tsx"` — expect exactly one file, `frontend/src/components/StatsStrip.tsx` (Space Grotesk/Display is StatsStrip-exclusive per `13-UI-SPEC.md`).
    6. `cd frontend && npx tsc -b --noEmit` — exits 0.
    7. `cd frontend && npm run build` — exits 0.
    8. `cd frontend && npx vitest run` — full suite exits 0 (every existing `*.test.tsx`/`*.test.ts` file, plus the new `StatsSparkline.test.tsx` and the updated `contrast.test.ts`).
    9. `grep -c "min-h-12" frontend/src/components/Header.tsx` — at least 7 (confirms the 4 previously-exempt utility buttons plus the 3 always-floored toggle buttons).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && npx tsc -b --noEmit && npm run build</automated>
  </verify>
  <done>All 9 checks pass in one clean run; results recorded verbatim in the SUMMARY.md.</done>
  <acceptance_criteria>
    <criterion type="behavior">All 9 checks above pass in a single clean run, with results recorded verbatim in the SUMMARY.md</criterion>
    <criterion type="test-command">cd frontend && npx vitest run exits 0 with no failing tests</criterion>
    <criterion type="behavior">cd frontend && npm run build exits 0</criterion>
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full cross-screen, cross-theme human walkthrough</name>
  <action>Walk through every screen listed below in both light and dark theme, using the header's theme toggle to switch mid-walkthrough, then respond with the resume-signal.</action>
  <what-built>The complete Phase 13 visual-identity replacement: every screen (Login, Dashboard, Command Bar, Filter/Overlay controls, Guide, Upload, Add Record) re-skinned to the "Slack Water" (light) / "Night Watch" (dark) nautical-minimalist theme, including the new Command Bar dark "feature panel" identity, the new per-tile sparkline/status-pill stat cards, and the new Inter/Space Grotesk typography.</what-built>
  <how-to-verify>
    Run `cd frontend && npm run dev` and open the printed local URL. Walk through, in BOTH light and dark theme (use the header's theme toggle to switch mid-walkthrough):
    1. Login gate — password card renders with the new palette; wrong password shows the calm notice; correct password enters the dashboard.
    2. Header — title, theme/voice-replies/guide toggles, Upload/Add Record/Log out controls all clearly >=48px and easy to hit; wave-curve divider visible under the header.
    3. Command Bar — confirm it visibly reads as the one dark, contrasting panel on the page (both themes); type a command and confirm the Working… state and the confirmation line are legible; tap the mic button and confirm the armed/off states are visually distinct.
    4. Filter Bar + Overlay Toggle — click through date presets, AM/PM, BP category chips, and Labs/Incidents/Procedures toggles; confirm active vs inactive states are visually and textually distinct (never color alone).
    5. Stats Strip — confirm each of the 4 tiles shows an icon, a large Space-Grotesk value, and (Systolic/Diastolic/Pulse only) a small trend sparkline, and (Systolic/Diastolic only) a status pill.
    6. Charts — cycle through all 4 (BP Timeline, Pulse, BP Categories, AM vs PM); confirm BP Timeline's band-label chips are fully legible with no line bleed-through; click a data point to confirm the tooltip renders correctly.
    7. Readings Table — confirm the desktop table renders correctly; narrow the browser below ~640px and confirm it reflows into the stacked-card layout.
    8. Guide overlay — open it from the header, confirm the backdrop is fully opaque with no bleed-through, jump to a section, close with Escape.
    9. Upload page and Add Record page (all 3 record types: Lab, Incident, Procedure) — confirm forms are legible and the type-switcher/submit states render correctly.
    10. Overall aesthetic judgment (D-03): confirm the theme reads as restrained/premium — a marine-instrument-panel feel, not literal/cartoonish/beach-themed.
  </how-to-verify>
  <resume-signal>Type "approved" once every screen above has been checked in both themes, or describe any issue found (which screen, which theme, what's wrong).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan runs existing tests/build tooling and performs a human visual walkthrough of already-implemented, already-reviewed screens. No new code, no new inputs, no new endpoints.

## STRIDE Threat Register

No new attack surface — a verification-only plan. No entries required.
</threat_model>

<verification>
All 9 automated checks in Task 1 pass; the human checkpoint in Task 2 is explicitly approved.
</verification>

<success_criteria>
Zero Phase-12 token/typography leaks anywhere in `frontend/src`; the full test suite and production build both pass; a human has confirmed, live, in both themes, that every screen works and looks like the intended "Slack Water"/"Night Watch" nautical-minimalist redesign with no functional or accessibility regression.
</success_criteria>

<output>
Create `.planning/phases/13-visual-redesign-nautical-minimalist-theme/13-12-SUMMARY.md` when done
</output>
