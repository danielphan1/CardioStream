---
phase: 05-upload-auth-gate-deployment
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - backend/app/routers/upload.py
  - backend/app/main.py
  - backend/tests/test_auth_upload.py
autonomous: true
requirements: [API-03, SEC-01]
user_setup: []
must_haves:
  truths:
    - "POST /upload of a valid OMRON .xlsx returns the locked IngestSummary (added/updated/unchanged/rejected/total/latest)"
    - "Re-uploading the same file is a visible no-op (added=0, all unchanged) — idempotent (D-08)"
    - "A non-OMRON / unparseable file returns a friendly HTTP 400 (never a 500 or traceback) and ingests nothing (D-10)"
    - "POST /upload without a valid Bearer token returns 401 (gated like every other route)"
  artifacts:
    - path: "backend/app/routers/upload.py"
      provides: "Thin gated POST /upload reusing the existing ETL, returning IngestSummary verbatim"
      contains: "merge_readings"
    - path: "backend/tests/test_auth_upload.py"
      provides: "Upload shape + idempotency + rejection + 401 tests"
      contains: "upload"
  key_links:
    - from: "backend/app/routers/upload.py"
      to: "app.etl (parse_omron → transform → merge_readings)"
      via: "file.file buffer handed straight to parse_omron"
      pattern: "parse_omron\\(file\\.file"
    - from: "backend/app/main.py"
      to: "app.routers.upload"
      via: "include_router WITH dependencies=[Depends(verify_token)] (gated)"
      pattern: "include_router\\(upload\\.router, dependencies"
---

<objective>
Add the thin `POST /upload` route: buffer the uploaded `UploadFile.file` straight into the already-tested, idempotent ETL (`parse_omron` → `transform` → `merge_readings`) and return the locked `IngestSummary` verbatim. Guard `.xlsx`-only and collapse any parse failure to a friendly HTTP 400 (never a 500/traceback). Wire it into `main.py` gated by `verify_token` like every other data route — `/auth` stays the sole exception. No new ETL logic: the route is a ~15-line adapter over `etl.py`.

Purpose: API-03 (upload endpoint) + SEC-01 (upload is gated).
Output: `routers/upload.py`, gated wiring in `main.py`, and passing upload tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md
@.planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md
@backend/app/etl.py
@backend/app/seed.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thin gated POST /upload over the existing ETL, wired into main.py</name>
  <files>backend/app/routers/upload.py, backend/app/main.py, backend/tests/test_auth_upload.py</files>
  <read_first>
    - backend/app/seed.py (lines 54-58 — the exact `parse_omron → transform → merge_readings` sequence the route reuses; note seed uses `SessionLocal` because it is a CLI — the route uses `Depends(get_db)` instead)
    - backend/app/etl.py (the `parse_omron` docstring at lines 1-28 confirming it accepts a file-like buffer; `IngestSummary`/`RejectedRow` definitions — the locked response model, do NOT rename or wrap)
    - backend/app/routers/agent.py (lines 32, 51 — the `db: Annotated[Session, Depends(get_db)]` injection + `response_model` shape to copy)
    - backend/app/main.py (lines 41-43 — gated include_router template)
    - .planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md ("backend/app/routers/upload.py (CREATE)" section)
    - .planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md ("Architecture Pattern 3" thin upload route, "Pitfall 8" log hygiene)
  </read_first>
  <behavior>
    - POST /upload with a valid token + a valid OMRON `.xlsx` (built via the `omron_xlsx` fixture) → 200 + IngestSummary with added > 0.
    - Immediately re-uploading the same file → 200 + IngestSummary with added=0 and the rows counted as unchanged (idempotent no-op, D-08/DATA-03).
    - A file whose name does not end `.xlsx` → 400 detail "not-omron", nothing ingested.
    - A `.xlsx` that is not an OMRON export (garbage bytes / wrong columns) → 400, nothing ingested, never a 500.
    - POST /upload with no Bearer token → 401.
  </behavior>
  <action>Create `backend/app/routers/upload.py` with `router = APIRouter()`. Define `@router.post("/upload", response_model=IngestSummary)` and a sync `def upload(file: UploadFile, db: Annotated[Session, Depends(get_db)]) -> IngestSummary:` (sync, runs in the threadpool like the other routes). First guard: if `(file.filename or "").lower()` does not end with `.xlsx`, raise `HTTPException(status_code=400, detail="not-omron")`. Then in a try/except: `raw = parse_omron(file.file)`, `clean, rejected = transform(raw)`, `return merge_readings(db, clean, rejected)` — returning the `IngestSummary` directly (Phase 1 D-06 lock; do not wrap or rename fields). Re-raise `HTTPException` unchanged; catch any other `Exception` and raise `HTTPException(status_code=400, detail="not-omron")` so a bad file never becomes a 500/traceback (D-10). Import `parse_omron, transform, merge_readings, IngestSummary` from `app.etl` and `get_db` from `app.deps` (never `SessionLocal` in a route). Log nothing about row values or the filename (rely on the ETL's already value-free `RejectedRow.reason`). In `backend/app/main.py`, extend the existing import to `from app.routers import auth, upload` and add `app.include_router(upload.router, dependencies=[Depends(verify_token)])` (gated exactly like readings/stats/agent). Add the upload tests (shape, idempotent re-upload, `.xlsx` guard, non-OMRON 400, no-token 401) to `test_auth_upload.py`, reusing the existing `omron_xlsx`/`omron_df` fixtures and the real-verify_token local client built in Plan 02.</action>
  <verify>
    <automated>cd backend && .venv/bin/pytest tests/test_auth_upload.py -q -k "upload or reject or idempotent" && .venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `routers/upload.py` contains `parse_omron(file.file`, `merge_readings(`, and `response_model=IngestSummary`.
    - `main.py` includes `upload.router` with `dependencies=[Depends(verify_token)]`.
    - Tests prove: valid xlsx → added>0; re-upload → added=0 (unchanged); non-.xlsx and garbage → 400 nothing ingested; no token → 401.
    - `merge_readings` return is passed through as `IngestSummary` unchanged (no wrapper class).
    - Full `cd backend && .venv/bin/pytest -q` green.
  </acceptance_criteria>
  <done>POST /upload ingests via the existing ETL, is idempotent, gated, never 500s on a bad file, and returns the locked IngestSummary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /upload | Untrusted multipart file upload crosses into the ETL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Denial of Service | huge/malicious upload file | mitigate | `.xlsx` extension guard + the ETL's existing `max_rows` cap (raises ValueError, caught → 400); file buffered to a spooled temp, never persisted to disk unmanaged. Verified by the non-OMRON 400 test. |
| T-05-07 | Elevation of Privilege | unauthenticated upload | mitigate | Router-level `Depends(verify_token)` — /upload gated exactly like every data route. Verified by the no-token 401 test. |
| T-05-08 | Information Disclosure | health values / filename in logs on reject | mitigate | Route logs nothing; relies on the ETL's value-free `RejectedRow.reason` (field+problem only). D-14 audit greps the diff for `print(`/`logger.`. |
| T-05-09 | Tampering | raw traceback / 500 leaking internals | mitigate | Broad-except collapses any parse failure to `HTTPException(400, "not-omron")` — never a 500. Verified by the garbage-file test asserting 400. |
</threat_model>

<verification>
- `cd backend && .venv/bin/pytest -q` green.
- Idempotent re-upload asserted (added=0 on second POST of the same file).
</verification>

<success_criteria>
- POST /upload reuses parse_omron/transform/merge_readings and returns IngestSummary verbatim (API-03).
- Upload is gated (401 without token) and never 500s on a bad file (SEC-01, D-10).
- Re-upload is a visible no-op (D-08).
</success_criteria>

<output>
Create `.planning/phases/05-upload-auth-gate-deployment/05-03-SUMMARY.md` when done.
</output>
