---
phase: 05-upload-auth-gate-deployment
plan: 06
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04", "05-05"]
files_modified:
  - railway.json
  - backend/.env.example
autonomous: false
requirements: [DEPL-01, SEC-03]
user_setup:
  - service: railway
    why: "Backend (FastAPI) + PostgreSQL hosting (kept warm, no cold start — D-11)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Existing key (SEC-02) — set in Railway service variables"
      - name: SITE_PASSWORD
        source: "Chosen shared password — set in Railway service variables only, never in git/frontend"
      - name: TOKEN_SECRET
        source: "Generate: python -c \"import secrets;print(secrets.token_urlsafe(32))\" — Railway variables only"
      - name: CORS_ORIGINS
        source: "The exact Vercel origin as a JSON array: [\"https://<app>.vercel.app\"] (Pitfall 3)"
    dashboard_config:
      - task: "Provision PostgreSQL (DATABASE_URL auto-injected); keep it on private networking — do NOT add a public TCP proxy (SEC-03)"
        location: "Railway project → New → Database → PostgreSQL"
      - task: "Set root directory = backend; confirm Railpack builder; start command runs alembic then uvicorn"
        location: "Railway service → Settings"
  - service: vercel
    why: "Frontend (Vite/React) static hosting on the edge (D-11)"
    env_vars:
      - name: VITE_API_URL
        source: "The Railway backend public URL, e.g. https://<app>.railway.app (baked at build time)"
    dashboard_config:
      - task: "Set root directory = frontend, build 'npm run build', output 'dist' (Vite default). No vercel.json needed (view-swap routing)"
        location: "Vercel project → Settings → Build & Output"
must_haves:
  truths:
    - "The backend runs on Railway with PostgreSQL on private networking (not publicly reachable) — SEC-03"
    - "alembic upgrade head runs on deploy against the normalized DATABASE_URL, creating the schema"
    - "The frontend runs on Vercel pointing at the Railway URL via VITE_API_URL, with CORS_ORIGINS allow-listing only the Vercel origin (no wildcard, no credentials)"
    - "An explicit decision is recorded: seed the 132 readings into prod OR start empty"
  artifacts:
    - path: "railway.json"
      provides: "Railpack builder + start command (alembic upgrade head && uvicorn)"
      contains: "alembic upgrade head"
    - path: "backend/.env.example"
      provides: "Documented env inventory (no secret values) for reproducible redeploy"
      contains: "TOKEN_SECRET"
  key_links:
    - from: "railway.json"
      to: "backend/app/config.py DATABASE_URL normalizer"
      via: "alembic + uvicorn both read the normalized get_settings().database_url"
      pattern: "alembic upgrade head"
---

<objective>
Ship the app to the public internet: FastAPI + PostgreSQL on Railway (kept warm, DB private — D-11, SEC-03) and the Vite frontend on Vercel, configured entirely via environment variables with a CORS allow-list for the exact Vercel origin. The config-code gotchas (psycopg3 `DATABASE_URL` normalizer, CORS JSON env parsing) already landed in Plan 02; this plan adds the deploy config, documents the env inventory, resolves the seed-prod-or-empty decision, and drives the interactive Railway/Vercel setup.

Purpose: DEPL-01 (Vercel + Railway via env vars) + SEC-03 (DB not publicly reachable). The MVP runs on the platform-provided `*.vercel.app` / `*.railway.app` URLs; a custom domain is deferred and addable later purely via `CORS_ORIGINS` + env with no code change (D-12).
Output: `railway.json`, `backend/.env.example`, a recorded seed decision, and a live deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md
@backend/app/config.py
@backend/app/seed.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide — seed the 132 readings into prod, or start empty</name>
  <read_first>
    - .planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md ("Runtime State Inventory" row 1 + "Open Questions" #1 + "Pitfall 7" — the 132 seed readings live only in dev SQLite; prod Postgres starts empty)
    - backend/app/seed.py (the one-time `python -m app.seed` path that would populate prod against the normalized DATABASE_URL after migration)
  </read_first>
  <decision>Should the fresh prod Postgres be seeded with the 132 real readings (Feb 22 – Jun 13, 2025) on first deploy, or start empty and fill via the first caregiver upload?</decision>
  <context>The dashboard shows the empty state until data exists. Seeding is a one-time `python -m app.seed` against the normalized prod URL right after `alembic upgrade head`. Note DATA-08: real data is gitignored — if the real OMRON export is not present in `data/`, the seeder falls back to the synthetic sample (which is NOT Chris's real history). Whether Chris's real 132 readings can be seeded depends on the real export being available to the deploy environment.</context>
  <options>
    <option id="seed-real">
      <name>Seed the real 132 readings</name>
      <pros>Dashboard shows Chris's real history on day one; the human smoke checklist can verify charts immediately.</pros>
      <cons>Requires the gitignored real OMRON export to be available to the deploy step; one extra one-time command.</cons>
    </option>
    <option id="start-empty">
      <name>Start empty, fill via first upload</name>
      <pros>Simplest; the smoke-test upload step is what first populates prod; no real-data handling in the deploy pipeline.</pros>
      <cons>Dashboard shows the empty state until the caregiver uploads; smoke checklist must do the upload before the view-charts step.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The chosen option is recorded in the SUMMARY (and, if seeding, the exact one-time command + source file used).
    - If seeding, the smoke checklist (Plan 07) assumes charts render; if empty, the checklist orders upload before view-charts.
  </acceptance_criteria>
  <resume-signal>Select: seed-real or start-empty.</resume-signal>
  <done>Seed-vs-empty decision recorded; it feeds the Plan 07 checklist ordering.</done>
</task>

<task type="auto">
  <name>Task 2: railway.json + backend/.env.example env inventory</name>
  <files>railway.json, backend/.env.example</files>
  <read_first>
    - backend/app/config.py (confirm the Plan 02 additions are present: `site_password`, `token_secret`, the `@field_validator("database_url")` psycopg3 normalizer, and `cors_origins`)
    - backend/app/main.py (lines 34-39 — CORS already reads `get_settings().cors_origins`; ONLY the env value changes for prod, no code change)
    - .planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md ("Deployment (DEPL-01) — concrete config" section: Railpack builder, start command, root dir, env inventory; "Pitfall 2" normalize, "Pitfall 3" CORS JSON)
    - .planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md ("No Analog Found" — deploy config uses the RESEARCH Deployment section)
  </read_first>
  <action>Create `railway.json` at the repo root pinning the builder to `RAILPACK` and setting the deploy start command to `alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT` (runs the migration on deploy against the injected+normalized `DATABASE_URL`, then serves; root directory is `backend`). Create `backend/.env.example` documenting the five backend env vars with placeholder (NON-secret) values and comments: `DATABASE_URL` (injected by Railway; SQLite locally), `ANTHROPIC_API_KEY` (SEC-02), `SITE_PASSWORD`, `TOKEN_SECRET` (generate with `python -c "import secrets;print(secrets.token_urlsafe(32))"`), and `CORS_ORIGINS` shown as a JSON array `["https://<app>.vercel.app"]` (Pitfall 3 — pydantic-settings JSON-parses list[str] from env). Note in the file that secrets live ONLY in the Railway dashboard, never in git, and that the frontend needs `VITE_API_URL` set in Vercel (baked at build). Do NOT put any real secret values in either file.</action>
  <verify>
    <automated>test -f railway.json && grep -q "alembic upgrade head" railway.json && grep -q "RAILPACK" railway.json && grep -q "TOKEN_SECRET" backend/.env.example && grep -q "CORS_ORIGINS" backend/.env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `railway.json` contains `RAILPACK` and the `alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT` start command.
    - `backend/.env.example` documents all five env vars with placeholder values only (no real secrets) and the CORS JSON-array note.
    - The verify command exits 0.
  </acceptance_criteria>
  <done>Deploy config + a reproducible, secret-free env inventory exist in the repo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Provision Railway (backend + private Postgres) and Vercel, set env vars, deploy</name>
  <read_first>
    - backend/.env.example (from Task 2 — the exact env inventory to enter in the dashboards)
    - .planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md ("Deployment (DEPL-01) — concrete config" — the Railway + Vercel + CORS steps)
  </read_first>
  <what-built>railway.json + the env inventory are in the repo; the app code is deploy-ready. This step is the interactive dashboard work that only a human with the accounts can do (provisioning, entering secrets, connecting the git repo).</what-built>
  <how-to-verify>
    1. Railway: create the project, add a PostgreSQL database (confirm `DATABASE_URL` is injected and the DB is on private networking — do NOT enable a public TCP proxy, SEC-03). Deploy the backend from `backend` (Railpack); set `ANTHROPIC_API_KEY`, `SITE_PASSWORD`, `TOKEN_SECRET`, and `CORS_ORIGINS` (JSON array of the Vercel origin). Confirm the deploy log shows `alembic upgrade head` running successfully (schema created) and uvicorn serving.
    2. If Task 1 chose seed-real: run the one-time seeder against the normalized prod URL after migration.
    3. Vercel: import the repo, root dir `frontend`, set `VITE_API_URL` to the Railway URL, deploy. Confirm the build succeeds and the site loads the LoginGate (no dashboard chrome pre-auth).
    4. Confirm `curl -s -o /dev/null -w "%{http_code}" https://<app>.railway.app/readings` returns 401 (gate is live in prod).
  </how-to-verify>
  <acceptance_criteria>
    - Railway backend is deployed; Postgres is provisioned and NOT publicly reachable (no public proxy).
    - The deploy log shows `alembic upgrade head` succeeded.
    - Vercel frontend is deployed with `VITE_API_URL` pointing at Railway; the site loads the LoginGate.
    - `curl .../readings` on the live Railway URL returns 401.
    - The live Vercel + Railway URLs are recorded in the SUMMARY for Plan 07's smoke test.
  </acceptance_criteria>
  <resume-signal>Type "deployed" with the live Vercel + Railway URLs, or describe what failed.</resume-signal>
  <done>Backend + private Postgres live on Railway (migrated), frontend live on Vercel, gate enforcing in prod; URLs recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → Railway | The app is now internet-exposed |
| Vercel → Railway | Cross-origin browser requests |
| Railway service → Postgres | DB access must stay private |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Information Disclosure/Tampering | public Postgres exposure | mitigate | Railway private networking (`postgres.railway.internal`); NO public TCP proxy (SEC-03). Verified in the dashboard step + Plan 07 SEC-03 audit. |
| T-05-17 | Information Disclosure | CORS misconfiguration leaking data to other origins | mitigate | `CORS_ORIGINS` set to the exact Vercel origin as a JSON array; no wildcard, no `allow_credentials` (locked model, unchanged in code). |
| T-05-18 | Spoofing | TOKEN_SECRET / SITE_PASSWORD weak or leaked | mitigate | `TOKEN_SECRET` generated via `secrets.token_urlsafe(32)`; both set only in Railway variables, never in git/frontend/.env.example. |
| T-05-19 | Information Disclosure | DATABASE_URL scheme crash exposing stack trace at boot | mitigate | psycopg3 normalizer (Plan 02) converts `postgresql://`→`postgresql+psycopg://` so the app boots cleanly; deploy log confirms migration + serve. |
</threat_model>

<verification>
- `railway.json` + `backend/.env.example` present and correct (Task 2 automated).
- Live: `curl .../readings` → 401; Vercel site loads the LoginGate; Postgres not publicly reachable.
</verification>

<success_criteria>
- Frontend on Vercel, backend + private Postgres on Railway, all via env vars with a CORS allow-list (DEPL-01).
- DB not publicly reachable; alembic ran on deploy (SEC-03).
- Seed-vs-empty decision recorded.
</success_criteria>

<output>
Create `.planning/phases/05-upload-auth-gate-deployment/05-06-SUMMARY.md` when done — include the live Vercel + Railway URLs and the seed decision.
</output>
