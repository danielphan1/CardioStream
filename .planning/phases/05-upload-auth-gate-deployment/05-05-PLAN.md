---
phase: 05-upload-auth-gate-deployment
plan: 05
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - frontend/src/store/view.ts
  - frontend/src/components/UploadPage.tsx
  - frontend/src/components/Header.tsx
  - frontend/src/App.tsx
  - frontend/src/components/UploadPage.test.tsx
autonomous: true
requirements: [DASH-10, API-03, SEC-01]
must_haves:
  truths:
    - "A caregiver reaches the upload page via a discreet header 'Upload' control and returns via 'Back to dashboard' (D-05, D-06)"
    - "Choosing an .xlsx file ingests immediately (no preview/confirm) and shows a plain-language sentence summary of added/already-on-file/skipped + latest date (D-08, D-09)"
    - "When rows are skipped, an optional expandable list shows each row's value-free reason; a non-OMRON file shows the friendly rejection and adds nothing (D-10)"
    - "A header 'Log out' control opens a confirm dialog; confirming clears the token and returns to the login gate (D-03)"
  artifacts:
    - path: "frontend/src/store/view.ts"
      provides: "zustand dashboard|upload view swap (no react-router)"
      contains: "dashboard"
    - path: "frontend/src/components/UploadPage.tsx"
      provides: "File picker + plain-language IngestSummary + reject disclosure + friendly errors"
      contains: "Choose a file"
    - path: "frontend/src/components/Header.tsx"
      provides: "Discreet Upload + Log out controls (with logout confirm)"
      contains: "Log out"
  key_links:
    - from: "frontend/src/components/UploadPage.tsx"
      to: "frontend/src/api/client.ts postFile"
      via: "postFile('/upload', file) on select"
      pattern: "postFile\\("
    - from: "frontend/src/components/Header.tsx"
      to: "frontend/src/store/view.ts"
      via: "useView.go('upload') / go('dashboard')"
      pattern: "useView"
---

<objective>
Add the caregiver upload surface and its access controls. A tiny zustand `view` store swaps `"dashboard" | "upload"` (no react-router, no Vercel rewrite — D-05); `Header` gains discreet "Upload" / "Back to dashboard" and "Log out" controls (D-06) with a lightweight logout-confirm dialog (justified by the no-expiry token, D-02/D-03); `UploadPage` offers a standard OS file picker (relaxed a11y, caregiver-only — D-07), ingests immediately on select via `postFile` (D-08), and renders the locked `IngestSummary` as plain-language sentences (D-09) with an optional expandable reject list and a friendly non-OMRON rejection (D-10). Follow 05-UI-SPEC.md exactly, including the accessibility split.

Purpose: DASH-10 (upload page) + API-03 frontend half + SEC-01 (logout control).
Output: `store/view.ts`, `UploadPage.tsx`, header controls, App view-switch, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md
@.planning/phases/05-upload-auth-gate-deployment/05-UI-SPEC.md
@frontend/src/components/Header.tsx
@frontend/src/components/EmptyState.tsx
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: view store + Header Upload/Log-out controls + logout confirm dialog</name>
  <files>frontend/src/store/view.ts, frontend/src/components/Header.tsx</files>
  <read_first>
    - frontend/src/store/theme.ts (the zustand `create` shape to mirror for the tiny view store)
    - frontend/src/components/Header.tsx (whole file — the theme-toggle button at lines 31-43 is the exact discreet-control template: `min-h-12`, 2px `--color-ink` border, `--color-sky` surface, icon + text label, never icon-only; add siblings in the same header-right flex row)
    - frontend/src/store/auth.ts (from Plan 04 — `useAuth.getState().logout()` for the logout action)
    - .planning/phases/05-upload-auth-gate-deployment/05-UI-SPEC.md ("Header admin controls (D-06)" copy + "Destructive / high-cost confirmation" logout dialog copy + "Layout" items 2 and 5 — controls discreet/never accent-filled, dialog traps focus, Escape/backdrop cancel)
    - .planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md ("frontend/src/store/view.ts (CREATE)" + "frontend/src/components/Header.tsx (MODIFY)" sections)
  </read_first>
  <action>Create `frontend/src/store/view.ts`: `useView` zustand store `{ view: "dashboard" | "upload"; go: (v) => void }`, default `"dashboard"`, no localStorage (ephemeral — RESEARCH Pattern 4). In `frontend/src/components/Header.tsx`, add to the existing header-right flex row (beside the theme toggle) two discreet controls styled like the toggle (2px `--color-ink` border, `--color-sky` surface, Label 20, `lucide-react` icon + text — NEVER accent-filled per D-06): (1) a view toggle showing "Upload" (`Upload` icon, `useView.go("upload")`) on the dashboard and "Back to dashboard" (`useView.go("dashboard")`) on the upload view — read `useView((s) => s.view)` to pick; (2) a "Log out" control (`LogOut` icon) that opens a confirm dialog. The logout confirm is a small centered modal on a dimmed backdrop (sky surface, 2px ink border): title "Log out?" (Heading 24), body "You'll need the password to unlock the dashboard again." (Body 18), buttons "Cancel" (bordered sky) and "Log out" (accent fill). Escape and backdrop-click cancel; trap focus; return focus to the "Log out" control on close; confirming calls `useAuth.getState().logout()` (clears the token → App falls back to LoginGate, D-03). Transition ≤250ms opacity with `motion-reduce:transition-none`. Header controls are exempt from the 48px floor (D-06) but keep the inherited 3px focus ring and keyboard operability.</action>
  <verify>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `store/view.ts` exports `useView` with `view` defaulting to `"dashboard"` and a `go` action.
    - `Header.tsx` contains `useView`, an "Upload"/"Back to dashboard" toggle, a "Log out" control, and a confirm dialog whose confirm calls `logout()`.
    - Header controls are NOT accent-filled (bordered sky, per D-06).
    - `npx vitest run` full suite green (existing Header/CommandBar tests unaffected).
  </acceptance_criteria>
  <done>Header carries discreet Upload + Log-out controls; logout confirms before clearing the token; view store toggles the two surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UploadPage (file picker → immediate ingest → plain-language summary + rejects) + App view-switch</name>
  <files>frontend/src/components/UploadPage.tsx, frontend/src/App.tsx, frontend/src/components/UploadPage.test.tsx</files>
  <read_first>
    - frontend/src/components/EmptyState.tsx (the nautical card + button styling tokens to reuse for the result panel)
    - frontend/src/App.tsx (lines 40-130 — the friendly-error block at 61-84 is the "never render raw status" pattern; wrap the dashboard branch and add the upload branch)
    - frontend/src/api/client.ts (from Plan 04 — `postFile("/upload", file)` and the `ApiError.status` used to distinguish 400 non-OMRON from generic failure)
    - frontend/src/api/types.ts (the `IngestSummary`/`RejectedRow` shape the summary renders — added/updated/unchanged/rejected/total/latest; if the type is not yet exported for the frontend, add it mirroring the backend etl.py IngestSummary)
    - .planning/phases/05-upload-auth-gate-deployment/05-UI-SPEC.md ("Upload page (DASH-10, D-08, D-09, D-10)" copy table + "Result-sentence assembly rules" (the 5 numbered rules) + "Layout" item 4 + "Accessibility Scope" — UploadPage EXEMPT from voice/48px but REQUIRED ≥18px/high-contrast/keyboard; native file input allowed)
    - .planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md ("frontend/src/components/UploadPage.tsx (CREATE)" + "frontend/src/App.tsx (MODIFY)" sections)
  </read_first>
  <behavior>
    - Choosing an .xlsx file calls `postFile("/upload", file)` immediately (no preview/confirm step, D-08) and shows "Reading your file…" while in flight.
    - On success the result panel renders plain sentences assembled from IngestSummary per the 5 UI-SPEC rules — e.g. "Added 12 new readings. 3 were already on file. 0 skipped. Your data now goes through June 13, 2025." — with pluralization; NOT numeric stat-tiles.
    - When `rejected.length > 0`, a "Show which rows" disclosure lists "Row {n}: {reason}." lines (reasons come verbatim from the value-free RejectedRow); `aria-expanded` wired.
    - A 400 (ApiError.status === 400) shows "This doesn't look like an OMRON export. Nothing was added…"; a non-400 failure shows "Something went wrong reading that file. Nothing was added — please try again." — never a status code or traceback.
    - App renders UploadPage when `useView().view === "upload"`, dashboard otherwise (both post-auth).
  </behavior>
  <action>Create `frontend/src/components/UploadPage.tsx`: single-column on `--color-foam`, order per UI-SPEC — page heading "Add new readings" (Heading 24) → instructions (Body 18) → a styled `<label>` "Choose a file" (accent fill, ≥48px, wrapping a visually-hidden `<input type="file" accept=".xlsx">` — native OS button allowed, D-07) → "Selected: {filename}" line → loading line → result panel. On file select, immediately call `postFile("/upload", file)`; render the result panel with a `CheckCircle2` icon + the assembled plain-language sentences (implement the 5 assembly rules incl. pluralization and the "Your data now goes through {latest as 'Month D, YYYY'}" line when total>0). When `rejected.length>0`, render a `<details>` disclosure ("Show which rows"/"Hide details", `ChevronDown` rotate, `aria-expanded`) listing each `Row {row_index}: {reason}.`. On `ApiError` with `status === 400` render the `TriangleAlert` non-OMRON notice; on any other error render the generic-failure notice — both in the sky notice panel, never a raw status/traceback (mirror App.tsx's centralized error copy). No numeric stat-tiles anywhere (D-09). In `frontend/src/App.tsx` (inside the authed branch from Plan 04), switch on `useView((s) => s.view)`: `"upload"` → `<> <Header /> <UploadPage /> </>`, else the existing dashboard assembly (Header stays in both). Write `UploadPage.test.tsx`: a mocked `postFile` resolving an IngestSummary renders the exact plain sentences + pluralization; rejected>0 renders the disclosure with value-free reasons; a mocked 400 renders the non-OMRON copy; a mocked network error renders the generic copy — assert no numeric-tile and no status code appear.</action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/UploadPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `UploadPage.tsx` contains "Choose a file", `postFile(`, `<details`, and the "Your data now goes through" sentence builder; no stat-tile markup.
    - The summary is assembled from IngestSummary fields as sentences (D-09), with pluralization ("reading"/"readings", "was"/"were").
    - A 400 renders the non-OMRON copy; a non-400 error renders the generic copy; neither shows a status code.
    - `App.tsx` renders `<UploadPage />` when view === "upload".
    - `npx vitest run src/components/UploadPage.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Caregivers upload an .xlsx, see an immediate plain-language summary with optional per-row rejects, and get friendly rejection copy for bad files — never a raw error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upload page → backend | File chosen in the browser POSTed to the gated /upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure | raw error / health value in the UI on a bad upload | mitigate | UploadPage renders only UI-SPEC copy — maps 400→non-OMRON, other→generic; never a status code, traceback, or health value (reject reasons are value-free from the backend). Verified by the 400/network-error render tests. |
| T-05-14 | Tampering | accidental upload trigger during Chris's voice session | accept | Upload lives on a separate caregiver-only view reached by a discreet header control (D-05/D-06); Chris's dashboard has no upload affordance. |
| T-05-15 | Denial of Service | accidental logout ending the hands-free session | mitigate | Logout is behind a confirm dialog (D-03) because the no-expiry token means only a caregiver can re-enter the password (D-02). |
</threat_model>

<verification>
- `cd frontend && npx vitest run` full suite green.
- Manual (Plan 07 checklist): live upload of the sample .xlsx shows the plain-language summary; re-upload shows the no-op summary.
</verification>

<success_criteria>
- Upload page ingests immediately and shows a plain-language summary + optional rejects (DASH-10, D-08, D-09, D-10).
- View swap and discreet header controls work without react-router (D-05, D-06).
- Logout confirms then returns to the login gate (D-03).
</success_criteria>

<output>
Create `.planning/phases/05-upload-auth-gate-deployment/05-05-SUMMARY.md` when done.
</output>
