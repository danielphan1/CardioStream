---
phase: 05-upload-auth-gate-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/tests/conftest.py
autonomous: false
requirements: [SEC-01, API-03]
user_setup: []
must_haves:
  truths:
    - "itsdangerous, python-multipart, psycopg[binary] are installed and importable in the backend venv"
    - "The existing readings/stats/agent test files keep passing once verify_token enforces (conftest overrides it to a no-op)"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "The 3 new backend dependencies in [project].dependencies"
      contains: "itsdangerous"
    - path: "backend/tests/conftest.py"
      provides: "client fixture verify_token override alongside get_db"
      contains: "verify_token"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "app.auth.verify_token"
      via: "app.dependency_overrides[verify_token] = lambda: None"
      pattern: "dependency_overrides\\[verify_token\\]"
---

<objective>
Lay the Wave-0 foundation the rest of Phase 5 depends on: add the three CLAUDE.md-named backend packages that are not yet installed (`itsdangerous`, `python-multipart`, `psycopg[binary]`), and pre-empt the single highest-risk landmine in this phase — the moment `verify_token` starts enforcing, the three existing gated-route test files (`test_api_readings.py`, `test_api_stats.py`, `test_agent_route.py`) call `/readings`/`/stats`/`/agent` with no token and turn the suite red. Overriding `verify_token` to a no-op in the shared `client` fixture NOW (harmless while the stub is still a no-op) means flipping the stub in Plan 02 keeps the suite green.

Purpose: unblock backend auth (Plan 02) and upload (Plan 03) without a test-suite regression.
Output: updated `pyproject.toml`, an installed venv, and a conftest `client` fixture that survives enforcement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md
@.planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (itsdangerous, python-multipart, psycopg[binary])</name>
  <read_first>
    - .planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md ("Package Legitimacy Audit" table — all three tagged [ASSUMED] because slopcheck was unavailable at research time)
  </read_first>
  <what-built>Nothing yet — this checkpoint gates the install in Task 2. All three packages are [ASSUMED] (slopcheck was unavailable), so per the package-legitimacy protocol they are verified by hand before any install runs.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/itsdangerous/ — confirm it is the Pallets project (github.com/pallets/itsdangerous), decade-old, high download count.
    2. Open https://pypi.org/project/python-multipart/ — confirm github.com/Kludex/python-multipart, the parser FastAPI's `[standard]` extra uses.
    3. Open https://pypi.org/project/psycopg/ — confirm github.com/psycopg/psycopg, the current-generation Postgres driver.
    All three are named verbatim in CLAUDE.md's locked STACK. Approve only if each matches.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all three PyPI pages match their canonical source repos and CLAUDE.md names.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or name any package that looks wrong.</resume-signal>
  <done>All three packages confirmed canonical; install may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 3 deps, install, and override verify_token in the client fixture</name>
  <files>backend/pyproject.toml, backend/tests/conftest.py</files>
  <read_first>
    - backend/pyproject.toml (the `[project].dependencies` list, lines 6-18 — `slowapi==0.1.*` is the last entry to append after)
    - backend/tests/conftest.py (the `client` fixture, lines 104-114 — the existing `app.dependency_overrides[get_db] = lambda: session` is the exact pattern to mirror)
    - .planning/phases/05-upload-auth-gate-deployment/05-PATTERNS.md ("backend/tests/conftest.py (MODIFY)" section — the one-line override + import to add)
    - .planning/phases/05-upload-auth-gate-deployment/05-RESEARCH.md ("Pitfall 1" — why this must precede the stub flip)
  </read_first>
  <action>Append three entries to `[project].dependencies` in `backend/pyproject.toml`: `itsdangerous==2.2.*`, `python-multipart==0.0.*`, `psycopg[binary]==3.3.*` (SQLite dev needs no driver; psycopg is for Railway Postgres). Then reinstall the editable package into the existing venv so the modules import. In `backend/tests/conftest.py`'s `client` fixture, add `from app.auth import verify_token` next to the existing `from app.deps import get_db`, and add `app.dependency_overrides[verify_token] = lambda: None` immediately after the existing `app.dependency_overrides[get_db] = lambda: session` line — mirroring the established get_db override discipline. This is harmless today (verify_token is still a no-op) and is what keeps the readings/stats/agent suites green after Plan 02 flips the stub to real enforcement. Do NOT touch `app/auth.py` in this plan — the stub stays a no-op until Plan 02.</action>
  <verify>
    <automated>cd backend && .venv/bin/pip install -e . -q && .venv/bin/python -c "import itsdangerous, multipart, psycopg; print('ok')" && .venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import itsdangerous, multipart, psycopg"` exits 0 (note: the PyPI distribution `python-multipart` imports as module `multipart`).
    - `pyproject.toml` `[project].dependencies` contains `itsdangerous`, `python-multipart`, and `psycopg[binary]`.
    - `backend/tests/conftest.py` contains `app.dependency_overrides[verify_token] = lambda: None` inside the `client` fixture.
    - Full `cd backend && .venv/bin/pytest -q` is green (existing suite unaffected).
  </acceptance_criteria>
  <done>The three packages import; the client fixture overrides verify_token; the existing suite is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → build | Third-party package code enters the backend supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | pip installs (itsdangerous, python-multipart, psycopg[binary]) | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies each against its canonical PyPI/GitHub source and CLAUDE.md before install; all three are decade-old ubiquitous packages FastAPI/SQLAlchemy already depend on. Never auto-approvable. |
</threat_model>

<verification>
- `cd backend && .venv/bin/pytest -q` green.
- The three modules import in the venv.
</verification>

<success_criteria>
- Backend venv has itsdangerous, python-multipart (module `multipart`), and psycopg installed and pinned in pyproject.toml.
- `client` fixture overrides `verify_token` so Plan 02's enforcement flip does not regress the existing 3 API test files.
</success_criteria>

<output>
Create `.planning/phases/05-upload-auth-gate-deployment/05-01-SUMMARY.md` when done.
</output>
