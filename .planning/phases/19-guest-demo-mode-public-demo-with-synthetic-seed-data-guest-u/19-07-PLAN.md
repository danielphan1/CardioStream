---
phase: 19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u
plan: 07
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-04"]
files_modified:
  - DEPLOY.md
  - README.md
autonomous: true
requirements: [D-01, D-02]

must_haves:
  truths:
    - "A literal, step-by-step instruction sequence exists for standing up a second, isolated Railway + Vercel deployment against a fresh demo Postgres DB (D-01)"
    - "The document is explicit that its steps are MANUAL and were never executed or verified end-to-end in this environment (no Railway/Vercel CLI access) — transparency, not a silent gap (D-02)"
    - "The env-var inventory names SITE_USERNAME, SITE_PASSWORD, TOKEN_SECRET, CORS_ORIGINS, DATABASE_URL, VITE_API_URL and explicitly warns TOKEN_SECRET must be freshly generated, never copied from Chris's real deployment"
    - "README.md points to DEPLOY.md so the instructions are discoverable from the repo's normal entry point"
  artifacts:
    - path: "DEPLOY.md"
      provides: "literal second-deployment instructions, env-var table, manual-verification callout"
      contains: "SITE_USERNAME"
      min_lines: 40
---

<objective>
Write `DEPLOY.md` — the literal, step-by-step instructions for standing up the second, isolated guest
demo deployment (D-01) — and add a short pointer to it from `README.md`.

Purpose: this is D-02's actual deliverable. The agent executing this phase has no Railway/Vercel CLI
access in this environment, so it cannot stand the second deployment up itself (D-02) — the
deliverable is code + config (Plans 19-01 through 19-04, already complete by this wave) plus this
literal instruction document for the user to execute in the Railway/Vercel dashboards. There is no
existing Railway/Vercel deployment documentation anywhere in this repo today (confirmed by
19-RESEARCH.md's repo-wide search) — this document is genuinely greenfield, not an update to an
existing doc.
Output: `DEPLOY.md` at the repo root; a new short section in `README.md` linking to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-CONTEXT.md
@.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-RESEARCH.md
@README.md
@backend/.env.example
@backend/railway.json
@railway.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write DEPLOY.md + link it from README.md</name>
  <files>DEPLOY.md, README.md</files>
  <read_first>
    README.md (first ~60 lines — the "Setup"/"Seeding"/"Synthetic sample" sections' exact style:
    plain markdown, fenced bash blocks, one-line explanation above each block, no prose padding; the
    section this plan inserts after "Synthetic sample" and before "Privacy")
    backend/.env.example (post-Plan-19-04 — the authoritative, currently-documented env var names:
    `DATABASE_URL`, `ANTHROPIC_API_KEY`, `SITE_PASSWORD`, `SITE_USERNAME`, `TOKEN_SECRET`,
    `CORS_ORIGINS`)
    backend/railway.json, railway.json (repo root — confirmed byte-identical; neither file states
    which one the existing deployed service actually reads, since that depends on a Root Directory
    setting configured in the Railway dashboard, not in git)
    .planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-RESEARCH.md
    ("Second-Deployment Mechanics" section — the fully-drafted 7-step instruction sequence and env-var
    inventory table to adapt; Pitfall 6 — the Root Directory confirmation caveat)
  </read_first>
  <action>
    Create `DEPLOY.md` at the repo root, plain markdown mirroring `README.md`'s style (fenced bash
    blocks, one-line explanation above each, no padding). Structure: (1) an intro paragraph stating
    this documents standing up a second, ISOLATED "guest demo" deployment per D-01 — a separate
    Railway project + a separate Vercel project + a fresh Postgres DB, never shared-DB row-tagging,
    same codebase, new env vars only; (2) an explicit, prominent callout that every step below is
    MANUAL — no Railway/Vercel CLI access exists in the environment that authored this document, so
    nothing here was executed or verified end-to-end (matches `19-VALIDATION.md`'s `human_needed`
    flag; this is transparency, not a gap being silently skipped); (3) an env-var inventory table with
    columns "Var / Chris's real deployment / Demo deployment" covering `DATABASE_URL`,
    `ANTHROPIC_API_KEY`, `SITE_PASSWORD`, `SITE_USERNAME`, `TOKEN_SECRET`, `CORS_ORIGINS`, and the
    frontend's `VITE_API_URL` — reference `backend/.env.example` as the authoritative name list rather
    than duplicating literal values, and state as an explicit MUST that `TOKEN_SECRET` is freshly
    generated for the demo deployment (`python -c "import secrets;print(secrets.token_urlsafe(32))"`)
    and never copied from Chris's real one — call this out in the same warning voice as
    `config.py`'s existing `_reject_dev_token_secret_in_deployment` boot guard; (4) the literal
    numbered sequence (at least 7 steps): Railway dashboard → New Project → Deploy from GitHub repo →
    select this repo; set the new service's Root Directory to `backend` (Settings tab) — flag
    explicitly that `backend/railway.json` and the root `railway.json` are byte-identical today and
    neither file alone proves which the EXISTING service reads, so the user should check the existing
    service's Settings → Root Directory before assuming either file is authoritative (Pitfall 6); add
    a Postgres plugin to the new project (auto-provisions `DATABASE_URL`); set `SITE_PASSWORD`,
    `SITE_USERNAME`, `TOKEN_SECRET`, `CORS_ORIGINS` in the Variables tab; deploy (note the
    `CORS_ORIGINS` circular dependency — either deploy backend first with a placeholder and fix it
    after the frontend URL is known, or deploy frontend first, either order works since Railway
    restarts on every variable change); Vercel dashboard → Add New → Project → import the same GitHub
    repo again → Root Directory `frontend` → set `VITE_API_URL` to the Railway backend URL → Deploy;
    visit the demo Vercel URL, log in with the guest username/password just set, confirm the "Guest
    Demo · Synthetic Data" badge and a populated dashboard; (5) a short "How the demo deployment
    self-populates" paragraph explaining the boot-time auto-seed gated on `SITE_USERNAME` (Plan 19-04)
    — the user's manual task really is just "set the env vars and redeploy," with the manual `python -m
    app.seed` command documented as a fallback only, never required in the normal path.

    In `README.md`, add a new `## Deploying a Second (Demo) Instance` section immediately after the
    existing "Synthetic sample" section and before "## Privacy," containing one short paragraph plus a
    link: standing up a separate, read-only guest-demo deployment (its own login, its own database,
    synthetic data only) is documented in `DEPLOY.md`.
  </action>
  <acceptance_criteria>
    - `test -f DEPLOY.md`
    - `grep -c "SITE_USERNAME" DEPLOY.md` is `>= 1`; `grep -c "TOKEN_SECRET" DEPLOY.md` is `>= 1`; `grep -c "Railway" DEPLOY.md` is `>= 1`; `grep -c "Vercel" DEPLOY.md` is `>= 1`
    - `grep -qi "manual\|human_needed\|no.*CLI access" DEPLOY.md` matches (transparency callout present)
    - `grep -c "freshly generat\|never.*copie\|MUST" DEPLOY.md` is `>= 1` (the TOKEN_SECRET warning is present)
    - `grep -c "^[0-9]\." DEPLOY.md` is `>= 7` (the full literal step sequence is represented as an ordered list)
    - `grep -n "DEPLOY.md" README.md` present (the pointer section was added)
  </acceptance_criteria>
  <verify>
    <automated>test -f DEPLOY.md && grep -q "SITE_USERNAME" DEPLOY.md && grep -q "TOKEN_SECRET" DEPLOY.md && grep -qi "manual\|human_needed\|no.*CLI access" DEPLOY.md && grep -q "DEPLOY.md" README.md && echo OK</automated>
  </verify>
  <done>`DEPLOY.md` documents the full literal second-deployment sequence, the env-var inventory, the TOKEN_SECRET warning, and an explicit manual/unverified callout; `README.md` links to it from a natural, discoverable location.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Deploy-time operator (the user) -> Railway/Vercel dashboards | Purely operational — this plan produces documentation, not code; no request-handling trust boundary is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-19-07-01 | Spoofing | Cross-deployment `TOKEN_SECRET` reuse | mitigate | No code can enforce this across two independent Railway projects — `DEPLOY.md` states the MUST-generate-a-fresh-secret rule explicitly, in the same warning voice as `config.py`'s existing boot-time guard, as the only available control for a purely operational risk |
| T-19-07-02 | Information Disclosure | `DEPLOY.md` itself | accept | Documents env var NAMES and dashboard click-paths only — no literal credential value is ever written into this file (D-09, consistent with every prior plan in this phase) |

</threat_model>

<verification>
- `test -f DEPLOY.md && grep -q "DEPLOY.md" README.md && echo OK`
- Manual, out-of-band verification (per `19-VALIDATION.md`'s Manual-Only Verifications table, `human_needed`): the user follows `DEPLOY.md`'s steps in the real Railway/Vercel dashboards, confirms the demo URL loads, logs in with guest credentials, sees the badge and a populated dashboard, and confirms a write action 403s. This step is explicitly OUT OF SCOPE for automated verification in this environment.
</verification>

<success_criteria>
- `DEPLOY.md` exists, is literal and complete (D-01, D-02), and is discoverable from `README.md`
- The document is honest about its own unverified-in-this-environment status
</success_criteria>

<output>
Create `.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-07-SUMMARY.md` when done
</output>
