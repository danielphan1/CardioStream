---
phase: 19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/hooks/useHealth.test.ts
  - frontend/src/components/AgentStatusBanner.test.tsx
autonomous: true
requirements: [D-03]

must_haves:
  truths:
    - "HealthStatus.demo is a required boolean field, mirroring the backend's /health extension (Plan 19-01) byte-for-byte"
    - "postAuth accepts an optional username, passed through unconditionally, so a demo-mode caller and a real-deployment caller share one function signature"
    - "The two existing test-mock helpers that build a HealthStatus object literal (useHealth.test.ts, AgentStatusBanner.test.tsx) are updated in the SAME commit as the type widening, so tsc -b never breaks on files this phase didn't otherwise intend to touch"
  artifacts:
    - path: "frontend/src/api/types.ts"
      provides: "HealthStatus.demo: boolean"
      contains: "demo: boolean"
    - path: "frontend/src/api/client.ts"
      provides: "postAuth(password, username?) signature"
      contains: "username?: string"
  key_links:
    - from: "frontend/src/components/LoginGate.tsx (Plan 19-06)"
      to: "frontend/src/api/client.ts postAuth"
      via: "postAuth(password, demoMode ? username : undefined)"
      pattern: "postAuth\\(password"
    - from: "frontend/src/components/Header.tsx (Plan 19-06)"
      to: "frontend/src/api/types.ts HealthStatus"
      via: "useHealth().data?.demo"
      pattern: "\\.demo"
---

<objective>
Widen `HealthStatus` with a `demo: boolean` field and `postAuth` with an optional `username` — the
shared frontend contract Plans 19-06 (LoginGate + Header) build against — and immediately fix the two
existing test-mock call sites this type change ripples into.

Purpose: 19-RESEARCH.md's Pitfall 2 confirms this exact ripple: `HealthStatus` gaining a REQUIRED
field breaks `tsc -b` at exactly two object-literal call sites
(`frontend/src/hooks/useHealth.test.ts` and `frontend/src/components/AgentStatusBanner.test.tsx`),
found by grepping `agent_configured` across `frontend/src` — no other file is affected. Fixing both in
this same plan (rather than leaving them for whichever later plan happens to run `tsc -b` first) keeps
the type change and its full blast radius in one commit, matching this codebase's established
discipline of never landing a wire-contract change with a known-broken build.
Output: `api/types.ts`'s widened `HealthStatus`, `api/client.ts`'s widened `postAuth`, and both fixed
test-mock helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-CONTEXT.md
@.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-RESEARCH.md
@frontend/src/api/types.ts
@frontend/src/api/client.ts
@frontend/src/hooks/useHealth.test.ts
@frontend/src/components/AgentStatusBanner.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen HealthStatus + postAuth</name>
  <files>frontend/src/api/types.ts, frontend/src/api/client.ts</files>
  <read_first>
    frontend/src/api/types.ts (`HealthStatus`, lines 213-217 — the current shape and its leading
    comment mirroring backend Plan 06-01's `/health` handler)
    frontend/src/api/client.ts (`postAuth`, lines 145-149 — the current single-argument signature and
    `postJson` call; `getHealth`, lines 163-165 — already returns `HealthStatus`, no change needed
    there)
  </read_first>
  <action>
    In `frontend/src/api/types.ts`, add `demo: boolean;` as a fourth field to `HealthStatus` (after
    `agent_reachable`). Update the type's leading comment to note it now also mirrors backend Plan
    19-01's `/health` extension (`demo: bool(site_username)`), alongside the existing Phase 6
    reference. In `frontend/src/api/client.ts`, change `postAuth`'s signature from `postAuth(password:
    string): Promise<{ token: string }>` to `postAuth(password: string, username?: string):
    Promise<{ token: string }>`, and its body from `postJson<{ password: string }, { token: string
    }>("/auth", { password })` to `postJson<{ password: string; username?: string }, { token: string
    }>("/auth", { password, username })` — passing `username` through unconditionally; when the caller
    omits it, `JSON.stringify` drops the `undefined` key entirely, matching the backend's `username:
    str | None = None` default (Plan 19-01) with no special-casing needed on either side. No change to
    `postJson`, `authHeaders`, or the `ApiError`/401-logout discipline — those stay generic and already
    correct.
  </action>
  <acceptance_criteria>
    - `grep -n "demo: boolean" frontend/src/api/types.ts` present inside `HealthStatus`
    - `grep -n "postAuth(password: string, username?: string)" frontend/src/api/client.ts` present
    - `grep -n "password, username" frontend/src/api/client.ts` present in the `postJson` call body
    - `cd frontend && tsc -b --noEmit 2>&1 | grep -c "useHealth.test.ts\|AgentStatusBanner.test.tsx"` reports exactly `2` — confirms the blast radius is scoped to exactly the two files RESEARCH.md identified, no other file breaks
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && tsc -b --noEmit 2>&1 | grep -c "useHealth.test.ts\|AgentStatusBanner.test.tsx"</automated>
  </verify>
  <done>`HealthStatus` carries `demo: boolean`; `postAuth` accepts an optional `username`; the only two files that fail to compile are the two known pre-existing test mocks, fixed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the two existing HealthStatus mock helpers (Pitfall 2)</name>
  <files>frontend/src/hooks/useHealth.test.ts, frontend/src/components/AgentStatusBanner.test.tsx</files>
  <read_first>
    frontend/src/hooks/useHealth.test.ts (the `health(overrides)` helper, lines 21-23)
    frontend/src/components/AgentStatusBanner.test.tsx (the identical `health(overrides)` helper,
    lines 24-26)
  </read_first>
  <action>
    In both files, change the `health(overrides: Partial&lt;HealthStatus&gt; = {})` helper's base
    object literal from `{ status: "ok", agent_configured: true, agent_reachable: true, ...overrides
    }` to `{ status: "ok", agent_configured: true, agent_reachable: true, demo: false, ...overrides
    }`. No other change to either file — this is a pure type-satisfaction fix, not a behavior change
    (every existing test in both files implicitly asserts `demo: false` going forward unless it
    explicitly overrides, which matches every current test's intent since none of them are
    demo-mode-aware yet).
  </action>
  <acceptance_criteria>
    - `grep -n "demo: false" frontend/src/hooks/useHealth.test.ts` present
    - `grep -n "demo: false" frontend/src/components/AgentStatusBanner.test.tsx` present
    - `cd frontend && tsc -b --noEmit` exits 0 (zero type errors anywhere in the project)
    - `cd frontend && npx vitest run src/hooks/useHealth.test.ts src/components/AgentStatusBanner.test.tsx` exits 0, all pre-existing tests in both files still pass unmodified
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && tsc -b --noEmit && npx vitest run src/hooks/useHealth.test.ts src/components/AgentStatusBanner.test.tsx</automated>
  </verify>
  <done>Both pre-existing `health()` mock helpers satisfy the widened `HealthStatus` type; the full frontend build type-checks cleanly; no test in either file regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure type/contract widening on the frontend — no new network call, no new trust boundary; the `demo` flag it carries is already confirmed non-secret by Plan 19-01's backend work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-19-05-01 | Information Disclosure | `frontend/src/api/types.ts` `HealthStatus.demo` | accept | Deliberately non-secret (confirmed by 19-RESEARCH.md and Plan 19-01) — it is the exact flag the frontend needs pre-auth; no credential value is ever carried by this type |

</threat_model>

<verification>
- `cd frontend && tsc -b --noEmit` — zero type errors project-wide
- `cd frontend && npx vitest run` — full frontend suite green, no regression
</verification>

<success_criteria>
- `HealthStatus`/`postAuth` widened to the exact shape Plan 19-06 needs
- Zero broken builds — both known ripple sites fixed in the same plan as the type change
</success_criteria>

<output>
Create `.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-05-SUMMARY.md` when done
</output>
