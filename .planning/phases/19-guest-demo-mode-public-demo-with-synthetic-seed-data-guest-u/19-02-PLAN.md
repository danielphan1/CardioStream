---
phase: 19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - backend/app/routers/labs.py
  - backend/app/routers/incidents.py
  - backend/app/routers/procedures.py
  - backend/app/routers/upload.py
  - backend/tests/test_demo_guard.py
autonomous: true
requirements: [D-03, D-11]

must_haves:
  truths:
    - "On a demo deployment (SITE_USERNAME set), a validly-authenticated caller gets 403 on POST /upload, POST /labs, POST /incidents, and POST /procedures — never a 500, never a distinct-looking error per route"
    - "On a demo deployment, GET /readings, /labs, /incidents, /procedures and POST /agent are completely unaffected — 200 as normal, proven by a dedicated test for /agent (not left as an untested structural side-effect)"
    - "An UNauthenticated caller (no Bearer token) on a demo deployment still gets 401 on a write route, never 403 — verify_token fires before reject_if_demo, proven by an explicit test, not trusted silently (RESEARCH A1)"
  artifacts:
    - path: "backend/tests/test_demo_guard.py"
      provides: "write-route 403 coverage + the 401-before-403 ordering regression test + a dedicated POST /agent-unaffected test"
      contains: "test_missing_token_still_401_on_demo"
      min_lines: 60
  key_links:
    - from: "backend/app/routers/labs.py create_lab"
      to: "backend/app/auth.py reject_if_demo"
      via: "dependencies=[Depends(reject_if_demo)] on the POST decorator only"
      pattern: "dependencies=\\[Depends\\(reject_if_demo\\)\\]"
    - from: "backend/app/routers/upload.py upload"
      to: "backend/app/auth.py reject_if_demo"
      via: "dependencies=[Depends(reject_if_demo)] on the POST decorator only"
      pattern: "dependencies=\\[Depends\\(reject_if_demo\\)\\]"
---

<objective>
Wire the `reject_if_demo` dependency (defined in Plan 19-01) onto the 4 mutating routes — `POST
/upload`, `POST /labs`, `POST /incidents`, `POST /procedures` — as a per-route dependency, never a
router-level one, and prove the whole guard (including auth-ordering AND that `/agent` stays fully
functional) with a dedicated test file.

Purpose: this is D-03's actual enforcement point. `labs.py`/`incidents.py`/`procedures.py` each carry
BOTH a `GET` and a `POST` route inside one `APIRouter()`, so attaching `reject_if_demo` at
`app.main`'s router-include level (like `verify_token`) would incorrectly 403 the GET routes too —
the guard must attach directly to each `@router.post(...)` decorator instead (19-RESEARCH.md's
explicit per-route-not-router-level finding). The ordering test is not optional: FastAPI's
dependency-resolution order (router-level `verify_token` before route-decorator-level
`reject_if_demo`) is consistent with its DI model but not pinned in the public API contract, so this
plan writes an explicit regression test rather than trusting it silently (19-RESEARCH.md Assumption
A1). `/agent` is architecturally untouched by this plan (its router never gains `reject_if_demo`), but
D-03 explicitly promises it stays available to guests — this plan adds a dedicated test proving that
promise rather than leaving it as an untested structural side-effect of "nobody happened to modify
`agent.py`."
Output: 4 one-line router changes + `backend/tests/test_demo_guard.py`, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-CONTEXT.md
@.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-RESEARCH.md
@backend/app/auth.py
@backend/app/routers/labs.py
@backend/app/routers/incidents.py
@backend/app/routers/procedures.py
@backend/app/routers/upload.py
@backend/tests/test_auth_upload.py
@backend/tests/test_agent_route.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire reject_if_demo onto the 4 write routes + prove it with test_demo_guard.py</name>
  <files>backend/app/routers/labs.py, backend/app/routers/incidents.py, backend/app/routers/procedures.py, backend/app/routers/upload.py, backend/tests/test_demo_guard.py</files>
  <read_first>
    backend/app/routers/labs.py, backend/app/routers/incidents.py, backend/app/routers/procedures.py,
    backend/app/routers/upload.py (full files — each router's current imports, the exact
    `@router.post(...)` decorator line to modify, and confirmation that each `@router.get(...)`
    decorator on the same file must stay untouched)
    backend/app/auth.py (post-19-01 — `reject_if_demo`'s exact signature and its "never inspects the
    token" contract)
    backend/tests/test_auth_upload.py (the `real_gate_client` fixture, lines 36-51 — a `TestClient`
    with only `get_db` overridden so `verify_token` genuinely enforces; the `valid_token` fixture,
    lines 255-263, minting a real Bearer token via `_serializer().dumps("authorized")`; the
    `auth_password`-style env-override idiom, lines 157-174, the template for a new `demo_mode`
    fixture; the existing 401-without-token gating block, lines 355-389, for style precedent)
    backend/tests/test_agent_route.py (the `_override` helper, `app.dependency_overrides[get_interpreter]
    = lambda: fake` — the exact pattern to reuse for the new `/agent`-unaffected test, so it never
    makes a real Claude API call; `get_interpreter` imports from `app.routers.agent`, `AgentReply`
    imports from `app.agent.schemas`)
    .planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-RESEARCH.md
    ("Auth Mechanism Shape" — dependency-ordering finding A1, why it must be tested not trusted)
  </read_first>
  <behavior>
    - `SITE_USERNAME` set + a valid Bearer token → `POST /upload`, `POST /labs`, `POST /incidents`,
      `POST /procedures` each return 403, body does not 500 and does not leak a distinct message per
      route (all four share `reject_if_demo`'s one fixed `detail` string).
    - `SITE_USERNAME` set + a valid Bearer token → `GET /readings`, `GET /labs`, `GET /incidents`,
      `GET /procedures` are completely unaffected, still 200.
    - `SITE_USERNAME` set + a valid Bearer token → `POST /agent` still returns 200, never 403 —
      `/agent` is read/view-only by design (D-03) and must remain completely unaffected by the
      write-guard, proven with a fake interpreter override (no network call).
    - `SITE_USERNAME` set + NO Bearer token at all → a write route (e.g. `POST /labs`) returns 401,
      NOT 403 — `verify_token` fires before `reject_if_demo` even when the deployment is in demo mode.
    - `SITE_USERNAME` UNSET (today's real-deployment default) + a valid Bearer token → all 4 write
      routes behave exactly as before this plan (200, unaffected) — the guard is inert dead code on
      Chris's real deployment.
  </behavior>
  <action>
    FIRST, create `backend/tests/test_demo_guard.py`. Define a local `real_gate_client` fixture
    (identical body to `test_auth_upload.py`'s, since pytest fixtures are not shared across files
    without a `conftest.py` entry — only `get_db` is overridden, `verify_token` runs for real). Define
    a `demo_mode` fixture: `monkeypatch.setenv("SITE_USERNAME", "guest-chris")`,
    `get_settings.cache_clear()`, yield, `get_settings.cache_clear()` after. Reuse/define a
    `valid_token` fixture minting a token via `_serializer().dumps("authorized")` with
    `get_settings.cache_clear()` before and after (mirror `test_auth_upload.py`'s `valid_token`
    fixture exactly). Write the test functions described in `<behavior>` above: one 403 test per
    write route (4 tests, or one parametrized test covering all 4 — either is acceptable, but every
    route must be individually asserted), one test confirming the 4 GET routes are unaffected under
    `demo_mode` + `valid_token`, `test_missing_token_still_401_on_demo` (exact name, per
    19-VALIDATION.md's own test map) asserting a write route with no Authorization header at all
    still 401s even when `demo_mode` is active, AND `test_agent_endpoint_returns_200_under_demo_mode`
    asserting `POST /agent` returns 200 (never 403) under `demo_mode` + `valid_token` — import
    `get_interpreter` from `app.routers.agent` and `AgentReply` from `app.agent.schemas`, override
    `app.dependency_overrides[get_interpreter]` to a fake lambda returning a minimal
    `AgentReply(kind="unclear", message="test")` (mirroring `test_agent_route.py`'s `_override`
    pattern — deterministic, no network call), POST `{"text": "show me blood pressure"}` to `/agent`,
    assert status 200. `real_gate_client`'s existing `app.dependency_overrides.clear()` teardown
    already cleans up this additional override, no extra teardown needed. Run `cd backend && python -m
    pytest tests/test_demo_guard.py -q` now and confirm it FAILS — the 4 routers don't call
    `reject_if_demo` yet, so every write route currently returns 200/other instead of the expected 403
    (the GET-unaffected and `/agent`-unaffected tests are expected to already pass at this point, since
    nothing in this plan touches those routes — they serve as regression guardrails, not fail-first
    targets). This is the required fail-first observation (D-11) before touching the routers.

    THEN, in each of `labs.py`, `incidents.py`, `procedures.py`, `upload.py`: add `from app.auth
    import reject_if_demo` to the import block (alongside the existing `app.deps` import). Add
    `dependencies=[Depends(reject_if_demo)]` to the `@router.post(...)` decorator ONLY — e.g.
    `labs.py`'s `create_lab` becomes `@router.post("/labs", response_model=LabResultOut,
    dependencies=[Depends(reject_if_demo)])`. Leave every `@router.get(...)` decorator in all four
    files completely untouched. Do NOT touch `app/main.py`'s router-level `Depends(verify_token)`
    wiring — that stays exactly as Plan 19-01 left it. Do NOT touch `app/routers/agent.py` at all —
    `/agent` must remain wired to `verify_token` only, no `reject_if_demo`. Re-run
    `test_demo_guard.py` and confirm it now passes in full.
  </action>
  <acceptance_criteria>
    - `grep -n "dependencies=\[Depends(reject_if_demo)\]" backend/app/routers/labs.py backend/app/routers/incidents.py backend/app/routers/procedures.py backend/app/routers/upload.py` returns exactly one match per file, each on a `@router.post` line
    - `grep -c "dependencies=\[Depends(reject_if_demo)\]" backend/app/routers/labs.py` is `1` (the file's `@router.get` line has no such dependency)
    - `grep -c "reject_if_demo" backend/app/routers/agent.py` is `0` (confirms `/agent` was never touched)
    - `test -f backend/tests/test_demo_guard.py` and `grep -c "^def test_" backend/tests/test_demo_guard.py` is `>= 7`
    - `grep -n "def test_missing_token_still_401_on_demo" backend/tests/test_demo_guard.py` present
    - `grep -n "def test_agent_endpoint_returns_200_under_demo_mode" backend/tests/test_demo_guard.py` present
    - `cd backend && python -m pytest tests/test_demo_guard.py -q` exits 0
    - `cd backend && python -m pytest tests -q` exits 0 (no regression anywhere else, notably `test_auth_upload.py`'s existing 401-gating block and the 4 GET-route tests)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && python -m pytest tests/test_demo_guard.py -q</automated>
  </verify>
  <done>All 4 mutating routes 403 on a demo deployment via a per-route `reject_if_demo` dependency; GET routes, the auth-ordering invariant (401 before 403), and `POST /agent`'s full availability are explicitly tested and green; the guard is inert on an unconfigured (real) deployment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated caller -> mutating routes (`POST /upload`, `/labs`, `/incidents`, `/procedures`) | On a demo deployment, EVERY valid token belongs to a guest by construction (D-01) — the boundary being enforced is "read-only for this whole deployment," not per-caller identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-19-02-01 | Elevation of Privilege | 4 mutating routes | mitigate | `reject_if_demo` fires unconditionally whenever `site_username` is configured, independent of any token content — no crafted/replayed/forged token bypasses it, since it never reads the token at all; proven by 4 dedicated 403 tests |
| T-19-02-02 | Tampering | Auth-ordering (401 vs 403) | mitigate | `test_missing_token_still_401_on_demo` explicitly pins that an unauthenticated caller gets 401, not 403, closing 19-RESEARCH.md's flagged Assumption A1 rather than leaving FastAPI's dependency-resolution order untested |
| T-19-02-03 | Denial of Service | GET routes / `/agent` on a demo deployment | accept | Explicitly unaffected by design (D-03) — read traffic and voice commands stay fully available to guests; no new DoS surface introduced by this plan; `/agent`'s continued 200 is now proven by `test_agent_endpoint_returns_200_under_demo_mode`, not just assumed from "the file wasn't touched" |
| T-19-02-04 | Tampering (untested regression) | `backend/tests/test_demo_guard.py` | mitigate | D-11's fail-first convention — every 403/ordering test in this plan is run and observed FAILING against pre-fix code before the routers are touched, so a future accidental removal of `reject_if_demo` from any of the 4 write routes cannot silently pass CI |

</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_demo_guard.py -q` — new guard suite green
- `cd backend && python -m pytest tests -q` — full backend suite green, zero regressions
- `grep -rn "dependencies=\[Depends(verify_token)\]" backend/app/main.py` unchanged from Plan 19-01 (router-level wiring untouched)
</verification>

<success_criteria>
- 403 on all 4 write routes when `SITE_USERNAME` is configured, regardless of token content (D-03)
- 401 (not 403) when no token is presented at all, even on a demo deployment
- GET routes and `/agent` fully unaffected, `/agent` proven by a dedicated test
- Zero behavior change on an unconfigured (Chris's real) deployment
</success_criteria>

<output>
Create `.planning/phases/19-guest-demo-mode-public-demo-with-synthetic-seed-data-guest-u/19-02-SUMMARY.md` when done
</output>
</output>
