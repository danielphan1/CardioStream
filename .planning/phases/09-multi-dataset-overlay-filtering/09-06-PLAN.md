---
phase: 09-multi-dataset-overlay-filtering
plan: 06
type: execute
wave: 4
depends_on: ["09-04", "09-05"]
files_modified:
  - frontend/src/App.tsx
autonomous: false
requirements: [OVERLAY-03, OVERLAY-04, OVERLAY-05, OVERLAY-06]

must_haves:
  truths:
    - "Toggling any of the 3 overlay buttons by click shows/hides matching markers on whichever of BP Timeline / Pulse Trend is the current hero chart"
    - "The Overlaid events table is visible exactly when >=1 overlay dataset is ON, and invisible when all 3 are off"
    - "Switching to BP Categories or AM vs PM visibly (but non-destructively) indicates overlays don't apply there"
    - "Backend and frontend test suites are both fully green after the full wiring lands"
  artifacts:
    - path: frontend/src/App.tsx
      provides: "Dashboard() wiring: overlay hooks, merged overlayEvents passed to ChartDeck, OverlayToggle + OverlayEventsList mounted at the UI-SPEC-locked positions"
  key_links:
    - from: frontend/src/App.tsx
      to: frontend/src/components/ChartDeck.tsx
      via: "<ChartDeck ... overlayEvents={mergedOverlayEvents} />"
      pattern: "overlayEvents=\\{"
    - from: frontend/src/App.tsx
      to: frontend/src/components/OverlayEventsList.tsx
      via: "<OverlayEventsList labs=... incidents=... procedures=... />"
      pattern: "<OverlayEventsList"
---

<objective>
Wire every piece built in Plans 09-01 through 09-05 together in `Dashboard()`: mount the three
new overlay read hooks (gated on the store's `overlayDatasets` flags), shape their results into
`OverlayEvent[]`, pass the merged array into `ChartDeck` for chart markers, mount
`&lt;OverlayToggle /&gt;` and `&lt;OverlayEventsList /&gt;` at the exact positions 09-UI-SPEC.md
locks, then run the mandatory manual verification pass this phase's Validation Architecture
flags as the only path to confirm marker shape/color/position correctness (Recharts renders 0×0
in jsdom — no automated path exists for that specific check).

Purpose: This is the phase's closing composition step — every requirement (OVERLAY-03 through
OVERLAY-06) becomes end-to-end observable only once this plan lands.
Output: A fully wired dashboard where toggling Labs/Incidents/Procedures — by click today, by
voice once Plan 09-01's backend has live API credits — overlays event markers on the timeline
charts and populates the accessible events table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-CONTEXT.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-UI-SPEC.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-VALIDATION.md
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx — wire overlay hooks, ChartDeck, OverlayToggle, OverlayEventsList</name>
  <files>frontend/src/App.tsx</files>
  <read_first>frontend/src/App.tsx (full file — Dashboard()'s existing hook-wiring and main assembly are the direct edit target), frontend/src/hooks/useStats.ts (useResolvedFilters — the store→params bridge already in use), frontend/src/hooks/useLabs.ts, frontend/src/hooks/useIncidents.ts, frontend/src/hooks/useProcedures.ts (post-Plan 09-03), frontend/src/lib/overlayEvents.ts (post-Plan 09-03 — labsToEvents/incidentsToEvents/proceduresToEvents/mergeOverlayEvents), frontend/src/components/OverlayToggle.tsx, frontend/src/components/OverlayEventsList.tsx (post-Plan 09-04), frontend/src/components/ChartDeck.tsx (post-Plan 09-05 — new overlayEvents prop), frontend/src/store/filters.ts (post-Plan 09-02 — overlayDatasets), 09-UI-SPEC.md's exact mount-order snippet in the OverlayToggle.tsx Phase-Specific Component Contract section (binding)</read_first>
  <action>
In `Dashboard()`, after the existing `const resolved = useResolvedFilters();`, add `const
overlayDatasets = useFilters((s) => s.overlayDatasets);` and a narrowed date window: `const
window = { start_date: resolved.start_date, end_date: resolved.end_date };` (matches Plan
09-03's hooks' `DateWindow` param shape — never pass the full `resolved` object, per the
Anti-Pattern warning against keying on `am_pm`/`bp_category`, which have no server-side effect
on these routes). Call the three new hooks: `const labs = useLabs(window,
overlayDatasets.labs);`, `const incidents = useIncidents(window, overlayDatasets.incidents);`,
`const procedures = useProcedures(window, overlayDatasets.procedures);`.

Compute per-type `OverlayEvent[]` via `lib/overlayEvents.ts`'s shaping functions, defaulting to
`[]` when data is undefined: `const labsEvents = labsToEvents(labs.data ?? []);`, `const
incidentsEvents = incidentsToEvents(incidents.data ?? []);`, `const proceduresEvents =
proceduresToEvents(procedures.data ?? []);`. Compute the merged chart-marker array:
`const overlayEvents = mergeOverlayEvents(labsEvents, incidentsEvents, proceduresEvents);`.

Pass `overlayEvents={overlayEvents}` as a new prop on the existing `&lt;ChartDeck readings={
readings.data ?? []} stats={stats.data} /&gt;` call site (the ONLY `chartRegion` branch that
renders `ChartDeck` — the pending/error/empty branches are untouched).

Mount `&lt;OverlayToggle /&gt;` directly after `&lt;FilterBar latestReading={latestReading} /&gt;`
and before `&lt;StatsStrip ... /&gt;` inside `main` (09-UI-SPEC.md's locked mount order — the
component takes no props, it reads the store directly). After the existing `&lt;section
aria-label="Readings table"&gt;...&lt;/section&gt;` block closes, add `&lt;OverlayEventsList labs={{
enabled: overlayDatasets.labs, events: labsEvents, isError: labs.isError }} incidents={{
enabled: overlayDatasets.incidents, events: incidentsEvents, isError: incidents.isError }}
procedures={{ enabled: overlayDatasets.procedures, events: proceduresEvents, isError:
procedures.isError }} /&gt;` — no conditional wrapper needed in App.tsx itself since
`OverlayEventsList` already self-gates via its own `onTypes.length === 0` null-return (Plan
09-04).

Import `OverlayToggle` from `./components/OverlayToggle`, `OverlayEventsList` from
`./components/OverlayEventsList`, `useLabs`/`useIncidents`/`useProcedures` from their respective
new hook files, and `labsToEvents, incidentsToEvents, proceduresToEvents, mergeOverlayEvents`
from `./lib/overlayEvents`.
  </action>
  <acceptance_criteria>
    - App.tsx imports `OverlayToggle`, `OverlayEventsList`, `useLabs`, `useIncidents`, `useProcedures`, and the four `overlayEvents.ts` functions
    - `&lt;OverlayToggle /&gt;` appears in the JSX immediately after `&lt;FilterBar` and before `&lt;StatsStrip`
    - The `&lt;ChartDeck` call site receives an `overlayEvents=` prop
    - `&lt;OverlayEventsList` appears after the Readings `&lt;section&gt;` closing tag
    - `cd frontend && npx tsc --noEmit` passes
    - `cd frontend && npm test -- --run` full suite passes (regression check across every prior phase's frontend tests, including agent-parity.test.ts's structural gate)
    - `cd backend && pytest` full suite passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test -- --run</automated>
  </verify>
  <done>Dashboard() fetches overlay data only when toggled on, shapes it once via lib/overlayEvents.ts, and renders OverlayToggle/ChartDeck markers/OverlayEventsList at the UI-SPEC-locked positions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verification — chart markers, doesn't-apply-here, cache freshness, full-suite gate</name>
  <files>none (verification-only task, no files modified)</files>
  <action>
Present the running dashboard to the user for manual sign-off, since Recharts renders 0x0 in
jsdom (documented project constraint) — marker shape/color/position correctness has no
automated test path anywhere in this codebase. Follow the exact steps in
&lt;how-to-verify&gt; below and wait for the resume signal before considering this plan (and the
phase) complete.
  </action>
  <what-built>
Full end-to-end overlay feature: click (and, once live API credits exist, voice) toggling of
Labs/Incidents/Procedures now renders shape+color-distinct markers on the BP Timeline and Pulse
Trend hero charts, an accessible "Overlaid events" table appears whenever any dataset is on, and
the toggle row visibly indicates when it doesn't apply (BP Categories / AM vs PM) without ever
disabling itself. Backend `toggle_dataset` schema/service/prompt support, the multi-select
store, cache-invalidated data fetching, and the full wiring are all in place.
  </what-built>
  <how-to-verify>
1. Run `cd backend && pytest` and `cd frontend && npm test -- --run` — confirm both suites are
   fully green (0 failures) before visual review.
2. Start both dev servers (`cd backend && uvicorn app.main:app --reload`, `cd frontend && npm
   run dev`), log in, and open the dashboard. Ensure at least one lab, one incident, and one
   procedure exist in the current date range (Phase 8's Add Record page, or seeded data).
3. Confirm the "Overlay:" row renders below the filter bar with 3 buttons — Labs, Incidents,
   Procedures — all unpressed by default (D-07).
4. Click "Labs" — the button fills with its violet color and `aria-pressed` becomes `true`
   (inspect via devtools), and diamond-shaped violet markers appear on the BP Timeline hero
   chart at the correct dates. Repeat for Incidents (magenta/berry triangle) and Procedures
   (olive square).
5. Confirm markers do NOT appear on the 3 mini preview cards in the chart-picker row (D-02).
6. Confirm the "Overlaid events" table appears below the Readings table once any button is
   pressed, and disappears again when all 3 are unpressed.
7. Click a mini card to switch the active chart to "BP Categories" or "AM vs PM" — confirm the
   overlay toggle row visibly dims (opacity-60) and shows "Overlays aren't shown on this
   chart…", but all 3 buttons remain clickable (never `disabled`).
8. Check an event whose date falls near the edge of the currently filtered date range still
   shows its marker on-chart. If a marker is silently missing on-chart despite the accessible
   table showing it, this is the flagged domain-overflow risk (RESEARCH Pitfall 2): apply the
   documented fallback — widen BPTimeline's/PulseTrend's XAxis `domain` calculation to
   `Math.min`/`Math.max` across both `toTimePoints(readings)` and the active `overlayEvents`
   timestamps, in addition to the existing `ifOverflow="extendDomain"` prop — then re-verify
   this step.
9. Switch the hero chart to Pulse Trend — confirm the same markers reappear there too (overlay
   state is independent of `activeChart`, per D-01).
10. If a lab/incident/procedure's `notes` field contains special characters (e.g. `&lt;script&gt;`
    or `&amp;`), confirm the Overlaid events table renders it as inert text, not executed markup.
11. Submit a new lab/incident/procedure via the Add Record page while its matching overlay is
    already toggled ON — confirm it appears in both the chart markers and the accessible table
    without a page reload (cache-invalidation check from Plan 09-03).
  </how-to-verify>
  <verify>
    <human-check>All 11 steps above pass as described</human-check>
  </verify>
  <done>User has typed "approved" after confirming all 11 verification steps pass.</done>
  <resume-signal>Type "approved" once every check above passes, or describe exactly which step failed (e.g. "step 8 — incident marker near the 30-day boundary is invisible on the chart but present in the table").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is pure composition — it wires together components/hooks whose individual trust boundaries were already assessed in Plans 09-01/09-03/09-04/09-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | none | App.tsx wiring | accept | No new data flow is introduced beyond passing already-validated, already-typed values between already-reviewed components; no new attack surface. |

</threat_model>

<verification>
`cd backend && pytest` and `cd frontend && npx tsc --noEmit && npm test -- --run` must both be
fully green before the manual checkpoint is presented, per 09-VALIDATION.md's phase-gate rule.
</verification>

<success_criteria>
- Every one of OVERLAY-03 through OVERLAY-06's success criteria is observable end-to-end in a
  running dashboard
- Full backend + frontend automated suites are green
- Human verification of chart marker shape/color/position (the one behavior with no automated
  test path in this codebase) is explicitly signed off
</success_criteria>

<output>
Create `.planning/phases/09-multi-dataset-overlay-filtering/09-06-SUMMARY.md` when done
</output>
