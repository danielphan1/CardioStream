---
phase: 09-multi-dataset-overlay-filtering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/client.ts
  - frontend/src/lib/overlayMeta.ts
  - frontend/src/store/filters.ts
  - frontend/src/store/filters.test.ts
  - frontend/src/index.css
  - frontend/src/lib/agent.ts
  - frontend/src/lib/agent.test.ts
  - frontend/src/lib/agent-parity.test.ts
autonomous: true
requirements: [OVERLAY-03, OVERLAY-04]

must_haves:
  truths:
    - "overlayDatasets defaults to {labs:false, incidents:false, procedures:false} at first load (D-07)"
    - "setOverlayDataset(dataset, on) mutates only that dataset's flag, independent of the others (D-01)"
    - "showAllData() resets overlayDatasets to all-false alongside date/AM-PM/category (D-08)"
    - "applyAgentFilters({overlayDataset, overlayState}) reaches setOverlayDataset via the single mutation surface and is voice-reachable per the agent-parity structural gate"
    - "backend AppliedFilters.overlayDataset/overlayState and frontend AppliedFilters.overlayDataset/overlayState are field-name-identical"
    - "The 3 overlay marker/toggle colors are visually distinct from every existing chart/category color token, in both light and dark themes (D-05)"
  artifacts:
    - path: frontend/src/api/types.ts
      provides: "OverlayDataset union type (single declaration site), AppliedFilters.overlayDataset/overlayState fields"
    - path: frontend/src/api/client.ts
      provides: "getLabs/getIncidents/getProcedures typed GET wrappers"
    - path: frontend/src/lib/overlayMeta.ts
      provides: "OVERLAY_META (label/tableLabel/Icon/color/glyph) + OVERLAY_ORDER — single source shared by Plan 09-04/09-05"
    - path: frontend/src/store/filters.ts
      provides: "overlayDatasets field + setOverlayDataset action + showAllData D-08 reset"
    - path: frontend/src/lib/agent.ts
      provides: "\"overlay\" PulseField member + applyAgentFilters overlay branch"
    - path: frontend/src/index.css
      provides: "--overlay-labs/--overlay-incidents/--overlay-procedures light+dark token pairs"
  key_links:
    - from: frontend/src/lib/agent.ts
      to: frontend/src/store/filters.ts
      via: "s.setOverlayDataset(f.overlayDataset, f.overlayState === \"on\")"
      pattern: "setOverlayDataset\\(f\\.overlayDataset"
    - from: frontend/src/lib/agent-parity.test.ts
      to: frontend/src/store/filters.ts
      via: "STORE_ACTIONS includes setOverlayDataset, structurally diffed against useFilters.getState()"
      pattern: "setOverlayDataset"
---

<objective>
Establish every shared frontend contract this phase's components/hooks/charts (Plans 09-03,
09-04, 09-05, 09-06) will build against: the `AppliedFilters` byte-identical mirror of Plan
09-01's new backend fields, three typed GET wrappers for the Phase 7 read routes, the
independent `overlayDatasets` multi-select on the filter store (D-01/D-04/D-07/D-08), the
`applyAgentFilters` bridge extension (single mutation surface, D-03), the mandatory
`agent-parity.test.ts` structural-gate extension, and the three locked per-dataset color tokens
(D-05) plus the shared icon/color/glyph map every downstream render surface consumes.

Purpose: Interface-first — every later plan in this phase imports from the files this plan
creates/extends, so getting the wire contract, store shape, and design tokens exactly right
here (with zero drift from `backend/app/agent/schemas.py`) prevents rework downstream.
Output: `overlayDatasets`/`setOverlayDataset` on `useFilters`, `applyAgentFilters` reaching it,
`OVERLAY_META`/`OVERLAY_ORDER`, and the three `--overlay-*` CSS tokens in both themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-CONTEXT.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-RESEARCH.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-UI-SPEC.md
@frontend/src/store/filters.ts
@frontend/src/lib/agent.ts
@frontend/src/api/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire contract — api/types.ts, api/client.ts, lib/overlayMeta.ts</name>
  <files>frontend/src/api/types.ts, frontend/src/api/client.ts, frontend/src/lib/overlayMeta.ts</files>
  <read_first>frontend/src/api/types.ts (full file — AppliedFilters mirror contract, LabResult/Incident/Procedure already exist from Phase 7/8), frontend/src/api/client.ts (full file — getReadings/getStatsSummary/postLab are the direct templates), backend/app/agent/schemas.py (Plan 09-01's DatasetToken/AppliedFilters additions — the byte-identical mirror target)</read_first>
  <action>
In api/types.ts, add `export type OverlayDataset = "labs" | "incidents" | "procedures";`
directly above the `AppliedFilters` type. This is the ONE declaration site for this union —
`store/filters.ts` imports it from here (Task 2), matching the file's existing convention of
importing `BPCategory`/`ChartId` from `api/types.ts` rather than re-declaring them. Extend the
`AppliedFilters` type with `overlayDataset?: OverlayDataset | null;` and `overlayState?: "on" |
"off" | null;`, inserted after `bpCategory` and before `reset`, mirroring
`backend/app/agent/schemas.py`'s `AppliedFilters` field order and the `X | None`→`X? | null`
convention already used for every other field.

In api/client.ts, add `type DateWindow = { start_date?: string; end_date?: string };` and three
new export functions after the existing `postProcedure` function: `getLabs(window:
DateWindow): Promise<LabResult[]>`, `getIncidents(window: DateWindow): Promise<Incident[]>`,
`getProcedures(window: DateWindow): Promise<Procedure[]>` — each a one-line `getJson<T>(path,
window)` call over `/labs`, `/incidents`, `/procedures` respectively, mirroring `getReadings`'s
exact shape. `LabResult`/`Incident`/`Procedure` are already imported into this file.

Create frontend/src/lib/overlayMeta.ts — the single shared icon/color/glyph map every
downstream render surface (Plan 09-04's OverlayToggle/OverlayEventsList, Plan 09-05's chart
markers) imports, avoiding a 3-4-way duplicated inline map. Export `OVERLAY_ORDER:
OverlayDataset[] = ["labs", "incidents", "procedures"]` and `OVERLAY_META: Record&lt;OverlayDataset,
{ label: string; tableLabel: string; Icon: ComponentType&lt;{ size?: number; "aria-hidden"?: string
}&gt;; color: string; glyph: string }&gt;` with exactly these three entries (all values locked by
09-UI-SPEC.md's Marker & Icon Contract table — copy verbatim, do not re-derive): labs → { label:
"Labs", tableLabel: "Lab", Icon: FlaskConical (from "lucide-react"), color:
"var(--overlay-labs)", glyph: "◆" }; incidents → { label: "Incidents", tableLabel: "Incident",
Icon: AlertTriangle, color: "var(--overlay-incidents)", glyph: "▲" }; procedures → { label:
"Procedures", tableLabel: "Procedure", Icon: ClipboardList, color: "var(--overlay-procedures)",
glyph: "■" }.
  </action>
  <acceptance_criteria>
    - api/types.ts exports `OverlayDataset` exactly once (`grep -c "export type OverlayDataset"` equals 1)
    - `AppliedFilters` type body contains both `overlayDataset?: OverlayDataset | null;` and `overlayState?: "on" | "off" | null;`
    - api/client.ts exports `getLabs`, `getIncidents`, `getProcedures`, each returning the correctly typed array Promise
    - overlayMeta.ts exports `OVERLAY_META` with exactly the keys `labs`, `incidents`, `procedures` and `OVERLAY_ORDER` with those three values in that order
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>The frontend wire contract mirrors backend/app/agent/schemas.py's new AppliedFilters fields byte-identically, three typed GET wrappers exist for the Phase 7 read routes, and one shared icon/color/glyph map exists for every downstream overlay render surface.</done>
</task>

<task type="auto">
  <name>Task 2: Store multi-select — store/filters.ts + store/filters.test.ts + index.css tokens</name>
  <files>frontend/src/store/filters.ts, frontend/src/store/filters.test.ts, frontend/src/index.css</files>
  <read_first>frontend/src/store/filters.ts (full file — every existing field/action pair is the direct template), frontend/src/store/filters.test.ts (full file — the INITIAL fixture and showAllData describe block are the direct templates), frontend/src/index.css (lines 1-70 — :root/.dark token block structure)</read_first>
  <action>
In store/filters.ts, import `OverlayDataset` from `../api/types` alongside the existing
`BPCategory, ChartId` named import (one import line, same source). Add `overlayDatasets:
Record&lt;OverlayDataset, boolean&gt;;` and `setOverlayDataset: (dataset: OverlayDataset, on:
boolean) =&gt; void;` to the `FilterState` interface, placed after `bpCategory`/`setBpCategory` and
before `showAllData`. In the `create&lt;FilterState&gt;` body, add the default `overlayDatasets: {
labs: false, incidents: false, procedures: false },` (D-07 neutral default) after `bpCategory:
"all",`, and the action `setOverlayDataset: (dataset, on) => set((s) => ({ overlayDatasets: {
...s.overlayDatasets, [dataset]: on } })),` after `setBpCategory`. Modify the existing
`showAllData` object literal to add `overlayDatasets: { labs: false, incidents: false,
procedures: false },` as a new key (D-08) — every other key in that literal stays unchanged.

In store/filters.test.ts, add `overlayDatasets: { labs: false, incidents: false, procedures:
false }` to the `INITIAL` fixture object. Add a new `describe("overlay multi-select
(D-01/D-07)")` block with two `it` cases: (1) `setOverlayDataset("labs", true)` then assert
`useFilters.getState().overlayDatasets` equals `{ labs: true, incidents: false, procedures:
false }` (proves independence); (2) `setOverlayDataset("incidents", true)` then
`setOverlayDataset("incidents", false)`, assert `overlayDatasets.incidents` is `false` again.
Extend the existing `showAllData (D-11)` test: before calling `showAllData()`, also call
`s0.setOverlayDataset("procedures", true)`; after `showAllData()`, add `expect(s.overlayDatasets
).toEqual({ labs: false, incidents: false, procedures: false })`.

In index.css, add three CSS custom properties to the `:root` block directly after the existing
`--cat-chip-text` line: `--overlay-labs: #6A3FA0;`, `--overlay-incidents: #A32672;`,
`--overlay-procedures: #5C6B1E;`. Add the matching dark-theme values to the `.dark` block
directly after its own `--cat-chip-text` line: `--overlay-labs: #C9A6EA;`,
`--overlay-incidents: #F0A8D0;`, `--overlay-procedures: #C9D48A;`. These exact hex pairs are
locked in 09-UI-SPEC.md's Color section (violet/magenta/olive hue families, chosen to sit
outside every existing clinical/chart hue range) — do not alter them.
  </action>
  <acceptance_criteria>
    - `FilterState` interface and `create()` body both reference `overlayDatasets` and `setOverlayDataset`
    - `showAllData`'s object literal contains `overlayDatasets: { labs: false, incidents: false, procedures: false }`
    - store/filters.test.ts's new describe block and the extended showAllData test pass
    - index.css's `:root` block contains all three `--overlay-*` light tokens and `.dark` contains all three dark variants
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/store/filters.test.ts</automated>
  </verify>
  <done>overlayDatasets is an independent multi-select field on the filter store, defaults to all-off, setOverlayDataset mutates one flag at a time, showAllData clears it alongside every other filter, and the three per-dataset color tokens exist in both themes.</done>
</task>

<task type="auto">
  <name>Task 3: Agent bridge — lib/agent.ts + lib/agent.test.ts + lib/agent-parity.test.ts (MANDATORY structural gate)</name>
  <files>frontend/src/lib/agent.ts, frontend/src/lib/agent.test.ts, frontend/src/lib/agent-parity.test.ts</files>
  <read_first>frontend/src/lib/agent.ts (full file — applyAgentFilters/PulseField are the direct edit targets), frontend/src/lib/agent.test.ts (full file — its beforeEach and the "reset marks all four pulse groups" test WILL BREAK once the reset block touches a 5th field, and MUST be updated in this task), frontend/src/lib/agent-parity.test.ts (full file — this file's structural STORE_ACTIONS/CASES gate fails the build unconditionally if not extended, per RESEARCH Pitfall 4)</read_first>
  <action>
In lib/agent.ts, extend `PulseField` to `export type PulseField = "chart" | "dateRange" |
"amPm" | "bpCategory" | "overlay";`. Inside `applyAgentFilters`, add a new field-check block
after the existing `bpCategory` check and before `const fields = [...touched];`: `if
(f.overlayDataset != null && f.overlayState != null) { s.setOverlayDataset(f.overlayDataset,
f.overlayState === "on"); touched.add("overlay"); }`. In the existing `if (f.reset)` block at
the top, add `touched.add("overlay");` alongside the existing four `touched.add(...)` calls
(D-08 — a reset pulses the overlay group too).

In lib/agent.test.ts: add `overlayDatasets: { labs: false, incidents: false, procedures: false
}` to the `beforeEach`'s `useFilters.setState({...})` object (keeps this file's own store reset
in lockstep with store/filters.test.ts's INITIAL). Rename the existing test "reset marks all
four pulse groups" to "reset marks all five pulse groups" and update its `expected` array from
`["amPm", "bpCategory", "chart", "dateRange"]` to `["amPm", "bpCategory", "chart", "dateRange",
"overlay"]` — THIS TEST WILL FAIL WITHOUT THIS UPDATE once Task 3's reset-block edit above
lands, since a 5th field is now touched on every reset. Add a new test in the
`applyAgentFilters` describe block: `applyAgentFilters({ overlayDataset: "labs", overlayState:
"on" })` then assert `useFilters.getState().overlayDatasets.labs` is `true` and
`useAgentPulse.getState().fields` includes `"overlay"`.

In lib/agent-parity.test.ts, make exactly three edits: (1) add `"setOverlayDataset"` as the 7th
entry in the `STORE_ACTIONS` array; (2) add `overlayDatasets: { labs: false, incidents: false,
procedures: false }` to the `beforeEach`'s `useFilters.setState({...})` object; (3) add a new
entry to the `CASES` array: `{ action: "setOverlayDataset", apply: () => applyAgentFilters({
overlayDataset: "labs", overlayState: "on" }), assert: () => expect(useFilters.getState(
).overlayDatasets.labs).toBe(true) }`. Do not touch the backend↔frontend token-parity block
(the `ChartToken`/`bpCategory` literal diff near the end of the file) — it does not assert
`AppliedFilters` field names, only literal values of two pre-existing fields, and is out of
scope for this task.
  </action>
  <acceptance_criteria>
    - `PulseField` union string contains `"overlay"`
    - `applyAgentFilters`'s body contains `s.setOverlayDataset(f.overlayDataset, f.overlayState`
    - the `if (f.reset)` block contains a `touched.add("overlay")` line
    - agent-parity.test.ts's `STORE_ACTIONS` array contains `"setOverlayDataset"`; its `beforeEach` setState object contains `overlayDatasets`; its `CASES` array contains an entry with action `"setOverlayDataset"`
    - agent.test.ts's renamed reset test asserts exactly 5 pulse fields including `"overlay"`
    - `cd frontend && npx vitest run src/lib/agent-parity.test.ts src/lib/agent.test.ts` passes, including agent-parity.test.ts's build-breaking "the covered actions equal the store's full mutating-action surface" assertion
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/agent-parity.test.ts src/lib/agent.test.ts</automated>
  </verify>
  <done>A server-composed overlayDataset/overlayState delta reaches setOverlayDataset through the single applyAgentFilters mutation surface, pulses the "overlay" group, and both agent.test.ts's reset-count regression and agent-parity.test.ts's structural STORE_ACTIONS/CASES gate are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server-composed AppliedFilters → zustand store | `applyAgentFilters()` mutates `useFilters` from OUTSIDE the React tree using only closed TS-union fields from an already-validated POST /agent response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | `applyAgentFilters`'s overlay branch | mitigate | The closed `OverlayDataset` TS union plus the `!= null` guard requiring BOTH `overlayDataset` and `overlayState` together (never a partial apply) constrains what reaches `setOverlayDataset`; runtime values ultimately originate from the backend's closed Pydantic Literal enums (Plan 09-01), so no free text ever reaches this bridge. |
| T-09-05 | Repudiation | none | accept | No new logging/audit surface introduced — matches `applyAgentFilters`'s existing no-op disposition for every other field. |

No new attack surface for the `index.css` token additions (pure decorative CSS custom
properties, no user-input path) or `overlayMeta.ts` (static config map, no runtime input).
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit && npx vitest run src/store/filters.test.ts src/lib/agent.test.ts src/lib/agent-parity.test.ts`
</verification>

<success_criteria>
- `overlayDatasets` defaults to all-`false`, `setOverlayDataset` mutates independently, and
  `showAllData` clears it (D-01/D-07/D-08)
- `applyAgentFilters({overlayDataset, overlayState})` reaches the store and pulses `"overlay"`
- `agent-parity.test.ts`'s structural gate is green with `setOverlayDataset` included
- Backend/frontend `AppliedFilters` field names for `overlayDataset`/`overlayState` match
  byte-for-byte
- Three `--overlay-*` tokens exist in both `:root` and `.dark`
</success_criteria>

<output>
Create `.planning/phases/09-multi-dataset-overlay-filtering/09-02-SUMMARY.md` when done
</output>
