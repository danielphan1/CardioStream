---
phase: 09-multi-dataset-overlay-filtering
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - frontend/src/components/OverlayToggle.tsx
  - frontend/src/components/OverlayToggle.test.tsx
  - frontend/src/components/OverlayEventsList.tsx
  - frontend/src/components/OverlayEventsList.test.tsx
autonomous: true
requirements: [OVERLAY-03, OVERLAY-04, OVERLAY-05, OVERLAY-06]

must_haves:
  truths:
    - "Each of the 3 overlay buttons (Labs/Incidents/Procedures) is independently aria-pressed and toggles only its own dataset flag on click (D-01)"
    - "When activeChart is bp_categories or am_pm_comparison, the toggle row shows an aria-live 'doesn't apply here' note while the 3 buttons stay fully clickable (never disabled) — D-01's independent-of-active-chart lock (OVERLAY-05)"
    - "An agent-driven overlay change visually pulses the toggle row's role=group exactly like FilterBar's other groups (voice/click parity)"
    - "OverlayEventsList renders nothing when no overlay dataset is ON, and renders a 4-column Date|Type|What happened|Notes table with 20-row paging when at least one is ON (OVERLAY-06)"
    - "One dataset's fetch error never suppresses another dataset's rows in the events table"
  artifacts:
    - path: frontend/src/components/OverlayToggle.tsx
      provides: "3-button role=group multi-select toggle row + doesn't-apply-here indicator + overlay sentence"
    - path: frontend/src/components/OverlayEventsList.tsx
      provides: "accessible paginated events table, conditional mount, per-type error isolation"
  key_links:
    - from: frontend/src/components/OverlayToggle.tsx
      to: frontend/src/store/filters.ts
      via: "onClick={() => setOverlayDataset(key, !overlayDatasets[key])}"
      pattern: "setOverlayDataset\\(key"
    - from: frontend/src/components/OverlayEventsList.tsx
      to: frontend/src/lib/overlayEvents.ts
      via: "mergeOverlayEvents / buildEmptyMessage / buildErrorMessage"
      pattern: "mergeOverlayEvents\\("
---

<objective>
Build the two new visible surfaces this phase adds to the dashboard: `OverlayToggle.tsx` (the
click half of OVERLAY-03's "by voice or click" multi-select, plus OVERLAY-05's "doesn't apply
here" indicator) and `OverlayEventsList.tsx` (OVERLAY-06's accessible list/table equivalent of
every overlaid event, independent of chart-marker limits).

Purpose: These are the codebase's first true multi-select toggle group and its accompanying
accessible-data-table twin — both are direct, read-verified extensions of `FilterBar.tsx`'s
role="group"/aria-pressed contract and `ReadingsTable.tsx`'s pagination/no-HTML-injection
contract respectively, per 09-PATTERNS.md. 09-UI-SPEC.md's Phase-Specific Component Contract is
the binding exact markup for both.
Output: A voice-and-click-reachable overlay toggle row and a conditionally-mounted, paginated,
accessible events table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-CONTEXT.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-UI-SPEC.md
@frontend/src/components/FilterBar.tsx
@frontend/src/components/ReadingsTable.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OverlayToggle.tsx + OverlayToggle.test.tsx</name>
  <files>frontend/src/components/OverlayToggle.tsx, frontend/src/components/OverlayToggle.test.tsx</files>
  <read_first>frontend/src/components/FilterBar.tsx (full file — role=group/aria-pressed/pulse pattern to mirror, especially the AM/PM segment lines ~127-144 and the pulse effect lines ~57-78), frontend/src/components/AddRecordPage.test.tsx (lines 70-115 — aria-pressed group test pattern), frontend/src/lib/agent.ts (post-Plan 09-02 — PulseField "overlay", useAgentPulse), frontend/src/store/filters.ts (post-Plan 09-02 — overlayDatasets/setOverlayDataset/activeChart), frontend/src/lib/overlayMeta.ts (post-Plan 09-02 — OVERLAY_META/OVERLAY_ORDER), frontend/src/lib/overlayEvents.ts (post-Plan 09-03 — buildOverlaySentence), 09-UI-SPEC.md's Phase-Specific Component Contract section for OverlayToggle.tsx (exact markup — binding, reproduce verbatim not paraphrase)</read_first>
  <behavior>
    - Clicking "Labs" when overlayDatasets.labs is false calls setOverlayDataset("labs", true); the button's aria-pressed flips to "true"; Incidents/Procedures aria-pressed stay unchanged.
    - Clicking an already-pressed button flips it back off (aria-pressed "false").
    - role="group" aria-label="Overlay events" contains exactly 3 buttons with accessible names "Labs"/"Incidents"/"Procedures".
    - When activeChart is "bp_categories", the note "Overlays aren't shown on this chart — switch to Blood Pressure or Pulse to see them." is present with aria-live="polite"; when activeChart is "bp_timeline" the note is absent.
    - The button row stays fully interactive (no `disabled` attribute on any button) regardless of activeChart.
    - The overlay sentence paragraph shows "No overlays selected" when all 3 are off, and "Labs, Incidents overlaid" when labs+incidents are on and procedures is off.
    - When useAgentPulse's seq increments with fields including "overlay", the role=group wrapper gains the pulse ring/motion-safe class.
  </behavior>
  <action>
Create OverlayToggle.tsx as a new component, mounted as a peer sibling of FilterBar (not nested
inside it — 09-UI-SPEC.md's resolved discretion item). Reproduce 09-UI-SPEC.md's exact markup: a
`&lt;section className="bg-[var(--color-sky)] p-4"&gt;` containing a `&lt;span&gt;` "Overlay:" label
(20px/700), a `role="group" aria-label="Overlay events"` div listing the 3 buttons in
`OVERLAY_ORDER` (imported from `../lib/overlayMeta` — do NOT re-declare a local map) with each
button's `aria-pressed={overlayDatasets[key]}`, `onClick={() => setOverlayDataset(key,
!overlayDatasets[key])}`, pressed styling via inline `style={{ backgroundColor: color,
borderColor: color }}` (the fill is per-dataset via `OVERLAY_META[key].color`, mirroring
FilterBar's own `categoryColor(cat)` inline-style technique for its category chip row — not a
`bg-[var(--color-accent)]` class), each button containing the `Icon` at `size={20}`
`aria-hidden="true"` plus the `label` text, `min-h-12` target per every other control on this
site.

Wrap the `role="group"` div in the same `useAgentPulse`/`pulseClass("overlay")` treatment
FilterBar.tsx already implements verbatim (subscribe to `seq`/`fields`, local `pulsing` state, a
1500ms `setTimeout` clear).

Render the "doesn't apply here" block: read `activeChart` from `useFilters`, compute
`overlayApplies = activeChart === "bp_timeline" || activeChart === "pulse_trend"`, apply
`opacity-60` to the button-row wrapper only (not the "Overlay:" label, not the note) when
`!overlayApplies`, and render the note paragraph (`aria-live="polite"`, Body 18px, exact copy:
"Overlays aren't shown on this chart — switch to Blood Pressure or Pulse to see them.") only
when `!overlayApplies` — the buttons remain fully clickable in both states (D-01: overlay state
is independent of active chart, so a caregiver can pre-set overlays before switching back to a
timeline chart).

Render the overlay-state sentence via `buildOverlaySentence(overlayDatasets)` from
`../lib/overlayEvents`, in an `aria-live="polite"` paragraph matching FilterBar's D-20 sentence
placement/styling.

Write OverlayToggle.test.tsx covering every item in `&lt;behavior&gt;` above, using
`render`/`screen`/`fireEvent` from `@testing-library/react`, resetting `useFilters`/
`useAgentPulse` state directly via `.setState(...)` in a `beforeEach` (matching
store/filters.test.ts's INITIAL-fixture convention).
  </action>
  <acceptance_criteria>
    - OverlayToggle.tsx exports a component with zero required props
    - The file imports `OVERLAY_META`/`OVERLAY_ORDER` from `../lib/overlayMeta` (not a locally-redeclared map)
    - The doesn't-apply note text matches "Overlays aren't shown on this chart — switch to Blood Pressure or Pulse to see them." byte-for-byte
    - `cd frontend && npx vitest run src/components/OverlayToggle.test.tsx` passes every `&lt;behavior&gt;` case
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/OverlayToggle.test.tsx</automated>
  </verify>
  <done>A voice-and-click-reachable 3-button multi-select toggle row exists, independently aria-pressed per button, visually indicates non-applicability on BP Categories/AM-PM without disabling itself, and participates in the agent-pulse visual-parity system.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OverlayEventsList.tsx + OverlayEventsList.test.tsx</name>
  <files>frontend/src/components/OverlayEventsList.tsx, frontend/src/components/OverlayEventsList.test.tsx</files>
  <read_first>frontend/src/components/ReadingsTable.tsx (full file — pagination/sr-only-caption/plain-text-node template), frontend/src/components/ReadingsTable.test.tsx (full file — rowCount/pagination/header test template), frontend/src/components/AgentStatusBanner.tsx (full file — the "if (!showBanner) return null" null-render convention this component reuses), frontend/src/lib/overlayEvents.ts (post-Plan 09-03 — OverlayEvent/mergeOverlayEvents/buildEmptyMessage/buildErrorMessage), frontend/src/lib/overlayMeta.ts (post-Plan 09-02 — OVERLAY_META for the Type-column badge), 09-UI-SPEC.md's Phase-Specific Component Contract section for OverlayEventsList.tsx (exact markup/columns/copy rules — binding)</read_first>
  <behavior>
    - Given all three of {labs,incidents,procedures}.enabled are false, the component renders nothing (container has no children) — D-07 default, mirrors AgentStatusBanner's null-render.
    - Given labs.enabled=true with 25 labs OverlayEvents and incidents/procedures disabled, the table renders 20 rows + "Show 20 more"; clicking it reveals all 25 and shows "Showing all 25 overlaid events".
    - Events from multiple enabled types merge and sort newest-first by ts, interleaved correctly regardless of input array order.
    - Given labs.enabled=true with events=[] and isError=false, and no other type enabled, "No labs recorded in this date range." renders in place of the table.
    - Given labs.enabled=true and incidents.enabled=true, labs.isError=true (events=[]) and incidents has 2 events: a role="alert" line reads "Couldn't load labs — try again in a moment." AND the table still renders the 2 incident rows — one failed fetch never blocks the other dataset.
    - The table has exactly 4 columnheader-role elements named Date, Type, What happened, Notes.
    - A row's Notes cell renders `notes` as plain text (never dangerouslySetInnerHTML) and is blank when notes is null.
    - The Type column shows the per-type icon badge with the type's color token, matching ReadingsTable's chip shape.
  </behavior>
  <action>
Create OverlayEventsList.tsx accepting `{ labs, incidents, procedures }` where each of the three
props is `{ enabled: boolean; events: OverlayEvent[]; isError: boolean }` — the caller (Plan
09-06's App.tsx) is responsible for passing `events: []` and the correct `isError` per TanStack
Query result; this component does no fetching itself.

Compute `onTypes` = `OVERLAY_ORDER` entries where `.enabled` is true; if `onTypes.length === 0`
return `null` immediately (mirrors AgentStatusBanner's `if (!showBanner) return null`). Compute
`errorNotes` = `onTypes.filter(t => props[t].isError).map(t => buildErrorMessage(t))`. Compute
`merged` via `mergeOverlayEvents(props.labs.isError ? [] : props.labs.events, props.incidents
.isError ? [] : props.incidents.events, props.procedures.isError ? [] : props.procedures
.events)`. Compute `healthyOnTypes = onTypes.filter(t => !props[t].isError)`; when
`merged.length === 0 && healthyOnTypes.length > 0`, compute `emptyMessage =
buildEmptyMessage(healthyOnTypes)` and render it in place of the table; when `merged.length ===
0 && healthyOnTypes.length === 0` (every ON type is erroring), render only `errorNotes` with no
table and no empty-message line — the section still mounts (onTypes.length > 0) but shows
nothing beyond the error alerts.

Reproduce 09-UI-SPEC.md's exact markup skeleton: `&lt;section aria-label="Overlaid events"&gt;`
containing an `&lt;h2&gt;` "Overlaid events" (24px/700, matches App.tsx's existing "Readings" h2
exactly), the `errorNotes.map(msg => &lt;p key={msg} role="alert" ...&gt;{msg}&lt;/p&gt;)` block, then
either the `emptyMessage` paragraph or the `&lt;section className="rounded-lg
bg-[var(--color-sky)] p-6"&gt;&lt;table&gt;...&lt;/table&gt;...&lt;/section&gt;` block (`&lt;caption
className="sr-only"&gt;Overlaid events&lt;/caption&gt;`, 4 `&lt;th scope="col"&gt;` headers
"Date"/"Type"/"What happened"/"Notes", `&lt;tbody&gt;` rows). Apply ReadingsTable's exact pagination
discipline: `PAGE_SIZE = 20`, `useState(PAGE_SIZE)` for `visible`, a `useEffect` resetting
`visible` to `PAGE_SIZE` whenever `merged`'s array identity changes, `merged.slice(0, visible)`
for shown rows, "Show 20 more" button / "Showing all {N} overlaid events" footer text — reused
verbatim, same Tailwind classes as ReadingsTable.tsx.

Each row renders `dateCell`, a Type badge (`&lt;span className="inline-block rounded-full px-3
py-1 text-lg"&gt;` with `style={{ backgroundColor: OVERLAY_META[evt.type].color, color: "white"
}}` containing the `Icon` + `OVERLAY_META[evt.type].tableLabel`), `whatHappened` as plain text,
and `notes` as a plain React text node (empty string when null) — never
`dangerouslySetInnerHTML` (T-02-08 discipline, matches ReadingsTable's `notes` row treatment).

Write OverlayEventsList.test.tsx covering every `&lt;behavior&gt;` item above, following
ReadingsTable.test.tsx's `rowCount()`/fixture-factory conventions adapted to a
`makeOverlayEvent(i, overrides)` factory and the new props shape (constructing `{ enabled,
events, isError }` objects directly per test case — no real TanStack Query/hooks involved, this
is a pure presentational-component test).
  </action>
  <acceptance_criteria>
    - `container.firstChild === null` when all three `enabled` flags are false
    - The rendered table has exactly 4 `columnheader`-role elements named "Date","Type","What happened","Notes"
    - `cd frontend && npx vitest run src/components/OverlayEventsList.test.tsx` passes every `&lt;behavior&gt;` case, including the per-type error-isolation case
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/OverlayEventsList.test.tsx</automated>
  </verify>
  <done>A conditionally-mounted, paginated, accessible 4-column events table exists satisfying OVERLAY-06, isolates per-dataset fetch errors, and never renders raw HTML from user-authored notes/labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Caregiver-authored free text (notes/test_name/incident_type/procedure_name, entered via Phase 8's forms, already stored in the DB) | Rendered into OverlayEventsList's table and consumed by OverlayToggle's own static UI-authored copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering (stored XSS) | OverlayEventsList's Notes/What-happened columns | mitigate | Every field renders exclusively as a plain React text node (JSX `{value}` interpolation, which React auto-escapes) — never `dangerouslySetInnerHTML`, matching ReadingsTable.tsx's existing T-02-08 discipline verbatim; no new HTML-injection surface. |
| T-09-09 | Denial of Service (UI layout) | Extremely long caregiver-entered notes/labels | accept | Identical exposure already exists for `Reading.notes` in ReadingsTable.tsx today; no new mitigation needed, matches existing project risk acceptance. |
| T-09-10 | Information Disclosure | none | accept | Overlay data is already Bearer-gated Phase 7 data; this plan only adds presentation, no new data exposure. |

No new auth/session surface (V2/V3/V4 unaffected).
</threat_model>

<verification>
`cd frontend && npx vitest run src/components/OverlayToggle.test.tsx src/components/OverlayEventsList.test.tsx`
</verification>

<success_criteria>
- OverlayToggle: 3 independently-pressable buttons, doesn't-apply-here indicator that never
  disables, agent-pulse parity, sentence copy exact
- OverlayEventsList: null-render at default, correct paging/sort/columns, per-type error
  isolation, no raw-HTML rendering
</success_criteria>

<output>
Create `.planning/phases/09-multi-dataset-overlay-filtering/09-04-SUMMARY.md` when done
</output>
