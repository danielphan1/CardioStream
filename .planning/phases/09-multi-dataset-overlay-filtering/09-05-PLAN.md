---
phase: 09-multi-dataset-overlay-filtering
plan: 05
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - frontend/src/components/charts/BPTimeline.tsx
  - frontend/src/components/charts/PulseTrend.tsx
  - frontend/src/components/ChartDeck.tsx
autonomous: true
requirements: [OVERLAY-04]

must_haves:
  truths:
    - "Toggling an overlay dataset ON renders one ReferenceLine per event on whichever of BP Timeline / Pulse Trend is the current hero chart (D-06)"
    - "Overlay markers NEVER render on ChartDeck's mini preview cards, only on the hero slot (D-02)"
    - "Each marker is shape+color distinguishable, not color alone (D-05) — diamond/violet for labs, triangle/magenta for incidents, square/olive for procedures"
    - "Markers render AFTER the existing Line/ReferenceArea elements in JSX so they sit visually on top (z-order discipline both chart files already document)"
  artifacts:
    - path: frontend/src/components/charts/BPTimeline.tsx
      provides: "overlayEvents?: OverlayEvent[] prop + hero-gated ReferenceLine marker block"
    - path: frontend/src/components/charts/PulseTrend.tsx
      provides: "overlayEvents?: OverlayEvent[] prop + hero-gated ReferenceLine marker block"
    - path: frontend/src/components/ChartDeck.tsx
      provides: "overlayEvents threaded through ChartDeckProps into the bp_timeline/pulse_trend registry entries only"
  key_links:
    - from: frontend/src/components/ChartDeck.tsx
      to: frontend/src/components/charts/BPTimeline.tsx
      via: "hero: ({ readings, overlayEvents }) => <BPTimeline readings={readings} variant=\"hero\" overlayEvents={overlayEvents} />"
      pattern: "overlayEvents=\\{overlayEvents\\}"
---

<objective>
Render overlay event markers on the two timeline hero charts (OVERLAY-04's core visual
requirement: "selected dataset types appear overlaid together on the BP Timeline and Pulse
Trend charts... rather than living in separate silos"), using the locked `ReferenceLine`
approach (D-06) — full-height vertical lines with a shape+color glyph, never a `Scatter` series.

Purpose: This is the chart-rendering half of the phase; BP Categories and AM/PM Comparison are
explicitly NOT touched (09-PATTERNS.md confirms 09-UI-SPEC.md resolved OVERLAY-05's indicator to
live inside `OverlayToggle.tsx`, not per-chart). `ChartDeck.tsx`'s existing hero/mini registry
pattern threads the new prop through without touching the two untouched chart files' registry
entries.
Output: `BPTimeline`/`PulseTrend` accept an `overlayEvents` prop and render it hero-only;
`ChartDeck` threads that prop from its own props into the bp_timeline/pulse_trend registry
entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-CONTEXT.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-RESEARCH.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-UI-SPEC.md
@frontend/src/components/charts/BPTimeline.tsx
@frontend/src/components/charts/PulseTrend.tsx
@frontend/src/components/ChartDeck.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: BPTimeline.tsx — overlayEvents prop + ReferenceLine markers</name>
  <files>frontend/src/components/charts/BPTimeline.tsx</files>
  <read_first>frontend/src/components/charts/BPTimeline.tsx (full file — the `bandLabel` plain-object label pattern lines ~46-53 and the file's own documented JSX z-order discipline lines ~5-6 govern this edit), frontend/src/components/charts/PulseTrend.tsx (its existing bradycardia ReferenceLine, lines ~81-95, is the direct ReferenceLine-usage template), frontend/src/lib/overlayEvents.ts (post-Plan 09-03 — OverlayEvent type), frontend/src/lib/overlayMeta.ts (post-Plan 09-02 — OVERLAY_META.color/glyph), 09-UI-SPEC.md's Marker & Icon Contract + "Chart markers on BPTimeline.tsx / PulseTrend.tsx" sections (locked implementation, including the deterministic fallback path)</read_first>
  <action>
Add `overlayEvents?: OverlayEvent[];` to `BPTimelineProps` (import `OverlayEvent` from
`../../lib/overlayEvents`). Implement the rendering using the deterministic, already-proven
plain-object `label` form (glyph character, NOT an attempted icon-as-function render — 09-UI-SPEC.md
explicitly designates this as a pre-approved equal alternative to the icon-function path, not a
degraded fallback, and it is the only implementation with confirmed Recharts behavior in this
codebase per RESEARCH's Pitfall 3/Assumption A4): inside `&lt;LineChart&gt;`, AFTER the two existing
`&lt;Line dataKey="systolic"...&gt;`/`&lt;Line dataKey="diastolic"...&gt;` elements and their closing
tags (i.e. as the LAST children, immediately before `&lt;/LineChart&gt;`), add `{hero &amp;&amp;
overlayEvents?.map((evt) => { const meta = OVERLAY_META[evt.type]; return (&lt;ReferenceLine
key={`${evt.type}-${evt.id}`} x={evt.ts} stroke={meta.color} strokeWidth={2}
ifOverflow="extendDomain" label={{ value: meta.glyph, position: "top", fontSize: 14, fill:
meta.color }} /&gt;); })}` — import `ReferenceLine` (add to the existing `from "recharts"` import)
and `OVERLAY_META` (from `../../lib/overlayMeta`). The `hero &amp;&amp;` gate matches this file's
existing "hero-only" idiom (used for `Tooltip`, band labels) and enforces D-02 (never on mini —
`variant` still controls `hero` exactly as today). `ifOverflow="extendDomain"` (RESEARCH's
primary recommendation) lets an overlay event outside the readings' own min/max still render by
widening the XAxis's existing dynamic `domain={["dataMin","dataMax"]}` rather than requiring a
manual domain-union computation — flagged for the Plan 09-06 manual-verification checkpoint
(RESEARCH Pitfall 2, MEDIUM confidence).
  </action>
  <acceptance_criteria>
    - `BPTimelineProps` contains `overlayEvents?: OverlayEvent[]`
    - The `ReferenceLine` marker block appears after both `&lt;Line dataKey="systolic"&gt;` and `&lt;Line dataKey="diastolic"&gt;` in file order, immediately before `&lt;/LineChart&gt;`
    - The marker block is gated on `hero &amp;&amp;`
    - Each `ReferenceLine`'s `label` is the plain-object form (`{ value, position, fontSize, fill }`), never a nested `&lt;Label content={...}&gt;`
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>BPTimeline renders one solid, shape+color-distinguishable ReferenceLine per overlaid event on the hero chart only, positioned by the same numeric-time axis the reading lines already use.</done>
</task>

<task type="auto">
  <name>Task 2: PulseTrend.tsx — identical overlayEvents treatment</name>
  <files>frontend/src/components/charts/PulseTrend.tsx</files>
  <read_first>frontend/src/components/charts/PulseTrend.tsx (full file — its own existing bradycardia `ReferenceLine`, lines ~81-95, is the closest in-file precedent), Task 1's completed BPTimeline.tsx edit (same shape, applied here), frontend/src/lib/overlayEvents.ts (OverlayEvent type), frontend/src/lib/overlayMeta.ts (OVERLAY_META)</read_first>
  <action>
Add `overlayEvents?: OverlayEvent[];` to `PulseTrendProps` (import `OverlayEvent` from
`../../lib/overlayEvents`, `OVERLAY_META` from `../../lib/overlayMeta`). Add the identical
marker-rendering block from Task 1, placed AFTER the existing `&lt;Line dataKey="pulse"
.../&gt;` and immediately before `&lt;/LineChart&gt;`, gated on `hero &amp;&amp;`. The overlay
`ReferenceLine`s differ from this file's existing bradycardia `ReferenceLine` (y={60},
`strokeDasharray="6 4"`) in three UI-SPEC-locked ways: `x={evt.ts}` (a vertical line, not
horizontal `y={60}`), solid (no `strokeDasharray`, `strokeWidth={2}`) so overlay events read as
visually distinct from the dashed clinical-threshold line already on this chart, and
`ifOverflow="extendDomain"` (the fixed bradycardia `y` line has no `ifOverflow` prop since a
fixed y-value on a fixed `[30,120]` Y-domain can't overflow — this is a new consideration
specific to the overlay lines' dynamic `x` positioning against the existing dynamic XAxis
domain).
  </action>
  <acceptance_criteria>
    - `PulseTrendProps` contains `overlayEvents?: OverlayEvent[]`
    - The `ReferenceLine` marker block appears after `&lt;Line dataKey="pulse"&gt;` in file order, immediately before `&lt;/LineChart&gt;`, gated on `hero &amp;&amp;`
    - The overlay `ReferenceLine`s use `strokeWidth={2}` with no `strokeDasharray` (solid), distinct from the existing bradycardia line's `strokeDasharray="6 4"`
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>PulseTrend renders the identical overlay marker treatment as BPTimeline, visually distinct from its own existing dashed bradycardia reference line.</done>
</task>

<task type="auto">
  <name>Task 3: ChartDeck.tsx — thread overlayEvents through the registry</name>
  <files>frontend/src/components/ChartDeck.tsx</files>
  <read_first>frontend/src/components/ChartDeck.tsx (full file — `ChartDeckProps`, the `data` object construction, and the fixed `CHART_REGISTRY` array are all direct edit targets), Task 1/2's completed BPTimeline.tsx/PulseTrend.tsx `overlayEvents` prop additions</read_first>
  <action>
Add `overlayEvents?: OverlayEvent[];` to `ChartDeckProps` (import `OverlayEvent` from
`../lib/overlayEvents`). Change the `ChartDeck({ readings, stats })` function signature to
`ChartDeck({ readings, stats, overlayEvents })`, and change `const data: ChartDeckProps = {
readings, stats };` to `const data: ChartDeckProps = { readings, stats, overlayEvents };`.
Modify ONLY the `bp_timeline` and `pulse_trend` entries in `CHART_REGISTRY`: change their
`hero`/`mini` lambdas from `({ readings }) => &lt;BPTimeline readings={readings} variant="hero"
/&gt;` (etc.) to destructure and forward `overlayEvents` too — e.g. `hero: ({ readings,
overlayEvents }) => &lt;BPTimeline readings={readings} variant="hero"
overlayEvents={overlayEvents} /&gt;` and the matching `mini` variant (passing `overlayEvents`
into the mini call is harmless — BPTimeline/PulseTrend already self-gate marker rendering on
`hero &amp;&amp;` internally, so the mini simply ignores it, exactly matching this file's
existing convention where minis already receive the full `ChartDeckProps` and self-gate their
own hero-only features like Tooltip and band labels). Do NOT modify the `bp_categories` or
`am_pm_comparison` registry entries — `CategoryBars`/`AmPmComparison` do not accept an
`overlayEvents` prop and are confirmed out of scope for this phase (09-PATTERNS.md).
  </action>
  <acceptance_criteria>
    - `ChartDeckProps` contains `overlayEvents?: OverlayEvent[]`
    - `ChartDeck`'s destructured parameters and its `data` object both include `overlayEvents`
    - Only the `bp_timeline` and `pulse_trend` registry entries' `hero`/`mini` lambdas reference `overlayEvents`; the `bp_categories`/`am_pm_comparison` entries are byte-identical to before this task
    - `cd frontend && npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>ChartDeck accepts and threads overlayEvents into exactly the two timeline chart registry entries, leaving the other two chart types byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan renders already-shaped, already-typed `OverlayEvent[]` data (produced by Plan 09-03's pure module, itself reading from already Bearer-gated Phase 7 data) as SVG chart primitives — no new user input, no new data source, no new text rendering path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | none | Chart marker rendering | accept | No new attack surface — this plan only lays out already-validated numeric (`ts`) and closed-enum (`type`) values as Recharts primitives; no free-text field from `LabResult`/`Incident`/`Procedure` is rendered on-chart (that only happens in Plan 09-04's `OverlayEventsList`, which has its own threat register). |

No new trust boundary is crossed in this plan.
</threat_model>

<verification>
`cd frontend && npx tsc --noEmit` — Recharts renders 0×0 in jsdom (documented project
constraint), so marker shape/color/position correctness has no automated test path; visual
verification is deferred to Plan 09-06's mandatory manual checkpoint (see 09-VALIDATION.md's
Manual-Only Verifications table).
</verification>

<success_criteria>
- BPTimeline and PulseTrend both accept `overlayEvents` and render hero-only, solid,
  shape+color-distinct `ReferenceLine`s using the proven plain-object label form
- ChartDeck threads `overlayEvents` into exactly the two timeline registry entries
- `cd frontend && npx tsc --noEmit` is clean across all three files
</success_criteria>

<output>
Create `.planning/phases/09-multi-dataset-overlay-filtering/09-05-SUMMARY.md` when done
</output>
