---
phase: 09-multi-dataset-overlay-filtering
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/OverlayEventsList.tsx
  - frontend/src/components/OverlayEventsList.test.tsx
  - frontend/src/components/OverlayToggle.tsx
  - frontend/src/components/OverlayToggle.test.tsx
  - frontend/src/index.css
  - frontend/src/lib/agent-parity.test.ts
autonomous: true
requirements: [OVERLAY-03, OVERLAY-04, OVERLAY-06]
gap_closure: true

must_haves:
  truths:
    - "Toggling a dataset OFF while another dataset stays ON immediately removes its events from the merged chart-marker array and from OverlayEventsList's rendered table rows — stale TanStack Query cache data for the toggled-off dataset never survives past the toggle (closes Gap 1 / CR-1)"
    - "Dark-mode active overlay toggle buttons and the OverlayEventsList Type-column badge render ink text that clears WCAG's 3:1 UI-component floor and 4.5:1 normal-text floor against their per-dataset fill color — no hardcoded white text remains on the light-pastel dark-mode fills (closes Gap 2 / CR-2)"
  artifacts:
    - path: frontend/src/App.tsx
      provides: "Dashboard()'s labsEvents/incidentsEvents/proceduresEvents/overlayEvents computed via useMemo, each short-circuiting to [] when its overlayDatasets.{type} flag is false, regardless of stale cached query data"
    - path: frontend/src/components/OverlayEventsList.tsx
      provides: "merged useMemo gated on {type}.enabled (defense-in-depth, in addition to the existing isError gate) + theme-aware --overlay-chip-text ink token on the Type badge (no hardcoded color: \"white\")"
    - path: frontend/src/components/OverlayToggle.tsx
      provides: "active-button inline style uses var(--overlay-chip-text) for text ink; activeClass no longer hardcodes text-white"
    - path: frontend/src/index.css
      provides: "--overlay-chip-text token pair (:root #FFFFFF / .dark #0B1626), mirroring the existing --cat-chip-text pattern"
  key_links:
    - from: frontend/src/App.tsx
      to: frontend/src/store/filters.ts
      via: "labsEvents/incidentsEvents/proceduresEvents each gate on overlayDatasets.{type} before calling labsToEvents/incidentsToEvents/proceduresToEvents"
      pattern: "overlayDatasets\\.labs \\?"
    - from: frontend/src/components/OverlayEventsList.tsx
      to: frontend/src/components/OverlayEventsList.tsx
      via: "merged useMemo requires labs.enabled (and incidents.enabled/procedures.enabled) alongside !isError before including a dataset's events"
      pattern: "labs\\.enabled"
    - from: frontend/src/components/OverlayToggle.tsx
      to: frontend/src/index.css
      via: "active button inline style references var(--overlay-chip-text)"
      pattern: "overlay-chip-text"
    - from: frontend/src/components/OverlayEventsList.tsx
      to: frontend/src/index.css
      via: "Type badge inline style references var(--overlay-chip-text)"
      pattern: "overlay-chip-text"
---

<objective>
Close the 2 BLOCKER-class gaps found by 09-VERIFICATION.md (score: 4/5 must-haves) so Phase 9 can
be re-verified clean: (1) stale TanStack Query cache data surviving a dataset toggle-off when
mixed with another active dataset, which silently contradicts the toggle button's own
`aria-pressed="false"` state on both the chart markers and the accessible events table; and
(2) a dark-mode WCAG contrast failure (1.58:1-2.07:1, all failing the 3:1 floor) caused by
hardcoded white text over light-pastel dark-mode overlay fills, violating CLAUDE.md's
non-negotiable "high contrast" constraint. Also closes WR-3 (agent-parity.test.ts's missing
DatasetToken/OverlayDataset reachability coverage) as a low-cost bundled fix.

Purpose: Phase 9's core "mix and match datasets reliably" value proposition and its accessibility
floor are both currently broken in already-shipped, already-tested code — these are narrow,
mechanically-scoped fixes to existing wiring/tokens, not new features or a redesign.
Output: App.tsx and OverlayEventsList.tsx correctly gate every per-dataset event array on its own
toggle state (not just query error state); OverlayToggle.tsx and OverlayEventsList.tsx use a new
theme-aware `--overlay-chip-text` ink token instead of hardcoded white; agent-parity.test.ts gains
the missing DatasetToken/OverlayDataset parity coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-VERIFICATION.md
@.planning/phases/09-multi-dataset-overlay-filtering/09-REVIEW.md
@frontend/src/App.tsx
@frontend/src/components/OverlayEventsList.tsx
@frontend/src/components/OverlayToggle.tsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Gap 1 — gate overlay event arrays on toggle state, not just query error state</name>
  <files>frontend/src/App.tsx, frontend/src/components/OverlayEventsList.tsx, frontend/src/components/OverlayEventsList.test.tsx</files>
  <read_first>frontend/src/App.tsx (full file — Dashboard()'s overlay derivation block, currently around lines 66-79, is the primary edit target), .planning/phases/09-multi-dataset-overlay-filtering/09-VERIFICATION.md (frontmatter `gaps:` block, and the "Data-Flow Trace (Level 4)" table — the authoritative description of this defect and exactly what's missing), .planning/phases/09-multi-dataset-overlay-filtering/09-REVIEW.md (CR-1 section, lines 59-113 — reproduction steps and suggested fix), frontend/src/components/OverlayEventsList.tsx (full file — the `merged` useMemo, currently around lines 61-76, is the second edit target), frontend/src/hooks/useLabs.ts (the `enabled` TanStack Query flag whose cache-retention behavior — stops refetching but never clears `data` — is the documented root cause; useIncidents.ts/useProcedures.ts are identical in shape), frontend/src/components/OverlayEventsList.test.tsx (existing `OFF`/`makeOverlayEvent`/`rowCount()` fixture conventions — the new regression test must follow these, not invent new ones)</read_first>
  <action>
In `frontend/src/App.tsx`'s `Dashboard()`: rename the local `const window = { start_date:
resolved.start_date, end_date: resolved.end_date };` to `const dateWindow = { ... }` (same shape,
new name only) and update its three call sites (`useLabs(dateWindow, ...)`, `useIncidents
(dateWindow, ...)`, `useProcedures(dateWindow, ...)`) — this removes the accidental shadowing of
the global `window` object (09-REVIEW.md IN-1) while touching this exact block for the real fix
below.

Add `useMemo` to the existing `import type { ReactNode } from "react";` line (becomes `import {
useMemo } from "react";` plus the existing `import type { ReactNode } from "react";`, or combine
into one import statement — either is fine as long as `useMemo` is imported from "react").

Replace the current un-gated derivations (`const labsEvents = labsToEvents(labs.data ?? []);` and
its `incidentsEvents`/`proceduresEvents`/`overlayEvents` siblings) with `useMemo`-wrapped versions
that short-circuit to `[]` when the matching `overlayDatasets.{type}` flag is false, regardless of
what `labs.data`/`incidents.data`/`procedures.data` currently holds (this is the actual gap: query
`enabled: false` stops future fetches but does NOT clear previously-cached `data`, so an un-gated
derivation keeps producing non-empty events after toggle-off). Concretely: `labsEvents` becomes a
`useMemo` computing `overlayDatasets.labs ? labsToEvents(labs.data ?? []) : []`, keyed on
`[overlayDatasets.labs, labs.data]`; mirror this exactly for `incidentsEvents`
(`overlayDatasets.incidents`, `incidents.data`) and `proceduresEvents` (`overlayDatasets.procedures`,
`procedures.data`). Compute `overlayEvents` as a `useMemo` calling `mergeOverlayEvents(labsEvents,
incidentsEvents, proceduresEvents)`, keyed on `[labsEvents, incidentsEvents, proceduresEvents]` —
this also closes 09-REVIEW.md's WR-2 (unmemoized arrays defeating OverlayEventsList's own
pagination-reset guard on unrelated re-renders), since TanStack Query keeps `.data` referentially
stable via structural sharing when content is unchanged, so these `useMemo`s only recompute when
the toggle flag or the underlying data actually changes.

In `frontend/src/components/OverlayEventsList.tsx`'s `merged` useMemo: add `labs.enabled`,
`incidents.enabled`, and `procedures.enabled` as defense-in-depth gates alongside the existing
`!isError` checks, so each dataset's contribution becomes `labs.enabled && !labs.isError ?
labs.events : []` (and the `incidents`/`procedures` equivalents), and add `labs.enabled`,
`incidents.enabled`, `procedures.enabled` to the `useMemo`'s dependency array alongside the
existing six entries. This is the second half of the fix per 09-VERIFICATION.md's explicit
instruction: `App.tsx` is the primary gate, `OverlayEventsList` is the reusable/testable unit that
must independently enforce the same invariant so the component is correct even if some future
caller forgets the `App.tsx`-level gate.

Add a new regression test to `OverlayEventsList.test.tsx` in a new `describe("stale-cache
toggle-off regression (Gap 1 / CR-1)", ...)` block: render with `labs={{ enabled: true, events:
[oneLabEvent], isError: false }}` and `incidents=OFF`/`procedures=OFF`, assert the lab event's
`whatHappened` text is present; then call the `render` result's `rerender` with `labs={{ enabled:
false, events: [the SAME stale lab event array — simulating TanStack Query's cache retaining the
prior fetch's data after `enabled` flips to false], isError: false }}` and `incidents={{ enabled:
true, events: [oneIncidentEvent], isError: false }}` — assert the stale lab event's text is now
absent (`screen.queryByText(...)).not.toBeInTheDocument()`), the incident event's text is present,
and `rowCount()` equals 2 (header + the 1 incident row only). This is exactly the "toggle on, fetch
succeeds, toggle off while a second dataset stays on" scenario 09-VERIFICATION.md confirms no
existing test covers (the current `OFF` fixture always pairs `events: []` with `enabled: false`,
which only exercises the initial-state case, never the stale-cache case).
  </action>
  <acceptance_criteria>
    - `frontend/src/App.tsx` no longer contains a local `const window = ...` binding (renamed to `dateWindow`) — `grep -c "const window ="  frontend/src/App.tsx` returns 0
    - `frontend/src/App.tsx`'s `labsEvents`/`incidentsEvents`/`proceduresEvents`/`overlayEvents` are each computed via `useMemo` and each per-type array's computation is gated on its `overlayDatasets.{type}` flag — `grep -c "overlayDatasets.labs ?" frontend/src/App.tsx` returns 1 (and equivalently for `.incidents`/`.procedures`)
    - `frontend/src/components/OverlayEventsList.tsx`'s `merged` useMemo dependency array includes `labs.enabled`, `incidents.enabled`, `procedures.enabled` — `grep -c "labs.enabled" frontend/src/components/OverlayEventsList.tsx` returns 2 (once in the computation, once in the deps array)
    - The new regression test in `OverlayEventsList.test.tsx` passes and fails if the `.enabled` gate is reverted (verify by temporarily reverting the gate and confirming the new test fails, then re-apply the fix)
    - `cd frontend && npx tsc --noEmit` passes with no errors
    - `cd frontend && npx vitest run` — full suite passes (regression check across every prior phase's frontend tests)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Toggling any of the 3 overlay datasets OFF while another stays ON immediately removes its markers from the merged chart-marker array and its rows from OverlayEventsList's table, verified by a new automated regression test simulating the exact stale-cache scenario 09-VERIFICATION.md identified as untested.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Gap 2 — theme-aware ink token replaces hardcoded white text in dark mode</name>
  <files>frontend/src/index.css, frontend/src/components/OverlayToggle.tsx, frontend/src/components/OverlayToggle.test.tsx, frontend/src/components/OverlayEventsList.tsx, frontend/src/components/OverlayEventsList.test.tsx</files>
  <read_first>frontend/src/index.css (full file — the `--cat-chip-text` token pair at lines 43 (`:root`, `#FFFFFF`) and 72 (`.dark`, `#0B1626`) is the exact pattern to mirror; the "Overlay dataset marker/toggle colors" blocks at lines 45-49 and 74-77 are where the new token is added), frontend/src/components/OverlayToggle.tsx (full file — `activeClass` at line ~30 hardcodes `text-white`; the button's inline `style` at lines ~77-78 sets `backgroundColor`/`borderColor` only), frontend/src/components/OverlayEventsList.tsx (the Type-badge inline `style` at lines ~163-168 hardcodes `color: "white"`), .planning/phases/09-multi-dataset-overlay-filtering/09-REVIEW.md (CR-2 section, lines 116-172 — independently-recomputed WCAG contrast ratios: 2.07:1 / 1.87:1 / 1.58:1, all failing the 3:1 UI-component floor), frontend/src/components/OverlayToggle.test.tsx and frontend/src/components/OverlayEventsList.test.tsx (existing test conventions for the new style-assertion additions)</read_first>
  <action>
In `frontend/src/index.css`: add a new custom property `--overlay-chip-text: #FFFFFF;` to the
`:root` block's existing "Overlay dataset marker/toggle colors" comment group (immediately after
`--overlay-procedures: #5C6B1E;`), and `--overlay-chip-text: #0B1626;` to the matching `.dark`
block (immediately after `--overlay-procedures: #C9D48A;`). This exactly mirrors the existing
`--cat-chip-text` token's light/dark values (`#FFFFFF` / `#0B1626`) — light-mode overlay fills
(`#6A3FA0`/`#A32672`/`#5C6B1E`) are dark enough that white ink stays high-contrast (unaffected,
per 09-REVIEW.md); dark-mode overlay fills (`#C9A6EA`/`#F0A8D0`/`#C9D48A`) are light pastels, so
`#0B1626` ink against them independently recomputes to approximately 8.75:1 / 9.69:1 / 11.47:1 —
all clearing both the 3:1 UI-component floor and the 4.5:1 normal-text floor by a wide margin.

In `frontend/src/components/OverlayToggle.tsx`: remove the `text-white` utility class from the
`activeClass` string (it currently reads "min-h-12 flex items-center gap-2 rounded-lg px-4
text-[20px] font-bold text-white border-2" — drop only `text-white`, keep every other class), and
add `color: "var(--overlay-chip-text)"` to the inline `style` object applied when a button is
pressed (`style={on ? { backgroundColor: color, borderColor: color } : undefined}` becomes
`style={on ? { backgroundColor: color, borderColor: color, color: "var(--overlay-chip-text)" } :
undefined}`).

In `frontend/src/components/OverlayEventsList.tsx`'s `OverlayEventRow`: change the Type badge's
inline `style={{ backgroundColor: color, color: "white" }}` to `style={{ backgroundColor: color,
color: "var(--overlay-chip-text)" }}`.

Update `frontend/src/components/OverlayToggle.test.tsx`: add a new test to the existing "OverlayToggle
button group" describe block asserting that after clicking the "Labs" button (pressed state), its
`style` attribute contains `"var(--overlay-chip-text)"` and its `className` does NOT contain
`"text-white"`.

Update `frontend/src/components/OverlayEventsList.test.tsx`'s existing "Type badge" test (`"shows
the per-type icon badge with the type's color token"`): add assertions that the badge's `style`
attribute also contains `"var(--overlay-chip-text)"` and does NOT contain the literal substring
`"white"`.
  </action>
  <acceptance_criteria>
    - `frontend/src/index.css` defines `--overlay-chip-text: #FFFFFF;` inside `:root` and `--overlay-chip-text: #0B1626;` inside `.dark`
    - `grep -c "text-white" frontend/src/components/OverlayToggle.tsx` returns 0
    - `grep -c 'color: "white"' frontend/src/components/OverlayEventsList.tsx` returns 0
    - `grep -c "overlay-chip-text" frontend/src/components/OverlayToggle.tsx` returns 1 and `grep -c "overlay-chip-text" frontend/src/components/OverlayEventsList.tsx` returns 1
    - `cd frontend && npx vitest run src/components/OverlayToggle.test.tsx src/components/OverlayEventsList.test.tsx` passes, including the 2 new/updated assertions
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/components/OverlayToggle.test.tsx src/components/OverlayEventsList.test.tsx</automated>
  </verify>
  <done>No component hardcodes white text against a per-dataset overlay fill color; both the active toggle button and the events-table Type badge use the new theme-aware --overlay-chip-text token, which independently recomputes to 8.7:1-11.5:1 contrast in dark mode (previously 1.58:1-2.07:1) and remains unchanged in light mode.</done>
</task>

<task type="auto">
  <name>Task 3: Close WR-3 — agent-parity.test.ts DatasetToken/OverlayDataset reachability coverage</name>
  <files>frontend/src/lib/agent-parity.test.ts</files>
  <read_first>frontend/src/lib/agent-parity.test.ts (full file — the `CHART_IDS`/`BP_CATEGORIES` constants, their `it.each` reachability blocks in the "enumeration reachability" describe, and the "backend ChartToken equals the frontend ChartId union verbatim" test in the "frontend voice vocabulary matches backend tokens" describe are the exact patterns to mirror), backend/app/agent/schemas.py line 49 (`DatasetToken = Literal["labs", "incidents", "procedures"]`), frontend/src/api/types.ts line 155 (`export type OverlayDataset = "labs" | "incidents" | "procedures";`), .planning/phases/09-multi-dataset-overlay-filtering/09-REVIEW.md (WR-3 section, lines 255-283 — exact gap description and suggested fix)</read_first>
  <action>
In `frontend/src/lib/agent-parity.test.ts`: add `import type { OverlayDataset } from "../api/types";`
alongside the existing `import type { BPCategory, ChartId } from "../api/types";` (combine into one
import statement). Add a new constant `const DATASETS = ["labs", "incidents", "procedures"] as
const satisfies readonly OverlayDataset[];` alongside the existing `CHART_IDS`/`BP_CATEGORIES`
constants.

In the "enumeration reachability — every UI filter is voice-reachable (ACC-03)" describe block, add
an `it.each(DATASETS)("applyAgentFilters({ overlayDataset: '%s', overlayState: 'on' }) mutates the
store", (dataset) => { applyAgentFilters({ overlayDataset: dataset, overlayState: "on" });
expect(useFilters.getState().overlayDatasets[dataset]).toBe(true); });` block, mirroring the
existing `CHART_IDS`/`AMPM` `it.each` blocks exactly.

In the "frontend voice vocabulary matches backend tokens (D-15)" describe block, add a new test
`it("backend DatasetToken equals the frontend OverlayDataset union verbatim", () => { const
backendDatasets = literalTokens(schemaText.match(/DatasetToken = Literal\[([^\]]*)\]/));
expect(backendDatasets).toEqual([...DATASETS].sort()); });`, mirroring the existing "backend
ChartToken equals..." test exactly (same `literalTokens` helper, same read-from-disk `schemaText`
already in scope).
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/agent-parity.test.ts` defines a `DATASETS` const of exactly `["labs", "incidents", "procedures"]`
    - A new `it.each(DATASETS)` block exists in the "enumeration reachability" describe, exercising `overlayDataset`/`overlayState` through `applyAgentFilters`
    - A new test "backend DatasetToken equals the frontend OverlayDataset union verbatim" exists and passes
    - `cd frontend && npx vitest run src/lib/agent-parity.test.ts` passes all cases (including the 3 new `it.each` cases and the 1 new parity test)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/agent-parity.test.ts</automated>
  </verify>
  <done>agent-parity.test.ts now enumerates all 3 dataset tokens through applyAgentFilters and read-compares backend DatasetToken against frontend OverlayDataset, matching the coverage depth already given to ChartToken/bpCategory — future drift in this vocabulary will fail the suite instead of passing silently.</done>
</task>

</tasks>

## Deferred (not fixed in this plan)

- **WR-1 (voice/text confirmation banner never mentions the overlay change it just applied)** —
  `composeConfirmation()`'s locked D-07 template (`frontend/src/lib/agent.ts`) has no overlay
  clause, so a voice "show my labs" command gets no primary-banner acknowledgment beyond
  `OverlayToggle`'s own separate `aria-live` sentence. Deferred because extending a locked D-07
  confirmation template is a copy/scope decision (clause ordering, whether it would duplicate
  `OverlayToggle`'s existing sentence) that deserves its own discussion rather than a mechanical
  fix bundled into a 2-BLOCKER gap-closure plan. Not a regression — the toggle itself still applies
  correctly and is still announced, just not in the primary banner.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This is a gap-closure plan: it corrects a data-gating omission (stale cached TanStack Query data reaching the render tree past a toggle-off) and swaps a hardcoded CSS text color for a theme-aware token. No new external input, no new component boundary, no new package install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Information Disclosure | App.tsx / OverlayEventsList.tsx merged event arrays | accept | The underlying labs/incidents/procedures data is already Bearer-gated Phase 7 data the authenticated user is fully authorized to see; the bug caused stale-but-still-authorized data to linger visually one toggle-cycle too long, not a cross-user or cross-auth leak. Fixing the gate is a correctness fix, not a new security boundary. |
| T-09-14 | n/a (accessibility, not a STRIDE category) | OverlayToggle.tsx / OverlayEventsList.tsx ink color | mitigate | Tracked for completeness per CLAUDE.md's non-negotiable "high contrast" constraint — Task 2 replaces hardcoded white text with the new `--overlay-chip-text` theme-aware token, independently recomputed at 8.7:1-11.5:1 in dark mode (previously 1.58:1-2.07:1). |

No new auth/session/data-flow surface introduced; V1-V4 (auth, session, DB access, external API) unaffected.
</threat_model>

<verification>
`cd backend && pytest` must remain fully green (unaffected — no backend files touched by this
plan). `cd frontend && npx tsc --noEmit && npx vitest run` must be fully green, including the new
Gap-1 regression test in `OverlayEventsList.test.tsx`, the new Gap-2 style assertions in
`OverlayToggle.test.tsx`/`OverlayEventsList.test.tsx`, and the new `DATASETS` coverage in
`agent-parity.test.ts`.
</verification>

<success_criteria>
- Toggling a dataset OFF while another stays ON removes its markers/rows immediately — no stale
  data survives, proven by an automated regression test that fails if the gate is reverted
- No component hardcodes white text against a dark-mode overlay fill; the new
  `--overlay-chip-text` token clears WCAG's 3:1/4.5:1 floors in both themes
- `agent-parity.test.ts` covers `DatasetToken`/`OverlayDataset` with the same depth as
  `ChartToken`/`bpCategory`
- Full backend + frontend automated suites are green
</success_criteria>

<output>
Create `.planning/phases/09-multi-dataset-overlay-filtering/09-07-SUMMARY.md` when done
</output>
