---
phase: 07-records-backend-labs-incidents-procedures-crud
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - backend/tests/test_api_labs.py
  - backend/tests/test_api_procedures.py
  - backend/tests/test_api_incidents.py
  - backend/tests/test_auth_upload.py
autonomous: true
requirements: [OVERLAY-01]

must_haves:
  truths:
    - "Every claim in Plan 07-01 (create+fetch works, date-range filtering, auth-gated) is proven by an automated test, not just manual smoke-checking"
    - "The inclusive end_date boundary on Incident's DateTime column behaves identically to Reading's (a 23:xx incident ON end_date is included)"
    - "Every one of the 6 new endpoints (GET/POST x labs/incidents/procedures) returns 401 with no Bearer token"
  artifacts:
    - path: "backend/tests/test_api_labs.py"
      provides: "GET/POST /labs test coverage"
      contains: "def test_post_lab"
    - path: "backend/tests/test_api_incidents.py"
      provides: "GET/POST /incidents test coverage incl. inclusive-end-date + datetime aliasing"
      contains: "def test_end_date_is_inclusive"
    - path: "backend/tests/test_api_procedures.py"
      provides: "GET/POST /procedures test coverage"
      contains: "def test_post_procedure"
    - path: "backend/tests/test_auth_upload.py"
      provides: "401 gating tests for the 3 new resources"
      contains: "test_labs_get_without_token_401"
  key_links:
    - from: "backend/tests/test_api_labs.py"
      to: "backend/app/routers/labs.py"
      via: "TestClient(app) HTTP calls against /labs, using conftest's client fixture"
      pattern: "client\\.(get|post)\\(\"/labs\""
    - from: "backend/tests/test_auth_upload.py"
      to: "backend/app/main.py"
      via: "real_gate_client fixture (verify_token NOT overridden) proving the router-level gate"
      pattern: "real_gate_client"
---

<objective>
Prove every behavior Plan 07-01 implemented: GET date-range filtering, POST create with the full-record response, malformed-input 422s, correct JSON key serialization (including `IncidentOut`'s `datetime_`→`datetime` alias), and the 401 Bearer gate on all 6 new routes.

Purpose: Plan 07-01's smoke script proved the happy path once, by hand. This plan is the durable, repeatable test suite that keeps `/labs`, `/incidents`, `/procedures` correct as Phase 8/9 build on top of them.
Output: Three new per-resource test files mirroring `test_api_readings.py`, plus 6 new gating tests appended to the existing `test_auth_upload.py`. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-records-backend-labs-incidents-procedures-crud/07-CONTEXT.md
@.planning/phases/07-records-backend-labs-incidents-procedures-crud/07-PATTERNS.md
@backend/app/models.py
@.planning/phases/07-records-backend-labs-incidents-procedures-crud/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test coverage for GET/POST /labs and /procedures</name>
  <files>backend/tests/test_api_labs.py, backend/tests/test_api_procedures.py</files>
  <read_first>
    backend/tests/test_api_readings.py
    backend/tests/conftest.py
    backend/app/routers/labs.py
    backend/app/routers/procedures.py
    backend/app/schemas.py
    backend/app/models.py
  </read_first>
  <action>
    Create `backend/tests/test_api_labs.py` mirroring `test_api_readings.py`'s structure, narrowed to date-range filtering only (D-04 — no `am_pm`/`bp_category` equivalents exist for labs):
    - `_lab_result(date, test_name, result=None, unit=None, range_low=None, range_high=None, notes=None) -> LabResult` builder, importing `LabResult` from `app.models`.
    - `seeded` fixture: 4 rows spanning at least 3 distinct dates (e.g. `2025-03-01`, `2025-03-03`, `2025-03-06`), one with `notes` set and one without.
    - `test_no_filters_returns_all_rows_ordered_by_date` — `GET /labs` with no params returns all 4, dates ascending.
    - `test_start_date_filter` / `test_end_date_filter` — each narrows the result set to the expected count/date range (direct `date` comparison — `LabResult.date` is a `Date` column, so the boundary date is trivially inclusive; no late-evening-boundary regression test is needed here, unlike readings/incidents).
    - `test_invalid_params_return_422`, parametrized over `{"start_date": "not-a-date"}` and `{"end_date": "2025-13-45"}`.
    - `test_serialization_keys` — asserts one item's key set equals exactly `{"id", "date", "test_name", "result", "unit", "range_low", "range_high", "notes"}`.
    - `test_post_lab_minimal_fields_creates_and_returns_record` — `POST /labs` with only `{"date": "2025-04-01", "test_name": "A1C"}`; asserts 200, response has an `id` (int), `test_name == "A1C"`, and all optional fields (`result`, `unit`, `range_low`, `range_high`, `notes`) are `None`.
    - `test_post_lab_all_fields_creates_and_returns_record` — `POST /labs` with every field populated; asserts every value round-trips exactly.
    - `test_post_lab_then_get_includes_new_record` — after a successful POST, `GET /labs` includes it (proves D-02's "shows up immediately" contract at the API layer, ahead of Phase 8's frontend claim).

    Create `backend/tests/test_api_procedures.py` with the identical structure and test names (swap `_lab_result`→`_procedure`, `test_name`→`procedure_name`, `result/unit/range_low/range_high`→`location/outcome`, `LabResult`→`Procedure`, `/labs`→`/procedures`). Serialization key-set assertion: `{"id", "date", "procedure_name", "location", "outcome", "notes"}`.
  </action>
  <acceptance_criteria>
    - `backend/tests/test_api_labs.py` and `backend/tests/test_api_procedures.py` each define a `seeded` fixture and at least 7 test functions covering: no-filter list, start_date, end_date, 422 on malformed date, serialization key-set, POST with minimal fields, POST with all fields.
    - Neither file references `am_pm`, `bp_category`, or any filter param beyond `start_date`/`end_date` (D-04 scope boundary).
    - Every POST test asserts the response contains a server-assigned `id` and that no field from the request body is silently dropped or renamed.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/test_api_labs.py tests/test_api_procedures.py -v</automated>
  </verify>
  <done>Both test files exist and every test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Test coverage for /incidents (DateTime aliasing + inclusive end_date) and 401 gating for all 3 resources</name>
  <files>backend/tests/test_api_incidents.py, backend/tests/test_auth_upload.py</files>
  <read_first>
    backend/tests/test_api_readings.py
    backend/tests/test_auth_upload.py
    backend/tests/conftest.py
    backend/app/routers/incidents.py
    backend/app/routers/labs.py
    backend/app/routers/procedures.py
    backend/app/schemas.py
    backend/app/models.py
  </read_first>
  <action>
    Create `backend/tests/test_api_incidents.py` mirroring `test_api_readings.py`'s DateTime-column tests:
    - `_incident(dt, incident_type, duration=None, notes=None) -> Incident` builder, importing `Incident` from `app.models`.
    - `seeded` fixture: rows spanning multiple dates including one at `datetime(2025, 3, 4, 23, 15)` (late-evening boundary case, same date used in `test_api_readings.py`'s `test_end_date_is_inclusive_of_late_evening_reading`).
    - `test_no_filters_returns_all_rows_ordered_by_datetime`, `test_start_date_filter`, `test_end_date_filter`.
    - `test_end_date_is_inclusive_of_late_evening_incident` — `GET /incidents?end_date=2025-03-04` includes the 23:15 row (regression test proving `IncidentFilters.apply` uses `datetime.combine(..., datetime.max.time())`, matching `ReadingFilters`).
    - `test_invalid_params_return_422`, parametrized over `{"start_date": "not-a-date"}` and `{"end_date": "2025-13-45"}`.
    - `test_serialization_uses_clean_datetime_key` — asserts `"datetime" in item`, `"datetime_" not in item`, and for every row `"Z" not in item["datetime"]` and `"+" not in item["datetime"]` (mirrors `test_serialization_uses_clean_keys_and_naive_iso`).
    - `test_post_incident_minimal_fields_creates_and_returns_record` — `POST /incidents` with only `{"datetime": "2025-04-01T08:00:00", "incident_type": "fall"}`; asserts 200, response `datetime == "2025-04-01T08:00:00"` (naive, no Z/offset), `duration is None`, `notes is None`.
    - `test_post_incident_all_fields_creates_and_returns_record` — every field populated, round-trips exactly.

    Extend `backend/tests/test_auth_upload.py`: add a new section after the existing `/upload` tests (do not modify the `real_gate_client` fixture — reuse it as-is). Add 6 tests, each following `test_upload_without_token_401`'s shape (call the route via `real_gate_client` with no `Authorization` header, assert `resp.status_code == 401`):
    - `test_labs_get_without_token_401` — `real_gate_client.get("/labs")`.
    - `test_labs_post_without_token_401` — `real_gate_client.post("/labs", json={})`.
    - `test_incidents_get_without_token_401` — `real_gate_client.get("/incidents")`.
    - `test_incidents_post_without_token_401` — `real_gate_client.post("/incidents", json={})`.
    - `test_procedures_get_without_token_401` — `real_gate_client.get("/procedures")`.
    - `test_procedures_post_without_token_401` — `real_gate_client.post("/procedures", json={})`.
    Use an empty `json={}` body for every POST variant deliberately — this proves `verify_token` is evaluated BEFORE Pydantic body validation (a missing token 401s even though `{}` would otherwise fail body validation with a 422; if any of these tests observe 422 instead of 401, that is a gate-ordering regression, not a passing test).
  </action>
  <acceptance_criteria>
    - `backend/tests/test_api_incidents.py` includes `test_end_date_is_inclusive_of_late_evening_incident` and `test_serialization_uses_clean_datetime_key`, both passing.
    - `backend/tests/test_auth_upload.py` gains exactly 6 new test functions, none of which define a new fixture or override `verify_token` — all reuse the file's existing `real_gate_client` fixture.
    - Every one of the 6 new gating tests asserts `status_code == 401` (not 422, not 404).
    - `pytest backend/tests -q` (full suite) reports zero failures.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/test_api_incidents.py tests/test_auth_upload.py -v && pytest tests -q</automated>
  </verify>
  <done>`test_api_incidents.py` exists and passes, including the inclusive-end-date-boundary regression and the datetime-key-aliasing serialization test; `test_auth_upload.py` gains 6 passing 401-gating tests; the full backend suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Caregiver/Chris browser -> `POST /labs`, `/incidents`, `/procedures` | Same boundary as 07-01 — this plan adds the automated proof that the boundary is actually enforced, not just implemented |
| Unauthenticated caller -> any of the 6 new routes | This plan's Task 2 gating tests are the enforcement evidence for T-07-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | labs/incidents/procedures routers (auth bypass) | mitigate | Verified here: 6 new tests in `test_auth_upload.py` (`test_labs_get_without_token_401`, etc.) prove every GET/POST on all 3 new resources 401s with no Bearer token, using the file's existing un-overridden `real_gate_client` fixture — the same real-dependency gate proof already established for `/readings`/`/upload`. |
| T-07-02 | Tampering | `LabResultCreate`/`IncidentCreate`/`ProcedureCreate` (mass-assignment / over-posting) | mitigate | Verified here: `test_post_*_minimal_fields_creates_and_returns_record` and `test_post_*_all_fields_creates_and_returns_record` assert exact response key-sets per resource — no extra/unexpected field ever appears, and `id` is always server-assigned (never echoed from the request). |
| T-07-03 | Tampering (injection) | Free-text fields persisted via the ORM | mitigate | Verified here: POST tests use representative free-text values (e.g. `test_name`, `incident_type`, `notes`) and assert they round-trip unmodified through the parameterized ORM insert/select path — no test needs a raw-SQL escape hatch, confirming none exists. |
| T-07-04 | Denial of Service | Unbounded `Text` columns | accept | No new test coverage required — disposition and rationale unchanged from 07-01 (accepted, consistent with existing precedent). |
| T-07-05 | Information Disclosure | Pydantic 422 responses | accept | Verified here: `test_invalid_params_return_422` tests confirm the 422 body only ever contains standard FastAPI field/type validation detail, no stack traces or internal paths. |
</threat_model>

<verification>
- `cd backend && pytest tests/test_api_labs.py tests/test_api_incidents.py tests/test_api_procedures.py tests/test_auth_upload.py -v` — all new and modified tests pass.
- `cd backend && pytest tests -q` — full suite (existing + new) reports zero failures, zero new skips.
- `grep -c "without_token_401" backend/tests/test_auth_upload.py` returns at least `7` (the existing `test_upload_without_token_401` + 6 new ones for labs/incidents/procedures) — confirms the new tests were actually added, not just planned.
</verification>

<success_criteria>
- `pytest backend/tests -q` passes with zero failures after this plan.
- All 3 of Phase 7's ROADMAP.md success criteria have automated test evidence: (1) create-and-store proven per resource by `test_post_*_creates_and_returns_record`, (2) date-range fetch proven by the filter tests, (3) Bearer-gating proven by the 6 new `test_auth_upload.py` tests.
</success_criteria>

<output>
Create `.planning/phases/07-records-backend-labs-incidents-procedures-crud/07-02-SUMMARY.md` when done
</output>
