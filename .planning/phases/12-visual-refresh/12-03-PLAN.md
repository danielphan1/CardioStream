---
phase: 12-visual-refresh
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/components/OverlayEventsList.tsx
  - frontend/src/components/ChartDeck.tsx
  - frontend/src/components/FilterBar.tsx
  - frontend/src/components/OverlayToggle.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "The overlaid-events accessible table (OverlayEventsList) shows the same rounded-xl + shadow-elevation depth as ReadingsTable, its structural analog (VISUAL-01 consistency)"
    - "ChartDeck's three mini chart-picker cards show rounded-xl + shadow-elevation depth, matching StatsStrip's own cited before/after example (D-05)"
    - "FilterBar's date/AM-PM/custom-range pill buttons and OverlayToggle's dataset toggle buttons show rounded-xl corners instead of rounded-lg, at unchanged min-h-12 height (D-05)"
    - "OverlayToggle's toggle buttons additionally show the shadow-elevation depth cue — the one interactive-control exception UI-SPEC's elevation table names explicitly alongside cards/panels/filter-chips"
    - "FilterBar's and OverlayToggle's D-07 animate-pulse ring classes (pulseClass) are completely untouched — no rounded-xl, no shadow, no class change of any kind"
    - "Every aria-pressed, aria-label, and non-color-only word/icon pairing on every toggle in these four files is unchanged (OVERLAY-03/04 conventions preserved)"
  artifacts:
    - path: frontend/src/components/OverlayEventsList.tsx
      provides: "Depth-treated overlaid-events table, formalized text-h2 heading and text-control CTA"
    - path: frontend/src/components/ChartDeck.tsx
      provides: "Depth-treated mini chart-picker cards, formalized text-h2/text-control"
    - path: frontend/src/components/FilterBar.tsx
      provides: "rounded-xl pill buttons via the shared inactiveClass/activeClass constants, text-control labels"
    - path: frontend/src/components/OverlayToggle.tsx
      provides: "rounded-xl + shadow-elevation toggle buttons via the shared inactiveClass/activeClass constants, text-control labels"
  key_links:
    - from: frontend/src/components/FilterBar.tsx
      to: frontend/src/index.css
      via: "shared inactiveClass/activeClass string constants referencing rounded-xl and text-control"
      pattern: "rounded-xl.*text-control|text-control.*rounded-xl"
    - from: frontend/src/components/OverlayToggle.tsx
      to: frontend/src/index.css
      via: "shared inactiveClass/activeClass string constants referencing rounded-xl, text-control, and shadow-[var(--shadow-elevation)]"
      pattern: "shadow-\\[var\\(--shadow-elevation\\)\\]"
---

<objective>
Apply the D-05 depth treatment to `OverlayEventsList.tsx` (ReadingsTable's structural analog) and
`ChartDeck.tsx`'s mini chart-picker cards, and apply the D-05 radius bump (plus, for `OverlayToggle.tsx`
only, the shadow-elevation depth cue too — the one interactive-control exception UI-SPEC's elevation table
names by name alongside cards/panels/filter-chips) to `FilterBar.tsx`'s and `OverlayToggle.tsx`'s shared
`inactiveClass`/`activeClass` toggle-button constants. Formalize the D-06 type scale
(`text-2xl`→`text-h2`, `text-[20px]`/`text-xl`→`text-control`) everywhere it appears in these four files.

This plan depends on 12-01 for the same reason as every other Wave 2 plan (consumes `--shadow-elevation`/
`--text-h2`/`--text-control`). It shares zero files with 12-02/12-04/12-05/12-06/12-07 and runs in parallel
with them.

Purpose: these four components are the interactive control surface of the dashboard (chart switching,
date/AM-PM/category filtering, overlay dataset toggling) — landing depth/type-scale here keeps the
control-surface visual language consistent with the data-surface work in 12-02.
Output: four components with rounded-xl (+ shadow on OverlayEventsList/ChartDeck cards and OverlayToggle
buttons) and formalized type-scale classes, zero change to aria-pressed/aria-label/min-h-12/pulse-animation
behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-visual-refresh/12-CONTEXT.md
@.planning/phases/12-visual-refresh/12-UI-SPEC.md
@.planning/phases/12-visual-refresh/12-PATTERNS.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverlayEventsList.tsx + ChartDeck.tsx — depth and type-scale</name>
  <files>frontend/src/components/OverlayEventsList.tsx, frontend/src/components/ChartDeck.tsx</files>
  <read_first>frontend/src/components/OverlayEventsList.tsx (full file, 184 lines — the h2 at line 108, the section at line 121, the "Show 20 more" button at lines 152-158), frontend/src/components/ChartDeck.tsx (full file, 173 lines — the hero h2 at line 140, the mini-card button at lines 153-159, the mini title span at line 160)</read_first>
  <action>
OverlayEventsList.tsx: on the `<h2>` (line 108), change `className="mb-4 text-2xl leading-tight font-bold
text-[var(--color-ink)]"` to use `text-h2` in place of `text-2xl`. On the section wrapping the table (line
121), change `className="rounded-lg bg-[var(--color-sky)] p-6"` to `"rounded-xl bg-[var(--color-sky)] p-6
shadow-[var(--shadow-elevation)]"`. On the "Show 20 more" button (line 155), change `className="mt-4
min-h-12 w-full rounded-lg bg-[var(--color-accent)] px-6 text-xl font-bold
text-[var(--color-accent-text)]"` to swap `rounded-lg`→`rounded-xl` and `text-xl`→`text-control`
(radius-only, no shadow — mirrors ReadingsTable's own CTA button treatment from Plan 12-02). Do not touch
the error/empty-message `<p>` elements (they stay `text-[18px]`, the unchanged body floor) or the table's
`<th>`/`<td>` cells.

ChartDeck.tsx: on the hero title `<h2>` (line 140), change `text-2xl` to `text-h2`. On each mini-card button
(line 158), change `className="min-h-12 rounded-lg bg-[var(--color-sky)] p-4 text-left"` to `"min-h-12
rounded-xl bg-[var(--color-sky)] p-4 text-left shadow-[var(--shadow-elevation)]"`. On the mini-card title
span (line 160), change `text-[20px]` to `text-control`. Do not touch the `FadeSwap` component's
`transition-[opacity,transform]` classes (lines 116-118) — that is the existing D-04 mount-fade animation,
untouched by this phase's D-07 motion boundary.
  </action>
  <acceptance_criteria>
    - `grep -c "text-h2" frontend/src/components/OverlayEventsList.tsx` equals 1 and `grep -c "text-2xl" frontend/src/components/OverlayEventsList.tsx` equals 0
    - `grep -c "rounded-xl bg-\[var(--color-sky)\] p-6 shadow-\[var(--shadow-elevation)\]" frontend/src/components/OverlayEventsList.tsx` equals 1
    - `grep -c "rounded-xl.*text-control" frontend/src/components/OverlayEventsList.tsx` equals 1 (Show 20 more button)
    - `grep -c "text-h2" frontend/src/components/ChartDeck.tsx` equals 1 and `grep -c "text-2xl" frontend/src/components/ChartDeck.tsx` equals 0
    - `grep -c "rounded-xl bg-\[var(--color-sky)\] p-4 text-left shadow-\[var(--shadow-elevation)\]" frontend/src/components/ChartDeck.tsx` equals 1
    - `grep -c "text-control" frontend/src/components/ChartDeck.tsx` equals 1 (mini title span)
    - `grep -c "transition-\[opacity,transform\]" frontend/src/components/ChartDeck.tsx` equals 1 (FadeSwap untouched)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>OverlayEventsList's table panel and ChartDeck's mini cards show rounded-xl + shadow-elevation depth; both files' headings/labels use the named type-scale tokens; ChartDeck's FadeSwap transition is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: FilterBar.tsx + OverlayToggle.tsx — toggle-button radius/shadow and type-scale</name>
  <files>frontend/src/components/FilterBar.tsx, frontend/src/components/OverlayToggle.tsx</files>
  <read_first>frontend/src/components/FilterBar.tsx (full file, 204 lines — the shared inactiveClass/activeClass constants at lines 25-28, the pulseClass function at lines 75-78, the category-chip button at line 168), frontend/src/components/OverlayToggle.tsx (full file, 108 lines — the shared inactiveClass/activeClass constants at lines 27-30, the pulseClass computation at lines 48-50, the "Overlay:" label at line 60)</read_first>
  <action>
FilterBar.tsx: in the shared `inactiveClass` constant (line 26), change `rounded-lg` to `rounded-xl` and
`text-[20px]` to `text-control`. In the shared `activeClass` constant (line 28), apply the identical
`rounded-lg`→`rounded-xl` and `text-[20px]`→`text-control` swap. Do NOT add a shadow to either class — per
UI-SPEC's elevation table, FilterBar's generic pill buttons ("filter chips" in UI-SPEC's list refers
specifically to OverlayToggle's dataset buttons, not FilterBar's date/AM-PM pills) get the radius bump only.
Do NOT touch `pulseClass` (lines 75-78, the `" rounded-lg ring-2 ring-[var(--color-accent)]
motion-safe:animate-pulse"` string) — this is the D-07-protected `animate-pulse` mechanism and must stay
byte-for-byte identical, including its own `rounded-lg`. Do NOT touch the category-chip button's
`rounded-full` (line 168) — different shape, unaffected by this phase.

OverlayToggle.tsx: in the shared `inactiveClass` constant (line 28), change `rounded-lg` to `rounded-xl`,
`text-[20px]` to `text-control`, and append ` shadow-[var(--shadow-elevation)]` to the string — OverlayToggle
is the one interactive-control exception UI-SPEC's elevation table names explicitly in both its radius AND
shadow "Applies to" columns. In the shared `activeClass` constant (line 30), apply the identical
`rounded-lg`→`rounded-xl`, `text-[20px]`→`text-control`, and append the same
` shadow-[var(--shadow-elevation)]`. On the "Overlay:" label (line 60), change `text-[20px]` to
`text-control`. Do NOT touch the `pulseClass` computation (lines 48-50, ` ring-2
ring-[var(--color-accent)] motion-safe:animate-pulse"`) — same D-07 protection as FilterBar's pulseClass.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-xl px-4 text-control" frontend/src/components/FilterBar.tsx` equals 2 (inactiveClass + activeClass)
    - `grep -c "rounded-lg ring-2 ring-\[var(--color-accent)\] motion-safe:animate-pulse" frontend/src/components/FilterBar.tsx` equals 1 (pulseClass untouched, still rounded-lg)
    - `grep -c "rounded-full" frontend/src/components/FilterBar.tsx` equals 1 (category chip untouched)
    - `grep -c "rounded-xl px-4 text-control" frontend/src/components/OverlayToggle.tsx` equals 2
    - `grep -c "shadow-\[var(--shadow-elevation)\]" frontend/src/components/OverlayToggle.tsx` equals 2 (inactiveClass + activeClass)
    - `grep -c "ring-2 ring-\[var(--color-accent)\] motion-safe:animate-pulse" frontend/src/components/OverlayToggle.tsx` equals 1 (pulseClass untouched)
    - `grep -c "aria-pressed" frontend/src/components/FilterBar.tsx frontend/src/components/OverlayToggle.tsx` unchanged from pre-task count
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>FilterBar's and OverlayToggle's shared toggle-button classes show rounded-xl at unchanged min-h-12 height; OverlayToggle's buttons additionally show shadow-elevation; both files' D-07 pulse-ring mechanisms are byte-identical to before; every aria-pressed attribute is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — these four components render zustand-store-derived filter/overlay UI state through
existing className string constants; this plan edits only those constants' Tailwind tokens.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | shared inactiveClass/activeClass string constants | accept | Module-level static string constants, never built from user/model input. |
| T-12-09 | Elevation of Privilege (scope creep into D-07) | pulseClass/ringClass computations | mitigate | Acceptance criteria explicitly grep-verify both files' pulse-animation strings are byte-identical to their pre-task form, preventing an accidental edit to the one motion mechanism D-07 protects. |
| T-12-10 | Information Disclosure | none | accept | No new data rendered; purely visual class changes to already-displayed toggle state. |

No ASVS category applies — CSS/class-only change, zero new input-handling surface.
</threat_model>

<verification>
`cd frontend && npx vitest run` — full suite must stay green; OVERLAY-03/04/05 tests assert on aria-pressed,
role="group", and text content, none on className, so this class-only change should not break them.
</verification>

<success_criteria>
- OverlayEventsList's panel and ChartDeck's mini cards show rounded-xl + shadow-elevation
- FilterBar's and OverlayToggle's toggle buttons show rounded-xl at unchanged min-h-12
- OverlayToggle's toggle buttons additionally show shadow-elevation
- Both files' D-07 pulse-ring mechanisms are untouched
- Full frontend suite green
</success_criteria>

<output>
Create `.planning/phases/12-visual-refresh/12-03-SUMMARY.md` when done
</output>
