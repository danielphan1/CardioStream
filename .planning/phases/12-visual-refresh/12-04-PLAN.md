---
phase: 12-visual-refresh
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/components/AgentStatusBanner.tsx
  - frontend/src/components/CommandBar.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "Header's LogoutConfirmDialog card shows rounded-xl + shadow-elevation depth, and its Cancel/Log out buttons show the radius-only bump, matching every other confirm/notice panel in the app (VISUAL-01)"
    - "The site's app-title h1 in Header uses the named text-h1 token at the unchanged 32px rendered size (D-06)"
    - "AgentStatusBanner's 'assistant unavailable' notice shows the same rounded-xl + shadow-elevation depth as every other bordered notice panel sitewide, for consistency (VISUAL-01)"
    - "CommandBar's mic button, text input, and Send button show rounded-xl corners at unchanged min-h-12/min-w-12 size, and the Send button's label uses text-control (D-05/D-06)"
    - "CommandBar's D-06/D-09 state ring (ringClass, the WORKING/listening highlight) is completely untouched — no class change of any kind, preserving the D-07 motion boundary"
    - "Header's Theme/Voice-Replies/Guide toggle buttons and view-toggle/logout-trigger buttons are left exactly as they are today (not in the design contract's named surface list) — no accidental scope creep beyond D-05's specified surfaces"
  artifacts:
    - path: frontend/src/components/Header.tsx
      provides: "Depth-treated LogoutConfirmDialog card, formalized text-h1 app title and text-h2/text-control dialog text"
    - path: frontend/src/components/AgentStatusBanner.tsx
      provides: "Depth-treated unavailable-assistant banner"
    - path: frontend/src/components/CommandBar.tsx
      provides: "rounded-xl mic/input/Send controls, formalized text-control Send label"
  key_links:
    - from: frontend/src/components/Header.tsx
      to: frontend/src/index.css
      via: "rounded-xl and shadow-[var(--shadow-elevation)] on the LogoutConfirmDialog card"
      pattern: "shadow-\\[var\\(--shadow-elevation\\)\\]"
---

<objective>
Apply the D-05 depth treatment to `Header.tsx`'s `LogoutConfirmDialog` card and `AgentStatusBanner.tsx`'s
notice panel, the D-05 radius-only bump to `CommandBar.tsx`'s mic/input/Send controls and
`LogoutConfirmDialog`'s Cancel/Log out buttons, and the D-06 type-scale formalization
(`text-[32px]`→`text-h1`, `text-2xl`→`text-h2`, `text-[20px]`/`text-xl`→`text-control`) across all three
files — while explicitly leaving Header's Theme/Voice-Replies/Guide toggle buttons and view-toggle/logout
trigger button untouched, since they are not named in the design contract's card/panel/accent-button list
(they are ink-bordered, never-accent-filled controls per Header.tsx's own header comment) and extending the
radius bump to them would be scope creep beyond what D-05/PATTERNS.md documents.

This plan depends on 12-01 for the `--shadow-elevation`/`--text-h1`/`--text-h2`/`--text-control` tokens. It
shares zero files with the other five Wave 2 plans.

Purpose: Header and CommandBar are the two components present on every screen of the app (persistent
header chrome, persistent command bar) — this plan is what makes the refresh legible as "the whole site,"
not just the dashboard body.
Output: three components with depth/radius/type-scale applied only where the design contract names them,
with Header's un-named toggle buttons and CommandBar's D-07-protected ring class fully preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-visual-refresh/12-CONTEXT.md
@.planning/phases/12-visual-refresh/12-UI-SPEC.md
@.planning/phases/12-visual-refresh/12-PATTERNS.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header.tsx — LogoutConfirmDialog depth/radius and app-title type-scale</name>
  <files>frontend/src/components/Header.tsx</files>
  <read_first>frontend/src/components/Header.tsx (full file, 301 lines — LogoutConfirmDialog spans lines 39-122: the card div at line 91, the h2 at line 95, the Cancel button at line 107, the Log out button at line 114; the app-title h1 at line 163; the Theme/Voice-Replies/Guide/Upload/Add-Record/Log-out-trigger buttons at lines 174-277, which this task must NOT modify)</read_first>
  <action>
On the `LogoutConfirmDialog` card div (line 91), change `className="flex w-full max-w-[28rem] flex-col
gap-4 rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-sky)] p-6
text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl` and append
` shadow-[var(--shadow-elevation)]`. On the `<h2 id="logout-title">` (line 95), change `text-2xl` to
`text-h2`. On the Cancel button (line 107), change `className="min-h-12 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-sky)] px-6 text-[20px] font-bold
text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl` and `text-[20px]`→`text-control`
(radius-only, no shadow — it is a button, not a panel). On the Log out button (line 114), apply the
identical `rounded-lg`→`rounded-xl` and `text-[20px]`→`text-control` swap to `className="min-h-12
rounded-lg bg-[var(--color-accent)] px-6 text-[20px] font-bold
text-[var(--color-accent-text)]"`.

On the app-title `<h1>` (line 163), change `text-[32px]` to `text-h1`.

Do NOT modify anything in the header-right controls row (lines 172-278): the Theme toggle (178), Voice
Replies toggle (196), Guide toggle (226), Upload/Add Record buttons (241, 249), Back-to-dashboard button
(259), and Log out trigger button (273) all keep their current `rounded-lg`/`text-[20px]` classes exactly
as-is — none of them are named in the design contract's accent-button or card/panel surface list (Header's
own comment documents they are intentionally never accent-filled), so touching them would be undocumented
scope expansion.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-xl.*shadow-\[var(--shadow-elevation)\]" frontend/src/components/Header.tsx` equals 1 (LogoutConfirmDialog card)
    - `grep -c "text-h2" frontend/src/components/Header.tsx` equals 1
    - `grep -c "text-h1" frontend/src/components/Header.tsx` equals 1
    - `grep -c "rounded-xl.*text-control\|text-control.*rounded-xl" frontend/src/components/Header.tsx` equals 2 (Cancel + Log out buttons)
    - `grep -c "text-\[32px\]\|text-2xl" frontend/src/components/Header.tsx` equals 0 (both old sizes fully renamed)
    - `grep -c "rounded-lg border-2 border-\[var(--color-ink)\] bg-\[var(--color-sky)\] px-4 text-\[20px\]" frontend/src/components/Header.tsx` equals 1 (Theme toggle button, still rounded-lg/text-[20px] — untouched, confirms no scope creep)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>LogoutConfirmDialog's card, heading, and two buttons show the depth/radius/type-scale treatment; the app-title h1 uses text-h1; every other header-right control is byte-identical to before this task.</done>
</task>

<task type="auto">
  <name>Task 2: AgentStatusBanner.tsx + CommandBar.tsx — depth/radius and type-scale</name>
  <files>frontend/src/components/AgentStatusBanner.tsx, frontend/src/components/CommandBar.tsx</files>
  <read_first>frontend/src/components/AgentStatusBanner.tsx (full file, 46 lines — the banner div at line 39), frontend/src/components/CommandBar.tsx (full file, 341 lines — the mic button at lines 251-263, the text input at lines 265-273, the Send button at lines 274-280, the ringClass computation at lines 192-196, which this task must NOT modify)</read_first>
  <action>
AgentStatusBanner.tsx: on the banner div (line 39), change `className="mt-3 flex items-center gap-3
rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-foam)] p-4 text-[18px]
text-[var(--color-ink)] transition-opacity duration-200 motion-reduce:transition-none"` to swap
`rounded-lg`→`rounded-xl` and append ` shadow-[var(--shadow-elevation)]` — leave `text-[18px)` (the body
floor) and the existing `transition-opacity` (a pre-existing fade, not a new D-07 motion layer) unchanged.

CommandBar.tsx: on the mic button (line 256), change `className="flex min-h-12 min-w-12 items-center
justify-center rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-foam)]
text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl` (radius-only). On the text input (line 272),
change `className="min-h-12 flex-grow rounded-lg border-2 border-[var(--color-ink)]
bg-[var(--color-foam)] px-4 text-[18px] text-[var(--color-ink)] disabled:opacity-70"` to swap
`rounded-lg`→`rounded-xl` — leave `text-[18px]` (body floor) unchanged. On the Send button (line 277),
change `className="min-h-12 rounded-lg bg-[var(--color-accent)] px-6 text-xl font-bold
text-[var(--color-accent-text)] disabled:opacity-70"` to swap `rounded-lg`→`rounded-xl` and
`text-xl`→`text-control`. Do NOT touch the `ringClass` computation (lines 192-196, the
`"rounded-lg ring-2 ring-[var(--color-accent)]"` / `"rounded-lg ring-2 ring-[var(--cat-normal)]
motion-safe:animate-pulse"` strings) — this is the same D-07-protected animate-pulse mechanism as
FilterBar's/OverlayToggle's pulseClass and must stay byte-for-byte identical, including its own
`rounded-lg`.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-xl.*shadow-\[var(--shadow-elevation)\]" frontend/src/components/AgentStatusBanner.tsx` equals 1
    - `grep -c "text-\[18px\]" frontend/src/components/AgentStatusBanner.tsx` equals 1 (body floor unchanged)
    - `grep -c "rounded-xl" frontend/src/components/CommandBar.tsx` equals 3 (mic button, input, Send button)
    - `grep -c "text-control" frontend/src/components/CommandBar.tsx` equals 1 (Send button) and `grep -c "text-xl" frontend/src/components/CommandBar.tsx` equals 0
    - `grep -c "rounded-lg ring-2 ring-\[var(--color-accent)\]\"" frontend/src/components/CommandBar.tsx` equals 1 (ringClass WORKING branch untouched, still rounded-lg)
    - `grep -c "rounded-lg ring-2 ring-\[var(--cat-normal)\] motion-safe:animate-pulse" frontend/src/components/CommandBar.tsx` equals 1 (ringClass listening branch untouched)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>AgentStatusBanner's notice panel shows rounded-xl + shadow-elevation; CommandBar's mic/input/Send controls show rounded-xl at unchanged min-h-12/min-w-12 size and the Send button uses text-control; CommandBar's D-07-protected ringClass is byte-identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — Header/AgentStatusBanner/CommandBar render zustand-store state and already-typed
API responses through existing className strings; this plan edits only Tailwind class tokens.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Tampering | className string literals | accept | Static, developer-authored strings; CommandBar's user-typed `text` value is never interpolated into a className. |
| T-12-12 | Elevation of Privilege (scope creep) | Header's un-named toggle buttons | mitigate | Acceptance criteria explicitly grep-verify the Theme toggle button is still `rounded-lg`/`text-[20px]` after this task, proving no undocumented scope expansion beyond D-05's named surfaces. |
| T-12-13 | Elevation of Privilege (scope creep into D-07) | CommandBar's ringClass | mitigate | Acceptance criteria explicitly grep-verify both ringClass branches remain byte-identical (still `rounded-lg`), preserving the D-07 motion boundary. |

No ASVS category applies — CSS/class-only change, zero new input-handling surface. CommandBar's existing
`/agent` Bearer-gated request path is entirely untouched by this plan.
</threat_model>

<verification>
`cd frontend && npx vitest run` — full suite must stay green; LIVE-01/02/03 and VOICE-06/07/08 tests assert
on aria-live text content and role, not className, so this change should not break them.
</verification>

<success_criteria>
- LogoutConfirmDialog's card/heading/buttons and the app-title show the depth/radius/type-scale treatment
- AgentStatusBanner's notice panel shows rounded-xl + shadow-elevation
- CommandBar's mic/input/Send controls show rounded-xl and the Send button uses text-control
- Header's un-named toggle buttons and CommandBar's ringClass are byte-identical to before
- Full frontend suite green
</success_criteria>

<output>
Create `.planning/phases/12-visual-refresh/12-04-SUMMARY.md` when done
</output>
