---
phase: 12-visual-refresh
plan: 07
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/components/records/SingleDateField.tsx
  - frontend/src/components/records/LabFields.tsx
  - frontend/src/components/records/IncidentFields.tsx
  - frontend/src/components/records/ProcedureFields.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "Every text/date/time/number/textarea input across the four record field-set components shows rounded-xl corners at unchanged min-h-12 size (D-05)"
    - "Every field label across the four components uses the named text-control token at the unchanged 20px rendered size (D-06)"
    - "SingleDateField's rdpSizing bridge and the underlying react-day-picker calendar chrome are untouched, mirroring DateRangePicker's own exclusion (VISUAL-01 consistency, RESEARCH.md Pitfall 3)"
    - "Every field-set's onDraftChange contract, canSubmit validation logic, and numericFieldValid/isValidDateText checks are byte-identical to before this plan (className-only pass)"
  artifacts:
    - path: frontend/src/components/records/SingleDateField.tsx
      provides: "rounded-xl input, formalized text-control label"
    - path: frontend/src/components/records/LabFields.tsx
      provides: "rounded-xl inputs/textarea, formalized text-control labels"
    - path: frontend/src/components/records/IncidentFields.tsx
      provides: "rounded-xl inputs/textarea, formalized text-control labels"
    - path: frontend/src/components/records/ProcedureFields.tsx
      provides: "rounded-xl inputs/textarea, formalized text-control labels"
  key_links:
    - from: frontend/src/components/records/LabFields.tsx
      to: frontend/src/index.css
      via: "shared inputClass/labelClass constants referencing rounded-xl and text-control"
      pattern: "rounded-xl"
---

<objective>
Apply the D-05 radius-only bump (`rounded-lg` → `rounded-xl`, no shadow — plain text inputs, not panels)
and the D-06 type-scale formalization (`text-[20px]` → `text-control`) to the shared `inputClass`/
`labelClass` constants in all four record field-set components: `SingleDateField.tsx`,
`LabFields.tsx`, `IncidentFields.tsx`, and `ProcedureFields.tsx`. These four files share a byte-identical
`inputClass`/`labelClass` pattern (confirmed by direct comparison during planning), so each file's edit is
a single two-line find-and-replace on its own local copy of the constants.

This plan depends on 12-01 for the `--text-control` token. It shares zero files with the other five Wave 2
plans. `SingleDateField.tsx` also owns a `rdpSizing` bridge object identical to `DateRangePicker.tsx`'s
(Plan 12-06) — this plan leaves it untouched for the identical reason (UI-SPEC's documented third-party-
widget scope decision).

Purpose: these four components are the actual input surface of the Add Record forms (OVERLAY-02) — the
form container's panels/buttons are handled by Plan 12-05's `AddRecordPage.tsx` edit; this plan completes
the visual consistency down to the individual field level.
Output: four field-set components with rounded-xl inputs and text-control labels, zero change to any
validation/draft-reporting logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-visual-refresh/12-CONTEXT.md
@.planning/phases/12-visual-refresh/12-UI-SPEC.md
@.planning/phases/12-visual-refresh/12-PATTERNS.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: SingleDateField.tsx + LabFields.tsx — radius and type-scale</name>
  <files>frontend/src/components/records/SingleDateField.tsx, frontend/src/components/records/LabFields.tsx</files>
  <read_first>frontend/src/components/records/SingleDateField.tsx (full file, 61 lines — the `rdpSizing` object at lines 20-27, which must NOT be touched; the `inputClass` constant at line 30; the label at line 37), frontend/src/components/records/LabFields.tsx (full file, 151 lines — the `inputClass` constant at line 22, the `labelClass` constant at line 23)</read_first>
  <action>
SingleDateField.tsx: change the `inputClass` constant (line 30) from `"min-h-12 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-foam)] px-3 text-[18px] text-[var(--color-ink)]"` by swapping
`rounded-lg`→`rounded-xl` — leave `text-[18px]` (body floor) unchanged. On the label (line 37), change
`text-[20px]` to `text-control`. Do NOT touch the `rdpSizing` object (lines 20-27) or the `<DayPicker
mode="single" ...>` element (lines 51-57) — identical exclusion rationale to `DateRangePicker.tsx` (Plan
12-06): the calendar's own chrome intentionally stays react-day-picker's default, per UI-SPEC's scope
decision.

LabFields.tsx: change the `inputClass` constant (line 22) from `"min-h-12 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-foam)] px-3 text-[18px] text-[var(--color-ink)]"` by swapping
`rounded-lg`→`rounded-xl`. Change the `labelClass` constant (line 23) from `"flex flex-col gap-1
text-[20px] font-bold text-[var(--color-ink)]"` by swapping `text-[20px]`→`text-control`. These two
constants are consumed by every field in the component (Test name, Result, Unit, Normal range low/high,
Notes textarea) — a single edit to each constant propagates to all of them. Do not touch the
`numericFieldValid`/`canSubmit` logic or the `useEffect` draft-assembly block.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-xl" frontend/src/components/records/SingleDateField.tsx` equals 1
    - `grep -c "text-control" frontend/src/components/records/SingleDateField.tsx` equals 1
    - `grep -c -- "--rdp-day-width\|--rdp-day_button-width\|--rdp-accent-color\|--rdp-accent-background-color" frontend/src/components/records/SingleDateField.tsx` equals 4 (rdpSizing untouched)
    - `grep -c "rounded-xl" frontend/src/components/records/LabFields.tsx` equals 1 (inputClass constant, propagates to all 6 fields)
    - `grep -c "text-control" frontend/src/components/records/LabFields.tsx` equals 1 (labelClass constant)
    - `grep -c "rounded-lg" frontend/src/components/records/SingleDateField.tsx frontend/src/components/records/LabFields.tsx` equals 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>SingleDateField's and LabFields' shared input/label constants show rounded-xl and text-control; SingleDateField's rdpSizing bridge is byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: IncidentFields.tsx + ProcedureFields.tsx — radius and type-scale</name>
  <files>frontend/src/components/records/IncidentFields.tsx, frontend/src/components/records/ProcedureFields.tsx</files>
  <read_first>frontend/src/components/records/IncidentFields.tsx (full file, 120 lines — the `inputClass` constant at line 27, the `labelClass` constant at line 28), frontend/src/components/records/ProcedureFields.tsx (full file, 106 lines — the `inputClass` constant at line 19, the `labelClass` constant at line 20)</read_first>
  <action>
IncidentFields.tsx: change the `inputClass` constant (line 27) from `"min-h-12 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-foam)] px-3 text-[18px] text-[var(--color-ink)]"` by swapping
`rounded-lg`→`rounded-xl`. Change the `labelClass` constant (line 28) from `"flex flex-col gap-1
text-[20px] font-bold text-[var(--color-ink)]"` by swapping `text-[20px]`→`text-control`. These constants
are consumed by the Date, Time, What happened, Duration, and Notes fields — a single edit to each
propagates to all of them.

ProcedureFields.tsx: change the `inputClass` constant (line 19) from the identical string by swapping
`rounded-lg`→`rounded-xl`. Change the `labelClass` constant (line 20) by swapping
`text-[20px]`→`text-control`. Consumed by the Date, Procedure name, Location, Outcome, and Notes fields.
Do not touch either file's `canSubmit`/`combineLocalDateTime` logic or `useEffect` draft-assembly block.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-xl" frontend/src/components/records/IncidentFields.tsx` equals 1
    - `grep -c "text-control" frontend/src/components/records/IncidentFields.tsx` equals 1
    - `grep -c "rounded-xl" frontend/src/components/records/ProcedureFields.tsx` equals 1
    - `grep -c "text-control" frontend/src/components/records/ProcedureFields.tsx` equals 1
    - `grep -c "rounded-lg" frontend/src/components/records/IncidentFields.tsx frontend/src/components/records/ProcedureFields.tsx` equals 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>IncidentFields' and ProcedureFields' shared input/label constants show rounded-xl and text-control; both files' validation/draft-assembly logic is byte-identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — these four components manage local form state that reaches the backend only via
Plan 12-05's `AddRecordPage.tsx` `useCreateLab`/`useCreateIncident`/`useCreateProcedure` mutation hooks,
entirely untouched by this plan's className-only edits.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-20 | Tampering | inputClass/labelClass string constants | accept | Static, developer-authored strings; user-typed field values never reach a className. |
| T-12-21 | Elevation of Privilege (scope creep) | SingleDateField's rdpSizing/DayPicker | mitigate | Acceptance criteria explicitly grep-verify all four rdpSizing keys are unchanged, mirroring Plan 12-06's identical DateRangePicker exclusion. |
| T-12-22 | Denial of Service | none | accept | No new render path, no new validation branch — field-count and Draft-shape logic unchanged. |

No ASVS category applies — CSS/class-only change. V5 Input Validation does not apply because
`numericFieldValid`/`isValidDateText`/`canSubmit` logic in all four files is completely untouched by this
plan.
</threat_model>

<verification>
`cd frontend && npx vitest run` — full suite must stay green; OVERLAY-02 record-form tests assert on
rendered labels/values and onDraftChange call arguments, not className, so this change should not break
them.
</verification>

<success_criteria>
- All four field-set components' shared input/label constants show rounded-xl/text-control
- SingleDateField's rdpSizing bridge is untouched
- Every file's validation/draft-assembly logic is byte-identical to before
- Full frontend suite green
</success_criteria>

<output>
Create `.planning/phases/12-visual-refresh/12-07-SUMMARY.md` when done
</output>
