---
phase: 12-visual-refresh
plan: 05
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/components/UploadPage.tsx
  - frontend/src/components/AddRecordPage.tsx
  - frontend/src/components/LoginGate.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "UploadPage's success panel, reject-disclosure toggle, and error panel all show rounded-xl + shadow-elevation depth, consistent with every other notice panel sitewide (VISUAL-01)"
    - "AddRecordPage's success and error confirmation panels show the same depth treatment as UploadPage's, and its record-type selector/submit buttons show the radius-only bump (VISUAL-01)"
    - "LoginGate's sign-in card and its wrong-password notice both show rounded-xl + shadow-elevation depth, matching every other card/notice panel sitewide (VISUAL-01)"
    - "Every h2/h1 heading and 20px control label across these three files uses the named text-h2/text-h1/text-control tokens at unchanged rendered sizes (D-06)"
    - "Every min-h-12 accessibility floor, every aria-live/role=status/role=alert attribute, and the ≥18px body-text sentences in all three files are unchanged (VISUAL-02)"
  artifacts:
    - path: frontend/src/components/UploadPage.tsx
      provides: "Depth-treated success/reject/error panels, formalized text-h2/text-control"
    - path: frontend/src/components/AddRecordPage.tsx
      provides: "Depth-treated success/error panels, radius-bumped type-selector/submit buttons, formalized text-h2/text-control"
    - path: frontend/src/components/LoginGate.tsx
      provides: "Depth-treated sign-in card and wrong-password notice, formalized text-h1/text-h2/text-control"
  key_links:
    - from: frontend/src/components/AddRecordPage.tsx
      to: frontend/src/index.css
      via: "shared inactiveClass/activeClass string constants referencing rounded-xl and text-control"
      pattern: "rounded-xl.*text-control|text-control.*rounded-xl"
---

<objective>
Apply the D-05 depth treatment (rounded-lg → rounded-xl + `shadow-[var(--shadow-elevation)]`) to every
bordered notice/confirmation panel in `UploadPage.tsx`, `AddRecordPage.tsx`, and `LoginGate.tsx` — the three
caregiver-facing form/admin screens — the D-05 radius-only bump to their CTA/type-selector buttons, and the
D-06 type-scale formalization (`text-2xl`→`text-h2`, `text-[32px]`→`text-h1`, `text-[20px]`/`text-xl`→
`text-control`) throughout.

This plan depends on 12-01 for the token definitions. It shares zero files with the other five Wave 2
plans. LoginGate.tsx's wrong-password notice div is included in the depth treatment even though
PATTERNS.md's line-citation only names the outer card (line 52) — this extends the documented "every
bordered sky/foam notice panel gets depth" pattern (already applied to UploadPage's and AddRecordPage's
error sections) to LoginGate's own equivalent notice, for genuine cross-file consistency rather than an
arbitrary exception.

Purpose: Upload, Add Record, and Login are the three "caregiver admin" screens VISUAL-01's success
criterion names explicitly ("every screen — dashboard, upload, guide, forms").
Output: three components with depth/radius/type-scale applied to every panel and button, zero change to
accessibility floors or ARIA roles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-visual-refresh/12-CONTEXT.md
@.planning/phases/12-visual-refresh/12-UI-SPEC.md
@.planning/phases/12-visual-refresh/12-PATTERNS.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: UploadPage.tsx — depth, radius, and type-scale</name>
  <files>frontend/src/components/UploadPage.tsx</files>
  <read_first>frontend/src/components/UploadPage.tsx (full file, 198 lines — the h2 at line 88, the "Choose a file" label-button at line 101, the success section at line 126, the "Show which rows" disclosure button at line 145, the error section at line 174)</read_first>
  <action>
On the `<h2>` (line 88), change `className="text-2xl font-bold leading-tight text-[var(--color-ink)]"` to
use `text-h2` in place of `text-2xl`. On the "Choose a file" label (line 101), change `className="inline-flex
min-h-12 w-fit cursor-pointer items-center gap-2 rounded-lg bg-[var(--color-accent)] px-6 text-[20px]
font-bold text-[var(--color-accent-text)]"` to swap `rounded-lg`→`rounded-xl` and
`text-[20px]`→`text-control` (radius-only, no shadow — it is a button-affordance label, not a panel). On
the success section (line 126), change `className="flex flex-col gap-4 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-sky)] p-6 text-[var(--color-ink)]"` to swap
`rounded-lg`→`rounded-xl` and append ` shadow-[var(--shadow-elevation)]`. On the "Show which rows"
disclosure button (line 145), change `className="flex w-fit items-center gap-2 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-sky)] px-4 py-2 text-[20px] font-bold
text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl`, append `
shadow-[var(--shadow-elevation)]`, and swap `text-[20px]`→`text-control` — PATTERNS.md's exhaustive
line-citation explicitly names this button alongside the panels for depth treatment. On the error section
(line 174), change `className="flex items-start gap-2 rounded-lg border-2 border-[var(--color-ink)]
bg-[var(--color-sky)] p-6 text-lg text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl` and append `
shadow-[var(--shadow-elevation)]` — leave `text-lg` (body-adjacent) unchanged. Do not touch the
`ChevronDown`'s `transition-transform` classes (line 150) — a pre-existing disclosure-arrow animation, not
a new D-07 motion layer.
  </action>
  <acceptance_criteria>
    - `grep -c "text-h2" frontend/src/components/UploadPage.tsx` equals 1 and `grep -c "text-2xl" frontend/src/components/UploadPage.tsx` equals 0
    - `grep -c "rounded-xl.*text-control\|text-control.*rounded-xl" frontend/src/components/UploadPage.tsx` equals 2 ("Choose a file" label + "Show which rows" button)
    - `grep -c "shadow-\[var(--shadow-elevation)\]" frontend/src/components/UploadPage.tsx` equals 3 (success section, "Show which rows" button, error section)
    - `grep -c "rounded-lg" frontend/src/components/UploadPage.tsx` equals 0 (every occurrence renamed)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>UploadPage's heading, file-choose affordance, success/error panels, and reject-disclosure button all show the depth/radius/type-scale treatment; the disclosure-arrow transition is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: AddRecordPage.tsx + LoginGate.tsx — depth, radius, and type-scale</name>
  <files>frontend/src/components/AddRecordPage.tsx, frontend/src/components/LoginGate.tsx</files>
  <read_first>frontend/src/components/AddRecordPage.tsx (full file, 227 lines — the shared inactiveClass/activeClass at lines 54-57, the h2 at line 145, the submit button's canSubmit-true/false className ternary at lines 187-191, the success section at line 200, the error section at line 213), frontend/src/components/LoginGate.tsx (full file, 119 lines — the form card at line 52, the h1 at line 60, the h2 at line 63, the label at line 71, the input at line 84, the rejected-notice div at line 94, the submit button at line 111)</read_first>
  <action>
AddRecordPage.tsx: in the shared `inactiveClass` constant (line 55), change `rounded-lg` to `rounded-xl` and
`text-[20px]` to `text-control` (radius-only, no shadow — type-selector buttons). In `activeClass` (line
57), apply the identical swap. On the `<h2>` (line 145), change `text-2xl` to `text-h2`. On the submit
button's className ternary (lines 189-190), change both branches' `rounded-lg`→`rounded-xl` and
`text-[20px]`→`text-control`. On the success section (line 200), change `className="flex items-start gap-2
rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-sky)] p-6
text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl` and append `
shadow-[var(--shadow-elevation)]`. On the error section (line 213), apply the identical
`rounded-lg`→`rounded-xl` + shadow swap to `className="flex items-start gap-2 rounded-lg border-2
border-[var(--color-ink)] bg-[var(--color-sky)] p-6 text-lg
text-[var(--color-ink)]"`.

LoginGate.tsx: on the form card (line 52), change `className="flex w-full max-w-[28rem] flex-col gap-4
rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-sky)] p-6 md:p-8"` to swap
`rounded-lg`→`rounded-xl` and append ` shadow-[var(--shadow-elevation)]`. On the `<h1>` (line 60), change
`text-[32px]` to `text-h1`. On the `<h2>` (line 63), change `text-2xl` to `text-h2`. On the "Password"
label (line 71), change `text-[20px]` to `text-control`. On the password input (line 84), change
`rounded-lg` to `rounded-xl` (radius-only; `text-lg` unchanged). On the rejected-password notice div (line
94), change `className="flex items-start gap-2 rounded-lg border-2 border-[var(--color-ink)]
bg-[var(--color-sky)] p-4 text-lg text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl` and append `
shadow-[var(--shadow-elevation)]` — this extends the same "every bordered notice panel gets depth" pattern
already applied to UploadPage's/AddRecordPage's error sections in this plan, for cross-file consistency. On
the submit button (line 111), change `className="min-h-12 rounded-lg bg-[var(--color-accent)] px-6
text-[20px] font-bold text-[var(--color-accent-text)] disabled:opacity-50"` to swap
`rounded-lg`→`rounded-xl` and `text-[20px]`→`text-control`.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-xl.*text-control\|text-control.*rounded-xl" frontend/src/components/AddRecordPage.tsx` equals 4 (inactiveClass, activeClass, submit-true, submit-false branches)
    - `grep -c "text-h2" frontend/src/components/AddRecordPage.tsx` equals 1
    - `grep -c "shadow-\[var(--shadow-elevation)\]" frontend/src/components/AddRecordPage.tsx` equals 2 (success + error sections)
    - `grep -c "rounded-lg" frontend/src/components/AddRecordPage.tsx` equals 0
    - `grep -c "text-h1" frontend/src/components/LoginGate.tsx` equals 1 and `grep -c "text-h2" frontend/src/components/LoginGate.tsx` equals 1
    - `grep -c "shadow-\[var(--shadow-elevation)\]" frontend/src/components/LoginGate.tsx` equals 2 (form card + rejected-notice div)
    - `grep -c "rounded-lg" frontend/src/components/LoginGate.tsx` equals 0
    - `grep -c "text-control" frontend/src/components/LoginGate.tsx` equals 2 (Password label + submit button)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>AddRecordPage's type-selector/submit buttons and success/error panels, and LoginGate's card, headings, input, notice panel, and submit button all show the depth/radius/type-scale treatment; no rounded-lg remains in either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| LoginGate password input → `/auth` request | Pre-existing boundary, entirely untouched by this plan (no logic change, className/token edits only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Tampering | className string literals | accept | Static, developer-authored strings; password value is never interpolated into a className. |
| T-12-15 | Information Disclosure | LoginGate rejected-notice styling | accept | Copy is unchanged (still the fixed "That password didn't work" string per D-10) — only depth/radius styling changes, no new information is disclosed by a rounder, shadowed panel. |
| T-12-16 | Elevation of Privilege | AddRecordPage record-type selector | accept | Selector still gates on `isSubmitting` exactly as before (untouched logic); only className tokens change. |

No ASVS category applies — CSS/class-only change. V2 Authentication does not apply because LoginGate's
`handleSubmit`/`postAuth` logic is completely untouched by this plan.
</threat_model>

<verification>
`cd frontend && npx vitest run` — full suite must stay green; SEC-01/OVERLAY-02 tests assert on rendered
text/role/aria attributes, not className, so this change should not break them.
</verification>

<success_criteria>
- UploadPage's heading, file-choose affordance, and all three panels show the treatment
- AddRecordPage's type-selector/submit buttons and both panels show the treatment
- LoginGate's card, headings, label, input, notice panel, and submit button show the treatment
- Zero `rounded-lg` remains in any of the three files
- Full frontend suite green
</success_criteria>

<output>
Create `.planning/phases/12-visual-refresh/12-05-SUMMARY.md` when done
</output>
