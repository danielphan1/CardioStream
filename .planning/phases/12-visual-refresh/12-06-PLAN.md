---
phase: 12-visual-refresh
plan: 06
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - frontend/src/components/GuideOverlay.tsx
  - frontend/src/components/DateRangePicker.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "GuideOverlay's Close button and every 'Jump to a section' link show rounded-xl + shadow-elevation depth, per PATTERNS.md's explicit line-citation of these two controls (VISUAL-01)"
    - "GuideOverlay's page title, all nine section h2 headings (via the shared h2Class constant), the nav label, and the 'What Can I Say' category labels all use the named text-h1/text-h2/text-control tokens at unchanged rendered sizes (D-06)"
    - "DateRangePicker's From/To text inputs and Apply button show rounded-xl corners at unchanged min-h-12 size, and its labels use text-control (D-05/D-06)"
    - "DateRangePicker's rdpSizing bridge object and the react-day-picker DayPicker's own internal chrome are completely untouched — the calendar's day-cell/month-nav corners intentionally stay visually distinct, per UI-SPEC's explicit third-party-widget scope decision"
    - "GuideOverlay's focus-management effects, Escape-to-close listener, and scroll-clearance math (CLOSE_BAR_HEIGHT/DEFAULT_CLEARANCE_ABOVE/CLEARANCE_BUFFER) are untouched — this is a className-only pass"
  artifacts:
    - path: frontend/src/components/GuideOverlay.tsx
      provides: "Depth-treated Close button and jump-links, formalized text-h1/text-h2/text-control"
    - path: frontend/src/components/DateRangePicker.tsx
      provides: "rounded-xl inputs and Apply button, formalized text-control labels"
  key_links:
    - from: frontend/src/components/DateRangePicker.tsx
      to: "react-day-picker's --rdp-* custom-property surface"
      via: "rdpSizing object bridging var(--color-accent)/var(--color-sky) only — unchanged by this plan"
      pattern: "rdp-accent-color"
---

<objective>
Apply the D-05 depth treatment to `GuideOverlay.tsx`'s Close button and jump-to-section links (the two
controls PATTERNS.md's exhaustive map names explicitly), the D-05 radius-only bump to
`DateRangePicker.tsx`'s text inputs and Apply button, and the D-06 type-scale formalization across both
files — while explicitly leaving `DateRangePicker.tsx`'s `rdpSizing` bridge and the underlying
`react-day-picker` calendar chrome untouched, per UI-SPEC's documented scope decision that the calendar's
internal day-cell/month-nav corners intentionally stay visually distinct from the app-owned surfaces this
phase refreshes (RESEARCH.md Pitfall 3).

This plan depends on 12-01 for the token definitions. It shares zero files with the other five Wave 2
plans. `GuideOverlay.tsx` is paired with `DateRangePicker.tsx` here (rather than with another
depth-treatment-heavy file) because GuideOverlay's ~9-section body means its own task is already
appropriately sized on its own; DateRangePicker is small enough to fit as this plan's second task without
overloading either.

Purpose: the Guide (GUIDE-01) and the custom-date-range picker (used from FilterBar's "Custom…"
disclosure) are both reachable from the dashboard and must carry the same visual language as everything
else — GuideOverlay in particular is explicitly named in ROADMAP.md's phase goal ("dashboard, upload,
guide, forms").
Output: two components with depth/radius/type-scale applied only to the app-owned surfaces named in the
design contract, with the third-party DayPicker widget's internal chrome fully preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-visual-refresh/12-CONTEXT.md
@.planning/phases/12-visual-refresh/12-UI-SPEC.md
@.planning/phases/12-visual-refresh/12-PATTERNS.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: GuideOverlay.tsx — depth, radius, and type-scale</name>
  <files>frontend/src/components/GuideOverlay.tsx</files>
  <read_first>frontend/src/components/GuideOverlay.tsx (full file, 321 lines — the shared `h2Class` constant at line 29, the Close button at line 126, the page `<h1>` at line 162, the nav label at line 167, the jump-link `<a>` at line 175, the "What Can I Say" `<h3>` at line 298)</read_first>
  <action>
Change the shared `h2Class` constant (line 29) from `"text-2xl leading-tight font-bold
text-[var(--color-ink)]"` to use `text-h2` in place of `text-2xl` — this single constant is consumed by all
nine section `<h2>` elements (Command Bar, Filters, Charts, Overlay, Voice Replies, Upload, Add a Record,
What Can I Say, About This Guide), so one edit propagates everywhere. On the Close button (line 126),
change `className="flex min-h-12 items-center gap-2 rounded-lg border-2 border-[var(--color-ink)]
bg-[var(--color-sky)] px-6 text-[20px] font-bold text-[var(--color-ink)]"` to swap
`rounded-lg`→`rounded-xl`, append ` shadow-[var(--shadow-elevation)]`, and swap
`text-[20px]`→`text-control` — PATTERNS.md's exhaustive citation names this control explicitly. On the page
`<h1>` (line 162), change `text-[32px]` to `text-h1`. On the nav label "Jump to a section" (line 167),
change `text-[20px]` to `text-control`. On each jump-link `<a>` (line 175), change `className="flex min-h-12
items-center rounded-lg border-2 border-[var(--color-ink)] bg-[var(--color-sky)] px-4 text-[20px]
font-bold text-[var(--color-ink)]"` to swap `rounded-lg`→`rounded-xl`, append `
shadow-[var(--shadow-elevation)]`, and swap `text-[20px]`→`text-control` — same explicit citation as the
Close button. On the "What Can I Say" per-category `<h3>` (line 298), change
`text-[20px]` to `text-control`. Do NOT touch `bodyClass` (line 30, stays `text-lg`), the
`scrollMarginTop`/`paddingTop` clearance math (lines 98-160), the focus-management `useEffect`s (lines
63-94), or the Escape-key listener.
  </action>
  <acceptance_criteria>
    - `grep -c "text-h2" frontend/src/components/GuideOverlay.tsx` equals 1 (the shared h2Class constant — propagates to all 9 sections)
    - `grep -c "text-2xl" frontend/src/components/GuideOverlay.tsx` equals 0
    - `grep -c "text-h1" frontend/src/components/GuideOverlay.tsx` equals 1
    - `grep -c "rounded-xl.*shadow-\[var(--shadow-elevation)\]" frontend/src/components/GuideOverlay.tsx` equals 2 (Close button + jump-link)
    - `grep -c "text-control" frontend/src/components/GuideOverlay.tsx` equals 4 (Close button, nav label, jump-link, What-Can-I-Say h3)
    - `grep -c "rounded-lg" frontend/src/components/GuideOverlay.tsx` equals 0
    - `grep -c "CLOSE_BAR_HEIGHT\|DEFAULT_CLEARANCE_ABOVE\|CLEARANCE_BUFFER" frontend/src/components/GuideOverlay.tsx` unchanged from pre-task count (clearance math untouched)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>GuideOverlay's Close button and jump-links show rounded-xl + shadow-elevation; the page title, all nine section headings, nav label, and category labels use the named type-scale tokens; the scroll-clearance math and focus-management logic are byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: DateRangePicker.tsx — radius and type-scale on app-owned inputs only</name>
  <files>frontend/src/components/DateRangePicker.tsx</files>
  <read_first>frontend/src/components/DateRangePicker.tsx (full file, 123 lines — the `rdpSizing` object at lines 21-28, which must NOT be touched; the shared `inputClass` at lines 57-58; the From/To labels at lines 63 and 80; the Apply button className ternary at lines 112-116)</read_first>
  <action>
On the shared `inputClass` constant (line 58), change `rounded-lg` to `rounded-xl` — leave `text-[18px]`
(body floor) unchanged. On the "From" label (line 63) and the "To" label (line 80), change `text-[20px]` to
`text-control` on both. On the Apply button's className ternary (lines 114-115), change both branches'
`rounded-lg`→`rounded-xl` and `text-[20px]`→`text-control`. Do NOT touch the `rdpSizing` object (lines
21-28) or anything passed to the `<DayPicker>` element (lines 100-105) — the calendar's own day-cell/
month-nav radius and shadow intentionally stay as react-day-picker's default chrome, per UI-SPEC's explicit
scope decision (RESEARCH.md Pitfall 3): only the accent color (already bridged via
`"--rdp-accent-color": "var(--color-accent)"`) updates automatically from Plan 12-01, with no new bridge
variable added this phase.
  </action>
  <acceptance_criteria>
    - `grep -c "rounded-xl" frontend/src/components/DateRangePicker.tsx` equals 3 (inputClass, Apply-true, Apply-false)
    - `grep -c "text-control" frontend/src/components/DateRangePicker.tsx` equals 4 (From label, To label, Apply-true, Apply-false)
    - `grep -c "rounded-lg" frontend/src/components/DateRangePicker.tsx` equals 0
    - `grep -c -- "--rdp-day-width\|--rdp-day_button-width\|--rdp-accent-color\|--rdp-accent-background-color" frontend/src/components/DateRangePicker.tsx` equals 4 (rdpSizing object fully unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=dot</automated>
  </verify>
  <done>DateRangePicker's From/To inputs and Apply button show rounded-xl at unchanged min-h-12 size and use text-control labels; the rdpSizing bridge and DayPicker's own internal chrome are byte-identical to before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — GuideOverlay renders static copy from `voiceCommands.ts`; DateRangePicker's typed
date-text inputs go through the existing `isValidDateText`/`parseDateOnly` validation path, entirely
untouched by this plan's className-only edits.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-17 | Tampering | className string literals | accept | Static, developer-authored strings; typed date text is never interpolated into a className. |
| T-12-18 | Elevation of Privilege (scope creep) | DateRangePicker's rdpSizing / DayPicker internals | mitigate | Acceptance criteria explicitly grep-verify all four rdpSizing keys are unchanged, preventing an accidental bridge-variable addition beyond UI-SPEC's documented scope decision. |
| T-12-19 | Denial of Service | none | accept | No new render path, no new loop — GuideOverlay's section list and DateRangePicker's calendar are unchanged in structure. |

No ASVS category applies — CSS/class-only change, zero new input-handling surface.
</threat_model>

<verification>
`cd frontend && npx vitest run` — full suite must stay green; GUIDE-01/02/03/04 tests assert on rendered
text/aria attributes and focus targets, not className, so this change should not break them.
</verification>

<success_criteria>
- GuideOverlay's Close button and jump-links show rounded-xl + shadow-elevation
- GuideOverlay's title/headings/labels use text-h1/text-h2/text-control at unchanged sizes
- DateRangePicker's inputs and Apply button show rounded-xl and text-control
- DateRangePicker's rdpSizing bridge and DayPicker internals are untouched
- Full frontend suite green
</success_criteria>

<output>
Create `.planning/phases/12-visual-refresh/12-06-SUMMARY.md` when done
</output>
