---
phase: 12-visual-refresh
plan: 08
type: execute
wave: 3
depends_on: ["12-02", "12-03", "12-04", "12-05", "12-06", "12-07"]
files_modified: []
autonomous: false
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "The full frontend suite (329 tests: 323 pre-existing + 6 new contrast tests) passes with zero regressions after every Phase 12 plan has landed (VISUAL-02c)"
    - "Every screen — dashboard (all four charts), upload, guide overlay, and all three record forms — visibly renders the new warm accent color, added surface depth, and formalized type scale consistently in BOTH light and dark theme, with no layout breakage (VISUAL-01)"
    - "No min-h-12, min-w-12, or --text-base occurrence was removed or altered anywhere in the frontend/src diff introduced by Phase 12 (VISUAL-02b)"
    - "Every D-04-locked token (--color-focus, --cat-hypotension..--cat-crisis, --band-opacity, --cat-chip-text, --overlay-labs/--overlay-incidents/--overlay-procedures, --overlay-chip-text, --line-systolic/--line-diastolic, --ref-bradycardia) remains byte-identical to its pre-Phase-12 value"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 12 with the two verifications RESEARCH.md/12-VALIDATION.md flag as requiring more than a
per-plan automated test run: (1) an automated full-suite + grep-based floor/exclusion audit across every
file the six Wave 2 plans touched, and (2) the manual cross-screen, cross-theme visual pass VISUAL-01
requires (no automated visual-regression tooling exists in this repo, and introducing one is explicitly
out of this phase's bounded scope per RESEARCH.md).

This plan depends on all six Wave 2 plans (12-02 through 12-07) because it is the single gate that proves
the whole-site consistency claim VISUAL-01 makes — no individual Wave 2 plan can prove "every screen," only
this final plan, once every file has landed, can.

Purpose: goal-backward closure for both phase requirements — VISUAL-01 (modernized theme, consistently
applied) and VISUAL-02 (accessibility floors preserved) — with a documented, repeatable command trail
rather than a one-time eyeball check.
Output: a green full-suite run, a clean floor/exclusion grep audit, and a recorded manual sign-off across
all screens/themes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-visual-refresh/12-CONTEXT.md
@.planning/phases/12-visual-refresh/12-UI-SPEC.md
@.planning/phases/12-visual-refresh/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression run + floor/exclusion grep audit</name>
  <files>none (verification-only task, no files modified)</files>
  <read_first>.planning/phases/12-visual-refresh/12-VALIDATION.md (Manual-Only Verifications table and Wave 0 Requirements — the exact commands and rationale this task automates the CLI-checkable half of), frontend/src/index.css (the post-Phase-12 state — confirms every D-04-locked token value)</read_first>
  <action>
Run the full frontend suite and confirm 329/329 tests pass (323 pre-existing + 6 contrast tests from Plan
12-01), zero failures, zero skipped. Then run a grep-based audit across every component file touched by
Plans 12-02 through 12-07 (StatsStrip.tsx, ChartTooltip.tsx, EmptyState.tsx, ReadingsTable.tsx,
OverlayEventsList.tsx, ChartDeck.tsx, FilterBar.tsx, OverlayToggle.tsx, Header.tsx,
AgentStatusBanner.tsx, CommandBar.tsx, UploadPage.tsx, AddRecordPage.tsx, LoginGate.tsx,
GuideOverlay.tsx, DateRangePicker.tsx, and the four `records/*.tsx` files) confirming every `min-h-12` and
`min-w-12` occurrence present before Phase 12 is still present (no accessibility-floor class was dropped
while bumping `rounded-lg`→`rounded-xl` in the same className string — RESEARCH.md Pitfall 4). Separately,
grep `frontend/src/index.css` to confirm the exact pre-Phase-12 hex/value strings for every D-04-locked
token (`--color-focus: #1D4E89`/`#8FC1D4`, `--cat-hypotension: #4A6B8A`/`#8FB3D1`,
`--cat-crisis: #7F1D1D`/`#F2564D`, `--band-opacity: 0.10`/`0.14`, `--overlay-labs: #6A3FA0`/`#C9A6EA`) are
still present verbatim.
  </action>
  <acceptance_criteria>
    - `cd frontend && npx vitest run` exits 0 with 329 passed, 0 failed
    - `grep -rc "min-h-12" frontend/src/components frontend/src/components/charts frontend/src/components/records | awk -F: '{s+=$2} END {print s}'` is greater than or equal to 60 (matches the ~67 pre-Phase-12 occurrence count PATTERNS.md's Spacing Scale table records — none removed)
    - `grep -c "color-focus: #1D4E89" frontend/src/index.css` equals 1 and `grep -c "color-focus: #8FC1D4" frontend/src/index.css` equals 1
    - `grep -c "cat-hypotension: #4A6B8A" frontend/src/index.css` equals 1 and `grep -c "cat-crisis: #F2564D" frontend/src/index.css` equals 1
    - `grep -c "band-opacity: 0.10" frontend/src/index.css` equals 1 and `grep -c "band-opacity: 0.14" frontend/src/index.css` equals 1
    - `grep -c "overlay-labs: #6A3FA0" frontend/src/index.css` equals 1 and `grep -c "overlay-labs: #C9A6EA" frontend/src/index.css` equals 1
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <done>The full 329-test suite is green, every min-h-12/min-w-12 accessibility-floor class is intact across every touched file, and every D-04-locked clinical/overlay/focus/band token is byte-identical to its pre-Phase-12 value.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cross-screen, cross-theme manual visual verification</name>
  <files>none (verification-only task, no files modified)</files>
  <action>
Present the running dashboard to the user for manual sign-off, since no automated visual-regression
tooling exists in this repo and introducing one is out of this phase's bounded scope (12-VALIDATION.md's
documented "manual only" rationale for VISUAL-01/VISUAL-02b). Follow the exact steps in
&lt;how-to-verify&gt; below and wait for the resume signal before considering this plan (and the phase)
complete.
  </action>
  <what-built>
The complete Phase 12 visual refresh: a new warm terracotta/coral-salmon accent (light `#B94927` / dark
`#DA6F4E`, replacing navy) applied sitewide via a single token change; added surface depth
(`rounded-xl` corners + a theme-aware `shadow-elevation`) on every named card/panel/notice surface across
the dashboard, upload, guide, and all three record forms; and a formalized `text-control`/`text-h2`/
`text-h1` type scale at unchanged rendered pixel sizes. All 20 touched components were verified via
automated grep/test checks in Task 1 above — this checkpoint is the visual/subjective pass no automated
tool in this repo can perform.
  </what-built>
  <how-to-verify>
    1. Start the dev server (`cd frontend && npm run dev`) and open the app in a browser.
    2. On the Dashboard, confirm in LIGHT theme: the accent color on the "Send" button, active filter
       pills, and active overlay toggles reads as a warm terracotta/coral tone (not navy); StatsStrip
       tiles, the readings table, and chart-picker mini cards show a visible soft shadow and slightly
       rounder corners than before.
    3. Toggle to DARK theme (Header's Theme button) and repeat: confirm the accent reads as a warm
       coral-salmon tone, and confirm cards are still visually distinguishable from the page background
       (the shadow uses a light hairline in dark mode, not just a black shadow) — this is the one pattern
       most likely to look "off" if the dark-mode shadow value is wrong.
    4. Visit Upload (Header → "Upload"): confirm the "Choose a file" button uses the new accent and the
       upload result/error panels show the depth treatment. Try uploading a file to see the success panel.
    5. Visit Add Record (Header → "Add Record"): confirm the Lab/Incident/Procedure type-selector pills and
       submit button use the new accent, and typing into any field shows rounded-xl input borders.
    6. Open the Guide (Header → "Guide"): confirm the Close button and "Jump to a section" links show the
       depth treatment, and every section heading renders at the same size as before (no visual size jump).
    7. Confirm nothing looks broken: no overlapping text, no button whose label got clipped by the corner
       radius change, no chart clipped or misaligned.
    8. Spot-check keyboard navigation (Tab through the Dashboard's controls) — confirm the 3px focus ring
       still appears clearly on every control and never blends into the new accent color.
  </how-to-verify>
  <verify>
    <human-check>Every screen (dashboard, upload, guide, forms) renders the new accent/depth/type consistently in both light and dark theme, with no layout breakage and no accessibility-floor regression</human-check>
  </verify>
  <done>User has typed "approved" after confirming all 8 verification steps pass in both light and dark theme.</done>
  <resume-signal>Type "approved" if every screen looks consistent and correct in both themes, or describe what looks wrong (screen, theme, and control name).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan runs verification commands and a manual visual review only; it modifies
no source file (`files_modified: []`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-23 | Repudiation | Phase-12 completion claim | mitigate | This plan's grep-based acceptance criteria and the human-verify checkpoint together produce a concrete, checkable record that VISUAL-01/VISUAL-02 were actually verified, not just assumed complete because every Wave 2 plan's own tests passed. |

No ASVS category applies — this is a verification-only plan with zero code change.
</threat_model>

<verification>
`cd frontend && npx vitest run` full suite green (329/329), the floor/exclusion grep audit in Task 1
clean, and the Task 2 human checkpoint approved.
</verification>

<success_criteria>
- Full frontend suite green (329/329)
- Every min-h-12/min-w-12 occurrence intact
- Every D-04-locked token byte-identical to its pre-Phase-12 value
- Human verification of cross-screen, cross-theme visual consistency approved
</success_criteria>

<output>
Create `.planning/phases/12-visual-refresh/12-08-SUMMARY.md` when done
</output>
