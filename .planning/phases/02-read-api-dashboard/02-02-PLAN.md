---
phase: 02-read-api-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
  - frontend/src/tests/setup.ts
  - frontend/src/tests/smoke.test.tsx
  - frontend/src/store/theme.ts
  - frontend/src/api/client.ts
  - frontend/src/api/types.ts
  - frontend/.env.development
autonomous: false
requirements: [ACC-01]

must_haves:
  truths:
    - "npm run build and npm test -- --run both exit 0 in frontend/"
    - "TypeScript is pinned to ~5.9 (npx tsc --version reports 5.9.x), not the npm-latest 6/7 line"
    - "index.css defines the complete light + dark token set from 02-UI-SPEC.md (surfaces, chart series, six clinical category vars) so later plans never invent colors"
    - "Toggling the theme store adds/removes .dark on <html> and persists to localStorage"
    - "index.html loads zero third-party scripts or font CDNs (SEC-03 discipline)"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Tailwind 4 CSS-first tokens: @custom-variant dark, @theme, all UI-SPEC color custom properties for both themes"
      contains: "@custom-variant dark"
    - path: "frontend/src/api/types.ts"
      provides: "Reading, StatsSummary, CategoryStat, BPCategory, ChartId TS types mirroring backend schemas.py JSON keys"
      contains: "Hypertensive Crisis"
    - path: "frontend/src/store/theme.ts"
      provides: "zustand theme store with .dark class + localStorage persistence (D-15)"
      contains: "localStorage"
    - path: "frontend/src/api/client.ts"
      provides: "fetch wrapper with VITE_API_URL base"
      contains: "VITE_API_URL"
    - path: "frontend/package.json"
      provides: "Locked dependency set incl. recharts 3.9, zustand 5, react-query 5, react-day-picker 9, TS ~5.9"
      contains: "~5.9"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/index.css"
      via: "import + @fontsource Atkinson Hyperlegible imports"
      pattern: "atkinson-hyperlegible"
    - from: "frontend/src/store/theme.ts"
      to: "document.documentElement"
      via: "classList toggle of .dark"
      pattern: "classList"
---

<objective>
Scaffold the `frontend/` React app (create-vite react-ts) with the locked dependency set, Vitest wiring, and the complete nautical design-token system from 02-UI-SPEC.md — both light and dark themes (D-12, D-13, D-15), the self-hosted Atkinson Hyperlegible font, the API client + TS types mirroring the backend schemas, and the theme store.

Purpose: Every later frontend plan builds on these tokens and types; defining ALL color custom properties here (including the six clinical category vars, D-14) guarantees chips, bars, and bands never drift apart.
Output: Building, testing frontend scaffold with tokens, theme store, API client/types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-api-dashboard/02-CONTEXT.md
@.planning/phases/02-read-api-dashboard/02-RESEARCH.md
@.planning/phases/02-read-api-dashboard/02-UI-SPEC.md
@.planning/phases/02-read-api-dashboard/02-PATTERNS.md
</context>

<interface_contract>
Types in `frontend/src/api/types.ts` mirror the plan 02-01 backend interface_contract exactly:

- `Reading`: `{ id: number; datetime: string; systolic: number; diastolic: number; pulse: number; am_pm: "AM" | "PM"; bp_category: BPCategory; pulse_category: string; map: number; pulse_pressure: number; notes: string | null }`
- `BPCategory = "Hypotension" | "Normal" | "Elevated" | "Stage 1" | "Stage 2" | "Hypertensive Crisis"` (verbatim, spaces included)
- `VitalStats = { avg: number; min: number; max: number }`
- `CategoryStat = { category: BPCategory; count: number; percent: number }`
- `StatsSummary = { count: number; systolic: VitalStats | null; diastolic: VitalStats | null; pulse: VitalStats | null; categories: CategoryStat[]; latest_reading: string | null }`
- `ChartId = "bp_timeline" | "pulse_trend" | "bp_categories" | "am_pm_comparison"` (RESEARCH Code Example 3 — future agent command vocabulary)

CSS custom properties defined in index.css (consumed by plans 02-03..02-06; light value / dark value):
`--color-foam` #F2F7F5 / #0B1626 (page bg), `--color-sky` #E2EDF2 / #13233A (cards), `--color-ink` #14213D / #E8F1EF (all text/icons/borders), `--color-accent` #14213D / #8FC1D4 (reserved fills), `--color-accent-text` #FFFFFF / #0B1626, `--color-focus` #1D4E89 / #8FC1D4, `--line-systolic` #1D3557 / #A8CBEA, `--line-diastolic` #26707D / #6FC7C4, `--ref-bradycardia` #4A6B8A / #8FB3D1, `--cat-hypotension` #4A6B8A / #8FB3D1, `--cat-normal` #2F7D5C / #63C68F, `--cat-elevated` #8F6A00 / #E0B84E, `--cat-stage1` #B34700 / #EE9B57, `--cat-stage2` #B3261E / #EF7B70, `--cat-crisis` #7F1D1D / #F2564D, `--band-opacity` 0.10 / 0.14, `--cat-chip-text` #FFFFFF / #0B1626.
</interface_contract>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification for packages not in the RESEARCH audit</name>
  <what-built>Nothing yet — this checkpoint verifies package legitimacy BEFORE any npm install. 02-UI-SPEC.md added two packages (`lucide-react`, `@fontsource/atkinson-hyperlegible`) that are NOT in the 02-RESEARCH.md Package Legitimacy Audit table, so they are treated as [ASSUMED]. Additionally `vitest` was flagged [SUS] by slopcheck (typosquat heuristic) and verified as a false positive by the researcher (official vitest-dev/vitest repo) — confirm that verification.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lucide-react — expect: publisher `lucide` org, repository github.com/lucide-icons/lucide, millions of weekly downloads, no postinstall script
    2. Open https://www.npmjs.com/package/@fontsource/atkinson-hyperlegible — expect: `fontsource` org (github.com/fontsource/fontsource), font-files-only package, no install scripts
    3. Open https://www.npmjs.com/package/vitest — expect: repository github.com/vitest-dev/vitest, published since 2021, latest 4.x
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name any package that looks illegitimate</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Vite react-ts app, pin TypeScript ~5.9, install locked deps, wire Vitest</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/index.html, frontend/src/tests/setup.ts, frontend/src/tests/smoke.test.tsx, frontend/src/vite-env.d.ts, frontend/.env.development</files>
  <read_first>.planning/phases/02-read-api-dashboard/02-RESEARCH.md (Standard Stack tables, Installation block, Pitfall 11), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Design System table)</read_first>
  <action>
    From the repo root: `npm create vite@latest frontend -- --template react-ts`. Immediately edit `frontend/package.json` BEFORE the first install: set `"typescript": "~5.9.0"` (Pitfall 11 — npm latest is now 7.x; CLAUDE.md locks ~5.9; also sanity-check `@types/react` majors match React 19).

    Then install exactly: runtime `recharts@^3.9.2 zustand@^5.0.14 @tanstack/react-query@^5.101.2 react-day-picker@^9.14.0 lucide-react @fontsource/atkinson-hyperlegible`; dev `tailwindcss@^4.3.2 @tailwindcss/vite@^4.3.2 vitest@^4.1.10 @testing-library/react @testing-library/jest-dom jsdom` (all audited: RESEARCH audit table + Task 1 checkpoint approval).

    `frontend/vite.config.ts`: add the `@tailwindcss/vite` plugin alongside `@vitejs/plugin-react`; add Vitest config (`test: { environment: "jsdom", setupFiles: "./src/tests/setup.ts", globals: true }` — use the triple-slash vitest/config reference or `defineConfig` from `vitest/config`). Add `"test": "vitest"` script to package.json (executors always run it with `-- --run`, never watch mode).

    `frontend/src/tests/setup.ts`: import `@testing-library/jest-dom/vitest`.
    `frontend/src/tests/smoke.test.tsx`: one trivial render test of `<App />` proving jsdom + RTL wiring (replaced by real tests in later plans, keep it as the suite floor).
    `frontend/index.html`: title "Chris's Health Dashboard"; confirm ZERO third-party script/link URLs — fonts are self-hosted via @fontsource (SEC-03 discipline, UI-SPEC privacy note).
    `frontend/.env.development`: `VITE_API_URL=http://localhost:8000`.
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npx tsc --version && npm run build && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --version` prints Version 5.9.x
    - frontend/package.json contains `"typescript": "~5.9.0"`, `"recharts"`, `"react-day-picker"`, `"lucide-react"`, `"@fontsource/atkinson-hyperlegible"`
    - `npm run build` exits 0; `npm test -- --run` exits 0 with ≥1 test
    - frontend/index.html contains no `https://` script or stylesheet URLs (grep for `src="http` and `href="http` returns nothing)
    - frontend/vite.config.ts contains `jsdom` and `@tailwindcss/vite` import
  </acceptance_criteria>
  <done>Scaffold builds and tests green on the locked versions; no third-party network resources.</done>
</task>

<task type="auto">
  <name>Task 3: Design tokens (both themes), theme store, API client and types</name>
  <files>frontend/src/index.css, frontend/src/store/theme.ts, frontend/src/api/client.ts, frontend/src/api/types.ts, frontend/src/main.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/index.css (scaffolded state), frontend/src/main.tsx, .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Color, Typography, Spacing sections — the LOCKED token source), .planning/phases/02-read-api-dashboard/02-RESEARCH.md (Code Example 7, Pitfall 12), backend/app/schemas.py</read_first>
  <action>
    `frontend/src/index.css`: replace scaffold CSS with the token system. `@import "tailwindcss";` then `@custom-variant dark (&:where(.dark, .dark *));` (Pitfall 12 — Tailwind 4 defaults to prefers-color-scheme; D-15 requires a manual toggle). `@theme` block: `--text-base: 1.125rem` (18px body floor, ACC-01), font-family `"Atkinson Hyperlegible", system-ui, -apple-system, "Segoe UI", sans-serif`. Define ALL custom properties from this plan's interface_contract on `:root` (light values) with `.dark` overrides (dark values) — the exact hex pairs are listed there and sourced from the UI-SPEC contrast-computed tables (D-12/D-13/D-14; do not re-derive). Set `body` background `var(--color-foam)`, color `var(--color-ink)`, font-size 18px, line-height 1.5. Add a global `:focus-visible` rule: 3px solid `var(--color-focus)` outline with 2px offset (UI-SPEC focus contract, ACC-02).

    `frontend/src/store/theme.ts`: zustand store `{ theme: "light" | "dark", toggleTheme, initTheme }` — toggling sets/removes `document.documentElement.classList` `.dark` and persists to `localStorage` key `hv-theme`; `initTheme` reads localStorage on boot (D-15). Keep it UI state only — no server data (CLAUDE.md zustand/Query separation).

    `frontend/src/api/client.ts`: tiny typed fetch wrapper — `const BASE = import.meta.env.VITE_API_URL ?? "http://localhost:8000"`; `getJson<T>(path, params?)` builds a query string (URLSearchParams, skipping undefined), throws an `ApiError` on `!res.ok` (never surfaces raw status text to UI — UI-SPEC error copy handles presentation); exports `getReadings(filters)` → `Reading[]` and `getStatsSummary(filters)` → `StatsSummary`.

    `frontend/src/api/types.ts`: the exact types from interface_contract — JSON keys `datetime` and `map` (mirroring schemas.py aliases, PATTERNS Shared Pattern 6), category labels verbatim with spaces (Shared Pattern 2), plus `ChartId` and a `ResolvedFilters` type `{ start_date?: string; end_date?: string; am_pm?: "AM" | "PM"; bp_category?: BPCategory }`.

    `frontend/src/main.tsx`: import `@fontsource/atkinson-hyperlegible/400.css` and `.../700.css`, `index.css`; wrap `<App />` in `QueryClientProvider` (new `QueryClient`); call `initTheme()` before render.
    `frontend/src/App.tsx`: temporary shell — an `<h1>` "Chris's Health Dashboard" on the token background (real layout lands in 02-06); keep the smoke test passing.
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm run build && npm test -- --run && grep -c -- "--cat-crisis" src/index.css && grep -c "Hypertensive Crisis" src/api/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css contains `@custom-variant dark`, `--cat-hypotension`, `--cat-crisis`, `--line-systolic`, `--band-opacity`, and a `.dark` block with `#0B1626`
    - src/index.css contains a `:focus-visible` rule referencing `var(--color-focus)`
    - src/api/types.ts contains `map: number` and the six-label BPCategory union including `"Stage 1"` and `"Hypertensive Crisis"`
    - src/store/theme.ts contains `classList` and `localStorage`
    - src/main.tsx contains `@fontsource/atkinson-hyperlegible/400.css` and `QueryClientProvider`
    - `npm run build` and `npm test -- --run` exit 0
  </acceptance_criteria>
  <done>Complete two-theme token system, theme store, typed API client — later plans consume tokens/types without inventing values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Supply-chain risk on first install |
| Frontend env → shipped bundle | Anything in frontend env vars is public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs | mitigate | RESEARCH slopcheck audit ([OK] for 13 packages) + blocking-human checkpoint (Task 1) for the two [ASSUMED] UI-SPEC additions (lucide-react, @fontsource/atkinson-hyperlegible) and [SUS] vitest confirmation; exact version pins; no postinstall scripts on key packages (verified in RESEARCH) |
| T-02-06 | Information Disclosure | Frontend env vars | mitigate | Only `VITE_API_URL` exists in frontend env; no API keys or secrets exist in this phase (CLAUDE.md: all Claude calls via backend, Phase 3) |
| T-02-07 | Information Disclosure | Third-party requests (fonts/CDNs/trackers) | mitigate | Atkinson Hyperlegible self-hosted via @fontsource; index.html verified to load zero external resources (SEC-03 discipline; acceptance criterion greps for http src/href) |
</threat_model>

<verification>
- `cd frontend && npm run build && npm test -- --run` — both exit 0
- `npx tsc --version` → 5.9.x
- Manually toggling `.dark` on `<html>` in devtools flips background from #F2F7F5 to #0B1626 (spot check; full visual verification in 02-06)
</verification>

<success_criteria>
- frontend/ scaffolded on locked versions (React 19.2, Vite 8, TS ~5.9, Recharts 3.9, zustand 5, Query 5, Tailwind 4) and building green
- All UI-SPEC color tokens for BOTH themes live in index.css as CSS custom properties (D-12/D-13/D-14/D-15)
- Theme store toggles .dark + persists; API client/types mirror backend schemas exactly
- No third-party network resources anywhere (SEC-03)
</success_criteria>

<output>
Create `.planning/phases/02-read-api-dashboard/02-02-SUMMARY.md` when done
</output>
