---
phase: 02-read-api-dashboard
plan: 05
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/components/FilterBar.tsx
  - frontend/src/components/DateRangePicker.tsx
autonomous: true
gap_closure: true
requirements: [DASH-07, DASH-11]

must_haves:
  truths:
    - "Header shows 'Chris's Health Dashboard' with a sailboat mark and a large labeled theme toggle — nautical theme, NOT the Tableau prototype (DASH-11, D-01, D-15, D-16)"
    - "Filter bar offers date presets (7/30/90 days/All + Custom), AM/PM segment, and category chips as ≥48px single-select buttons whose active state is always visible (DASH-07, D-17/D-19)"
    - "Custom date range works via an oversized calendar with ≥48px day cells AND direct typed YYYY-MM-DD entry (D-18)"
    - "The filter bar reads left-to-right like a sentence of the current state (D-20)"
    - "Every control is keyboard operable with the visible focus ring — no drag, hover-only, or precision interactions (ACC-02 foundations)"
  artifacts:
    - path: "frontend/src/components/Header.tsx"
      provides: "Header bar: Sailboat mark (aria-hidden), 32px title, labeled Sun/Moon theme toggle, wave-curve divider"
      contains: "Chris's Health Dashboard"
    - path: "frontend/src/components/FilterBar.tsx"
      provides: "Preset row, AM/PM segment, category chip row, filter-state sentence, Custom affordance"
      contains: "aria-pressed"
      min_lines: 80
    - path: "frontend/src/components/DateRangePicker.tsx"
      provides: "From/To typed inputs + react-day-picker calendar with ≥48px day cells"
      contains: "DayPicker"
  key_links:
    - from: "frontend/src/components/FilterBar.tsx"
      to: "frontend/src/store/filters.ts"
      via: "setDatePreset/setAmPm/setBpCategory/setCustomRange actions"
      pattern: "useFilters"
    - from: "frontend/src/components/FilterBar.tsx"
      to: "frontend/src/lib/palette.ts"
      via: "category chip colors"
      pattern: "categoryColor"
    - from: "frontend/src/components/Header.tsx"
      to: "frontend/src/store/theme.ts"
      via: "toggleTheme"
      pattern: "useTheme"
---

<objective>
Build the header (DASH-11) and the manual filter controls (DASH-07) — the interactive surface whose state the Phase 3 agent will later mutate. Every control is a ≥48px, high-contrast, keyboard-operable button; the filter bar always shows the current state as a readable sentence.

Purpose: Closes the VERIFICATION.md gaps "Header bar (DASH-11)" and "Filter controls UI + zustand filter store (DASH-07)" at the UI layer (the store shipped in 02-03).
Output: frontend/src/components/{Header,FilterBar,DateRangePicker}.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-api-dashboard/02-CONTEXT.md
@.planning/phases/02-read-api-dashboard/02-UI-SPEC.md
@frontend/src/store/filters.ts
@frontend/src/store/theme.ts
@frontend/src/lib/dates.ts
@frontend/src/lib/palette.ts
@frontend/src/index.css
</context>

<interface_contract>
- `Header`: no props — reads/writes `useTheme` only. Rendered first in App (plan 02-07).
- `FilterBar`: props `{ latestReading: string | null }` (for the honest "to Jun 13, 2025" sentence and preset anchoring display) — all filter state via `useFilters`.
- `DateRangePicker`: props `{ from: string | null; to: string | null; onApply: (from: string, to: string) => void }` — controlled by FilterBar.

Shared control styling contract (UI-SPEC accent rules — encode as Tailwind utilities, no new CSS):
- Inactive control: `bg-[var(--color-sky)] text-[var(--color-ink)] border-2 border-[var(--color-ink)]`, `min-h-12` (48px), 20px/700 label.
- Active (accent, reserved list only): `bg-[var(--color-accent)] text-[var(--color-accent-text)]`.
- Segmented buttons expose state via `aria-pressed`; focus ring comes free from the global `:focus-visible` rule.
</interface_contract>

<tasks>

<task type="auto">
  <name>Task 1: Header — title, sailboat mark, labeled theme toggle, wave divider</name>
  <files>frontend/src/components/Header.tsx</files>
  <read_first>frontend/src/store/theme.ts (useTheme: theme/toggleTheme — the store is done, only the button is missing), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Layout item 1, Copywriting: header copy + aria-hidden Sailboat; Color: accent reserved list — the toggle is NOT on it, style as inactive control), frontend/src/index.css (tokens)</read_first>
  <action>
    Create `Header.tsx` (DASH-11 per D-01: standalone nautical design, the Tableau prototype is explicitly NOT a reference):
    - `<header>` on foam background: lucide `Sailboat` icon (`aria-hidden="true"`, size ~32, ink color) beside `<h1>` "Chris's Health Dashboard" at 32px/700 (Display role).
    - Theme toggle (D-15): a single `min-h-12` button showing lucide `Sun` + text "Light" when light, `Moon` + "Dark" when dark — ALWAYS icon + 20px text label, never icon-only (UI-SPEC theme mechanics). onClick `toggleTheme()`. `aria-pressed={theme === "dark"}`. Styled as an inactive control (sky surface, ink text, 2px ink border) — navy accent fill is reserved for the UI-SPEC list, which excludes the toggle.
    - Below the header row: a decorative wave-curve divider (D-16) — an inline SVG path filled `var(--color-sky)` (flips to deep sea in dark theme automatically), `aria-hidden="true"`, height ~24px, width 100%. No imagery behind data anywhere.
    Imports: `import { Sailboat, Sun, Moon } from "lucide-react"` (tree-shaken named imports only, UI-SPEC icon contract).
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm run build && grep -c "Chris's Health Dashboard" src/components/Header.tsx && grep -c "aria-hidden" src/components/Header.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Header.tsx contains `Sailboat`, `aria-hidden` (≥2: icon + wave divider), `toggleTheme`, `min-h-12`, and both "Light" and "Dark" label strings
    - Header.tsx contains no `#` hex literal and no `http` URL
  </acceptance_criteria>
  <done>The DASH-11 header exists: title + sailboat mark + accessible labeled theme toggle + wave divider, all token-driven.</done>
</task>

<task type="auto">
  <name>Task 2: FilterBar — preset row, AM/PM segment, category chips, filter-state sentence</name>
  <files>frontend/src/components/FilterBar.tsx</files>
  <read_first>frontend/src/store/filters.ts (exact action names and state fields — wire these, do not redefine), frontend/src/lib/palette.ts (CLINICAL_ORDER, categoryColor, CHIP_TEXT), frontend/src/lib/dates.ts (fmtLongDate and presetLabel for the sentence), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Copywriting: preset labels, filter-state sentence format; Color accent reserved list; Layout item 2)</read_first>
  <action>
    Create `FilterBar.tsx` (DASH-07 UI half; D-17/D-19/D-20) on a sky-surface bar that wraps on narrow screens (`flex flex-wrap gap-4 p-4`):

    1. Date preset segmented row (D-17): five `min-h-12` buttons labeled exactly "7 days" / "30 days" / "90 days" / "All" / "Custom…" (UI-SPEC preset labels). First four call `setDatePreset("7d" | "30d" | "90d" | "all")`; active one gets the navy accent fill (`aria-pressed={true}`). "Custom…" toggles the DateRangePicker section open (it shows active when `datePreset === "custom"`). Wrap the group in `role="group"` with `aria-label="Date range"`.

    2. AM/PM segment (D-19): three buttons `[All | AM | PM]` calling `setAmPm("all" | "AM" | "PM")`, same active/accent + aria-pressed pattern, `role="group" aria-label="Time of day"`.

    3. Category chip row (D-19): one chip per `CLINICAL_ORDER` label, `min-h-12`, 20px label, solid `categoryColor(cat)` background with `CHIP_TEXT` text color; single-select toggle — clicking an inactive chip calls `setBpCategory(cat)`, clicking the active chip calls `setBpCategory("all")` (tap again returns to all). Active chip gets a 3px ink outline + `aria-pressed`; inactive chips stay solid-colored (clinical colors are information, D-14 — never grey them out).

    4. Filter-state sentence (D-20, UI-SPEC copy): an 18px always-visible line assembling current state left-to-right, e.g. "Last 30 days · to Jun 13, 2025 · AM · All categories" — segments: preset label from the shared `presetLabel(datePreset)` helper in lib/dates.ts ("All data" / "Last 7 days" / "Last 30 days" / "Last 90 days" / "Custom range" — imported from 02-03, never re-derived locally) + (for day presets, honest anchor per Open Question 1) "· to {fmtLongDate(latestReading)}" + "· {All times|AM|PM}" + "· {All categories|{category}}". Use `aria-live="polite"` so filter changes are announced.

    5. Render `<DateRangePicker from={customRange.from} to={customRange.to} onApply={setCustomRange} />` in an expanding section below the row when Custom is open (conditional render, not a floating popover — no focus-trap complexity, keyboard-friendly disclosure).

    All state from `useFilters` selectors; component takes only `latestReading` as a prop. No fetching here.
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm run build && grep -c "aria-pressed" src/components/FilterBar.tsx && grep -c "min-h-12" src/components/FilterBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - FilterBar.tsx contains the exact labels "7 days", "30 days", "90 days", "All", "Custom…", and `setDatePreset`, `setAmPm`, `setBpCategory`
    - `grep -c "aria-pressed" src/components/FilterBar.tsx` ≥ 3 (presets, AM/PM, chips)
    - `grep -c "min-h-12" src/components/FilterBar.tsx` ≥ 3
    - Contains `aria-live` and `CLINICAL_ORDER` (chips generated from the palette module, not a hardcoded list)
    - Clicking the active category chip path calls `setBpCategory("all")` (source assertion: the toggle branch exists)
  </acceptance_criteria>
  <done>All manual filter controls exist as large, single-select, state-visible buttons wired to the store — the exact surface voice commands will mirror.</done>
</task>

<task type="auto">
  <name>Task 3: DateRangePicker — oversized calendar + typed entry</name>
  <files>frontend/src/components/DateRangePicker.tsx</files>
  <read_first>frontend/src/lib/dates.ts (parseDateOnly, formatDateParam — reuse for typed entry validation), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Design System: react-day-picker 9; Spacing: 48px calendar day cells; D-18), frontend/src/store/filters.ts (setCustomRange signature)</read_first>
  <action>
    Create `DateRangePicker.tsx` (D-18) with props from the interface_contract:
    - Two labeled text inputs "From" / "To" (`inputMode="numeric"`, placeholder "YYYY-MM-DD", 18px text, `min-h-12`) supporting direct typed entry for keyboard users. Validate on change with a strict `/^\d{4}-\d{2}-\d{2}$/` match + `parseDateOnly` round-trip (formatDateParam(parseDateOnly(s)) === s catches impossible dates like 2025-02-31); show an 18px inline error message ("Enter a date like 2025-06-13") on invalid input — never crash, never send an invalid param.
    - Below the inputs, `<DayPicker mode="range">` from `react-day-picker` (import its stylesheet `react-day-picker/style.css` once here) with day cells sized ≥48px — set the v9 CSS custom properties (`--rdp-day-width: 48px; --rdp-day-height: 48px; --rdp-day_button-width: 48px; --rdp-day_button-height: 48px`) via a wrapping element's style, and selected-day styling to the accent tokens (`--rdp-accent-color: var(--color-accent)`). Selecting a range fills both inputs (via `formatDateParam`).
    - An "Apply" button (`min-h-12`, accent fill — range application is a deliberate action) calls `onApply(from, to)` only when BOTH values are valid; disabled otherwise (with `aria-disabled` and visible disabled styling that still passes contrast).
    - DayPicker range values convert through `parseDateOnly`/`formatDateParam` ONLY — never `new Date("YYYY-MM-DD")` (Pitfall 1).
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm run build && grep -c "DayPicker" src/components/DateRangePicker.tsx && grep -c "parseDateOnly" src/components/DateRangePicker.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - DateRangePicker.tsx contains `DayPicker`, `mode="range"`, `react-day-picker/style.css`, `parseDateOnly`, `formatDateParam`, and `--rdp-day` sizing custom properties set to 48px
    - Contains zero occurrences of `new Date("` (typed strings go through parseDateOnly)
    - Apply path guards on both-valid dates (source assertion: disabled state exists)
  </acceptance_criteria>
  <done>Custom ranges are settable by calendar (≥48px cells) or keyboard typing, with strict validation feeding setCustomRange.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard/typed input → filter store → API query params | The only free-text input surface in the phase (date fields) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | Typed date entry in DateRangePicker | mitigate | Strict `\d{4}-\d{2}-\d{2}` regex + parseDateOnly round-trip validation before any value reaches the store; invalid input renders a friendly inline error, never a query param; backend independently 422s malformed dates (plan 02-01, incl. the WR-01 overflow fix) |
| T-02-08 | Tampering (XSS) | Filter sentence / labels | mitigate | All strings are React text nodes from closed unions and app-owned formatters; no dangerouslySetInnerHTML |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages — react-day-picker 9.14 and lucide-react already installed and human-approved in 02-02 (Task 1 checkpoint); adding packages is forbidden without a legitimacy audit |
</threat_model>

<verification>
- `cd frontend && npm run build && npm test -- --run` — green (no regressions; these components have no jsdom-hostile chart internals but interactive behavior is verified at the 02-07 checkpoint)
- grep gates per task: exact preset labels, aria-pressed counts, 48px day-cell custom properties, parseDateOnly usage
</verification>

<success_criteria>
- Header (DASH-11) and complete filter controls (DASH-07 UI) exist, wired to the 02-03 store
- Every control ≥48px, labeled, aria-pressed, single-select, keyboard operable — no drag/hover/precision anywhere
- Filter state is always glanceable as a sentence with the honest data-anchored date
</success_criteria>

<output>
Create `.planning/phases/02-read-api-dashboard/02-05-SUMMARY.md` when done
</output>
