# Phase 02 Deferred Items

| Item | Found During | Detail | Suggested Owner |
|------|--------------|--------|-----------------|
| Starlette deprecation warning: "Using `httpx` with `starlette.testclient` is deprecated; install `httpx2` instead" | 02-01 Task 2 (TestClient introduction) | Third-party warning from starlette 1.3.1 internals. CLAUDE.md/RESEARCH lock httpx 0.28.x this phase; revisit when the stack pins are next refreshed (httpx2 migration is a starlette/fastapi ecosystem move, not project code). | Future stack-refresh task |
