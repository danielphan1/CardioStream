---
phase: 02-read-api-dashboard
plan: 07
type: execute
wave: 3
depends_on: [02-04, 02-05, 02-06]
files_modified:
  - frontend/src/components/ChartDeck.tsx
  - frontend/src/App.tsx
  - frontend/src/tests/smoke.test.tsx
autonomous: false
gap_closure: true
requirements: [DASH-07, ACC-01, ACC-02]

must_haves:
  truths:
    - "Anyone can see and manually explore Chris's data across all four charts — the phase goal is observable in a running browser"
    - "BP Timeline is the default hero; clicking/tapping a live mini rotates it into the hero slot with a ~250ms animated swap (instant under prefers-reduced-motion) (D-02/D-03/D-04)"
    - "Changing any filter updates all charts, the stats strip, and the readings table consistently together (DASH-07, ROADMAP SC2)"
    - "Charts render Chris's full range (systolic 60–211, bradycardic pulse) without clipping in the real browser (DASH-06, ROADMAP SC4)"
    - "Every interactive target ≥48px, body text ≥18px, keyboard-only walk works with visible focus, tooltips close on Escape — no drag/hover/precision (ACC-01, ACC-02)"
    - "Zero-result filters show the guided empty state; API failure shows the friendly error with Try again (D-11, UI-SPEC error copy)"
  artifacts:
    - path: "frontend/src/components/ChartDeck.tsx"
      provides: "Hero slot + three live mini buttons + rotation state + swap animation (D-02/D-03/D-04, Pitfall 8)"
      contains: "setActiveChart"
      min_lines: 60
    - path: "frontend/src/App.tsx"
      provides: "Full dashboard assembly: Header → FilterBar → StatsStrip → hero → minis → ReadingsTable, data wiring, loading/error/empty states"
      contains: "ChartDeck"
      min_lines: 60
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/hooks/useStats.ts"
      via: "useResolvedFilters + useStats + useReadings wired once at App level"
      pattern: "useResolvedFilters"
    - from: "frontend/src/components/ChartDeck.tsx"
      to: "frontend/src/store/filters.ts"
      via: "activeChart drives hero slot; mini click calls setActiveChart"
      pattern: "activeChart"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/EmptyState.tsx"
      via: "zero-result readings render EmptyState in place of the chart deck"
      pattern: "EmptyState"
---

<objective>
Assemble the dashboard: the hero/mini rotating ChartDeck (the user's signature interaction, D-02) and the full App layout wiring every 02-03..02-06 component to live data — then verify the whole phase goal visually and with a keyboard-only accessibility pass.

Purpose: Closes the final VERIFICATION.md gap layer — App.tsx is still the documented shell; after this plan the phase goal "anyone can see and manually explore Chris's data across all four charts" is TRUE in a browser. Also discharges the two deferred verification items (.dark visual spot check; ≥48px targets + keyboard navigability).
Output: ChartDeck.tsx, assembled App.tsx, updated smoke test, human-verified dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-api-dashboard/02-CONTEXT.md
@.planning/phases/02-read-api-dashboard/02-UI-SPEC.md
@frontend/src/App.tsx
@frontend/src/api/types.ts
@frontend/src/hooks/useReadings.ts
@frontend/src/hooks/useStats.ts
@frontend/src/store/filters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChartDeck — hero slot, live mini buttons, animated rotation</name>
  <files>frontend/src/components/ChartDeck.tsx</files>
  <read_first>frontend/src/components/charts/BPTimeline.tsx and frontend/src/components/charts/CategoryBars.tsx (the variant prop contracts from plan 02-04 — hero/mini and readings vs stats props), frontend/src/store/filters.ts (activeChart, setActiveChart), .planning/phases/02-read-api-dashboard/02-RESEARCH.md (Pattern 4, Pitfall 8), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Layout items 4-5, mini-card aria-labels + titles, Motion contract)</read_first>
  <action>
    Create `ChartDeck.tsx`, props `{ readings: Reading[]; stats: StatsSummary | undefined }` (D-02/D-03/D-04):
    - Chart registry: a const array mapping the four `ChartId`s to `{ title, ariaName, element }` — titles per UI-SPEC mini-card labels: "Blood Pressure" (bp_timeline), "Pulse" (pulse_trend), "BP Categories" (bp_categories), "AM vs PM" (am_pm_comparison). CategoryBars takes `stats`; the other three take `readings`. Because `stats` arrives as `StatsSummary | undefined`, narrow it before rendering CategoryBars (early-return null/skeleton for that entry when undefined) — App renders ChartDeck only after both queries succeed, so the guard is type narrowing, not a reachable UI state.
    - Hero slot: `h-[420px]` (Pitfall 2 — the parent supplies the height) rendering the `activeChart` component with `variant="hero"`, plus a 24px/700 heading of its title above it. Default is bp_timeline via the store (D-03).
    - Mini row: the OTHER three charts in fixed registry order, 3-across ≥768px / stacked below (`grid md:grid-cols-3 gap-6`). Each mini is ONE `<button>` (whole card is the ≥48px target): visible 20px title, `aria-label="Show {ariaName} chart"`, `h-36` chart area with `variant="mini"` and a `pointer-events-none` wrapper div around the chart (Pitfall 8 — the button handles activation; minis have accessibilityLayer off from plan 02-04). onClick `setActiveChart(id)`.
    - Swap animation (D-04, UI-SPEC motion): 250ms opacity + scale CSS transition, `ease-in-out`, on the hero and the affected mini only — implement by keying the hero wrapper on `activeChart` with `transition-[opacity,transform] duration-[250ms] ease-in-out motion-reduce:transition-none` plus a mount-fade (e.g. starting opacity-0 scale-95 toggled to opacity-100 scale-100 via useEffect/requestAnimationFrame, or an equivalent CSS-only approach). Under `prefers-reduced-motion` the swap is instant (`motion-reduce:transition-none`).
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm run build && grep -c "setActiveChart" src/components/ChartDeck.tsx && grep -c "motion-reduce" src/components/ChartDeck.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - ChartDeck.tsx contains `aria-label` mini buttons, `variant="hero"`, `variant="mini"`, `pointer-events-none`, `h-[420px]`, `h-36`, and `motion-reduce`
    - Exactly one hero renders per activeChart; the registry covers all four ChartIds (source assertion: four registry entries)
  </acceptance_criteria>
  <done>The signature rotation interaction exists: live minis, whole-card buttons, animated hero swap with reduced-motion fallback.</done>
</task>

<task type="auto">
  <name>Task 2: App assembly — layout order, data wiring, loading/error/empty states, smoke test update</name>
  <files>frontend/src/App.tsx, frontend/src/tests/smoke.test.tsx</files>
  <read_first>frontend/src/App.tsx (current shell — replace it), frontend/src/tests/smoke.test.tsx (current test — it renders <App/> bare; App now uses Query hooks so the test must wrap in QueryClientProvider and stub fetch), frontend/src/hooks/useStats.ts (useResolvedFilters, useStats), frontend/src/hooks/useReadings.ts, .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Layout vertical order + gutters + section rhythm; Copywriting: error + loading states), frontend/src/components/FilterBar.tsx and frontend/src/components/StatsStrip.tsx and frontend/src/components/ReadingsTable.tsx and frontend/src/components/EmptyState.tsx (prop contracts from plans 02-05/02-06)</read_first>
  <action>
    Rewrite `App.tsx` as the real dashboard (UI-SPEC vertical order, D-20/D-22):
    - Data wiring ONCE at App level: `const resolved = useResolvedFilters();` then `useReadings(resolved)` and `useStats(resolved)`; pass data down as props (charts/strip/table stay presentational). latestReading = `stats.data?.latest_reading ?? null`.
    - Layout: `<Header />` → `<FilterBar latestReading={...} />` → `<StatsStrip stats={stats.data} isLoading={initial pending} />` → chart region → `<ReadingsTable readings={readings.data ?? []} />`. Vertical rhythm 32px between sections (`space-y-8`); page gutters responsive 16px / 32px (≥768px) / 64px (≥1280px) (`px-4 md:px-8 xl:px-16`); `max-w-screen-xl mx-auto`; single column (UI-SPEC responsive contract).
    - Chart region logic: while either query is initially pending → skeleton hero + mini placeholders with `aria-busy="true"` (no spinner after first load — keepPreviousData). If either query `isError` → error panel with 24px heading "Couldn't load the readings", 18px body "The dashboard couldn't reach the data server. It will keep retrying — or press Try again.", accent `min-h-12` "Try again" button calling both queries' `refetch()`; NEVER render error messages, status codes, or stack traces (ApiError details stay in the console at most — T-02-11). If readings succeeded and `readings.data.length === 0` → `<EmptyState latestReading={...} amPm={...} bpCategory={...} presetLabel={presetLabel(datePreset)} />` (using the shared `presetLabel` helper from lib/dates.ts — the same source FilterBar uses, never a locally re-derived label) in place of the deck (D-11; FilterBar stays visible so the user can adjust). Otherwise → `<ChartDeck readings={readings.data} stats={stats.data} />`.
    - Keep the document structure keyboard-clean: one `<main>`, headings in order (h1 in Header, h2s for sections "Readings" etc. per UI-SPEC heading role).

    Update `smoke.test.tsx`: render `<App />` inside a fresh `QueryClientProvider` (queries `retry: false`) with `global.fetch` stubbed (vi.stubGlobal) to return `[]` for `/readings` and a minimal zero-count StatsSummary (six zero categories, `latest_reading: null`) for `/stats/summary`; assert the heading "Chris's Health Dashboard" renders. Do not assert chart internals (Pitfall 2).
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm test -- --run && npm run build && grep -c "useResolvedFilters" src/App.tsx && grep -c "Couldn't load the readings" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run` (full suite) and `npm run build` exit 0
    - App.tsx contains, in source order: `<Header`, `<FilterBar`, `<StatsStrip`, `<ChartDeck`, `<ReadingsTable` (UI-SPEC vertical order)
    - App.tsx contains `useResolvedFilters`, `EmptyState`, "Couldn't load the readings", "Try again", `aria-busy`, and no raw `error.message` rendering
    - smoke.test.tsx contains `QueryClientProvider` and a fetch stub; asserts "Chris's Health Dashboard"
  </acceptance_criteria>
  <done>The dashboard is fully assembled and data-wired; every state (loading, error, empty, populated) has its contracted presentation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + accessibility verification of the assembled dashboard</name>
  <what-built>The complete Phase 2 dashboard: four rotating charts (hero + live minis), filter bar, stats strip, readings table, header with theme toggle — wired to the live read API with Chris's 132 seeded readings. This checkpoint also discharges the two verification items deferred from plan 02-02 (.dark visual spot check) and the ACC-01/ACC-02 interactive audit that was untestable before UI existed.</what-built>
  <how-to-verify>
    Start both servers:
    1. `cd /Users/dp/Documents/GitHub/Health-Visualizer/backend && .venv/bin/uvicorn app.main:app --reload` (API on :8000)
    2. `cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm run dev` → open http://localhost:5173

    Visual + functional pass:
    3. Page loads: header "Chris's Health Dashboard" with sailboat mark; BP Timeline is the hero (D-03); three live minis below; stats strip populated; table shows 20 newest rows. (Note: UAT test 6 previously reported "page does not load" — if that reproduces, capture the browser console + terminal output and report instead of approving.)
    4. BP Timeline: six subtle tinted bands BEHIND the lines, systolic line reaches 211 and dips to 60 with no clipping on the fixed 40–220 axis; "Systolic"/"Diastolic" labels at the line ends; a dot at every reading (DASH-01/05/06, D-06/D-07/D-08).
    5. Click a reading point: large-text tooltip appears and PERSISTS; close via the 48px Close button and via Escape. Confirm no hover-only behavior (D-09).
    6. Click the "Pulse" mini: it rotates into the hero with a quick (~250ms) swap; the former hero becomes a mini (D-02/D-04). Pulse hero shows the dashed "60 bpm — Bradycardia" line with most readings below it (DASH-02). Repeat for "BP Categories" (six clinical-order bars labeled like "Stage 1 — N readings (X%)", D-10) and "AM vs PM" (grouped bars, both panels).
    7. Filters: click "30 days" — charts, stats strip, AND table all update together; the sentence reads "Last 30 days · to Jun 13, 2025 · …" (DASH-07/08, D-20). Set AM, then a category chip; then pick a combination yielding zero results (e.g. Hypertensive Crisis + 7 days) — the guided empty state appears with the newest-reading date and "Show all data" restores everything (D-11). Try Custom: calendar day cells are visibly large (≥48px) and typing dates works (D-18).
    8. Stats strip spot check: open http://localhost:8000/stats/summary?am_pm=AM in another tab with AM filtered in the UI — count/avgs/percents match the tiles exactly (ROADMAP SC3).
    9. Theme: click the Dark toggle — background flips to the night-sea navy-black (#0B1626), all text/charts/chips stay readable, reload preserves the choice (D-15; deferred .dark spot check).
    10. Keyboard-only pass (ACC-02): unplug the mouse mentally — Tab through header toggle → presets → AM/PM → chips → custom fields → hero chart (single tab stop, then ArrowLeft/ArrowRight walks points and the tooltip follows) → minis → table button. Every stop shows the 3px focus ring; Escape closes the tooltip; nothing requires drag, hover, or precision (ACC-01 target sizes look ≥48px on all buttons/chips/toggle/calendar cells).
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe exactly which step failed and what you saw</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API responses / failures → rendered UI | App centralizes error presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | App error state | mitigate | UI-SPEC error copy only ("Couldn't load the readings" + Try again); ApiError from client.ts never rendered — acceptance criterion asserts no raw error.message in App.tsx |
| T-02-08 | Tampering (XSS) | Assembled data rendering | mitigate | All child components render React text nodes (verified in plans 02-04/02-06); App introduces no HTML injection surface |
| T-02-03 | Spoofing | Unauthenticated read API consumed by the dashboard | accept | Phase-scope decision carried from plan 02-01: verify_token no-op until Phase 5; local/dev exposure only, no PII beyond the single-user health data already accepted for this phase |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages in this plan; adding packages is forbidden without a legitimacy audit |
</threat_model>

<verification>
- `cd frontend && npm test -- --run && npm run build` — green
- Human checkpoint passes all 10 steps (four charts, rotation, consistent filtering, stats parity with the API, empty state, dark theme, keyboard-only walk)
- This plan's checkpoint is the evidence source for ROADMAP SC1–SC5 and closes the VERIFICATION.md `deferred:` items
</verification>

<success_criteria>
- Phase goal observable: anyone can see and manually explore Chris's data across all four charts with consistent manual filters (the state Phase 3 voice commands will mutate)
- ACC-01/ACC-02 verified against real interactive UI: ≥48px targets, ≥18px text, keyboard navigable, visible focus, no drag/hover/precision
- All loading/error/empty states match the UI-SPEC copywriting contract
</success_criteria>

<output>
Create `.planning/phases/02-read-api-dashboard/02-07-SUMMARY.md` when done
</output>
