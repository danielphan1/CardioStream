---
phase: 02-read-api-dashboard
plan: 06
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - frontend/src/components/StatsStrip.tsx
  - frontend/src/components/EmptyState.tsx
  - frontend/src/components/ReadingsTable.tsx
  - frontend/src/components/ReadingsTable.test.tsx
autonomous: true
gap_closure: true
requirements: [DASH-08, DASH-09]

must_haves:
  truths:
    - "Stats strip renders the FULL /stats/summary payload as tiles: avg/min/max × 3 vitals, reading count, % per category — never recomputed client-side (DASH-08, D-21)"
    - "Readings table shows newest-first rows, 20 at a time with a giant 'Show 20 more' button, category chips in clinical colors (DASH-09, D-23/D-24)"
    - "Zero-result filters get the guided empty state: why it's empty, newest-reading date, big 'Show all data' button — never auto-widened (D-11)"
  artifacts:
    - path: "frontend/src/components/StatsStrip.tsx"
      provides: "Tile strip rendering StatsSummary verbatim (D-21/D-22)"
      contains: "latest_reading"
      min_lines: 50
    - path: "frontend/src/components/ReadingsTable.tsx"
      provides: "Newest-first table, Show 20 more slicing, category chips, notes only when present"
      contains: "Show 20 more"
      min_lines: 60
    - path: "frontend/src/components/EmptyState.tsx"
      provides: "D-11 guided empty state with showAllData action"
      contains: "Show all data"
  key_links:
    - from: "frontend/src/components/StatsStrip.tsx"
      to: "frontend/src/api/types.ts"
      via: "props typed StatsSummary — renders API payload verbatim"
      pattern: "StatsSummary"
    - from: "frontend/src/components/ReadingsTable.tsx"
      to: "frontend/src/lib/palette.ts"
      via: "chip colors from categoryColor"
      pattern: "categoryColor"
    - from: "frontend/src/components/EmptyState.tsx"
      to: "frontend/src/store/filters.ts"
      via: "Show all data button calls showAllData"
      pattern: "showAllData"
---

<objective>
Build the stats strip (DASH-08), the readings table (DASH-09), and the D-11 guided empty state — the non-chart data surfaces that must agree with `GET /stats/summary` and `GET /readings` exactly.

Purpose: Closes the VERIFICATION.md gaps "Stats strip consuming GET /stats/summary (DASH-08)", "Readings table with category chips (DASH-09)", and the partial "Summary stats strip shows correct aggregates" truth (backend half already verified — this is the missing UI half).
Output: frontend/src/components/{StatsStrip,EmptyState,ReadingsTable}.tsx + a table behavior test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-api-dashboard/02-CONTEXT.md
@.planning/phases/02-read-api-dashboard/02-UI-SPEC.md
@frontend/src/api/types.ts
@frontend/src/lib/palette.ts
@frontend/src/lib/dates.ts
@frontend/src/store/filters.ts
</context>

<interface_contract>
- `StatsStrip`: props `{ stats: StatsSummary | undefined; isLoading: boolean }` — presentational, data fetched at App level (plan 02-07).
- `ReadingsTable`: props `{ readings: Reading[] }`.
- `EmptyState`: props `{ latestReading: string | null; amPm: "all" | "AM" | "PM"; bpCategory: string; presetLabel: string }` — copy assembly per UI-SPEC; the button calls `useFilters` `showAllData` directly.

Anti-pattern guard (RESEARCH): the strip renders `stats` fields VERBATIM — no client-side averaging, min/max, or percent math anywhere in this plan. The only client math allowed is table sorting/slicing.
</interface_contract>

<tasks>

<task type="auto">
  <name>Task 1: StatsStrip + EmptyState</name>
  <files>frontend/src/components/StatsStrip.tsx, frontend/src/components/EmptyState.tsx</files>
  <read_first>frontend/src/api/types.ts (StatsSummary: systolic/diastolic/pulse are VitalStats | null; categories always six zero-filled clinical-order rows; latest_reading is UNFILTERED), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Layout item 3 tile spec, Typography Display/Label roles, Copywriting: empty-state heading/body/CTA — copy the exact strings), frontend/src/lib/dates.ts (fmtLongDate), frontend/src/lib/palette.ts, frontend/src/store/filters.ts (showAllData)</read_first>
  <action>
    `StatsStrip.tsx` (DASH-08, D-21/D-22): a responsive row/grid of sky-surface tiles (`lg` 24px inner padding) rendering the full payload:
    - Three vital tiles ("Systolic" / "Diastolic" / "Pulse" captions at 20px/700): avg as the 32px/700 Display value, "min {min} · max {max}" at 18px. When the VitalStats is null (count 0), render an em dash "—" for all three values — never 0, never blank.
    - Reading count tile: Display value `stats.count`, caption "Readings".
    - Category percent row: one small tile/chip per `stats.categories` entry in payload order (already clinical order): category label + "{percent}%" at 18px with a solid `categoryColor` swatch dot beside it (display-only, not interactive — table-chip sizing rules allow shorter than 48px).
    - `isLoading` (initial load only): render skeleton tiles with `aria-busy="true"` (UI-SPEC loading state; after first load keepPreviousData prevents blanks).
    - No arithmetic on readings anywhere — every number comes from the `stats` prop (Architectural Responsibility Map: strip must match API-02 exactly).

    `EmptyState.tsx` (D-11, UI-SPEC copy verbatim): heading 24px/700 "No readings match these filters"; body 18px "There are no {AM|PM|(omit segment when all)} readings in {presetLabel}{ in {bpCategory}}. The newest reading is from {fmtLongDate(latestReading)}."; then the accent-filled `min-h-12` button "Show all data" calling `useFilters(s => s.showAllData)`. Never auto-widen the user's range — the button is the only escape. Optional subtle nautical touch (D-16): the lucide `Sailboat` icon aria-hidden above the heading.
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm run build && grep -c "aria-busy" src/components/StatsStrip.tsx && grep -c "Show all data" src/components/EmptyState.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - StatsStrip.tsx contains `aria-busy`, `categoryColor`, and an em-dash null branch; contains no `reduce(`, no `.length /`, and no avg/min/max computation over readings (payload rendered verbatim)
    - EmptyState.tsx contains the exact strings "No readings match these filters" and "Show all data", plus `showAllData` and `fmtLongDate`
  </acceptance_criteria>
  <done>The strip mirrors /stats/summary tile-for-tile and the guided empty state exists with its escape hatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReadingsTable — newest-first, Show 20 more, category chips</name>
  <files>frontend/src/components/ReadingsTable.tsx, frontend/src/components/ReadingsTable.test.tsx</files>
  <read_first>frontend/src/api/types.ts (Reading fields; notes: string | null), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Layout item 6, Copywriting: "Show 20 more" / "Showing all {n} readings", D-23/D-24 column spec), frontend/src/lib/palette.ts (categoryColor, CHIP_TEXT), frontend/src/lib/dates.ts (fmtLongDate or a short date formatter for the Date column), frontend/src/tests/setup.ts (RTL wiring already configured)</read_first>
  <behavior>
    - Given 45 readings, renders 20 data rows and a "Show 20 more" button; clicking it shows 40; clicking again shows all 45, the button disappears, and "Showing all 45 readings" appears (18px body text)
    - Rows sort newest-first: the first row is the reading with the max datetime regardless of input order
    - Each row: Date, Time, AM/PM, BP rendered as "128 / 74", Pulse, and a BP-category chip colored `categoryColor(bp_category)` with `CHIP_TEXT` text
    - A notes line renders ONLY when `notes` is non-null/non-empty (D-24); MAP/pulse-pressure/pulse-category never appear as columns
    - "Show 20 more" resets to 20 visible when the readings prop identity changes (filter change — stale expansion doesn't leak across filter sets)
  </behavior>
  <action>
    Create `ReadingsTable.tsx` (DASH-09, D-23/D-24): semantic `<table>` with `<caption class="sr-only">` "Readings", `<th scope="col">` headers at 20px/700 (Date, Time, AM/PM, Blood pressure, Pulse, Category), 18px cells, sky-surface container card. Sort a copy newest-first (`[...readings].sort((a, b) => b.datetime.localeCompare(a.datetime))` — naive ISO strings sort chronologically). Slice state `visible` (useState 20, +20 per click, reset via useEffect on readings change). Date/Time from `new Date(r.datetime)` locale formatting (naive ISO parses local — safe). BP cell literally `{systolic} / {diastolic}`. Chip: `<span>` solid category fill + CHIP_TEXT, 18-20px label — display-only, may be shorter than 48px (UI-SPEC table-chip exemption). Notes render as a full-width secondary row (`colSpan`) under the reading, only when present, prefixed "Note:" — plain React text (T-02-08: notes is the one free-text field; never dangerouslySetInnerHTML). Button "Show 20 more": accent fill, `min-h-12`, full-width below the table; when all visible, replace with the 18px line "Showing all {n} readings". No pagination chrome, no inner scrollbars (D-23).

    `ReadingsTable.test.tsx` (RTL + jsdom — tables render fine, unlike charts): build a 45-reading fixture factory and assert every behavior case above (row counts via getAllByRole("row"), button clicks via user-event or fireEvent, first-row newest assertion, chip text, notes conditionality).
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm test -- --run src/components/ReadingsTable.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/components/ReadingsTable.test.tsx` exits 0 with assertions covering: 20→40→45 slicing, button removal + "Showing all 45 readings", newest-first ordering, notes-only-when-present
    - ReadingsTable.tsx contains `Show 20 more`, `scope="col"`, `categoryColor`, and no `dangerouslySetInnerHTML`
    - ReadingsTable.tsx contains no column for `map`, `pulse_pressure`, or `pulse_category` (D-24 exclusion)
  </acceptance_criteria>
  <done>The zero-precision data fallback exists: every reading reachable by big-button paging with clinical-color chips, behavior locked by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON (incl. free-text notes) → DOM | Table/empty-state render server-provided strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering (XSS) | ReadingsTable notes rendering | mitigate | notes is the only free-text field in the payload; rendered exclusively as a React text node (acceptance criterion asserts no dangerouslySetInnerHTML in the file) |
| T-02-11 | Information Disclosure | StatsStrip/EmptyState copy | mitigate | Components never render error objects or raw status text — error presentation is centralized in App (plan 02-07) per UI-SPEC error copy; these components receive typed data props only |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages — everything needed installed and audited in 02-02; adding packages is forbidden without a legitimacy audit |
</threat_model>

<verification>
- `cd frontend && npm test -- --run && npm run build` — green including the new table behavior tests
- Strip verbatim-rendering guard: no aggregate math over readings in StatsStrip.tsx (grep gates in Task 1)
</verification>

<success_criteria>
- Stats strip renders the full API-02 payload as accessible tiles with correct null handling (DASH-08)
- Readings table: newest-first, 20+20 slicing, chips, notes-when-present — all behavior-tested (DASH-09)
- D-11 empty state with exact UI-SPEC copy and showAllData escape
</success_criteria>

<output>
Create `.planning/phases/02-read-api-dashboard/02-06-SUMMARY.md` when done
</output>
