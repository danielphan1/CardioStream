---
phase: 02-read-api-dashboard
plan: 03
type: execute
wave: 1
depends_on: [02-01, 02-02]
files_modified:
  - frontend/src/lib/dates.ts
  - frontend/src/lib/dates.test.ts
  - frontend/src/store/filters.ts
  - frontend/src/store/filters.test.ts
  - frontend/src/lib/palette.ts
  - frontend/src/lib/palette.test.ts
  - frontend/src/hooks/useReadings.ts
  - frontend/src/hooks/useStats.ts
autonomous: true
gap_closure: true
requirements: [DASH-07]

must_haves:
  truths:
    - "The zustand filter store holds exactly the future agent command fields (activeChart, datePreset, customRange, amPm, bpCategory) with machine-callable setters (D-17/D-19, RESEARCH Pattern 2)"
    - "resolveFilters turns a preset into a concrete inclusive date range anchored to the newest reading, never to today (RESEARCH Pitfall 9)"
    - "One palette module maps all six canonical category labels to CSS vars so chips, bars, and bands can never drift apart (D-14)"
    - "npm test -- --run passes with new unit tests for dates, store, and palette"
  artifacts:
    - path: "frontend/src/store/filters.ts"
      provides: "zustand filter store — THE Phase 3 agent command schema (D-02/D-03/D-11/D-17/D-19)"
      contains: "showAllData"
    - path: "frontend/src/lib/dates.ts"
      provides: "resolveFilters, parseDateOnly (split-parse, Pitfall 1), long/short/tooltip date formatters"
      contains: "resolveFilters"
    - path: "frontend/src/lib/palette.ts"
      provides: "category → CSS var map + CLINICAL_ORDER (single source for chips/bars/bands)"
      contains: "--cat-crisis"
    - path: "frontend/src/hooks/useReadings.ts"
      provides: "TanStack Query hook keyed on resolved filters, keepPreviousData"
      contains: "keepPreviousData"
    - path: "frontend/src/hooks/useStats.ts"
      provides: "stats query hook + useResolvedFilters (store → anchor → ResolvedFilters)"
      contains: "useResolvedFilters"
  key_links:
    - from: "frontend/src/hooks/useStats.ts"
      to: "frontend/src/store/filters.ts"
      via: "useResolvedFilters subscribes to the filter store"
      pattern: "useFilters"
    - from: "frontend/src/hooks/useReadings.ts"
      to: "frontend/src/api/client.ts"
      via: "queryFn calls getReadings"
      pattern: "getReadings"
    - from: "frontend/src/lib/dates.ts"
      to: "frontend/src/api/types.ts"
      via: "resolveFilters returns ResolvedFilters"
      pattern: "ResolvedFilters"
---

<objective>
Build the filter-state foundations every dashboard component consumes: the zustand filter store (the exact shape Phase 3's agent command schema will mutate — DASH-07, D-17/D-19), pure date-resolution utilities anchored to the newest reading, the single-source category palette (D-14), and the two TanStack Query hooks.

Purpose: Closes the VERIFICATION.md gap "No filter controls, filter store … exist" at the state layer, so the three wave-2 plans (charts, filter bar, strip/table) can build UI against ready contracts instead of inventing them.
Output: frontend/src/{store/filters.ts, lib/dates.ts, lib/palette.ts, hooks/useReadings.ts, hooks/useStats.ts} + unit tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-api-dashboard/02-CONTEXT.md
@.planning/phases/02-read-api-dashboard/02-RESEARCH.md
@.planning/phases/02-read-api-dashboard/02-PATTERNS.md
@frontend/src/api/types.ts
@frontend/src/api/client.ts
</context>

<interface_contract>
Exports consumed by plans 02-04..02-07:

- `store/filters.ts`: `useFilters` zustand store — state `{ activeChart: ChartId; datePreset: DatePreset; customRange: { from: string | null; to: string | null }; amPm: "all" | "AM" | "PM"; bpCategory: "all" | BPCategory }`, actions `setActiveChart, setDatePreset, setCustomRange, setAmPm, setBpCategory, showAllData`; exported type `DatePreset = "7d" | "30d" | "90d" | "all" | "custom"`.
- `lib/dates.ts`: `parseDateOnly(s: string): Date`, `formatDateParam(d: Date): string` ("YYYY-MM-DD" local), `fmtLongDate(iso: string): string` ("June 13, 2025"), `fmtShortDate(ts: number): string` (axis ticks, e.g. "Jun 13"), `fmtTooltipTitle(iso: string): string` ("June 3, 2025 · 7:42 AM"), `resolveFilters(state, latestReading: string | null): ResolvedFilters`.
- `lib/palette.ts`: `CLINICAL_ORDER: BPCategory[]` (six labels, clinical order), `categoryColor(cat: BPCategory): string` (returns `var(--cat-...)` string), `CHIP_TEXT = "var(--cat-chip-text)"`.
- `hooks/useReadings.ts`: `useReadings(resolved: ResolvedFilters)` → Query of `Reading[]`.
- `hooks/useStats.ts`: `useStats(resolved: ResolvedFilters)` → Query of `StatsSummary`; `useResolvedFilters(): ResolvedFilters` (subscribes to the store, fetches the unfiltered anchor, applies `resolveFilters`).
</interface_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/dates.ts — split-parse helpers, formatters, resolveFilters anchored to latest_reading</name>
  <files>frontend/src/lib/dates.ts, frontend/src/lib/dates.test.ts</files>
  <read_first>frontend/src/api/types.ts (ResolvedFilters, BPCategory), .planning/phases/02-read-api-dashboard/02-RESEARCH.md (Pitfall 1, Pitfall 9, Open Questions 1–2, Code Example 3 comment on resolveFilters), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Copywriting Contract — date formats "June 13, 2025", filter sentence, tooltip title)</read_first>
  <behavior>
    - parseDateOnly("2025-02-22") returns a local-midnight Date for Feb 22 (never UTC — asserted via getFullYear/getMonth/getDate, Pitfall 1)
    - formatDateParam(new Date(2025, 5, 13)) === "2025-06-13" (local components, zero-padded)
    - fmtLongDate("2025-06-13T09:21:00") === "June 13, 2025"
    - fmtTooltipTitle("2025-06-03T07:42:00") === "June 3, 2025 · 7:42 AM"
    - resolveFilters({datePreset:"30d", customRange:{from:null,to:null}, amPm:"all", bpCategory:"all"}, "2025-06-13T09:21:00") === {start_date:"2025-05-15", end_date:"2025-06-13"} (N-day preset = anchor − (N−1) days through anchor, inclusive)
    - "7d" with same anchor → start_date "2025-06-07"
    - "all" → {} (no date keys); amPm:"AM" adds am_pm:"AM"; bpCategory:"Stage 1" adds bp_category:"Stage 1" (verbatim label with space)
    - "custom" with customRange {from:"2025-03-01", to:"2025-03-31"} → {start_date:"2025-03-01", end_date:"2025-03-31"}
    - Day-preset with latestReading null → no date keys (defensive fallback; behaves as "all" until the anchor arrives)
  </behavior>
  <action>
    Create `frontend/src/lib/dates.ts` implementing the interface_contract signatures. Rules:
    - Date-only strings ("YYYY-MM-DD") are ALWAYS parsed by splitting into parts and calling `new Date(y, m - 1, d)` — never `new Date("YYYY-MM-DD")` (parses as UTC midnight, off-by-one in negative-offset timezones — RESEARCH Pitfall 1). API datetimes ("2025-06-13T09:21:00", naive ISO with time) MAY be parsed with `new Date()` directly (spec: local time — correct for DATA-05).
    - `resolveFilters(state, latestReading)`: anchor = date component of latestReading (`latestReading.slice(0, 10)` then parseDateOnly). Presets "7d"/"30d"/"90d" → `end_date` = anchor formatted, `start_date` = anchor minus (N−1) days via `new Date(y, m-1, d-(N-1))` (JS handles month rollover in local time). "all" → no date keys. "custom" → pass through non-null from/to. Append `am_pm`/`bp_category` only when store values ≠ "all". When latestReading is null and a day preset is active, omit date keys. This implements RESEARCH Open Question 1's recommendation: presets anchor to the newest reading, never to today (Pitfall 9 — today-anchored presets render an empty dashboard forever). Record this in the module docstring so Phase 3's server-side resolver (API-05) copies the same anchor.
    - Formatters use `toLocaleDateString("en-US", ...)`/`toLocaleTimeString` (month long for fmtLongDate/fmtTooltipTitle, month short + day for fmtShortDate; 12-hour time without leading zero for the tooltip title, matching UI-SPEC "June 3, 2025 · 7:42 AM").
    Write `dates.test.ts` covering every behavior case above (Vitest, no DOM needed). Cites: DASH-07, D-17, D-18.
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm test -- --run src/lib/dates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/lib/dates.test.ts` exits 0 with ≥8 assertions
    - dates.ts contains `resolveFilters` and no occurrence of the pattern `new Date("` applied to a date-only literal (date-only parsing goes through parseDateOnly)
    - Test file asserts the exact pair start_date "2025-05-15" / end_date "2025-06-13" for the 30d preset anchored to "2025-06-13T09:21:00"
  </acceptance_criteria>
  <done>Preset → concrete-range resolution is pure, tested, and anchored to the newest reading; all date formatting for later plans lives in one module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: store/filters.ts — the zustand filter store (future agent command schema)</name>
  <files>frontend/src/store/filters.ts, frontend/src/store/filters.test.ts</files>
  <read_first>.planning/phases/02-read-api-dashboard/02-RESEARCH.md (Code Example 3 — copy this shape verbatim; Pattern 2), frontend/src/store/theme.ts (existing store conventions: doc comment, typed interface, create<T>), frontend/src/api/types.ts (ChartId, BPCategory)</read_first>
  <behavior>
    - Initial state: activeChart "bp_timeline" (D-03), datePreset "all" (safe default — RESEARCH Open Question 2), customRange {from:null,to:null}, amPm "all", bpCategory "all"
    - setDatePreset("7d") after a custom range clears customRange back to {from:null,to:null} (preset ↔ custom exclusivity)
    - setCustomRange("2025-03-01","2025-03-31") sets datePreset to "custom" and stores the range
    - setAmPm/setBpCategory set single-select values (D-19); setting back to "all" clears the filter
    - showAllData() resets datePreset→"all", customRange→nulls, amPm→"all", bpCategory→"all" but leaves activeChart untouched (D-11 button clears filters, not the chart choice)
  </behavior>
  <action>
    Create `frontend/src/store/filters.ts` from RESEARCH Code Example 3 verbatim (import `ChartId`/`BPCategory` from `../api/types` instead of redeclaring them; export `DatePreset`). Module docstring must state: this store IS the Phase 3 agent command schema (each action ↔ one future voice command — DASH-07, D-17/D-19/D-11); UI state only, server data stays in TanStack Query (CLAUDE.md separation); derived concrete dates live in `lib/dates.ts` `resolveFilters`, never in the store. Default `datePreset: "all"` — decision recorded: always-safe default so first load shows Chris's full range (also satisfies ROADMAP SC4 on load).
    Write `filters.test.ts` covering every behavior case above using `useFilters.getState()` / calling actions directly (zustand stores are testable without React), resetting state between tests via `useFilters.setState(...)` of the initial snapshot.
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm test -- --run src/store/filters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/store/filters.test.ts` exits 0
    - filters.ts contains `activeChart`, `datePreset`, `customRange`, `amPm`, `bpCategory`, `showAllData`, and imports ChartId/BPCategory from "../api/types"
    - Test asserts showAllData leaves activeChart unchanged and setDatePreset clears customRange
  </acceptance_criteria>
  <done>The exact command-schema store exists and is tested; Phase 3 mutates this shape without redesign.</done>
</task>

<task type="auto">
  <name>Task 3: lib/palette.ts + query hooks (useReadings, useStats, useResolvedFilters)</name>
  <files>frontend/src/lib/palette.ts, frontend/src/lib/palette.test.ts, frontend/src/hooks/useReadings.ts, frontend/src/hooks/useStats.ts</files>
  <read_first>frontend/src/index.css (the token names: --cat-hypotension, --cat-normal, --cat-elevated, --cat-stage1, --cat-stage2, --cat-crisis, --cat-chip-text — palette must reference these EXACT names), frontend/src/api/client.ts (getReadings/getStatsSummary signatures), .planning/phases/02-read-api-dashboard/02-RESEARCH.md (Code Example 4, Pattern 3, Pitfall 13), .planning/phases/02-read-api-dashboard/02-UI-SPEC.md (Clinical category colors — single source of truth rule)</read_first>
  <action>
    `frontend/src/lib/palette.ts` (D-14 — UI-SPEC: "single source of truth … never three separate color lists"): export `CLINICAL_ORDER: BPCategory[]` = ["Hypotension", "Normal", "Elevated", "Stage 1", "Stage 2", "Hypertensive Crisis"] (verbatim, matches backend CLINICAL_ORDER); `categoryColor(cat)` returning the mapped CSS var string — "Hypotension"→"var(--cat-hypotension)", "Normal"→"var(--cat-normal)", "Elevated"→"var(--cat-elevated)", "Stage 1"→"var(--cat-stage1)", "Stage 2"→"var(--cat-stage2)", "Hypertensive Crisis"→"var(--cat-crisis)"; `CHIP_TEXT = "var(--cat-chip-text)"`. No hex values anywhere in this file — themes flip via the CSS vars defined in index.css. `palette.test.ts`: CLINICAL_ORDER has exactly those six labels in that order; categoryColor returns a `var(--cat-...)` string for every label.

    `frontend/src/hooks/useReadings.ts`: RESEARCH Code Example 4 — `useQuery({ queryKey: ["readings", resolved], queryFn: () => getReadings(resolved), placeholderData: keepPreviousData, staleTime: 5 * 60_000 })` (v5 rename — Pitfall 13; import `keepPreviousData` from "@tanstack/react-query").

    `frontend/src/hooks/useStats.ts`: `useStats(resolved)` same pattern with queryKey ["stats", resolved] and getStatsSummary. Also export `useResolvedFilters(): ResolvedFilters` — subscribes to `useFilters` (select the five filter fields), fetches the anchor via `useQuery({ queryKey: ["stats", {}], queryFn: () => getStatsSummary({}), staleTime: Infinity })` (latest_reading is UNFILTERED in every stats response, so the empty-filter query is the stable anchor; TanStack dedupes it with the main stats query whenever filters are empty), and returns `resolveFilters(storeState, anchor?.latest_reading ?? null)`. Cites: DASH-07, DASH-08.
  </action>
  <verify>
    <automated>cd /Users/dp/Documents/GitHub/Health-Visualizer/frontend && npm test -- --run && npm run build && grep -c "var(--cat-" src/lib/palette.ts && grep -c "keepPreviousData" src/hooks/useReadings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run` (full suite incl. dates/filters/palette tests) and `npm run build` both exit 0
    - `grep -c "var(--cat-" src/lib/palette.ts` ≥ 6; palette.ts contains no `#` hex literal
    - useReadings.ts contains `placeholderData: keepPreviousData` and `["readings"`; useStats.ts contains `["stats"` and `useResolvedFilters`
    - useStats.ts contains `staleTime: Infinity` on the anchor query
  </acceptance_criteria>
  <done>Palette and server-state hooks exist; wave-2 plans import them without inventing colors, query keys, or anchor logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → filter store → query params | Store values become GET query params to the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | resolveFilters output → /readings, /stats/summary params | mitigate | Params are closed unions (DatePreset, "AM"/"PM", six-label BPCategory) URL-encoded by URLSearchParams in client.ts; backend rejects anything else with 422 (verified in plan 02-01). No free-text reaches the query string. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages installed in this plan — all dependencies (zustand, @tanstack/react-query) were installed and audit-approved in plan 02-02. Adding any new package is out of scope and forbidden without a legitimacy audit. |
</threat_model>

<verification>
- `cd frontend && npm test -- --run` — all suites pass (dates, filters store, palette, existing smoke)
- `npm run build` — exit 0
- resolveFilters 30d/anchor-2025-06-13 test asserts start 2025-05-15 / end 2025-06-13 (inclusive-range contract matches backend Pitfall-4 semantics)
</verification>

<success_criteria>
- Filter store shape = agent command schema (DASH-07 state half done); presets resolve anchored to latest_reading, never today
- Palette is the single category-color source with zero hex values
- Query hooks follow the two-key pattern with keepPreviousData
- All new units tested; frontend suite and build green
</success_criteria>

<output>
Create `.planning/phases/02-read-api-dashboard/02-03-SUMMARY.md` when done
</output>
